# AXM LEGO City External Gates

Status: **EXPERIMENTAL — DISABLED BY DEFAULT**

The committed registry describes MCP, A2A, GitHub draft, OCI/ORAS export, and
public attestation boundaries. Every adapter is disabled and contains no
transport client. External protocols never become AXM ontology or authority.

The core can compile an inert outbound plan only from a runtime-enabled copy of
an adapter plus an exact Authority Grid decision and external identity check.
It never sends the plan. Inbound bytes are always untrusted Harbor material.
GitHub readback requires exact head/base/title/body, open, unmerged, and final
`draft === true`; a draft-state race fails. Artifact exports verify fully local.
