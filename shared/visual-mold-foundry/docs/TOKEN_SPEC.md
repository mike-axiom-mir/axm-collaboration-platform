# Token Specification

The v0.2 registry contains 148 semantic visual tokens across palette,
typography, spacing, radius, line, shadow, glow, motion, depth, material,
performance, state, accessibility, composition, and proof concerns.

Tokens express intent rather than one renderer's implementation detail. A target
adapter may translate the same semantic token differently, but must disclose
fallbacks and validation status.
