# Test Report

Version: **0.5.0**  
Date: **2026-07-28**  
Status: **PASS — WORKING / TEST**

## Automated unit suite

Command: `npm test`

Final result: **60 PASS · 0 FAIL**

Coverage includes:

- deterministic recipes, phrase parsing, seed variation, and SHA-256;
- 25 editable preskins, six families, fusion, and five palette harmonies;
- 16 semantic molds and valid generated game contracts;
- seven game-skin organs and all 33 portable semantic surfaces;
- deterministic mold-maker output across six moods;
- explicit-only binding remaps with provenance;
- four performance profiles and zero-motion token/material enforcement;
- seven-organ composition and namespaced scope isolation;
- literal Studio control/HTML ID matching and seven mixer selectors;
- full Test Chamber and conformance-evidence control presence;
- structural adapter conformance, pending evidence, ready-for-review evidence,
  and invalid-contract rejection;
- presentation-only resolution, fallbacks, protected cues, and receipts;
- approval/apply/rollback and preview separation;
- raster integrity, MIME, size, and hostile-field rejection;
- remote asset, raw SVG, prototype-pollution, and authority denial.

## Package verifier

Command: `npm run verify`

Final result: **101 PASS · 0 FAIL**

The verifier parses every JSON file, validates and verifies all 25 portable
preskins and all 20 generated mold kits, generates contracts for every mold,
checks deterministic mold growth and fusion, proves scoped materials stay
isolated, checks performance caps at material and shared-token levels, resolves
the example contract, verifies 33-slot coverage across seven organs, verifies
integrity, checks the honest pending and runtime-evidence conformance routes,
and rejects gameplay and script injection.

## Build reproducibility

- `npm run preskins:build` → 25 deterministic portable preskins.
- `npm run molds:build` → 20 deterministic mold kits and 16 game contracts.
- `npm run example:compile` → valid v0.5 pack.
- Example digest:
  `ace646ed74e4d3d07356fb2e7efc937f5307f0a92f2af1293f078717db60cad3`
- Node syntax checks: PASS.

## Local HTTP smoke

- `/` → `302` to `/studio/`
- Studio HTML, CSS, JavaScript, and mold catalog → `200`
- Content Security Policy present
- runtime remains loopback-only by default

Result: **PASS**

## Browser verification boundary

The cloud browser refused access to the loopback preview, so no v0.5 screenshot
or live interaction result is claimed. Static module syntax, every literal
Studio `byId` reference, the chamber controls, all seven mixer selectors, and
the HTTP routes were verified locally. Predecessor visual evidence is not
silently reused as proof of the changed v0.5 layout.

Result: **NOT RE-PROVEN — BROWSER LOOPBACK ACCESS DENIED**

## Archive verification

- 136 source checksum entries.
- clean extraction: **136 / 136 PASS**
- ZIP structure: **PASS**

The final archive digest is reported alongside the downloadable ZIP.

## Not run

- Real AXM game or Game Hub integration.
- Physical Windows launcher.
- Physical Android/mobile viewport.
- Public hosting, gallery, accounts, or moderation.
- Cross-engine adapter execution and automatic renderer certification.
- Cross-OS deterministic comparison.
- Sandbox decoder fuzzing.
