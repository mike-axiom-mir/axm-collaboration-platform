axm.access.accessible-media-export-gate
Accessible Media Export Gate

Purpose: Hold export when required captions, descriptions, transcript, controls, language, or metadata are missing from the declared target profile.

Default: disabled; reviewed local staging only. Real accessibility effect is not proven.
