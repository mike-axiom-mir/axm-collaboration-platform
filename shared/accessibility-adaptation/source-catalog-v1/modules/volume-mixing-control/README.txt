axm.access.volume-mixing-control
Independent Volume and Audio Mixing Control

Purpose: Let users control speech, music, effects, alerts, and spatial channels separately when the media architecture supports it.

Default: disabled; reviewed local staging only. Real accessibility effect is not proven.
