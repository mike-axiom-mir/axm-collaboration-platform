axm.access.low-memory-resume-capsule
Low-Memory Resume Capsule

Purpose: Restore where the person stopped, what was completed, what changed, and the next step without requiring recall of the whole session.

Default: disabled; reviewed local staging only. Real accessibility effect is not proven.
