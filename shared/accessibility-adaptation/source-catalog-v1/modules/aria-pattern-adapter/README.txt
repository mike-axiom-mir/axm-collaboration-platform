axm.access.aria-pattern-adapter
ARIA Pattern Adapter

Purpose: Apply reviewed ARIA roles, states, properties, relationships, and keyboard behaviour only when native semantics are insufficient.

Default: disabled; reviewed local staging only. Real accessibility effect is not proven.
