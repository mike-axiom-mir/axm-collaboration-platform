axm.access.semantic-native-control-selector
Native Semantic Control Selector

Purpose: Prefer native HTML and platform controls before custom widgets when they provide the required meaning and behaviour.

Default: disabled; reviewed local staging only. Real accessibility effect is not proven.
