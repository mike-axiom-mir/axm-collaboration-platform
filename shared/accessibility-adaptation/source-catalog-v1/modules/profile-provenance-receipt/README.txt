axm.access.profile-provenance-receipt
Accessibility Profile Provenance Receipt

Purpose: Record what came from the human, a helper, a device, a test, a default, or a machine proposal and keep those sources separate.

Default: disabled; reviewed local staging only. Real accessibility effect is not proven.
