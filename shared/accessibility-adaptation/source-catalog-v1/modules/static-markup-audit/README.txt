axm.access.static-markup-audit
Static Accessibility Markup Audit

Purpose: Detect definite structural omissions such as missing language, names, labels, alt attributes, viewport, or obvious pointer-only controls.

Default: disabled; reviewed local staging only. Real accessibility effect is not proven.
