axm.access.user-controlled-adaptation
User-Controlled Adaptation Gate

Purpose: Preview and require approval before an accessibility profile changes layout, input, language, media, or timing.

Default: disabled; reviewed local staging only. Real accessibility effect is not proven.
