axm.access.component-accessibility-contract
Component Accessibility Contract

Purpose: Require names, roles, states, keyboard behaviour, focus, contrast, target size, errors, motion, and alternatives for reusable components.

Default: disabled; reviewed local staging only. Real accessibility effect is not proven.
