(function(root,factory){var api=factory();if(typeof module!=='undefined'&&module.exports)module.exports=api;if(root)root.AXMPhysics2D=api;})(typeof self!=='undefined'?self:this,function(){
  'use strict';
  var VERSION='0.2.2',WORLD_SCHEMA='axm.physics-world/v1',BODY_SCHEMA='axm.physics-body/v1',RESULT_SCHEMA='axm.physics-step-result/v1';
  function clone(v){return JSON.parse(JSON.stringify(v));}
  function number(v,min,max,fallback){var n=Number(v);return Number.isFinite(n)?Math.max(min,Math.min(max,n)):fallback;}
  function text(v,max){var s=String(v==null?'':v).trim();return max?s.slice(0,max):s;}
  function round(v){return Math.round(v*1e9)/1e9;}
  function vec(v,x,y){v=v||{};return{x:number(v.x,-1e9,1e9,x||0),y:number(v.y,-1e9,1e9,y||0)};}
  function dot(a,b){return a.x*b.x+a.y*b.y;}
  function length(v){return Math.sqrt(dot(v,v));}
  function normalized(v,fallback){var m=length(v);return m>1e-12?{x:v.x/m,y:v.y/m}:(fallback||{x:1,y:0});}
  function bodyExtent(body){return body.shape.kind==='circle'?body.shape.radius:Math.min(body.shape.halfWidth,body.shape.halfHeight);}
  function makeBody(input,index){
    input=input||{};var type=['static','dynamic','kinematic'].indexOf(input.type)>=0?input.type:'dynamic',kind=input.shape&&input.shape.kind==='box'?'box':'circle';
    var mass=type==='dynamic'?number(input.mass,.000001,1e9,1):Infinity,shape=kind==='circle'?{kind:'circle',radius:number(input.shape&&input.shape.radius,.001,1e6,.5)}:{kind:'box',halfWidth:number(input.shape&&input.shape.halfWidth,.001,1e6,.5),halfHeight:number(input.shape&&input.shape.halfHeight,.001,1e6,.5)};
    return{schema:BODY_SCHEMA,id:text(input.id,100)||('body-'+String(index+1).padStart(3,'0')),label:text(input.label,120)||text(input.id,100)||('Body '+(index+1)),type:type,shape:shape,position:vec(input.position,0,0),velocity:vec(input.velocity,0,0),force:vec(input.force,0,0),mass:Number.isFinite(mass)?mass:null,invMass:Number.isFinite(mass)?1/mass:0,restitution:number(input.restitution,0,1,.25),friction:number(input.friction,0,2,.45),linearDamping:number(input.linearDamping,0,.999,.01),gravityScale:number(input.gravityScale,-20,20,1),collision:{category:(Math.round(number(input.collision&&input.collision.category,1,2147483647,1))>>>0),mask:(Math.round(number(input.collision&&input.collision.mask,0,2147483647,2147483647))>>>0),group:Math.round(number(input.collision&&input.collision.group,-32768,32767,0))},allowSleep:input.allowSleep!==false,sleeping:!!input.sleeping,sleepFrames:Math.round(number(input.sleepFrames,0,100000,0)),sensor:!!input.sensor,enabled:input.enabled!==false,userData:clone(input.userData||{})};
  }
  function createWorld(input){input=input||{};return{schema:WORLD_SCHEMA,engine:{id:'axm-physics-2d',version:VERSION},gravity:vec(input.gravity,0,9.81),fixedDelta:number(input.fixedDelta,1/1000,.1,1/60),solverIterations:Math.round(number(input.solverIterations,1,32,8)),maxSubsteps:Math.round(number(input.maxSubsteps,1,16,8)),sleep:{enabled:!input.sleep||input.sleep.enabled!==false,linearThreshold:number(input.sleep&&input.sleep.linearThreshold,0,10,.75),positionThreshold:number(input.sleep&&input.sleep.positionThreshold,0,1,.02),frames:Math.round(number(input.sleep&&input.sleep.frames,1,10000,30)),wakeImpulse:number(input.sleep&&input.sleep.wakeImpulse,0,1e6,2),wakeSpeed:number(input.sleep&&input.sleep.wakeSpeed,0,1e6,.75),kinematicWakeSpeed:number(input.sleep&&input.sleep.kinematicWakeSpeed,0,1e6,.05)},broadphase:{mode:input.broadphase&&input.broadphase.mode==='spatial-hash'?'spatial-hash':'all-pairs',cellSize:number(input.broadphase&&input.broadphase.cellSize,.05,1e6,1)},bounds:input.bounds===false?null:{minX:number(input.bounds&&input.bounds.minX,-1e9,1e9,0),minY:number(input.bounds&&input.bounds.minY,-1e9,1e9,0),maxX:number(input.bounds&&input.bounds.maxX,-1e9,1e9,16),maxY:number(input.bounds&&input.bounds.maxY,-1e9,1e9,9),restitution:number(input.bounds&&input.bounds.restitution,0,1,.5)},bodies:[],contacts:[],contactKeys:[],contactEvents:[],time:0,stepIndex:0,integrationStepCount:0,accumulator:0,diagnostics:{status:'READY',bodyCount:0,dynamicCount:0,sleepingCount:0,contactCount:0,contactEvents:0,substeps:0,integrationStepCount:0,broadphaseMode:input.broadphase&&input.broadphase.mode==='spatial-hash'?'spatial-hash':'all-pairs',broadphasePairs:0,kineticEnergy:0,potentialEnergy:0,totalEnergy:0,energyDelta:0,momentum:{x:0,y:0},maxSpeed:0,maxPenetration:0,checksum:'00000000'}};}
  function addBody(world,input){var out=clone(world),body=makeBody(input,out.bodies.length);if(out.bodies.some(function(b){return b.id===body.id;}))throw Error('body id already exists');out.bodies.push(body);out.bodies.sort(function(a,b){return a.id.localeCompare(b.id);});out.diagnostics=measure(out,0,0);return{world:out,body:clone(body)};}
  function removeBody(world,id){var out=clone(world),before=out.bodies.length;out.bodies=out.bodies.filter(function(b){return b.id!==id;});if(out.bodies.length===before)throw Error('body not found');out.contacts=[];out.diagnostics=measure(out,0,0);return out;}
  function find(out,id){var b=out.bodies.find(function(x){return x.id===id;});if(!b)throw Error('body not found: '+id);return b;}
  function wake(b){if(!b||b.type!=='dynamic')return;b.sleeping=false;b.sleepFrames=0;}
  function applyForce(world,id,force){var out=clone(world),b=find(out,id);if(b.type!=='dynamic')throw Error('forces require a dynamic body');var f=vec(force,0,0);wake(b);b.force.x+=f.x;b.force.y+=f.y;return out;}
  function applyImpulse(world,id,impulse){var out=clone(world),b=find(out,id);if(b.type!=='dynamic')throw Error('impulses require a dynamic body');var p=vec(impulse,0,0);wake(b);b.velocity.x+=p.x*b.invMass;b.velocity.y+=p.y*b.invMass;return out;}
  function setVelocity(world,id,velocity){var out=clone(world),b=find(out,id),v=vec(velocity,0,0);if(b.type==='static')throw Error('velocity requires a dynamic or kinematic body');b.velocity=v;wake(b);return out;}
  function aabb(body){return body.shape.kind==='circle'?{minX:body.position.x-body.shape.radius,maxX:body.position.x+body.shape.radius,minY:body.position.y-body.shape.radius,maxY:body.position.y+body.shape.radius}:{minX:body.position.x-body.shape.halfWidth,maxX:body.position.x+body.shape.halfWidth,minY:body.position.y-body.shape.halfHeight,maxY:body.position.y+body.shape.halfHeight};}
  function overlap(a,b){return a.minX<=b.maxX&&a.maxX>=b.minX&&a.minY<=b.maxY&&a.maxY>=b.minY;}
  function circleCircle(a,b){var d={x:b.position.x-a.position.x,y:b.position.y-a.position.y},dist=length(d),sum=a.shape.radius+b.shape.radius;if(dist>=sum)return null;return{normal:normalized(d,{x:1,y:0}),penetration:sum-dist,point:{x:a.position.x+normalized(d,{x:1,y:0}).x*a.shape.radius,y:a.position.y+normalized(d,{x:1,y:0}).y*a.shape.radius}};}
  function boxBox(a,b){var dx=b.position.x-a.position.x,px=a.shape.halfWidth+b.shape.halfWidth-Math.abs(dx);if(px<=0)return null;var dy=b.position.y-a.position.y,py=a.shape.halfHeight+b.shape.halfHeight-Math.abs(dy);if(py<=0)return null;if(px<py)return{normal:{x:dx<0?-1:1,y:0},penetration:px,point:{x:(a.position.x+b.position.x)/2,y:(a.position.y+b.position.y)/2}};return{normal:{x:0,y:dy<0?-1:1},penetration:py,point:{x:(a.position.x+b.position.x)/2,y:(a.position.y+b.position.y)/2}};}
  function circleBox(circle,box){var bx=box.position.x,by=box.position.y,hw=box.shape.halfWidth,hh=box.shape.halfHeight,cx=Math.max(bx-hw,Math.min(circle.position.x,bx+hw)),cy=Math.max(by-hh,Math.min(circle.position.y,by+hh)),d={x:cx-circle.position.x,y:cy-circle.position.y},dist=length(d);if(dist>circle.shape.radius)return null;if(dist>1e-12)return{normal:{x:d.x/dist,y:d.y/dist},penetration:circle.shape.radius-dist,point:{x:cx,y:cy}};var left=Math.abs(circle.position.x-(bx-hw)),right=Math.abs((bx+hw)-circle.position.x),top=Math.abs(circle.position.y-(by-hh)),bottom=Math.abs((by+hh)-circle.position.y),m=Math.min(left,right,top,bottom),n=m===left?{x:-1,y:0}:m===right?{x:1,y:0}:m===top?{x:0,y:-1}:{x:0,y:1};return{normal:n,penetration:circle.shape.radius+m,point:{x:circle.position.x+n.x*m,y:circle.position.y+n.y*m}};}
  function collide(a,b){var hit;if(a.shape.kind==='circle'&&b.shape.kind==='circle')hit=circleCircle(a,b);else if(a.shape.kind==='box'&&b.shape.kind==='box')hit=boxBox(a,b);else if(a.shape.kind==='circle')hit=circleBox(a,b);else{hit=circleBox(b,a);if(hit)hit.normal={x:-hit.normal.x,y:-hit.normal.y};}if(hit){hit.a=a.id;hit.b=b.id;hit.sensor=!!(a.sensor||b.sensor);}return hit;}
  function canCollide(a,b){var ac=a.collision||{category:1,mask:2147483647,group:0},bc=b.collision||{category:1,mask:2147483647,group:0};if(ac.group&&ac.group===bc.group)return ac.group>0;return!!((ac.category&bc.mask)!==0&&(bc.category&ac.mask)!==0);}
  function resolve(a,b,c,sleep){var inv=a.invMass+b.invMass;if(!inv||c.sensor)return;var rv={x:b.velocity.x-a.velocity.x,y:b.velocity.y-a.velocity.y},relativeSpeed=length(rv),along=dot(rv,c.normal),wakeThreshold=(a.type==='kinematic'||b.type==='kinematic')?sleep.kinematicWakeSpeed:sleep.wakeSpeed;if(sleep&&relativeSpeed>wakeThreshold&&(a.sleeping||b.sleeping)){wake(a);wake(b);rv={x:b.velocity.x-a.velocity.x,y:b.velocity.y-a.velocity.y};along=dot(rv,c.normal);}if(along<0){var e=Math.min(a.restitution,b.restitution),j=-(1+e)*along/inv,ix=c.normal.x*j,iy=c.normal.y*j;if(sleep&&j>sleep.wakeImpulse){wake(a);wake(b);}if(a.type==='dynamic'&&!a.sleeping){a.velocity.x-=ix*a.invMass;a.velocity.y-=iy*a.invMass;}if(b.type==='dynamic'&&!b.sleeping){b.velocity.x+=ix*b.invMass;b.velocity.y+=iy*b.invMass;}rv={x:b.velocity.x-a.velocity.x,y:b.velocity.y-a.velocity.y};var tangent={x:rv.x-c.normal.x*dot(rv,c.normal),y:rv.y-c.normal.y*dot(rv,c.normal)},tm=length(tangent);if(tm>1e-12){tangent.x/=tm;tangent.y/=tm;var jt=-dot(rv,tangent)/inv,mu=Math.sqrt(a.friction*b.friction),limit=j*mu;jt=Math.max(-limit,Math.min(limit,jt));if(a.type==='dynamic'&&!a.sleeping){a.velocity.x-=tangent.x*jt*a.invMass;a.velocity.y-=tangent.y*jt*a.invMass;}if(b.type==='dynamic'&&!b.sleeping){b.velocity.x+=tangent.x*jt*b.invMass;b.velocity.y+=tangent.y*jt*b.invMass;}}}
    var correction=Math.max(c.penetration-.001,0)/inv*.72;if(a.type==='dynamic'&&!a.sleeping){a.position.x-=c.normal.x*correction*a.invMass;a.position.y-=c.normal.y*correction*a.invMass;}if(b.type==='dynamic'&&!b.sleeping){b.position.x+=c.normal.x*correction*b.invMass;b.position.y+=c.normal.y*correction*b.invMass;}}
  function resolveBounds(b,bounds,contacts){if(!bounds||b.type!=='dynamic'||!b.enabled)return;var e=b.shape.kind==='circle'?{x:b.shape.radius,y:b.shape.radius}:{x:b.shape.halfWidth,y:b.shape.halfHeight},hits=[];if(b.position.x-e.x<bounds.minX){b.position.x=bounds.minX+e.x;b.velocity.x=Math.abs(b.velocity.x)*bounds.restitution;hits.push('left');}if(b.position.x+e.x>bounds.maxX){b.position.x=bounds.maxX-e.x;b.velocity.x=-Math.abs(b.velocity.x)*bounds.restitution;hits.push('right');}if(b.position.y-e.y<bounds.minY){b.position.y=bounds.minY+e.y;b.velocity.y=Math.abs(b.velocity.y)*bounds.restitution;hits.push('top');}if(b.position.y+e.y>bounds.maxY){b.position.y=bounds.maxY-e.y;b.velocity.y=-Math.abs(b.velocity.y)*bounds.restitution;hits.push('bottom');}hits.forEach(function(side){contacts.push({a:b.id,b:'world:'+side,normal:{x:0,y:0},penetration:0,sensor:false});});}
  function allPairs(out){var pairs=[];for(var i=0;i<out.bodies.length;i++)for(var j=i+1;j<out.bodies.length;j++)pairs.push([out.bodies[i],out.bodies[j]]);return pairs;}
  function spatialHashPairs(out){var size=ensureBroadphase(out).cellSize,cells={},pairMap={},fallback=false;out.bodies.forEach(function(b){if(!b.enabled)return;var box=aabb(b),minX=Math.floor(box.minX/size),maxX=Math.floor(box.maxX/size),minY=Math.floor(box.minY/size),maxY=Math.floor(box.maxY/size);if((maxX-minX+1)*(maxY-minY+1)>4096){fallback=true;return;}for(var x=minX;x<=maxX;x++)for(var y=minY;y<=maxY;y++){var key=x+','+y;(cells[key]||(cells[key]=[])).push(b);}});if(fallback)return allPairs(out);Object.keys(cells).sort().forEach(function(key){var list=cells[key].sort(function(a,b){return a.id.localeCompare(b.id);});for(var i=0;i<list.length;i++)for(var j=i+1;j<list.length;j++){var a=list[i],b=list[j],pairKey=a.id<b.id?a.id+'|'+b.id:b.id+'|'+a.id;pairMap[pairKey]=a.id<b.id?[a,b]:[b,a];}});return Object.keys(pairMap).sort().map(function(key){return pairMap[key];});}
  function detectAll(out,forcedMode){var mode=forcedMode||ensureBroadphase(out).mode,pairs=mode==='spatial-hash'?spatialHashPairs(out):allPairs(out),contacts=[];pairs.forEach(function(pair){var a=pair[0],b=pair[1];if(!a.enabled||!b.enabled||(a.type!=='dynamic'&&b.type!=='dynamic')||!canCollide(a,b)||!overlap(aabb(a),aabb(b)))return;var c=collide(a,b);if(c)contacts.push(c);});return{contacts:contacts,pairCount:pairs.length,mode:mode};}
  function adaptiveSubsteps(world,dt){var maxTravel=0,minFeature=Infinity;world.bodies.forEach(function(b){if(!b.enabled||b.type==='static')return;var accel=b.type==='dynamic'?{x:world.gravity.x*b.gravityScale+b.force.x*b.invMass,y:world.gravity.y*b.gravityScale+b.force.y*b.invMass}:{x:0,y:0},predicted=length(b.velocity)+length(accel)*dt;maxTravel=Math.max(maxTravel,predicted*dt);minFeature=Math.min(minFeature,bodyExtent(b));});if(!Number.isFinite(minFeature)||minFeature<=0)return 1;return Math.max(1,Math.min(world.maxSubsteps,Math.ceil(maxTravel/(minFeature*.5))));}
  function contactKey(c){return[c.a,c.b].sort().join('|');}
  function uniqueContacts(contacts){var seen={};return contacts.filter(function(c){var key=contactKey(c);if(seen[key])return false;seen[key]=true;c.key=key;return true;});}
  function contactLifecycle(previous,current,stepIndex){var before={};(previous||[]).forEach(function(k){before[k]=true;});var after={};current.forEach(function(c){after[c.key]=c;});var events=[];Object.keys(after).sort().forEach(function(k){events.push({type:before[k]?'STAY':'ENTER',key:k,a:after[k].a,b:after[k].b,sensor:after[k].sensor,stepIndex:stepIndex});});Object.keys(before).sort().forEach(function(k){if(!after[k]){var ids=k.split('|');events.push({type:'EXIT',key:k,a:ids[0],b:ids[1],sensor:false,stepIndex:stepIndex});}});return events;}
  function ensureSleep(world){world.sleep||(world.sleep={enabled:true,linearThreshold:.75,positionThreshold:.02,frames:30,wakeImpulse:2,wakeSpeed:.75,kinematicWakeSpeed:.05});if(!Number.isFinite(world.sleep.wakeSpeed))world.sleep.wakeSpeed=.75;if(!Number.isFinite(world.sleep.kinematicWakeSpeed))world.sleep.kinematicWakeSpeed=.05;return world.sleep;}
  function ensureBroadphase(world){world.broadphase||(world.broadphase={mode:'all-pairs',cellSize:1});if(world.broadphase.mode!=='spatial-hash')world.broadphase.mode='all-pairs';world.broadphase.cellSize=number(world.broadphase.cellSize,.05,1e6,1);return world.broadphase;}
  function updateSleeping(world,beforePositions){var sleep=ensureSleep(world),touching={};(world.contacts||[]).forEach(function(c){if(c.sensor)return;if(c.a.indexOf('world:')!==0)touching[c.a]=true;if(c.b.indexOf('world:')!==0)touching[c.b]=true;});world.bodies.forEach(function(b){if(b.type!=='dynamic')return;if(!sleep.enabled||b.allowSleep===false||!touching[b.id]){wake(b);return;}if(b.sleeping){b.velocity={x:0,y:0};return;}var before=beforePositions[b.id]||b.position,displacement=Math.hypot(b.position.x-before.x,b.position.y-before.y),speed=Math.hypot(b.velocity.x,b.velocity.y);if(speed<=sleep.linearThreshold&&displacement<=sleep.positionThreshold){b.sleepFrames=(b.sleepFrames||0)+1;if(b.sleepFrames>=sleep.frames){b.sleeping=true;b.velocity={x:0,y:0};}}else b.sleepFrames=0;});}
  function checksum(world){var data=world.bodies.slice().sort(function(a,b){return a.id.localeCompare(b.id);}).map(function(b){return[b.id,round(b.position.x),round(b.position.y),round(b.velocity.x),round(b.velocity.y),!!b.sleeping,b.sleepFrames||0];});var s=JSON.stringify([round(world.time),world.stepIndex,data]),h=2166136261;for(var i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619);}return('00000000'+(h>>>0).toString(16)).slice(-8);}
  function measure(world,substeps,maxPenetration,broadphasePairs){var kinetic=0,potential=0,mx=0,my=0,maxSpeed=0;world.bodies.forEach(function(b){if(b.type!=='dynamic'||!b.enabled)return;var speed=length(b.velocity),mass=b.mass||0;kinetic+=.5*mass*speed*speed;potential-=mass*(world.gravity.x*b.position.x+world.gravity.y*b.position.y)*b.gravityScale;mx+=mass*b.velocity.x;my+=mass*b.velocity.y;maxSpeed=Math.max(maxSpeed,speed);});return{status:'COMPLETE',bodyCount:world.bodies.length,dynamicCount:world.bodies.filter(function(b){return b.type==='dynamic'&&b.enabled;}).length,sleepingCount:world.bodies.filter(function(b){return b.type==='dynamic'&&b.enabled&&b.sleeping;}).length,contactCount:(world.contacts||[]).length,contactEvents:(world.contactEvents||[]).length,substeps:substeps,integrationStepCount:world.integrationStepCount||0,broadphaseMode:ensureBroadphase(world).mode,broadphasePairs:broadphasePairs||0,kineticEnergy:round(kinetic),potentialEnergy:round(potential),totalEnergy:round(kinetic+potential),energyDelta:0,momentum:{x:round(mx),y:round(my)},maxSpeed:round(maxSpeed),maxPenetration:round(maxPenetration||0),checksum:checksum(world)};}
  function step(world,dt){var out=clone(world),valid=validate(out);if(!valid.ok)throw Error(valid.errors.join('; '));ensureSleep(out);ensureBroadphase(out);var before=measure(out,0,0),beforePositions={};out.bodies.forEach(function(b){beforePositions[b.id]=clone(b.position);});dt=number(dt,1/1000,.1,out.fixedDelta);var substeps=adaptiveSubsteps(out,dt),h=dt/substeps,maxPen=0,maxPairs=0,all=[];out.bodies.sort(function(a,b){return a.id.localeCompare(b.id);});for(var s=0;s<substeps;s++){out.bodies.forEach(function(b){if(!b.enabled||b.sleeping)return;if(b.type==='dynamic'){b.velocity.x+=(out.gravity.x*b.gravityScale+b.force.x*b.invMass)*h;b.velocity.y+=(out.gravity.y*b.gravityScale+b.force.y*b.invMass)*h;var damp=Math.max(0,1-b.linearDamping*h);b.velocity.x*=damp;b.velocity.y*=damp;}if(b.type!=='static'){b.position.x+=b.velocity.x*h;b.position.y+=b.velocity.y*h;}});for(var iteration=0;iteration<out.solverIterations;iteration++){var detected=detectAll(out),contacts=detected.contacts;maxPairs=Math.max(maxPairs,detected.pairCount);contacts.forEach(function(c){var a=find(out,c.a),b=find(out,c.b);maxPen=Math.max(maxPen,c.penetration);resolve(a,b,c,out.sleep);});if(iteration===0)all=all.concat(contacts);}out.bodies.forEach(function(b){resolveBounds(b,out.bounds,all);});}
    out.bodies.forEach(function(b){b.force={x:0,y:0};b.position.x=round(b.position.x);b.position.y=round(b.position.y);b.velocity.x=round(b.velocity.x);b.velocity.y=round(b.velocity.y);});out.time=round(out.time+dt);out.stepIndex++;out.integrationStepCount=(out.integrationStepCount||0)+substeps;out.contacts=uniqueContacts(all).slice(0,2048);out.contactEvents=contactLifecycle(out.contactKeys||[],out.contacts,out.stepIndex);out.contactKeys=out.contacts.map(function(c){return c.key;}).sort();updateSleeping(out,beforePositions);out.diagnostics=measure(out,substeps,maxPen,maxPairs);out.diagnostics.energyDelta=round(out.diagnostics.totalEnergy-before.totalEnergy);return{schema:RESULT_SCHEMA,ok:true,world:out,diagnostics:clone(out.diagnostics),events:clone(out.contactEvents),evidence:['Fixed-step integration executed as '+substeps+' explicit substep(s)',out.diagnostics.broadphaseMode+' broadphase evaluated '+maxPairs+' candidate pair(s) with collision filters and exact shape contacts','Contact ENTER/STAY/EXIT lifecycle derived','Contact-aware sleep state evaluated and remains wakeable by force, support motion or impact','Impulse and positional correction solver executed','State checksum '+out.diagnostics.checksum]};}
  function simulate(world,steps,dt){var out=clone(world),last=null,n=Math.round(number(steps,1,100000,1));for(var i=0;i<n;i++){last=step(out,dt);out=last.world;}return{schema:'axm.physics-simulation-result/v1',ok:true,steps:n,world:out,diagnostics:last?last.diagnostics:measure(out,0,0),evidence:(last&&last.evidence||[]).concat([n+' explicit step(s) completed'])};}
  function simulateTrace(world,steps,dt,sampleEvery){var out=clone(world),n=Math.round(number(steps,1,100000,1)),every=Math.round(number(sampleEvery,1,n,1)),frames=[];function capture(){frames.push({stepIndex:out.stepIndex,time:out.time,checksum:checksum(out),contacts:(out.contacts||[]).length,bodies:out.bodies.map(function(b){return{id:b.id,position:clone(b.position),velocity:clone(b.velocity)};})});}capture();for(var i=0;i<n;i++){out=step(out,dt).world;if((i+1)%every===0||i===n-1)capture();if(frames.length>2000)throw Error('trace frame limit exceeded');}return{schema:'axm.physics-trace/v1',engine:{id:'axm-physics-2d',version:VERSION},steps:n,sampleEvery:every,frames:frames,finalWorld:out,diagnostics:clone(out.diagnostics),replay:{initialChecksum:frames[0].checksum,finalChecksum:frames[frames.length-1].checksum,configuration:{fixedDelta:world.fixedDelta,solverIterations:world.solverIterations,maxSubsteps:world.maxSubsteps}}};}
  function queryPoint(world,point){var p=vec(point,0,0);return world.bodies.filter(function(b){if(!b.enabled)return false;if(b.shape.kind==='circle'){var dx=p.x-b.position.x,dy=p.y-b.position.y;return dx*dx+dy*dy<=b.shape.radius*b.shape.radius;}var box=aabb(b);return p.x>=box.minX&&p.x<=box.maxX&&p.y>=box.minY&&p.y<=box.maxY;}).map(function(b){return b.id;});}
  function rayCircle(origin,direction,maxDistance,b){var oc={x:origin.x-b.position.x,y:origin.y-b.position.y},proj=dot(oc,direction),c=dot(oc,oc)-b.shape.radius*b.shape.radius,disc=proj*proj-c;if(disc<0)return null;var root=Math.sqrt(disc),t=-proj-root;if(t<0)t=-proj+root;if(t<0||t>maxDistance)return null;return t;}
  function rayBox(origin,direction,maxDistance,b){var box=aabb(b),tmin=0,tmax=maxDistance,axes=[['x','minX','maxX'],['y','minY','maxY']];for(var i=0;i<axes.length;i++){var axis=axes[i][0],d=direction[axis],o=origin[axis],min=box[axes[i][1]],max=box[axes[i][2]];if(Math.abs(d)<1e-12){if(o<min||o>max)return null;continue;}var t1=(min-o)/d,t2=(max-o)/d;if(t1>t2){var swap=t1;t1=t2;t2=swap;}tmin=Math.max(tmin,t1);tmax=Math.min(tmax,t2);if(tmin>tmax)return null;}return tmin<=maxDistance?tmin:null;}
  function raycast(world,input){input=input||{};var origin=vec(input.origin,0,0),direction=normalized(vec(input.direction,1,0),{x:1,y:0}),maxDistance=number(input.maxDistance,0,1e9,1e3),hits=[];world.bodies.forEach(function(b){if(!b.enabled)return;var distance=b.shape.kind==='circle'?rayCircle(origin,direction,maxDistance,b):rayBox(origin,direction,maxDistance,b);if(distance==null)return;hits.push({bodyId:b.id,shape:b.shape.kind,distance:round(distance),point:{x:round(origin.x+direction.x*distance),y:round(origin.y+direction.y*distance)}});});hits.sort(function(a,b){return a.distance-b.distance||a.bodyId.localeCompare(b.bodyId);});return{schema:'axm.physics-raycast/v1',origin:origin,direction:direction,maxDistance:maxDistance,hits:hits};}
  function compareBroadphases(input){input=input||{};var seed=(Math.round(number(input.seed,1,4294967295,1096305973))>>>0),scenes=Math.round(number(input.scenes,1,100,12)),bodies=Math.round(number(input.bodies,2,256,32)),steps=Math.round(number(input.steps,1,240,8)),cellSize=number(input.cellSize,.05,100,1),mismatches=[],allPairPeak=0,hashPairPeak=0;function random(){seed=(Math.imul(seed,1664525)+1013904223)>>>0;return seed/4294967296;}for(var scene=0;scene<scenes;scene++){var base=createWorld({gravity:{x:0,y:0},fixedDelta:1/120,solverIterations:6,maxSubsteps:4,sleep:{enabled:false},bounds:{minX:0,minY:0,maxX:16,maxY:9,restitution:.6},broadphase:{mode:'all-pairs',cellSize:cellSize}});for(var i=0;i<bodies;i++){var circle=random()>.45,extent=.08+random()*.3,body={id:'s'+scene+'-b'+String(i).padStart(3,'0'),type:'dynamic',shape:circle?{kind:'circle',radius:extent}:{kind:'box',halfWidth:extent,halfHeight:.08+random()*.3},position:{x:.5+random()*15,y:.5+random()*8},velocity:{x:(random()-.5)*5,y:(random()-.5)*5},mass:.25+random()*3,restitution:random(),friction:random(),linearDamping:0};base=addBody(base,body).world;}var reference=clone(base),candidate=clone(base);candidate.broadphase={mode:'spatial-hash',cellSize:cellSize};for(var s=0;s<steps;s++){reference=step(reference).world;candidate=step(candidate).world;allPairPeak=Math.max(allPairPeak,reference.diagnostics.broadphasePairs);hashPairPeak=Math.max(hashPairPeak,candidate.diagnostics.broadphasePairs);var a=(reference.contactKeys||[]).join(','),b=(candidate.contactKeys||[]).join(',');if(a!==b||checksum(reference)!==checksum(candidate)){mismatches.push({scene:scene,step:s+1,referenceContacts:a,candidateContacts:b,referenceChecksum:checksum(reference),candidateChecksum:checksum(candidate)});break;}}}return{schema:'axm.physics-broadphase-equivalence/v1',ok:mismatches.length===0,seed:Math.round(number(input.seed,1,4294967295,1096305973))>>>0,scenes:scenes,bodiesPerScene:bodies,stepsPerScene:steps,cellSize:cellSize,mismatches:mismatches.slice(0,20),allPairsPeak:allPairPeak,spatialHashPeak:hashPairPeak,pairReductionAtPeak:allPairPeak?round(1-hashPairPeak/allPairPeak):0,truth:{allPairsIsReference:true,scientificValidation:false}};}
  function validate(world){var errors=[];if(!world||world.schema!==WORLD_SCHEMA)errors.push('world schema must be '+WORLD_SCHEMA);if(!world||!Array.isArray(world.bodies))errors.push('bodies must be an array');if(world&&Array.isArray(world.bodies)){if(world.bodies.length>10000)errors.push('body limit exceeded');var ids={};world.bodies.forEach(function(b){if(!b.id)errors.push('body id required');else if(ids[b.id])errors.push('duplicate body id '+b.id);ids[b.id]=true;if(!b.shape||['circle','box'].indexOf(b.shape.kind)<0)errors.push('unsupported shape on '+b.id);});}if(world&&world.bounds&&!(world.bounds.maxX>world.bounds.minX&&world.bounds.maxY>world.bounds.minY))errors.push('world bounds must have positive size');return{ok:!errors.length,errors:errors,warnings:['This early 2D engine is suitable for prototypes and game logic; it is not a validated scientific solver.']};}
  return{VERSION:VERSION,WORLD_SCHEMA:WORLD_SCHEMA,BODY_SCHEMA:BODY_SCHEMA,RESULT_SCHEMA:RESULT_SCHEMA,createWorld:createWorld,addBody:addBody,removeBody:removeBody,applyForce:applyForce,applyImpulse:applyImpulse,setVelocity:setVelocity,step:step,simulate:simulate,simulateTrace:simulateTrace,queryPoint:queryPoint,raycast:raycast,compareBroadphases:compareBroadphases,measure:measure,checksum:checksum,validate:validate};
});
