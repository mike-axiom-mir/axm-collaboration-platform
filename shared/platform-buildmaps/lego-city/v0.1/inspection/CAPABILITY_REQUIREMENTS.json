{
  "requirements": [
    {
      "id": "phase0-compile",
      "capabilities": [
        "filesystem.scan.readonly",
        "json.schema.validate",
        "graph.compile.deterministic"
      ],
      "required": true
    },
    {
      "id": "phase0-proof",
      "capabilities": [
        "test.negative.deterministic",
        "git.diff.review"
      ],
      "required": true
    },
    {
      "id": "all-local-blocks",
      "capabilities": [
        "artifact.store.local",
        "event.journal.local",
        "authority.decide.typed",
        "executor.registry.measured",
        "workflow.resume.local",
        "evidence.correlate.local",
        "state.merge.typed",
        "surface.twin.compile",
        "intake.quarantine.local"
      ],
      "required": true
    },
    {
      "id": "external-gates",
      "capabilities": [
        "network.adapter.disabled",
        "network.write.authorized",
        "public.attestation.verify"
      ],
      "required": false
    }
  ]
}
