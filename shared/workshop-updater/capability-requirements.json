{
  "requirements": [
    { "id": "off-means-off", "capabilities": ["workshop.update.network.explicit-opt-in"], "required": true },
    { "id": "canonical-check", "capabilities": ["workshop.update.github.commit-pinned-read"], "required": true },
    { "id": "trusted-stage", "capabilities": ["workshop.update.archive.signed-hash-stage"], "required": true },
    { "id": "heartbeat-timing", "capabilities": ["workshop.update.heartbeat.due-check"], "required": true },
    { "id": "pulse-budget", "capabilities": ["workshop.update.body-pulse.one-lease"], "required": true },
    { "id": "whole-install", "capabilities": ["workshop.update.install.transactional"], "required": true },
    { "id": "whole-rollback", "capabilities": ["workshop.update.rollback.whole-workshop"], "required": true }
  ]
}
