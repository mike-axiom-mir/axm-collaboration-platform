{
  "capabilities": [
    { "id": "workshop.update.network.explicit-opt-in", "status": "available", "constraints": ["disabled by default", "no network adapter call while disabled"] },
    { "id": "workshop.update.github.commit-pinned-read", "status": "available", "constraints": ["canonical repository and main ref only", "bounded GitHub API and raw-manifest reads"] },
    { "id": "workshop.update.archive.signed-hash-stage", "status": "degraded", "constraints": ["staging engine is implemented", "no Ed25519 trust root is promoted yet", "512 MiB maximum", "state staging only"] },
    { "id": "workshop.update.heartbeat.due-check", "status": "available", "constraints": ["scheduled beats only", "configured interval", "Hub must be running"] },
    { "id": "workshop.update.body-pulse.one-lease", "status": "available", "constraints": ["one bounded lease per due or explicit check", "Pulse STOP, REST, RED pressure, and capacity holds prevent network use"] },
    { "id": "workshop.update.install.transactional", "status": "unavailable", "constraints": ["whole-Workshop conflict merge and restart-safe apply hand not built"] },
    { "id": "workshop.update.rollback.whole-workshop", "status": "unavailable", "constraints": ["whole-Workshop generation restore hand not built"] }
  ]
}
