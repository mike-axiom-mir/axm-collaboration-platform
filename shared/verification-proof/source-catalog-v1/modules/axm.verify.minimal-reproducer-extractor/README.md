# Minimal Reproducer Extractor

Detached AXM Verification & Proof organ for seed 029.

It uses deterministic delta debugging plus a one-item removal pass to reduce a supplied failing input. It never runs external commands itself, stops at a hard evaluation budget, and distinguishes a preserved failure from proof of a globally smallest reproducer.

Status: TEST-HOLD, detached, v0.1.0.
