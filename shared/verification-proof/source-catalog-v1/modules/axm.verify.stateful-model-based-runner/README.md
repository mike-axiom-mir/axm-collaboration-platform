# Stateful Model-Based Test Runner

Detached AXM Verification & Proof organ for seed 033.

Enumerates bounded valid action sequences from a deterministic declared state machine and compares a caller-supplied implementation step against model state after every transition. It returns the first trace mismatch and preserves the explored depth and sequence ceiling.

Status: TEST-HOLD, detached, v0.1.0.
