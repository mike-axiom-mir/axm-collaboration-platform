# Golden Fixture Registry

Detached AXM Verification & Proof organ for seed 026.

It keeps valid, invalid, edge, legacy, adversarial, and recovery fixtures as immutable metadata plus locally held bytes, verifies later bytes against the reviewed digest, and exports a deterministic manifest without granting the fixture authority beyond its declared claims.

Status: TEST-HOLD, detached, v0.1.0.
