{
  "contract_version": "0.1.0",
  "module_id": "axm.verify.canonical-evidence-serializer",
  "purpose": "Normalize a bounded JSON-compatible evidence value and serialize it deterministically so its digest can be recomputed across handoff and restoration.",
  "inputs": {
    "value": "bounded JSON-compatible evidence value; string mapping keys; no floats or bytes"
  },
  "outputs": {
    "serialization_report": "canonical bytes, digest, size, and normalization profile"
  },
  "refusals": [
    "floating-point numbers",
    "non-string keys",
    "Unicode-normalized key collisions",
    "bytes or unsupported object types"
  ],
  "non_authority": [
    "does_not_prove_semantic_truth",
    "does_not_sign_content",
    "does_not_encrypt_content",
    "does_not_promote_canon"
  ],
  "dependencies": "Python standard library only"
}
