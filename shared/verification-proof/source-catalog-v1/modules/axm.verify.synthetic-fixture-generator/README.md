# Synthetic Fixture Generator

Detached AXM Verification & Proof organ for seed 027.

It recursively generates objects, arrays, strings, numbers, booleans, enums, and synthetic identity-shaped values from a declared schema using a private seeded generator. It rejects source-value copying and marks every output as synthetic.

Status: TEST-HOLD, detached, v0.1.0.
