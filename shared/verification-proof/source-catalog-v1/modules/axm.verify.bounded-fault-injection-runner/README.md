# Bounded Fault-Injection Runner

Detached AXM Verification & Proof organ for seed 040.

Runs a finite caller-supplied fault executor against declarative fault cases, enforces case and abort ceilings, and keeps operation failure, recovery failure, execution error, truncation, and non-run states visible.

Status: TEST-HOLD, detached, v0.1.0.
