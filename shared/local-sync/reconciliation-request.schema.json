{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "axm.reconciliation-request/v1",
  "type": "object",
  "required": ["schema", "namespace", "reason", "localDigest", "remoteDigest", "ruleDigest", "requiresHuman", "grantsAuthority", "requestDigest"],
  "additionalProperties": false,
  "properties": {
    "schema": { "const": "axm.reconciliation-request/v1" },
    "namespace": { "type": "string" },
    "reason": { "type": "string" },
    "localDigest": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "remoteDigest": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "ruleDigest": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "requiresHuman": { "const": true },
    "grantsAuthority": { "const": false },
    "requestDigest": { "type": "string", "pattern": "^[a-f0-9]{64}$" }
  }
}
