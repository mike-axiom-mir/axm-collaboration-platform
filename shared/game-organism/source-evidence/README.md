# Game Organism source evidence

This directory contains the bounded, reviewable evidence needed by the
Workshop's existing Game Organism adapters after intake processing.

- `audio-music-run106/module-registry.json` preserves the 100-module static
  audio registry and its explicit dependency holds.
- `cartoon-3d-run100/organ-registry.json` preserves the 100-organ TEST-HOLD
  registry.
- `sim-living-run102/` preserves the static registry, derived wiring packet,
  and trusted Workshop wiring self-test.

The large source corpora, generated histories, audio assets, nested packages,
and supplier runtimes remain in the recoverable local intake archive. Nothing
in this directory grants activation, external effects, promotion, or CANON.

