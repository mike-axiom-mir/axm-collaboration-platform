{
  "schema": "axm.foundation-service-plane/v1",
  "purpose": "Permanent infrastructure underneath every AXM workspace",
  "rules": [
    "All nine services remain represented even while offline or degraded",
    "Dashboard visibility never changes service lifecycle",
    "No service has a retire or uninstall control in this plane",
    "Controls require explicit user action and report their real result",
    "Installed per-workspace foundations may report AVAILABLE instead of falsely reporting OFFLINE"
  ],
  "services": ["wisdom", "identity", "gate", "storage", "connectors", "guardian", "plugins", "backup", "runtime"]
}
