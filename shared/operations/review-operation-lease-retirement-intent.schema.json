{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "axm.review-operation-lease-retirement-intent/v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "status", "state", "retirementId", "recordedAt", "ownerDigest", "ownerEvidenceValid", "observedProcessId", "observedAcquiredAt", "callerAssertion", "reason", "confirmationMatched", "truth"],
  "properties": {
    "schema": { "const": "axm.review-operation-lease-retirement-intent/v1" },
    "status": { "const": "TEST" },
    "state": { "const": "AUTHORIZED_PENDING_RETIREMENT" },
    "retirementId": { "type": "string", "format": "uuid" },
    "recordedAt": { "type": "string", "format": "date-time" },
    "ownerDigest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
    "ownerEvidenceValid": { "type": "boolean" },
    "observedProcessId": { "type": ["integer", "null"], "minimum": 1 },
    "observedAcquiredAt": { "type": ["string", "null"], "format": "date-time" },
    "callerAssertion": { "const": "HOLDER_TERMINATED_OR_ABANDONED" },
    "reason": { "type": "string", "minLength": 20, "maxLength": 500 },
    "confirmationMatched": { "const": true },
    "truth": {
      "allOf": [
        { "$ref": "review-operation-lease-retirement-plan.schema.json#/$defs/truth" },
        { "properties": { "ownerEvidenceQuarantined": { "const": false } } }
      ]
    }
  }
}
