# Runtime Channel Observatory focused test report

Status: `PASS` for 29 focused fixture and live Workshop checks. Candidate remains detached `EXPERIMENTAL`.

- All 282 unique graph-bounded HTML/JavaScript sources matched their graph hashes.
- The live map contains 40 static communication observations and four named channel groups.
- It observed three CustomEvent patterns, 15 selected environment-event consumers, and 22 postMessage calls.
- Three named seams request review. One four-question packet selects the `message` environment event across ten source ownership scopes.
- Fixture checks cover BroadcastChannel constants, dynamic holds, paired and producer-only CustomEvent patterns, selected listeners, postMessage, MessageChannel, WebSocket/EventSource text, source drift, and deterministic receipts.
- Browser visual judgment is `NOT_RUN`.
- No browser/channel/socket/stream was opened; no message/event was sent; no delivery, protocol compatibility, install, permission, promotion, GitHub, or CANON claim is made.
