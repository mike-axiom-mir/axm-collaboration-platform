{
  "capabilities": [
    { "id": "git-pr.checkpoint.verify", "status": "available", "constraints": ["exact READY checkpoint plus PASS verification"] },
    { "id": "git.remote.refs.read-exact", "status": "available", "constraints": ["approved remote configured in an isolated worktree"] },
    { "id": "git.merge-tree.analyze-exact", "status": "available", "constraints": ["planning may write immutable unreachable Git objects"] },
    { "id": "git.feature-branch.merge-local-no-rewrite", "status": "available", "constraints": ["exact confirmation; codex/ or agent/ branch; clean publish lane"] },
    { "id": "git.failed-merge.abort-and-prove", "status": "available", "constraints": ["typed RECOVERY_REQUIRED if exact restoration cannot be proved"] },
    { "id": "git.refresh.post-state.verify", "status": "available", "constraints": ["tree, parents, branch, cleanliness, ancestry, and remote refs"] },
    { "id": "git.remote-write.refuse", "status": "available", "constraints": ["no push or GitHub write command exists in the adapter"] }
  ]
}
