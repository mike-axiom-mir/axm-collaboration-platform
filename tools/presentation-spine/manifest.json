{
  "schema": "axm.tool-manifest/v1",
  "kind": "product",
  "id": "presentation-spine",
  "name": "Presentation Spine",
  "version": "v0.3.0",
  "status": "TEST",
  "entry": "index.html",
  "contract": "module.contract.json",
  "type": "hub-module",
  "hubApiVersion": "1.0",
  "category": "CREATE",
  "layer": "create",
  "audience": "human-machine",
  "risk": "LOW",
  "tags": ["presentation", "ui", "profiles", "recipe", "skin", "migration", "scanner", "local", "deterministic"],
  "uses": ["visual-kernel", "filesystem-read"],
  "permissions": [],
  "actions": ["preview four shared presentation profiles", "compose and explicitly apply presentation recipe layers", "resolve bounded module screen contracts", "import or export a bounded presentation recipe", "inspect a deterministic read-only migration report"],
  "accepts": ["axm.presentation-spine.profiles/v1", "axm.presentation-recipe/v1", "axm.screen-contract/v1", "axm.presentation-spine.migration-report/v1"],
  "produces": ["axm.presentation-recipe/v1", "axm.screen-contract/v1", "axm.presentation-spine.migration-report/v1"],
  "readiness": ["local-only", "reduced-motion", "read-only-advice", "human-appearance-gate"],
  "summary": "Shared 2030 presentation vocabulary with profiles, portable skin-recipe layers and a deterministic read-only migration map.",
  "notes": "Profiles and recipes change appearance only. Recipe application is explicit; the scanner recommends but never rewrites, approves or promotes a host surface."
  ,"presentation": {
    "schema": "axm.presentation-policy/v1",
    "defaultMode": "shared",
    "sharedProfile": "lab",
    "moduleVisual": true,
    "screenPreset": "creative",
    "editableLayers": ["surface", "depth", "motion", "density", "signal"],
    "modeEditable": true
  }
}
