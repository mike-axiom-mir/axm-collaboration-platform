# Native mobile adapter

## iOS / iPadOS

Use semantic text styles. For a custom family, scale it relative to a semantic role instead of using fixed point sizes. Test every accessibility text size and allow multiline growth.

## Android

Use `sp`, keep typography in the app theme, and test large font scaling plus RTL pseudolocales. Avoid fixed-height text boxes.
