'use strict';const assert=require('assert'),fs=require('fs'),path=require('path');const d=__dirname,m=JSON.parse(fs.readFileSync(path.join(d,'manifest.json'))),c=JSON.parse(fs.readFileSync(path.join(d,'module.contract.json'))),a=fs.readFileSync(path.join(d,'app.js'),'utf8');assert.equal(m.id,'mirror-intake-monitor');assert.equal(m.schema,'axm.tool-manifest/v1');assert.equal(m.kind,'product');assert.equal(m.integratedInto,'cognitive-resource-meter');assert.deepStrictEqual(m.permissions,c.permissions);assert(c.boundaries.refuses.includes('absolute-mirror-path'));assert(c.boundaries.refuses.includes('mirror-write'));assert(a.includes('load an exact Workshop draft first'));console.log('Mirror Intake Monitor selftest: PASS');
