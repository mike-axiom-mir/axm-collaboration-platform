{
  "schema": "axm.tool-manifest/v1",
  "id": "asset-filesystem-service", "name": "AXM Asset Filesystem Service", "version": "v0.2", "status": "TEST", "entry": "index.html", "kind": "service", "contract": "module.contract.json", "type": "hub-module", "hubApiVersion": "1.0", "category": "CREATE", "layer": "create", "audience": "human-machine", "risk": "MEDIUM",
  "tags": ["assets", "filesystem", "watcher", "hashes", "duplicates", "packs", "retrieval"], "uses": ["files", "storage", "export", "asset-vault"], "permissions": [],
  "actions": ["index the Workshop asset filesystem", "find duplicate assets by hash", "retrieve an indexed asset", "build a verified asset pack"],
  "accepts": ["axm.asset-selection/v1", "axm.asset-record/v1"],
  "produces": ["axm.asset-index/v1", "axm.asset-pack/v1"],
  "readiness": ["files", "storage", "export"],
  "summary": "Index the real assets folder, watch for changes, detect duplicates, retrieve files and build verified local ZIP asset packs with AXM's dependency-free archive engine.",
  "notes": "The service is the filesystem body beneath Asset Vault. It reads only assets, never scans arbitrary user folders, and pack creation never alters sources or requires PowerShell, an archive package, or another executable."
}
