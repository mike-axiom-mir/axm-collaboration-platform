# Capability scout receipt

Status: **DEGRADED** overall; **READY** for the requested private-local
EXPERIMENTAL intake.

## Satisfied route

- The delivered ZIP is preserved and content-bound for this intake.
- Two useful portable patterns have bounded local implementations.
- Deterministic and live-browser proof surfaces are available.
- Installation, promotion, execution, root change, permanent merge, and CANON
  remain disabled.

## Exact gaps

1. `archive.content-ledger.verify.general`
   - Gap type: `HAND`
   - Current state: Archive Intake Cartographer deliberately stops at
     central-directory evidence. This intake used a one-off independent
     checksum-ledger verifier instead of silently expanding that shared organ.
   - Cheapest closure test: a separately reviewed read-only verifier must reject
     one mismatched digest, one unlisted file, one unsafe normalized path, and
     one compressed-content corruption fixture.

2. `authority.errol.commercial-rights.confirmed`
   - Gap type: `AUTHORITY`
   - Current state: unavailable. It cannot be closed by code or tests.
   - Cheapest closure evidence: a current, attributable permission record from
     Errol covering the intended public/commercial use.

The comparator's proposed-hand entry for the second identifier is mechanical;
the real route is contributor authority, not a new software hand.
