# Module One Stable Anchor Repair Request

```text
/returncore /soulcheck /mergegate

Module 2 independently consumed:

AXM_HUMAN_CAPABILITY_ATLAS_STABLE_HANDOFF_ANCHOR_v0_1_0

The anchor is useful and its package integrity, identities, AXM-CJ-1 vectors, fixture identities, and export guarantees passed. The paired merge remains blocked only on stable shared-boundary conflicts.

Do not freeze or redesign mutable Atlas explanations, teaching logic, categories, search, internal architecture, or capability coverage.

Create an additive repaired anchor package. Preserve the old anchor unchanged for audit history.

Required stable repairs:

1. Consume the exact standalone shared contract package:

AXM_CAPABILITY_INTERFACE_SHARED_CONTRACT_v0_1_0

Do not recreate its schemas from memory or from Atlas-local copies.

Expected canonical schema hashes currently held by Module 2:

- capability-interface-contract.schema.json
  cf5b3d94f6dafcd9b1f2d93e7af9219db5c85f57931ecae7481f27924929a644

- evidence-annotation.schema.json
  d6cfdcfbc9b7da03e6eb55a269a8d5746109db23d038bc91b93cd931a73736fe

- interface-recommendation.schema.json
  90ca0bb938fba5595ef29e8fab8ca18a7c91e1b46b14000456525230a40f4062

2. Use the exact v0.1.0 evidence-state serialization accepted by those schema files:

KNOWN
INFERRED
UNKNOWN
CONFLICTED
NOT_APPLICABLE

Do not silently translate or relabel them.

3. Remove `knowledge` from the claim that it is a shared-contract-owned v0.1.0 field unless it exists in the exact standalone schema.

It may remain an Atlas-internal or proposed future field, but it may not be presented as current shared contract structure.

4. Keep existing v0.1.0 inference metadata limited to what the canonical evidence annotation can represent:

- path
- state
- reasoning
- source_basis
- confidence

Evidence-reference and producer/build identity are valuable proposals, but they are not representable as additional evidence-annotation fields under current additionalProperties=false. Record them as a shared-contract change proposal rather than a stable v0.1.0 guarantee.

5. Rerun the same ten stable fixtures using the exact shared contract.

Report separately:

- shared-schema validation;
- stable invariant validation;
- current evidence-policy validation;
- producer execution state;
- source and output hashes.

6. Include all three exact canonical shared schema hashes in the repaired manifest.

The Atlas source-declaration schema may remain module-specific and separately fingerprinted. Do not label it as a canonical shared-contract schema unless the independent contract package actually owns it.

7. Exclude __pycache__ and .pyc files from the package.

8. Keep the current module identity, capability IDs, provenance rules, fixture identities, and AXM-CJ-1 rules stable unless a genuine conflict is explicitly reported.

Do not claim Module 2 ran.
Do not claim local integration.
Do not silently upgrade six currently policy-nonconformant fixture inferences.
Do not change the standalone contract version without a formal shared change package and migration plan.

Return:

- repaired standalone anchor ZIP;
- validation report;
- exact file inventory;
- unresolved conflicts;
- honest producer execution states.
```
