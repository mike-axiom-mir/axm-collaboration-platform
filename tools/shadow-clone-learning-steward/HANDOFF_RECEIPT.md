# Shadow Clone Learning Steward handoff receipt

Receipt date: 2026-08-22  
Status: **EXPERIMENTAL**  
Branch: `codex/shadow-clone-learning-module-20260822`  
Frozen Workshop base: `c6e7909267f51a6fa14395e46d6917678ef87d06`

This is append-only closeout evidence for the first Workshop integration. A
later correction should append or supersede this receipt explicitly rather
than silently rewriting its claims.

## Result

The Workshop now has a reviewable machine-audience module that:

- accepts only Mike-authorized, compact, redacted task snapshots;
- preserves observations, user feedback, candidate lessons, dissent,
  proposals, verification, and inheritance decisions as separate stages;
- emits explicit anti-drift shadow-clone pickup profiles;
- emits review-only private Mirror lesson proposals;
- imports digest-valid compact Grand Garden ledgers without a runtime
  dependency on the Garden worktree;
- exposes an inert-by-default periodic provider adapter;
- scores a predeclared held-out second-value trial without converting a
  synthetic score into a broad 2x claim;
- returns typed missing-capability or missing-evidence receipts instead of
  faking a live task provider, Mirror intake, or measured multiplier.

The module does not modify model weights, copy a human identity, claim
consciousness, rewrite a prompt silently, message another task, change
permissions, write a parent or canonical checkout, merge, promote, inherit,
or canonize.

## Changed files

- `tools-index.json`
- `tools/shadow-clone-learning-steward/CODE_CAPABILITY_FABRIC_INTERFACE_PROPOSAL.json`
- `tools/shadow-clone-learning-steward/HANDOFF_RECEIPT.md`
- `tools/shadow-clone-learning-steward/README.md`
- `tools/shadow-clone-learning-steward/app.js`
- `tools/shadow-clone-learning-steward/cli.js`
- `tools/shadow-clone-learning-steward/discovery-seam-review.js`
- `tools/shadow-clone-learning-steward/fixtures/synthetic-compact-snapshot-set.example.json`
- `tools/shadow-clone-learning-steward/fixtures/synthetic-value-trial.example.json`
- `tools/shadow-clone-learning-steward/index.html`
- `tools/shadow-clone-learning-steward/lib/periodic-compact-snapshot-adapter.js`
- `tools/shadow-clone-learning-steward/lib/shadow-clone-learning-core.js`
- `tools/shadow-clone-learning-steward/manifest.json`
- `tools/shadow-clone-learning-steward/module.contract.json`
- `tools/shadow-clone-learning-steward/schemas/anti-drift-pickup.schema.json`
- `tools/shadow-clone-learning-steward/schemas/compact-snapshot-set.schema.json`
- `tools/shadow-clone-learning-steward/schemas/mirror-private-lesson-proposal.schema.json`
- `tools/shadow-clone-learning-steward/schemas/scored-value-trial.schema.json`
- `tools/shadow-clone-learning-steward/selftest.js`
- `tools/shadow-clone-learning-steward/styles.css`

The public discovery registry was deliberately not changed. The module is
registered in the local generated `tools-index.json`, where connected AI can
discover an EXPERIMENTAL machine capability without publishing it.

## Focused module evidence

- `node tools/shadow-clone-learning-steward/selftest.js`: **PASS, 91 checks**.
- `node tools/shadow-clone-learning-steward/discovery-seam-review.js`:
  **PASS**.
- JavaScript syntax checks for the CLI, UI, core, and adapter: **PASS**.
- JSON parse checks for manifests, contracts, schemas, proposal, and synthetic
  fixtures: **PASS**.
- `git diff --check`: **PASS**.
- committed-literal private path / provider-id scan: **PASS**.

The focused suite covers spoofed authorization, unallowlisted and altered
source digests, verification/source mismatch, stale observations, exact
duplicates, incompatible patterns, direct contradiction, raw/private fields,
machine paths, unauthorized inheritance, replay path overlap, replay drift,
empty/fake learning, missing provider capability, provider-output binding,
explicit periodic start/stop, primary-score arm shifting, and underpowered
value trials.

## Workshop required checks

All ten required commands were run from the isolated Workshop worktree.

- `node verify.js`: **PASS** with existing backlog warnings.
- `node hub/hub-selftest.js`: **FAIL**, three existing unrelated gaps:
  `radio: station source map is incomplete`,
  `growth: isolated incremental measurement reuse is not wired`, and
  `visual stewardship: responsive command-bar polish missing`.
- `node hub/route-selftest.js`: **PASS**.
- `node hub/graft-selftest.js`: **PASS**.
- `node hub/skin-selftest.js`: **PASS**.
- `node hub/verify-plus.js`: **PASS** with existing backlog warnings.
- `node tests/html-script-syntax-test.js`: **PASS, 53 pages / 0 failures**.
- `node tests/tool-forge-package-test.js`: **PASS**.
- `node tools/agent-tool-forge/selftest.js`: **PASS, 15 / 0**.
- `node tools/evidence-desk/selftest.js`: **PASS, 36 / 0**.

The integration diff touches neither radio, growth, nor the command bar. Those
three Hub failures are retained as baseline failures; 9/10 required commands
passed.

## Garden evidence and moving-workspace warning

The committed Garden suites relevant to this lane passed **31/31**:

- descendant Garden fabric: 6/6;
- permissioned actor turns: 5/5;
- bounded instance learning: 10/10;
- instance-learning steward: 5/5;
- compact active-instance ingress: 5/5.

The Garden branch remained at `41c181279a342cb1d52df001e2749291b6e10cc8`
during the recorded comparison, but its dirty status changed from six to nine
entries while tests ran. Classification: **MOVING_WORKSPACE**. A foreign
uncommitted full-return suite also reported 5/5, but it is excluded from the
authoritative 31-test total because it was not part of the frozen committed
learning lane.

## Separate browser render/click evidence

Visual backend: `BROWSER_PRIMARY`.

- Desktop 1280 x 720: route and pickup views rendered; click changed exactly
  one visible panel; no horizontal overflow; no console warning/error.
- Mobile 390 x 844: navigation wrapped; all buttons remained 44 px high;
  authority panel remained readable; no horizontal overflow.
- Value-trial view exposed the bounded formula and synthetic-evidence warning.
- Temporary viewport override was reset, the test tab was closed, and the
  local server was stopped. No screenshot or rolling-video archive was kept.

This proves only the named render and click behavior. It does not prove
persistence, provider transport, learning quality, or value amplification.

## Live compact pilot and local retention

A previously authorized compact Garden pilot was imported locally through the
new compatibility seam. No raw task histories were read or copied.

- source Garden ledger digest:
  `955bd47813c2d5fab707b6665b2fc35c26978df3a7d7238bf124fd7b77815880`;
- Workshop import ledger digest:
  `e21846309ff075109312d7d45e2b0e3efa5d5156f33c0e3ab8eb8da2af8b2ad2`;
- held pickup digest:
  `9eb64d8f96dd0295c93e8637c7a6217847920ae65d23da5a31d6728f70d2e493`;
- trial-readiness receipt digest:
  `2332aa81161c80c750a2dc5238408dba1c1689bbf6ab8051391ae929f13081bf`.

The pilot reported seven active aliases, secondary reuse for all seven, four
cross-instance lessons, one contested lesson, and zero measured primary task
outcomes. The Workshop pickup selected
`integration.review-first-busy-canonical` but remained
`HOLD_CANDIDATES_AWAIT_VERIFICATION`, with zero active instructions. The
`stability.claim-timing` dissent travelled with it.

Local-only evidence IDs:

- `workshop-module-import-20260822-01`;
- `workshop-module-import-20260822-01-pickup`;
- `workshop-module-trial-readiness-20260822-01`.

They live under the operator-selected local instance-learning evidence root,
are not committed, contain no raw task data, and inherit the source retention
end `2026-09-05T19:30:00.000Z`. Automatic deletion is not implemented; the
local evidence owner must remove them after review or expiry.

## Mutation proof

Before and after the Workshop test sweep, the busy canonical checkout stayed
at `c6e7909267f51a6fa14395e46d6917678ef87d06`, with 4,039 status entries and
the same compact status digest
`10c991057b4685fd46c0f5584c202ff2676720f9ee5a336fc16e1d792a532c4d`.
The learning run recorded zero parent writes and zero canonical writes.

## Honest limits and what remains unrun

- No installed Codex compact-task provider exists in the frozen Workshop
  base, so no real periodic timer was left running. The adapter was exercised
  with a synthetic conforming provider and with the typed missing-capability
  route.
- No held-out live baseline/learning-route pairs have been scored. The live
  readiness state is `MISSING_MEASUREMENT_EVIDENCE`; 2x is not proven.
- No Mirror private lesson intake provider was invoked. The current live
  candidate lacks imported detached verification and remains held.
- No model weights were trained, no prompt was rewritten, and no other task
  was messaged, interrupted, or redirected.
- No automatic merge, promotion, inheritance, canonical integration, public
  publication, or CANON decision was attempted.
- The module is a repository/process boundary, not OS-grade process isolation.

## Safe integration route and Mike-owned decisions

1. Review the branch diff and this receipt from a clean Workshop descendant.
2. Rebase or recompare against the intended target if Workshop HEAD has moved.
3. Cherry-pick the integration commit into a clean review branch; do not use
   the busy canonical checkout as the application surface.
4. Regenerate local tools discovery and rerun the focused suite plus all ten
   Workshop checks.
5. Keep the module EXPERIMENTAL unless Mike explicitly makes a separate status
   decision.

Mike still owns selection of a trusted compact-task provider, the periodic
schedule and retention owner, the held-out value rubric and evaluators, each
anti-drift pickup, every Mirror lesson intake, all inheritance and merge
decisions, publication, promotion, and CANON.

## Append-only continuation: authorized periodic compact pilot

Mike subsequently authorized the periodic compact-snapshot adapter and a
measured second-value trial while explicitly retaining EXPERIMENTAL status.
This section supersedes only the earlier statement that no real periodic timer
was left running; all authority and inheritance boundaries above remain in
force.

One immediate host-supplied compact cycle observed five aliased AXM build,
test, and steward tasks through task-list summaries and immediate compact
status snapshots only. It did not open task histories or message, interrupt,
or redirect any task. Before local persistence, task identifiers and paths
were removed and the summaries were reduced to synthetic aliases and
digest-bound statements.

- live learning ledger digest:
  `95d788403bd44c30c226e0d3d3514538372122aa6201e6998fb7e5dc2f29095c`;
- clean replay receipt digest:
  `077dc52cfe9272e7aef3087be6c3f7ab320b6897ee97675ddf87d5698444e97e`;
- held connected-AI pickup digest:
  `d30f225822839504220501d46fcf6fad4863628fda998eb504489e606608ddce`;
- bounded heartbeat schedule receipt digest:
  `aae7318c260f36796344635090c5e922d51e37e5866a168c20b48b6a09bbd6bd`;
- prospective held-out trial plan digest:
  `4ecfeb430344ae0d6cba143b3e58897df0b50eeb3d5dc751eae51d7569721b50`.

The cycle produced two proposed lessons: a high-confidence pattern that
reference material and structural checks are bounded evidence rather than
authority, and a medium-confidence pattern that structurally valid outputs
remain retention-bounded candidates until authenticated human acceptance. It
also retained incompatible timing advice about whether a complete shared-tree
inventory must finish before isolated lane work continues. The disagreement
was not averaged away.

Both proposed lessons remain `NOT_YET_VERIFIED`. The connected-AI pickup is
therefore `HOLD_CANDIDATES_AWAIT_VERIFICATION`, contains zero active lessons,
marks both candidates `PROPOSAL_ONLY_DO_NOT_APPLY`, and carries the contested
timing evidence. No Mirror proposal or inheritance decision was emitted.

The host heartbeat `axm-shadow-clone-compact-learning-pilot` is active at a
60-minute interval with a finite eight-cycle count. Each wake is restricted to
one compact, redacted, local-only cycle and must emit a typed hold on missing
capability, insufficient relevant sources, retention failure, or replay drift.
It cannot read task histories, message tasks, inherit, integrate, or canonize.

The prospective value trial was frozen before future outcomes. It requires at
least three future held-out paired cases, the same primary score in both arms,
downstream verification for secondary value, and at least two evaluator
aliases. Stored or unverified text scores zero secondary value. It currently
contains no scores, remains
`AWAITING_THREE_HELDOUT_PAIRED_CASES`, and proves no multiplier.

New local-only evidence IDs are:

- `workshop-live-compact-cycle-20260822-02-control`;
- `workshop-live-compact-cycle-20260822-02`;
- `workshop-live-compact-cycle-20260822-02-replay`;
- `workshop-live-compact-cycle-20260822-02-pickup-control`;
- `workshop-live-compact-cycle-20260822-02-pickup`;
- `workshop-periodic-pilot-20260822-01`;
- `workshop-value-trial-prospective-20260822-01`.

These artifacts are not committed, contain no raw task data or task
identifiers, and expire no later than `2026-09-05T01:03:15.777Z`; the local
evidence owner must remove them after review or expiry. The pickup refreshes
earlier, at `2026-08-23T01:02:00.000Z`.

Continuation verification repeated the 91-check focused adversarial suite and
the discovery-seam review: both passed. Privacy and digest validation passed
for six core local artifacts, and clean replay reproduced the live ledger
digest. The module worktree remained clean at
`2a9aeda668d5f2a361047b5d8aa535a1335b2463` before this receipt-only append.
The canonical Workshop HEAD remained
`c6e7909267f51a6fa14395e46d6917678ef87d06` with 4,039 status entries, but its
status digest changed from the earlier frozen receipt while other tasks were
active. Classification: **MOVING_WORKSPACE**. The continuation made no writes
to that checkout.

The measured trial, replay-bound verification of either live candidate,
reviewed pickup application, Mirror intake, and human inheritance decision
remain unrun. Mike still owns evaluator selection, each application or
inheritance decision, schedule cancellation or extension, integration into a
clean target branch, promotion, publication, and CANON.

## Append-only continuation: cumulative cycle continuity

The next bounded collection exposed a continuity gap in the first periodic
implementation: repeated provider calls compiled valid isolated ledgers, but
the adapter did not own and carry the prior ledger into the next cycle. That
would create repeated snapshots rather than cumulative learning. Commit
`64497451eae71fee88331c9b1ea519495243e105` repairs this seam.

Changed implementation files:

- `tools/shadow-clone-learning-steward/README.md`;
- `tools/shadow-clone-learning-steward/cli.js`;
- `tools/shadow-clone-learning-steward/lib/periodic-compact-snapshot-adapter.js`;
- `tools/shadow-clone-learning-steward/selftest.js`.

The CLI now accepts an optional, digest-validated `--previous-ledger`. It
rejects an input that already embeds a predecessor so two competing history
sources cannot be combined. The periodic adapter now owns its predecessor,
injects it after provider-boundary validation, records the predecessor digest
in the cycle result, and rejects provider attempts to supply or replace that
history. Tampered predecessor ledgers fail before compilation.

One new compact cycle observed four currently active aliased AXM tasks without
opening histories or messaging tasks. It was compiled through the new chained
CLI route against the prior live ledger.

- predecessor ledger digest:
  `95d788403bd44c30c226e0d3d3514538372122aa6201e6998fb7e5dc2f29095c`;
- cumulative two-cycle ledger digest:
  `7d7e52ce120e0df43ea75470959fc3f1657d4f856408536fd9bad25e290fccbc`;
- clean replay receipt digest:
  `dd45219e15e05f770bc16f79d1cf440338af1117b01cb229f8805c8a457bb22e`;
- cumulative held pickup digest:
  `7d813e1c7e186b956d08813b9b17ac6fdb043f809c088bfac70229f3f49ec1a0`;
- trial-cycle assessment digest:
  `042ce0b9ff374beea3384dea6c1f9c37cc46fa0ac99fa9eafd0b72dccef578ef`.

The new cycle added two medium-confidence proposals: separate buildable
deterministic capability from unavailable authority/execution before
implementation, and bind acceptance to current dynamic state so changed
preferences or evidence invalidate stale approval. It also preserved a new
visual-evidence disagreement: endpoint, source, screenshot, and digest proof
can establish visible state changes, but cannot establish smooth frame cadence
without rolling-frame evidence.

The cumulative pickup carries four unverified candidate lessons and two
contested seams, with zero active lessons. Its state remains
`HOLD_CANDIDATES_AWAIT_VERIFICATION`; every candidate remains
`PROPOSAL_ONLY_DO_NOT_APPLY`.

The frozen prospective trial rejected this cycle as
`NO_ELIGIBLE_HELDOUT_CASE`. The same tasks supplied the evidence that derived
the new candidates, no reviewed lesson was presented downstream, no
replay-bound implementation verification exists, and no evaluator supplied
digest-bound scores. Zero scores were recorded and no value multiplier was
claimed.

New local-only evidence IDs:

- `workshop-live-compact-cycle-20260822-03-control`;
- `workshop-live-compact-cycle-20260822-03`;
- `workshop-live-compact-cycle-20260822-03-replay`;
- `workshop-live-compact-cycle-20260822-03-pickup-control`;
- `workshop-live-compact-cycle-20260822-03-pickup`;
- `workshop-value-trial-cycle-assessment-20260822-02`.

They contain no raw task data or task identifiers, remain local-only, and
expire no later than `2026-09-05T01:17:01.377Z`; the earliest carried lesson
refresh boundary is `2026-08-23T01:02:00.000Z`.

Continuation verification:

- focused module self-test: **PASS, 101 checks**;
- discovery seam review: **PASS**;
- CLI and periodic-adapter syntax: **PASS**;
- `git diff --check`: **PASS**;
- all ten required Workshop commands rerun: **9/10 command pass**;
- `node hub/hub-selftest.js`: **FAIL** on the same three unrelated baseline
  gaps: incomplete radio source map, missing isolated growth-measurement reuse,
  and responsive command-bar polish;
- committed core Garden suites: **31/31 PASS**;
- committed full-return Garden suite: **5/5 PASS**.

The Garden snapshot stayed stable at
`3fbf77e2be7c46ef2cf7b5f4ec7e5912b9efe27b` with two preserved untracked
instance-learning value-experiment files. The canonical Workshop snapshot
stayed at `c6e7909267f51a6fa14395e46d6917678ef87d06` with 4,040 dirty entries and
the same status digest throughout this verification subwindow. Neither
checkout was edited, staged, merged, or promoted by this continuation.

The next honest value step remains a future recipient task that did not derive
the lesson, an explicitly reviewed replay-bound pickup, recipient outcome and
test receipts, and two evaluator aliases. Until that evidence exists, the
program is learning only at the candidate-evidence layer and remains
**EXPERIMENTAL**.

### Schedule continuity correction

After sealing the cumulative code and cycle evidence, the active heartbeat was
updated so its future instructions require the latest valid local ledger
through the digest-validated predecessor route. It must refuse ambiguous,
altered, expired, or capacity-exhausted continuity instead of silently
starting an independent history. The updated local schedule receipt digest is
`4340584cc4641b3d96abe849dc6ea377cbfa30dc61d2badb5e4edf7bd9abc323`
(`workshop-periodic-pilot-20260822-02`). The hourly interval, eight-cycle cap,
retention boundary, EXPERIMENTAL status, no-message rule, and human-only
inheritance gate are unchanged.

## Append-only continuation: source-novel cycle gate and third live cycle

Commit `1131a836cd4f2cc79301d4c243780fb80548e710` closes a second
continuity gap: a valid predecessor was chained, but unchanged compact source
receipts could still be compiled again and appear to refresh a candidate. The
compiler now retains cumulative source-digest history, derives evidence only
from source-digest-novel snapshots, and returns
`INSUFFICIENT_NOVEL_EVIDENCE` before creating an output directory when fewer
than two novel sources remain. Legacy ledgers migrate their bound source
digests into this history.

Changed implementation files:

- `tools/shadow-clone-learning-steward/README.md`;
- `tools/shadow-clone-learning-steward/lib/shadow-clone-learning-core.js`;
- `tools/shadow-clone-learning-steward/selftest.js`.

One immediate compact cursor poll found new revisions from three aliased AXM
build, test, or steward tasks. No task history was opened and no task was
messaged, interrupted, redirected, or identified in retained evidence. The
redacted cycle was compiled against the second live ledger and replayed
cleanly.

- predecessor ledger digest:
  `7d7e52ce120e0df43ea75470959fc3f1657d4f856408536fd9bad25e290fccbc`;
- cumulative three-cycle ledger digest:
  `b95682cd9a45ca3fe68e3f4d9fdb81bdbab1faf79c03228a3c11c305efeb6444`;
- run receipt digest:
  `7a625e9d8f56b431aed9c2fc8252eea10c4b2953f4b7081a58c9a3af979feecc`;
- clean replay receipt digest:
  `225a2d0ec022af0b573652afdaa2bf231f048ef44260135bfafc429897869cf3`;
- cumulative held pickup digest:
  `e8a798a38ad77c00898db9ec688cf4dba9de2b576ce2ebf7015a00b74ef31f5a`;
- trial-cycle assessment digest:
  `46b55e32a9d3126354b8d59e3ca1614521364afbd6ab0118c830efdd0bb77ee2`;
- novelty-gated schedule receipt digest:
  `57ad4078b022e8c74c8c82a3455ae206720d00dce96b4a8a17f515f8c6c6330b`.

The cycle produced one high-confidence candidate: when an assumption fails or
a public surface changes, inspect the existing receipt, inventory, and live
evidence before adding capability; preserve working producers and add only the
missing seam. It also retained an incompatible verification threshold:
historical receipt evidence can be enough for an additive contract-only
change with an untouched intake, while a changed public diagnostic requires
separate live verification, and inventory alone cannot prove handoff or viewer
quality. Those positions remain contested and were not averaged.

The cumulative pickup contains five unverified candidate lessons and three
contested seams, with zero active lessons. Its state is
`HOLD_CANDIDATES_AWAIT_VERIFICATION`; every candidate is
`PROPOSAL_ONLY_DO_NOT_APPLY`. Refeeding the same three source receipts against
the new ledger returned `INSUFFICIENT_NOVEL_EVIDENCE` with exit code 1 and
created no output directory. Repeated polls therefore do not consume a cycle
or refresh confidence.

The frozen trial again returned `NO_ELIGIBLE_HELDOUT_CASE`: the three source
tasks derived the lesson, no reviewed lesson was applied downstream, no
replay-bound implementation verification exists, and no evaluator supplied a
digest-bound score. Zero scores were recorded and no multiplier was claimed.

New local-only evidence IDs:

- `workshop-live-compact-cycle-20260822-04-control`;
- `workshop-live-compact-cycle-20260822-04`;
- `workshop-live-compact-cycle-20260822-04-replay`;
- `workshop-live-compact-cycle-20260822-04-pickup-control`;
- `workshop-live-compact-cycle-20260822-04-pickup`;
- `workshop-value-trial-cycle-assessment-20260822-03`;
- `workshop-periodic-pilot-20260822-03`.

They contain no raw task data, raw prompts, chat transcripts, task identifiers,
credentials, or machine-specific source paths. They remain local-only and
expire no later than `2026-09-05T01:31:12.443Z`; the earliest carried lesson
refresh boundary remains `2026-08-23T01:02:00.000Z`. The local evidence owner
must remove them after review or expiry.

Final continuation verification:

- focused module self-test: **PASS, 107 checks**;
- discovery seam review: **PASS**;
- syntax and `git diff --check`: **PASS**;
- deterministic cycle replay: **PASS**, observed ledger digest exactly matched
  `b95682cd9a45ca3fe68e3f4d9fdb81bdbab1faf79c03228a3c11c305efeb6444`;
- exact-source duplicate probe: **PASS**, typed hold and no output directory;
- all ten required Workshop commands: **9/10 command pass**;
- `node hub/hub-selftest.js`: **FAIL** on the same three unrelated baseline
  gaps: incomplete radio source map, missing isolated growth-measurement reuse,
  and responsive command-bar polish;
- committed Garden snapshot
  `3fbf77e2be7c46ef2cf7b5f4ec7e5912b9efe27b`: **44/44 core PASS** and
  **5/5 full-return PASS** from an isolated archive.

The first Garden test attempt observed 45 core passes but is excluded from
stable evidence because five tracked instance-learning steward files advanced
during its window. Classification: **MOVING_WORKSPACE**. Those changes and the
two untracked value-experiment files were preserved untouched. The canonical
Workshop also moved from 13,088 to 13,092 status entries during verification
while its HEAD remained `c6e7909267f51a6fa14395e46d6917678ef87d06`.
Neither busy checkout was edited, staged, merged, or promoted by this learning
continuation.

The isolated committed Garden snapshot used for stable verification is
temporarily retained under the local evidence root together with its archive;
it contains committed repository material only, no task data, and must be
removed after review or by the same `2026-09-05` retention boundary.

Still unrun: a browser render/click test, a reviewed downstream application of
a candidate lesson, replay-bound verification of that application, three
eligible held-out paired cases, and two independent evaluator scores. Mike
still owns candidate review, recipient and evaluator selection, heartbeat
cancellation or extension, integration into a clean Workshop target branch,
inheritance, promotion, publication, and CANON. Status remains
**EXPERIMENTAL**.

### Final moving-Garden correction

After the verification section was sealed, the concurrent Garden steward
committed its five tracked changes and advanced Garden HEAD to
`777a0f55cffcbd8728dcfa45687109ffe604e7b6`. The two pre-existing untracked
value-experiment files remained unchanged. A fresh current-HEAD run then
passed **45/45 core** and **5/5 full-return** tests while both HEAD and the
two-entry status digest remained stable before and after the run. This
supersedes the excluded moving-window result but does not alter the separately
valid 44/44 plus 5/5 evidence for the older committed snapshot. No Garden file
was edited, staged, committed, or promoted by this learning continuation.

## Append-only continuation: immutable lesson versions and first replay-bound lesson

Commit `2a8331fff3379acfce48cdf43a9247f07066deef` closes a cumulative
dissent-loss gap. A later cycle using an existing lesson key previously
replaced the prior current entry, so an older contested version could disappear
from the latest ledger and pickup even though the predecessor digest remained.
The compiler now keeps one current `lessons` entry per key plus a
digest-deduplicated, capacity-bounded `lessonVersions` history. Anti-drift
pickups carry every contested or refresh-required version. Legacy Workshop and
Grand Garden ledgers seed this version history during migration.

Changed implementation files:

- `tools/shadow-clone-learning-steward/README.md`;
- `tools/shadow-clone-learning-steward/lib/shadow-clone-learning-core.js`;
- `tools/shadow-clone-learning-steward/selftest.js`.

Two bounded compact collections then used only immediate aliased AXM status,
test, and steward receipts. No histories were opened and no task was messaged,
interrupted, redirected, or identified in retained evidence.

Cycle 05:

- predecessor ledger digest:
  `b95682cd9a45ca3fe68e3f4d9fdb81bdbab1faf79c03228a3c11c305efeb6444`;
- ledger digest:
  `16ad39fd3d5bb93b4d2c6c3111e99affb6a3eba0bb92446026766d580fd4c6dc`;
- run receipt digest:
  `381574e3e58c436037af5bb100b3859bd4f1de215cb49e6026a22621774d649e`;
- clean replay receipt digest:
  `cc216f3042cb96530fd8bae98b7c046e9ae23ecc67c18dfa99095934fc0a5758`.

It derived one medium-confidence candidate: prefer an independent verifier
that recomputes claims from actual output bytes or measured state under an
externally defined bound, while keeping self-declared or structurally inspected
receipts non-promoting. It also observed a new incompatible version of the
receipt-versus-live-verification threshold. The older contested version digest
`231ccd6d208e39d421867f6ec0109516d0311c8c8325418dd2d3a758badb9201`
and the new version digest
`de8511cab03bcf48d1736092f9d9e2c53cde7601188f7135c531c05ae1cd49c4`
both remain in `lessonVersions`.

Cycle 06:

- predecessor ledger digest:
  `16ad39fd3d5bb93b4d2c6c3111e99affb6a3eba0bb92446026766d580fd4c6dc`;
- cumulative five-cycle ledger digest:
  `576f2c9b6097eb699b7d778efed88de67b9c6267bded9d3c7e6556cdc4ce0b64`;
- run receipt digest:
  `e85e35cac9d6bcf60c0a0211fdd0b95f83521f0b14fb5ef5700be5153a367ef9`;
- clean replay receipt digest:
  `0be34a2ae40f7e8e0383b0d8ec028ab5a26d261569b6fbd168a0f76272df1f6e`.

Two fresh compact task observations re-supported the recomputation lesson and
also produced a second medium-confidence candidate: preserve a reproduced
failure, reject timing excuses or weakened assertions, and repair the
implementation or acceptance bound without hiding measured residue. A local
test receipt recomputed cycle 05 from its redacted control and matched expected
and observed ledger digest
`16ad39fd3d5bb93b4d2c6c3111e99affb6a3eba0bb92446026766d580fd4c6dc`.
The recomputation lesson is therefore `PASS_REPLAY_BOUND` with candidate digest
`693189e3bc61f5a7c16c0278109d851b95312e7544a68cd440151c6599d72786`
and verification digest
`9716849afaa543ecd06962f1963195574131f16643e5cee43223245afdba3e52`.
This is the first live replay-bound lesson in the cumulative Workshop route;
it is still `PROPOSED_NOT_APPLIED`.

The cumulative ledger contains 10 current lesson keys and 12 immutable lesson
versions: seven candidates, three contested current lessons, and exactly one
replay-bound candidate. Refeeding cycle 06 unchanged returned
`INSUFFICIENT_NOVEL_EVIDENCE` with exit code 1 and created no output directory.

A local candidate review packet was sealed with digest
`4de27399abfe172b00156c483192132ecef372a72e26beb44fe0fba9212de1d4`.
Its state is `MIKE_REVIEW_REQUIRED`; it proposes only one future held-out paired
trial case and permits `ACCEPT_FOR_ONE_HELD_OUT_TRIAL_CASE`,
`HOLD_FOR_REFRESH`, or `REJECT_CANDIDATE`. It performs no presentation,
application, inheritance, merge, or prompt rewrite.

The frozen trial assessment digest is
`740faf12f4aa682074055c12ddf45024781668edadfce0c708131d994b0808ef`.
Its state advanced from no replay-bound lesson to
`REPLAY_BOUND_LESSON_AWAITS_HUMAN_REVIEW`, but still records zero downstream
applications, zero held-out paired cases, zero evaluators, zero scores, and no
2x claim. A replay-bound routing receipt proves determinism, not downstream
value.

The active hourly heartbeat was updated to retain every digest-distinct lesson
version and require Mike's specific review before presentation or trial
application. The local schedule update receipt digest is
`44d8a230e262ef13dd7ce715bafffcacc84bb13bc22786d564d86586cca4f366`.
Its interval, finite eight-cycle cap, no-message rule, and human-only
inheritance gate are unchanged.

New local-only evidence IDs:

- `workshop-live-compact-cycle-20260822-05-control`;
- `workshop-live-compact-cycle-20260822-05`;
- `workshop-live-compact-cycle-20260822-05-replay`;
- `workshop-live-compact-cycle-20260822-06-control`;
- `workshop-live-compact-cycle-20260822-06`;
- `workshop-live-compact-cycle-20260822-06-replay`;
- `workshop-lesson-review-20260822-01`;
- `workshop-value-trial-cycle-assessment-20260822-04`;
- `workshop-periodic-pilot-20260822-04`.

They contain no raw task data, prompts, transcripts, task identifiers,
credentials, or source-machine paths. They remain local-only and expire no
later than `2026-09-05T01:52:31.137Z`; the candidate review expires sooner if
its evidence refreshes at `2026-08-23T01:48:20.000Z`. The local evidence owner
must remove them after review or expiry.

Continuation verification:

- focused module self-test: **PASS, 115 checks**;
- discovery seam review: **PASS**;
- syntax and `git diff --check`: **PASS**;
- cycle 05 and cycle 06 clean replay: **PASS**;
- exact-source duplicate probe: **PASS**, typed hold and no output directory;
- all ten required Workshop commands: **9/10 command pass**;
- `node hub/hub-selftest.js`: **FAIL** on the same three unrelated baseline
  gaps: incomplete radio source map, missing isolated growth-measurement reuse,
  and responsive command-bar polish;
- Garden HEAD `777a0f55cffcbd8728dcfa45687109ffe604e7b6`:
  **45/45 core PASS** and **5/5 full-return PASS**, with HEAD and the preserved
  two-entry untracked status stable before and after.

The canonical Workshop HEAD remained
`c6e7909267f51a6fa14395e46d6917678ef87d06`, but its dirty status continued
moving under other tasks. Neither the canonical checkout nor Garden was
edited, staged, merged, or promoted by this continuation.

Still unrun: Mike's specific candidate decision, host presentation to a future
recipient outside both source aliases, a paired baseline and learning-route
outcome, two evaluator seats, two additional eligible cases, browser
render/click testing of this non-visual module, inheritance, integration,
promotion, and CANON. Status remains **EXPERIMENTAL**.

---

## Append-only continuation: recipient-initiated held-out trial gate

Date: 2026-08-22  
Status: **EXPERIMENTAL; preparation and typed-hold evidence only**

### Result

Module version `v0.2` now separates trial assignment from trial scoring. The
new provider-neutral `shadow-clone.trial-assignment.prepare` capability can
prepare one paired held-out case only when all of these are simultaneously
true:

- the selected lesson is the fresh current replay-bound candidate;
- a digest-valid Mike decision accepts that exact candidate from that exact
  learning ledger for one held-out case;
- the host attests that it authenticated Mike's review;
- two distinct tasks that did not derive the lesson explicitly request the
  baseline and learning-route seats; and
- the predeclared common rubric remains unchanged.

Otherwise it emits `HOLD_TRIAL_ASSIGNMENT` with typed blockers. A hold hides
the lesson pattern and creates no trial case. Even a ready packet remains
preparation only: `presented` and `applied` stay false, and the module gains no
messaging, prompt, inheritance, merge, promotion, or CANON authority.

Implementation commit:
`d91386ff0298d23b776e7d3779a86c88c914e486`.

The implementation commit changed exactly these 13 files:

- `tools/shadow-clone-learning-steward/README.md`;
- `tools/shadow-clone-learning-steward/app.js`;
- `tools/shadow-clone-learning-steward/cli.js`;
- `tools/shadow-clone-learning-steward/discovery-seam-review.js`;
- `tools/shadow-clone-learning-steward/fixtures/synthetic-trial-assignment-request.example.json`;
- `tools/shadow-clone-learning-steward/index.html`;
- `tools/shadow-clone-learning-steward/lib/shadow-clone-learning-core.js`;
- `tools/shadow-clone-learning-steward/manifest.json`;
- `tools/shadow-clone-learning-steward/module.contract.json`;
- `tools/shadow-clone-learning-steward/schemas/candidate-review-decision.schema.json`;
- `tools/shadow-clone-learning-steward/schemas/trial-assignment-request.schema.json`;
- `tools/shadow-clone-learning-steward/schemas/trial-assignment.schema.json`;
- `tools/shadow-clone-learning-steward/selftest.js`.

This append-only handoff file is the only additional closeout change after
that implementation commit.

### Live bounded hold and capability truth

The live route used cumulative learning ledger digest
`576f2c9b6097eb699b7d778efed88de67b9c6267bded9d3c7e6556cdc4ce0b64`
and replay-bound candidate digest
`693189e3bc61f5a7c16c0278109d851b95312e7544a68cd440151c6599d72786`.
No specific candidate decision or recipient request was supplied, so the
assignment state was honestly `HOLD_TRIAL_ASSIGNMENT` with digest
`a4f2b80b3366c285c685c787688f9cf559baad45f6a2f192bebad2e21427341f`
and exactly four blockers:

- `MISSING_SPECIFIC_MIKE_REVIEW`;
- `MISSING_AUTHENTICATED_REVIEW_PROVIDER`;
- `MISSING_BASELINE_RECIPIENT_REQUEST`;
- `MISSING_LEARNING_RECIPIENT_REQUEST`.

The packet contained a null candidate pattern, null trial-case template, and
false presentation, application, messaging, prompt rewrite, inheritance,
merge, and CANON fields. A clean second CLI run produced the same assignment
digest and byte-identical output; both files have SHA-256
`7214d120101662c941d4cf39880e51f6969487ba463ca74aae9f964919b70843`.
No active task was contacted, enrolled, interrupted, or redirected.

The separate capability-gap report is `BLOCKED` for starting or claiming a
trial, while confirming the replay-bound lesson and assignment preparation
contract are ready. Its file digest is
`26c69869c8791fe1796c802571781aef3b65efbfbd855f2e0a83daf86825c907`.
Missing capabilities remain authenticated review transport, two explicit
paired-recipient requests, paired outcome receipts, and independent evaluator
receipts. `BLOCKED` here is the trial capability result, not a fabricated
failure of the module or a broad project-status claim.

The existing hourly heartbeat was updated in place without changing its
active status, one-hour interval, or finite eight-cycle cap. Future cycles now
must use this assignment route and must emit the typed hold instead of
inventing review, consent, outcomes, tests, or evaluator scores. The local
schedule receipt digest is
`db0dd50a0d1ae81bf0d6aa6b3d13d5cba71508e73f15a84f0aaf509840feaedc`.

### Verification

- focused module self-test: **PASS, 141 checks**;
- discovery seam review: **PASS**, including the new advertised capability;
- core, CLI, and browser script syntax: **PASS**;
- manifest, contract, schemas, and fixture JSON parsing: **PASS**;
- module-contract validation: **PASS**;
- `git diff --check`: **PASS** with line-ending notices only;
- live hold replay: **PASS**, exact digest and bytes;
- all ten required Workshop commands from the isolated module worktree:
  **9/10 command pass**;
- `node verify.js`: exit 0, **0 fail and 38 existing warnings**;
- `node hub/hub-selftest.js`: exit 1 on the same three unrelated baseline
  gaps: incomplete radio source map, missing isolated incremental growth
  measurement reuse, and missing responsive command-bar polish;
- Route, Graft, Skin, Verify Plus, Tool Forge package, Agent Tool Forge, and
  Evidence Desk checks: **PASS**;
- HTML script syntax: **53 pass, 0 fail**;
- Garden HEAD `777a0f55cffcbd8728dcfa45687109ffe604e7b6`:
  **45/45 core PASS** and **5/5 full-return PASS**.

The Garden retained its two pre-existing untracked value-experiment files and
status digest
`c072843afa6d992f2aab306c54cecd29d3a8cb4008760553858523211fcb8277`.
Neither file was edited. The isolated module worktree was clean at the
implementation commit before this handoff append.

The busy canonical Workshop remained on
`c6e7909267f51a6fa14395e46d6917678ef87d06`; its concurrent dirty state now
contains 4,042 compact status entries and digest
`6a7960f6f6cf372ad869e300dc234a34ae43fa063dda963da9c973314f40c976`.
That dirty state has moved across previous checkpoints while HEAD stayed
fixed, so it remains classified **MOVING_WORKSPACE**. It was not used as an
application surface and was not edited, staged, reset, merged, or promoted by
this continuation.

### Local retention and remaining decisions

New local-only evidence aliases are:

- `workshop-live-trial-assignment-20260822-01-control`;
- `workshop-live-trial-assignment-20260822-01`;
- `workshop-live-trial-assignment-20260822-01-replay`;
- `workshop-trial-assignment-capability-gap-20260822-01`;
- `workshop-periodic-pilot-20260822-05`.

They contain bounded request, hold, replay, capability-gap, and schedule
evidence only. No raw prompt, transcript, task identifier, credential,
personal account data, or source-machine path is committed. The candidate
requires refresh at `2026-08-23T01:48:20.000Z`; the new schedule evidence is
local-only through `2026-09-05T02:13:04.727Z` and its owner must remove it
after review or expiry because automatic deletion is not implemented.

No real paired case has begun. There are still zero presented lessons, zero
applications, zero paired outcome receipts, zero independent evaluator
scores, and no evidence that the second-value route approaches 1.8x or 2x.
The host-authenticated-review field is a host assertion, not cryptographic
identity proof. Browser render/click testing remains unrun because this change
does not add an interactive workflow claim.

Mike still decides the specific candidate outcome (`ACCEPT_FOR_ONE_HELD_OUT_TRIAL_CASE`,
`HOLD_FOR_REFRESH`, or `REJECT_CANDIDATE`), whether two future held-out tasks
may opt into the paired trial, evaluator selection, every presentation and
lesson application, retention deletion, inheritance, merge, promotion, and
CANON.

For integration, use a clean review worktree based on Mike's selected
Workshop target, verify that the frozen base is still an ancestor or rebase
and recompare the full branch, then review and apply the ordered descendant
commits from `2a9aeda6` through this closeout commit. Regenerate local tool
discovery, rerun the focused suite and all ten Workshop checks, and inspect
the complete diff before Mike merges. Do not apply the range over the busy
canonical checkout. Keep status **EXPERIMENTAL** unless Mike separately
changes it.

---

## Append-only continuation: receipt-bound conservative value route

Date: 2026-08-22  
Status: **EXPERIMENTAL; evidence intake ready, real trial not authorized**

### Result

Module version `v0.3` closes the largest evidence gap in the held-out value
route: a supplied score is no longer enough to support a real-world value
claim. The provider-neutral route now composes the Workshop's existing
`axm.review-receipt/v1` human-review organ and requires separately bound
review, participation, lesson-presentation, paired outcome/test, and
independent evaluation receipts. It does not duplicate Foundation review or
grant itself a provider.

Implementation commit:
`1c033519d4ed739603f0020a01214c0695825978`.

That commit changed exactly these 20 files:

- `tools/shadow-clone-learning-steward/README.md`;
- `tools/shadow-clone-learning-steward/app.js`;
- `tools/shadow-clone-learning-steward/cli.js`;
- `tools/shadow-clone-learning-steward/discovery-seam-review.js`;
- `tools/shadow-clone-learning-steward/fixtures/synthetic-trial-participation-receipt.example.json`;
- `tools/shadow-clone-learning-steward/index.html`;
- `tools/shadow-clone-learning-steward/lib/shadow-clone-learning-core.js`;
- `tools/shadow-clone-learning-steward/manifest.json`;
- `tools/shadow-clone-learning-steward/module.contract.json`;
- `tools/shadow-clone-learning-steward/schemas/candidate-review-decision-v2.schema.json`;
- `tools/shadow-clone-learning-steward/schemas/candidate-review-intake-request.schema.json`;
- `tools/shadow-clone-learning-steward/schemas/evidence-bound-value-receipt.schema.json`;
- `tools/shadow-clone-learning-steward/schemas/evidence-bound-value-trial.schema.json`;
- `tools/shadow-clone-learning-steward/schemas/lesson-presentation-ack.schema.json`;
- `tools/shadow-clone-learning-steward/schemas/trial-arm-outcome.schema.json`;
- `tools/shadow-clone-learning-steward/schemas/trial-assignment-request-v2.schema.json`;
- `tools/shadow-clone-learning-steward/schemas/trial-assignment-v2.schema.json`;
- `tools/shadow-clone-learning-steward/schemas/trial-case-evaluation.schema.json`;
- `tools/shadow-clone-learning-steward/schemas/trial-participation-receipt.schema.json`;
- `tools/shadow-clone-learning-steward/selftest.js`.

The exact review subject is digest-bound to the cumulative ledger, candidate,
review packet, prospective plan, and one assignment request. One Mike-owned
review decision can authorize at most one case. A v2 assignment also requires
two current, unwithdrawn, host-authenticated participation receipts from
distinct non-source task seats. Assignment remains preparation only:
`presented` and `applied` are false.

Evidence-bound scoring requires sender and recipient presentation
acknowledgements, baseline and learning-route final/outcome/test receipts,
candidate exclusion from the baseline, recipient-elected use in the learning
route, and at least two authenticated independent evaluators per case. A
bounded conclusion requires three unique cases under one frozen plan and
rubric. Every evaluator statement and score is retained. Incompatible target
classifications yield `CONTESTED_EVALUATION`; scores are never averaged, and
the lowest complete receipt-bound case ratio controls the bounded result.

The older direct score route remains available only for synthetic or supplied
score checks and now declares `realWorldClaimable: false`,
`evidenceAssurance: SUPPLIED_SCORES_NOT_RECEIPT_BOUND`, and
`literalTwoXProven: false`. No route auto-reviews, enrolls, presents, applies,
scores, inherits, messages another task, rewrites a prompt, merges, promotes,
or canonizes.

### Live bounded holds and capability truth

The current replay-bound lesson was routed through the new v2 assignment
interface without inventing Mike's decision or recipient consent. The
assignment digest is
`a99cd6505a4461369b96a83589bd7463454516b9aa21aef5e1c8356e93d4b931`;
its file SHA-256 is
`83c195dd1674c3c36fc5e9c055a8a15d1cdc7efb20daf289e74f3fbda5c11495`.
It is `HOLD_TRIAL_ASSIGNMENT` with exactly these blockers:

- `MISSING_SPECIFIC_MIKE_REVIEW`;
- `MISSING_AUTHENTICATED_REVIEW_PROVIDER`;
- `MISSING_AUTHENTICATED_RECIPIENT_PROVIDER`;
- `MISSING_BASELINE_RECIPIENT_REQUEST`;
- `MISSING_LEARNING_RECIPIENT_REQUEST`.

The held assignment exposes no candidate pattern or trial case and grants no
adoption or authority. The matching evidence-trial probe is
`HOLD_EVIDENCE_BOUND_VALUE_TRIAL`, with receipt digest
`c07d1f2674bba80505d448f9217363333467ef0b0b0f2ac422e1c9e5114b5405`
and blockers `MISSING_AUTHENTICATED_EVIDENCE_PROVIDER` and
`TRIAL_ASSIGNMENT_STATE`. It has one submitted template case, zero complete
cases, zero evaluators, null ratios, and no claimable conclusion. Original and
clean replay files are byte-identical with SHA-256
`a2af42c28c96047db93d9524baeee6634bd28a1fdb6a4be751572e7e935fb9de`.

A fully synthetic three-case, two-evaluator-per-case fixture proves only that
the deterministic mechanism can reach its predeclared 1.8 threshold when all
receipts agree. It does not prove AXM gained value. A separate adversarial
fixture proves incompatible evaluator classifications remain contested and
produce no value claim.

The capability-gap report file SHA-256 is
`8db46f3b6ee3dd5f29b73f8c8f7f470627eafae41bc456efe7e6ed56a2713f98`.
It records the replay-bound lesson, review composition, participation
validation, and evidence-bound scoring machinery as ready. The missing items
are current external state and authority, not fabricated module completion:

- `shadow-clone.lesson.review.authenticated.current`;
- `shadow-clone.trial.recipient.paired-request.current`;
- `shadow-clone.trial.outcome.paired.current`;
- `shadow-clone.trial.evaluate.independent.current`.

No active task was contacted, interrupted, redirected, enrolled, or messaged
while producing these holds.

### Periodic steward and retention

The finite hourly heartbeat remains active under its existing alias with an
eight-cycle cap. Its prompt now requires the receipt-bound v2 route, an actual
shared review receipt, two current unwithdrawn opt-ins, sender and recipient
acknowledgements, paired outcome/test evidence, two independent evaluators per
case, three unique cases, preserved disagreement, and the lowest qualifying
ratio. It forbids invented receipts and real-world claims from the direct
score route.

The heartbeat configuration digest is
`c29a6e8427ed7829e72502741dc72d0eaf37a70b0d164610455338640328e177`.
The local schedule receipt digest is
`5f5fb7b71e7ec161140f9750b2bc5064068ab59b538092932b4cf215694acc3e`
and its local retention deadline is `2026-09-05T02:45:28.037Z`.

New local-only evidence aliases are:

- `workshop-live-trial-assignment-v2-20260822-01-control`;
- `workshop-live-trial-assignment-v2-20260822-01`;
- `workshop-live-evidence-trial-20260822-01-control`;
- `workshop-live-evidence-trial-20260822-01`;
- `workshop-live-evidence-trial-20260822-01-replay`;
- `workshop-trial-evidence-capability-gap-20260822-02`;
- `workshop-periodic-pilot-20260822-06`.

They remain outside Git and contain only bounded controls, typed holds, replay,
capability, and schedule evidence. No raw prompt, transcript, task/thread ID,
credential, personal account data, private project content, or source-machine
path is committed. The local evidence owner must remove these artifacts after
review or expiry because automatic deletion is not implemented.

### Verification and moving-workspace classification

- focused module self-test: **PASS, 193 checks**;
- discovery seam review: **PASS**;
- core, CLI, and browser script syntax: **PASS**;
- all 21 module schemas and fixtures parse: **PASS**;
- module contract and `git diff --check`: **PASS**;
- live evidence hold replay: **PASS**, exact digest and bytes;
- all ten required Workshop commands from the isolated module worktree:
  **9/10 command pass**;
- `node verify.js`: exit 0, **0 fail and 38 existing warnings**;
- `node hub/hub-selftest.js`: exit 1 on the same three unrelated gaps:
  incomplete radio station source map, missing isolated incremental growth
  measurement reuse, and missing responsive command-bar polish;
- Route, Graft, Skin, Verify Plus, Tool Forge package, Agent Tool Forge, and
  Evidence Desk checks: **PASS**;
- HTML script syntax: **53 pass, 0 fail**;
- Garden HEAD `3193182f9c85961c7926891192a8be25b4a67da6`:
  **45/45 core PASS** and **5/5 full-return PASS**.

The Garden advanced independently from `777a0f55` to `3193182f` while this
continuation was active. It retained the same two untracked value-experiment
files and now has compact status digest
`941658c993d9fbc7d698d2a526d6614276782cfe6c00f4a37dd915d98cda82b3`.
The canonical Workshop stayed at
`c6e7909267f51a6fa14395e46d6917678ef87d06`, but its dirty snapshot expanded
to 13,122 compact entries with digest
`4c86714329bf3bd8214ecc561925992f72125813ddfa8797593066385822e32a`.
Both are classified **MOVING_WORKSPACE**. Neither checkout was edited, staged,
reset, merged, or promoted by this continuation.

### Unrun work, Mike's gates, and integration route

No actual held-out trial has begun. Still unrun are an exact shared Mike review
receipt for the current subject, two recipient opt-ins, lesson presentation,
paired baseline and learning-route task outcomes and tests, independent
evaluation, and three qualifying cases. Therefore there is no evidence that
the route approaches 1.8x or 2x value, and no literal 2x claim.

Mike still owns the exact candidate decision, recipient and evaluator
selection, every application, the retained-evidence deletion decision,
inheritance, merge, promotion, and CANON. A host-authenticated assertion is an
interface claim, not cryptographic proof of a human identity.

For safe integration, create a clean review worktree from Mike's chosen current
Workshop target. Verify or re-establish ancestry, rebase/recompare the whole
reviewable branch as needed, and review the ordered commits beginning at
`2a9aeda6` through this handoff closeout commit. Regenerate discovery, rerun
the 193-check focused suite and all ten Workshop commands, then inspect the
complete diff. Do not apply or merge this range over the busy canonical
checkout. Status remains **EXPERIMENTAL** unless Mike explicitly changes it.

### Closeout correction after final race check

The Garden moved again after the preceding checkpoint. Its final tested HEAD
for this continuation is `e992f6925321342b2de6f73e3793eab18bd0b9b5`.
The expanded core suite passed **46/46** and the full-return suite passed
**5/5** at that exact HEAD. The earlier `3193182f` / 45-pass statement remains
an accurate intermediate checkpoint but is superseded for closeout.

After those tests, the Garden still had HEAD `e992f692`, but its concurrent
status moved to three entries with digest
`140382f3a4a7ecfec45c63f22ea659be3d9a83f94cb9c5cd90efb4b5c187a3e4`:
the two preserved untracked value-experiment files plus a concurrently modified
`INSTANCE_LEARNING_STEWARD_HANDOFF.md`. The canonical Workshop remained at
`c6e7909267f51a6fa14395e46d6917678ef87d06` with 13,139 compact dirty entries
and digest
`ecc6c4ab94548eb67f48efdb8ebd356fff4bff3399103f8742d7795e9c437b0c`.
These changes reinforce the **MOVING_WORKSPACE** classification; none was made
or staged by this module continuation.
