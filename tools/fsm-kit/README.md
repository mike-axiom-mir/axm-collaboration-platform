# AXM Portable Game FSM Kit

Open `index.html` to inspect the source proof and the reconciled role. Import `shapeable-block-pack.json` into Shapeable Builder for machine, state and transition blocks.

The executable runtime is `shared/game-fsm/`. The original Opus export remains unchanged in `exports/fsm-kit/`.
