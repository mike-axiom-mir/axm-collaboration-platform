const expected={11:'texture-trimsheet-decal-studio',12:'character-creature-studio',13:'character-animation-mocap-studio',14:'vehicle-assembly-damage-studio',15:'deterministic-build-cook-patch-pipeline',16:'world-partition-streaming-compiler',17:'physics-destruction-ragdoll-runtime',18:'terrain-biome-studio',19:'procedural-city-interior-studio',20:'navigation-crowd-behavior-studio',21:'gameplay-ability-rules-graph',22:'vfx-particle-shader-studio',23:'lighting-probe-baker',24:'reference-to-3d-reconstruction-lab',25:'vram-memory-streaming-analyzer'};function check(id,label,passed,evidence){return{id,label,status:passed?'pass':'fail',evidence}}
export function verifyP1Scenario(report){const ids=(report.checks||[]).map(item=>item.id),checks=[check('schema','Known P1 scenario schema',report.schema==='axm.p1-production-scenario/v1',report.schema||'missing'),check('identity','Rank and module identity agree',expected[report.rank]===report.id,`${report.rank} · ${report.id}`),check('checks','Checks are unique and all pass',ids.length>=5&&new Set(ids).size===ids.length&&report.checks.every(item=>item.status==='pass'),`${report.summary?.passed}/${report.summary?.checks}`),check('metrics','Metrics are machine-readable',report.metrics&&Object.keys(report.metrics).length>=4,`${Object.keys(report.metrics||{}).length} metrics`),check('artifact','At least one typed artifact exists',(report.artifacts||[]).length>0&&report.artifacts.every(item=>/^axm\./.test(item.schema)),`${report.artifacts?.length||0} artifacts`),check('writes','Scenario performs no automatic writes',Array.isArray(report.writes)&&report.writes.length===0,'zero writes'),check('promotion','Candidate promotion remains off',report.promotion==='off','promotion off'),check('boundary','At least one honest boundary is visible',(report.boundaries||[]).length>0,report.boundaries?.[0]||'missing')];const failed=checks.filter(item=>item.status==='fail').length;return{schema:'axm.p1-production-scenario-verification/v1',id:report.id,status:failed?'blocked':'pass',summary:{passed:checks.length-failed,failed,checks:checks.length},checks}}
