# Grammar Glass / Code Twister — steward pass 2

This browser surface renders only a recorded `axm.code.grammar-glass-visual-snapshot.v1` produced from the live AXM grammar-profile registry. The first steward pass added bounded cross-grammar influence carries and lineage-preserving composite formations. This pass adds **decaying contact memory, bounded multi-hop influence, a derivation inspector, a visible structural Draft Mirror response, and actual browser motion**.

Generate a grounded snapshot from the repository root:

```bash
node shared/code-capability-fabric/language-organs/generate-code-grammar-glass-snapshot.js \
  --day 2026-08-24 --ticks 8 --stars 8 > /tmp/grammar-glass-snapshot.json
```

For exact replay, pass the recorded 64-character root seed with `--seed <seed>`. If `--seed` is omitted, Node draws from the operating-system cryptographic entropy source and records the resulting root seed. The day label is not randomness, and RNG does not prove novelty.

Open `tools/grammar-glass/index.html` and choose **Load snapshot JSON**. No canned language activity is shown before a snapshot is supplied.

The moving viewer now uses `requestAnimationFrame` to interpolate the snapshot's recorded atom phase and angular velocity. Direct carry pulses come from recorded direct influence receipts. Purple multi-hop pulses come from recorded contact-memory paths. The glass shell and motion are visualization only: interpolation does not create evidence and does not mutate the Code Twister cycle.

The **Reactive Draft Mirror Glass** shows a structural response generated from the selected recorded formation, grammar components, typed relationships, contact-memory application state, mirror lens and inert draft-recipe digest. It is readable output derived from the code/grammar data, but it is **not source code, not executable, not a winner, and not runtime evidence**.

Contact memory is bounded deterministic derived state. Direct contacts decay over explicit ticks. Two-to-three-hop paths can produce a bounded next-tick memory carry, allowing an earlier grammar interaction to influence later circulation without claiming that the grammars are equivalent or that semantic transitivity exists. Paths refuse repeated-atom loops and retain source/target grammar identities and edge digests.

The **Why did this composite form?** inspector explains the simulator derivation only: exact formation digest, atom ancestry, relation classes/reasons, collision-threshold state, composite lineage and exact applied memory-carry receipts when available. It does not expose or require private chain-of-thought and it does not prove that the resulting draft is useful, novel, correct or compilable.

The viewer and memory layer do not execute, compile, install, use network, admit, select, promote, merge, or change CANON. Mike remains the final merge gate.

Grounded claim: Grammar Glass is a seeded, replayable, provenance-bound polyglot structural field and observation simulator. It uses real AXM grammar knowledge to generate, influence, remember and photograph temporary draft formations without granting those formations authority.
