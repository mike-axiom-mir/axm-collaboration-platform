'use strict';const assert=require('assert'),fs=require('fs'),path=require('path');const d=__dirname,m=JSON.parse(fs.readFileSync(path.join(d,'manifest.json'))),c=JSON.parse(fs.readFileSync(path.join(d,'module.contract.json'))),a=fs.readFileSync(path.join(d,'app.js'),'utf8');assert.equal(m.id,'cognitive-calibration-lab');assert.equal(m.schema,'axm.tool-manifest/v1');assert.equal(m.kind,'product');assert.equal(m.integratedInto,'cognitive-resource-meter');assert.deepStrictEqual(m.permissions,c.permissions);assert(c.boundaries.refuses.includes('model-ranking'));assert(c.boundaries.refuses.includes('calibrated-accuracy-claim-from-insufficient-evidence'));assert(a.includes('load an exact observation first'));console.log('Cognitive Calibration Lab selftest: PASS');
