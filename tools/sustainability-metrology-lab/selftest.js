'use strict';const assert=require('assert'),fs=require('fs'),path=require('path');const d=__dirname,m=JSON.parse(fs.readFileSync(path.join(d,'manifest.json'))),c=JSON.parse(fs.readFileSync(path.join(d,'module.contract.json'))),a=fs.readFileSync(path.join(d,'app.js'),'utf8');assert.equal(m.id,'sustainability-metrology-lab');assert.equal(m.integratedInto,'cognitive-resource-meter');assert.deepStrictEqual(m.permissions,c.permissions);assert(c.boundaries.refuses.includes('token-to-energy-conversion'));assert(c.boundaries.refuses.includes('green-claim'));assert(a.includes('DIRECT_POWER_METER'));console.log('Sustainability Metrology Lab selftest: PASS');
