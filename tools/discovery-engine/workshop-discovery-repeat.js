'use strict';
const fs=require('fs'),path=require('path'),Discovery=require('./discovery-core'),Previous=require('./workshop-discovery'),Index=require('../../shared/capabilities/workshop-capability-index'),Continuity=require('../../shared/continuity/workshop-continuity');
const ROOT=path.resolve(__dirname,'..','..'),BASE=Date.parse('2026-07-13T00:30:00.000Z');
function meta(id,n){return{actorId:'axiom-mir',actorKind:'MACHINE',provider:'codex-local-workshop',model:'gpt-5',now:new Date(BASE+n*1000).toISOString(),recordId:id,eventId:'event-'+id};}
function add(state,stage,id,text,rationale,n){const r=Discovery.recordDiscovery(state,stage,{text,claimLabel:'HYPOTHESIS',source:'Authored capability metadata, separate readiness snapshot, continuity core, Hub resume surface and executable local controls',rationale,status:'INTERNAL_MACHINE_REVIEW'},meta(id,n));if(!r.ok)throw Error(r.errors[0].message);return r.state;}
function candidate(state,id,data,n){const r=Discovery.createCandidate(state,data,meta(id,n));if(!r.ok)throw Error(r.errors[0].message);return r.state;}
function manifests(){const metadata=JSON.parse(fs.readFileSync(path.join(ROOT,'shared','capabilities','capability-metadata.json'),'utf8')).modules||{};return fs.readdirSync(path.join(ROOT,'tools'),{withFileTypes:true}).filter(x=>x.isDirectory()&&x.name[0]!=='_').flatMap(x=>{try{const m=JSON.parse(fs.readFileSync(path.join(ROOT,'tools',x.name,'manifest.json'),'utf8')),a=metadata[m.id]||metadata[x.name]||{};return[Object.assign({},m,a,{folder:x.name,capabilityMetadataSource:Object.keys(a).length?'authored-registry':'manifest-fallback'})];}catch(e){return[];}});}
function run(){const prior=Previous.run(),tools=manifests(),report=Index.report(tools),coverage={summaries:tools.filter(x=>x.summary).length,actions:tools.filter(x=>Array.isArray(x.actions)&&x.actions.length).length,artifacts:tools.filter(x=>Array.isArray(x.accepts)&&x.accepts.length&&Array.isArray(x.produces)&&x.produces.length).length,readiness:tools.filter(x=>Array.isArray(x.readiness)&&x.readiness.length).length};let state=prior.state;
  const pass3=[
    ['knownSpace','w3-authored','Authored capability envelopes now cover '+coverage.actions+' of '+tools.length+' valid tool manifests with plain actions, accepted artifacts, produced artifacts and readiness dependencies.','The first seam is closed without forcing every older manifest file to migrate at once.'],
    ['knownSpace','w3-readiness','Capability results now attach a separate READY, AVAILABLE, NEEDS_ACTION or BLOCKED readiness observation while preserving capabilityUnchanged=true.','A temporarily disconnected connector no longer makes a tool appear retired or incapable.'],
    ['knownSpace','w3-continuity','A local continuity service stores only project references and exposes explicit Continue and Forget controls on Hub Home.','Continuation is reachable without copying project contents or opening anything automatically.'],
    ['realityChecks','w3-boundary','The readiness layer performs no network probes, installation, service start, permission change or lifecycle change.','Read-only local evidence keeps recommendation and setup as separate user choices.'],
    ['realityChecks','w3-data','Continuity records require projectDataCopied=false and contain only destination, project identity, schema, hint and timestamp.','The resume seam must not quietly become another database or synchronization layer.'],
    ['patterns','w3-pattern','Capabilities, current readiness and recent project references form three joined but independent truth dimensions.','Separating these dimensions prevents one failure state from erasing what a workspace can do.'],
    ['seams','w3-portable-handoff','Continue currently reopens the owning workspace and its local state; typed cross-workspace artifact handoffs are still implemented per workspace rather than by one universal broker.','A universal broker would need deeper schema compatibility and conflict rules before it is safe.'],
    ['seams','w3-guidance','Readiness labels identify that attention is needed but do not yet provide one beginner-safe, evidence-backed repair explanation for every dependency.','Explanation is useful; automatic repair would violate the chosen boundary.'],
    ['minimalChecks','w3-controls','Require complete authored registry coverage, deterministic matching, separate readiness flags, bounded continuity records, explicit forget and no copied project data.','These controls directly disconfirm the three repaired seams.']
  ];pass3.forEach((x,i)=>{state=add(state,x[0],x[1],x[2],x[3],100+i);});
  state=candidate(state,'candidate-typed-artifact-handoff-broker',{term:'Typed Artifact Handoff Broker',summary:'A future explicit chooser that shows which current project artifact can be accepted by another workspace and prepares a reviewable handoff packet.',mechanism:'Compare produces[] with accepts[], show compatible destinations and conversion requirements, then create a proposal packet only after explicit choice.',whyNotYet:'Current continuity deliberately reopens owning local state; cross-workspace schema conversion needs per-format validation.',viableNowBecause:'Every valid tool now has an authored input/output envelope and the Hub has an explicit resume surface.',dependencies:['capability envelopes','project schemas','evidence/version engine','universal exporter'],noveltyStatus:'UNCHECKED',soulGate:{genuineNeed:'PASS',nonCommercialWorth:'PASS',reducesPain:'PASS',sameGateAccess:'PASS',avoidsLockIn:'PASS',notes:['Recommendation first; conversion remains explicit.']},minimalForm:'A read-only compatibility list and proposal packet with no automatic copy or import.',cheapestDisconfirmingCheck:'Test Studio image to Film, Audio to Film and Knowledge handoff to Project Room; reject if any destination claims unsupported import.',disconfirmingOutcome:'False compatibility, silent conversion, data loss or automatic opening rejects the broker.',limitations:['Proposal only','Requires schema-specific adapters'],supersedes:'candidate-authored-capability-readiness'},120);
  const continuity=Continuity.upsert(Continuity.create(),{workspaceId:'studio',projectId:'p1',projectName:'Poster',documentSchema:'axm.studio.workspace/v2',updatedAt:'2026-07-13T00:00:00Z'}).state;
  const controls={authoredCoverage:coverage.actions===tools.length&&coverage.artifacts===tools.length&&coverage.readiness===tools.length,capabilityReport:report.withAuthoredActions===tools.length&&report.withArtifactEnvelope===tools.length&&report.withReadinessEnvelope===tools.length,continuityValid:Continuity.validate(continuity).ok&&Continuity.list(continuity)[0].projectDataCopied===false,serverReadinessSeparate:fs.readFileSync(path.join(ROOT,'server.js'),'utf8').includes('capabilityAndReadinessSeparate: true'),hubExplicitResume:fs.readFileSync(path.join(ROOT,'hub','hub-shell.js'),'utf8').includes('forgetContinuity')};
  const pass4=[
    ['knownSpace','w4-result','All executable controls passed: authored metadata coverage, capability/readiness separation, continuity validation, explicit Hub resume and explicit forget.','The three previously open seams are implemented rather than merely renamed.'],
    ['realityChecks','w4-scope','The repair does not claim cloud synchronization, universal schema conversion or automatic dependency repair.','Preserving the negative space makes the result understandable and reversible.'],
    ['soulChecks','w4-soul','The Workshop is more free when people and machines can discover, judge readiness and continue work without surrendering control of setup or data movement.','Reachability and agency improve together.'],
    ['seams','w4-next','The strongest next seam is a typed, explicit artifact handoff broker; beginner readiness explanations are a smaller parallel seam.','Both can build on today’s contracts without reopening navigation fragmentation.'],
    ['minimalChecks','w4-regression','Keep this repeat pass in the full Workshop test chain so new tools cannot silently omit authored envelopes or copy project data into recents.','The seam repair should remain an architectural invariant.']
  ];pass4.forEach((x,i)=>{state=add(state,x[0],x[1],x[2],x[3],140+i);});
  if(!Object.values(controls).every(Boolean))throw Error('repeated workshop discovery controls failed: '+JSON.stringify(controls));const validation=Discovery.validate(state);if(!validation.ok)throw Error(validation.errors.join('; '));return{schema:'axm.workshop-discovery-loop/v1',passes:4,state,coverage,controls,closedSeams:prior.remainingSeams,nextSeams:state.discovery.seams.filter(x=>/^w4-/.test(x.id)).map(x=>x.text),truth:{modelReviewIsIndependent:false,findingsAreCanon:false,automaticSetup:false,projectDataCopied:false,implementedCandidate:'Authored Capability, Readiness and Continuity Envelopes',proposedCandidate:'Typed Artifact Handoff Broker'}};}
if(require.main===module){const r=run();console.log('AXM Workshop Discovery Repeat: PASS · passes='+r.passes+' · authored='+r.coverage.actions+'/'+r.coverage.summaries);r.closedSeams.forEach((x,i)=>console.log('CLOSED '+(i+1)+' · '+x));r.nextSeams.forEach((x,i)=>console.log('NEXT '+(i+1)+' · '+x));}
module.exports={run};
