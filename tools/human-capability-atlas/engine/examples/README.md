# Examples

`reference_batched_run_summary.json` reflects the current v0.10 verification
shape.

`change_tracking/` contains retained historical v0.5 snapshot/diff fixtures.
Their embedded `module_version` values are intentionally historical regression
evidence, not a claim that the active Atlas implementation is v0.5.
