{
  "axm.example.map.alpha": {
    "name": "map_alpha",
    "summary": "Capability identified by its registry key.",
    "inputs": {"input_types": ["json"]},
    "outputs": {"output_types": ["report"], "preview_available": "UNKNOWN"}
  },
  "axm.example.map.beta": {
    "display_name": "Map Beta",
    "description": "Uses its display name to derive a machine name.",
    "input_types": ["text"],
    "output_types": ["text"]
  }
}
