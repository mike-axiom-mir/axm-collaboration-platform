# Machine / human collaboration receipt

Status: **HUMAN LANE STABLE AT TEST; SHARED SPINE PENDING INDEPENDENT RERUN**. Exact-path overlap: zero by agreement.

## Machine lane (frozen while this lane builds)

The collaborating AXM task owns only:

- `shared/asset-hands/hands/pbr-material-bake.js`
- `shared/asset-hands/pbr-material-bake-hand-selftest.js`
- `shared/deterministic-material-fabric/**`

It delivered `pbr-material-bake@1.1.0`, exact create/edit routing, explicit map sampling metadata, candidate-only READY/PASS truth, six full PNG SHA-256 records, bounded serialized-result measurements, and a CommonJS gate. It reported focused machine tests passing and froze those paths before the human lane began. Broad shared verification intentionally waits for both lanes to stabilize.

## Human lane (stable)

This task owns only `tools/asset-material-sensory-workbench/**`. It consumes the frozen contract, keeps HOLD/FAIL diagnostic-only, preserves prior candidates and reviews on failed regeneration, and never writes through the host bridge. Viewer preferences are source-neutral and review-staling; downloads are explicit user actions. Focused core/host/syntax checks and live browser journeys pass. The constrained-browser same-origin form fallback was added without widening host, request, response, origin or filesystem authority.

## Shared truth

- Machine technical PASS does not become human aesthetic approval.
- A browser sphere is a look-development reference, not target-engine parity or a physical surface.
- Neither lane installs, promotes or canonizes its own output.
- Mike Tobi remains the founder, tester, direction-setter and merge/canon gate.

## Evidence seal

Human-lane evidence includes real create/edit, WebGL2 sphere/plane, raw channel, light/keyboard state, full SHA display, A/B, real HOLD preservation, viewer-bound review staleness, explicit acceptance refusal, high-contrast/reduced-motion, 390×844 responsive capture and live degraded mode. Synthetic review fixtures are explicitly excluded from aesthetic evidence. Frame cadence, target-renderer parity, physical-surface behavior, representative GPU performance, assistive technology, external conformance and real human taste remain open.

The lane is now frozen for broad shared verification and the collaborating task's independent rerun. The independent result will be appended without rewriting this evidence history.
