'use strict';
const assert=require('assert'),fs=require('fs'),path=require('path');
const ContractVerifier=require('../../hub/module-contract-verifier'),AccessibilityAudit=require('../../scripts/accessibility-static-audit');
const dir=__dirname,htmlPath=path.join(dir,'index.html'),manifest=JSON.parse(fs.readFileSync(path.join(dir,'manifest.json'),'utf8')),contract=JSON.parse(fs.readFileSync(path.join(dir,'module.contract.json'),'utf8')),html=fs.readFileSync(htmlPath,'utf8'),app=fs.readFileSync(path.join(dir,'app.js'),'utf8');
assert.equal(manifest.id,'modular-intake-gate');assert.equal(contract.id,manifest.id);assert.ok(manifest.permissions.includes('component.promote'));assert.ok(contract.boundaries.refuses.includes('unknown-family-coercion'));assert.ok(html.includes('Unknown categories stay visible'));assert.ok(app.includes('PROMOTE REVIEWED PIECE'));assert.equal(ContractVerifier.validateContract(contract,manifest).errors.length,0);assert.equal(AccessibilityAudit.auditHtml(htmlPath).length,0);assert.doesNotThrow(function(){new Function(app);});console.log('modular intake gate self-test passed - 9 assertions');
