# UI Collaboration Receipt

Status: TEST/STABLE for the human lane; independent task-to-task closure remains
the final verification seat. This file records evidence, not canonization.

Machine ownership is frozen outside this tool in exactly the ui-component 1.2
provider/schema seams and shared/deterministic-ui-fabric. The human task owns
only tools/asset-ui-sensory-workbench. Exact path overlap is zero.

The machine task supplied a TEST/STABLE whole-result gate, public create/edit
route, typed failures and frozen aggregate. The human task independently
reproduced that aggregate before creating this lane.

The human lane provides the loopback host, inert-image canvas renderer,
state/nine-slice journey, viewer-state digest, A/B preservation and explicit
human receipt. It never changes the machine lane, shared index, prior sensory
lanes, provider registry, Foundation, installation or promotion state.

Focused evidence completed:

- provider edit and deterministic UI gate selftests passed independently;
- human core selftest passed create/edit replacement, inert nine-slice math,
  journey gating, viewer-state staleness, failed-result preservation and the
  human-only receipt boundary;
- loopback server selftest passed create/edit, exact artifact replacement,
  request/response budgets, CSP/static delivery, origin/content-type refusal
  and degraded mode;
- JavaScript syntax and all eight human-lane JSON documents passed.

Browser-primary evidence completed in the Codex in-app browser at
127.0.0.1:8795. The created candidate rendered in raw and nine-slice modes;
default, hover, active and disabled states were visibly exercised; 100% and
200% zoom, light/dark backgrounds, mobile safe area, reduced-motion preference,
pointer and keyboard focus completed the 12/12 journey. A recipe edit replaced
all three artifacts and produced a visibly distinct A/B candidate. A deliberately
invalid reduced-motion edit was refused before transport and preserved the
current candidate. The initial fresh-state 390x844 observation appeared to use
a single column without horizontal overflow, but independent counterevidence
later showed that create plus nine-slice 720x140 stretch populated enough lower
diagnostics content to widen the document to 473 CSS px. After a narrow
min-width containment fix, repeating that exact state measured root/main
scrollWidth 375, lower-grid 354, both lower panels 353 and diagnostics 329,
all within innerWidth 390. Browser error/warning logs were empty after the
decode fix. The viewer has no rolling-frame-buffer capability, so cadence
remains UNKNOWN rather than inferred from screenshots.

The live browser initially exposed a real inert-image decode defect: the
renderer incorrectly assumed the browser URL constructor was an object. The
check now validates createObjectURL/revokeObjectURL capabilities directly, and
the focused and browser journeys passed afterward.

The required ten-command shared spine passed on the frozen human code: verify.js
reported 0 FAIL / 18 warn with spine b618c5762240070c, verify-plus reported
VERIFIED_WITH_LIMITS, HTML script syntax reported 55 PASS / 0 FAIL, Tool Forge
packaging passed, Agent Tool Forge reported 17 PASS / 0 FAIL and Evidence Desk
reported 36 PASS / 0 FAIL. The first broad verifier run caught three unsupported
descriptive lifecycle enum values in module.contract.json; changing them to the
repository vocabulary (browser/reset/graceful-degrade) closed all three before
this final pass. The independent aggregate and rerun belong to the collaborating
task's closure message rather than being preclaimed here. Mike's aesthetic and
usefulness decision remains separate.
