'use strict';

// Stable Audio Studio route to the governed headless back half.
// Requiring this module never writes a file or starts audio playback.
module.exports = require('../../shared/audio-sfx-bake/index.js');
