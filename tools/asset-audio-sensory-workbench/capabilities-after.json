{
  "capabilities": [
    { "id": "asset.audio.hand.create-edit", "status": "available", "constraints": ["deterministic-audio-fabric v1.0.0", "Node CommonJS", "candidate-only"] },
    { "id": "asset.audio.wav.genuine-pcm16", "status": "available", "constraints": ["mono 44100 Hz signed PCM16", "0.01–2 seconds"] },
    { "id": "asset.audio.waveform.visualize", "status": "available", "constraints": ["browser-decoded PCM16 envelope", "not an audible-quality claim"] },
    { "id": "asset.audio.playback.user-gesture", "status": "available", "constraints": ["HTMLMediaElement events", "browser transport only"] },
    { "id": "asset.audio.listener-state.digest", "status": "available", "constraints": ["volume", "playback speed", "loop", "mute", "waveform zoom", "contrast", "unverified output route"] },
    { "id": "asset.audio.review.receipt.nonpromoting", "status": "available", "constraints": ["human report", "exact WAV SHA-256", "listener-state digest", "no approval authority"] },
    { "id": "asset.audio.bridge.loopback-bounded", "status": "available", "constraints": ["127.0.0.1 only", "same-origin", "1 MiB request bound", "no filesystem writes"] },
    { "id": "asset.audio.audibility.physical.verify", "status": "unavailable", "constraints": ["no acoustic capture or identified output device"] }
  ]
}
