{
  "requirements": [
    { "id": "audio-hand", "capabilities": ["asset.audio.hand.create-edit"], "required": true },
    { "id": "wav-delivery", "capabilities": ["asset.audio.wav.genuine-pcm16"], "required": true },
    { "id": "waveform", "capabilities": ["asset.audio.waveform.visualize"], "required": true },
    { "id": "playback", "capabilities": ["asset.audio.playback.user-gesture"], "required": true },
    { "id": "listener-binding", "capabilities": ["asset.audio.listener-state.digest"], "required": true },
    { "id": "review", "capabilities": ["asset.audio.review.receipt.nonpromoting"], "required": true },
    { "id": "host-bridge", "capabilities": ["asset.audio.bridge.loopback-bounded"], "required": true },
    { "id": "physical-audibility", "capabilities": ["asset.audio.audibility.physical.verify"], "required": false }
  ]
}
