# LOD, HLOD & Impostor Builder

P0 module #6. Builds error-bounded LOD0–LOD3 families, derives transitions from projected screen error, creates an eight-yaw CPU impostor atlas, and emits digest-bound HLOD grouping metadata.

Requested reduction and achieved reduction remain separate. Error-constrained levels are visible, and already-minimal foliage is routed to its impostor rather than pointlessly damaged.

```powershell
node "C:\axm workshop\tools\lod-hlod-impostor-builder\selftest.js"
```
