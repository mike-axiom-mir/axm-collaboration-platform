const expected={26:'game-ui-hud-menu-runtime',27:'quest-dialogue-narrative-runtime',28:'save-migration-replay-service',29:'network-replication-prediction-lab',30:'dedicated-session-orchestrator',31:'input-accessibility-haptics-studio',32:'visual-art-director-lab',33:'automated-playtester-scenario-agent',34:'audio-listening-quality-lab',35:'game-balance-telemetry-advisor',36:'ai-tool-skill-regression-harness',37:'consent-agent-memory-dataset-curator'};
function check(id,label,passed,evidence){return{id,label,status:passed?'pass':'fail',evidence}}
export function verifyP2Scenario(report){const ids=(report.checks||[]).map(item=>item.id),checks=[check('schema','Known P2 experience schema',report.schema==='axm.p2-experience-scenario/v1',report.schema||'missing'),check('identity','Rank and module identity agree',expected[report.rank]===report.id,`${report.rank} · ${report.id}`),check('checks','Scenario checks are unique and all pass',ids.length>=5&&new Set(ids).size===ids.length&&report.checks.every(item=>item.status==='pass'),`${report.summary?.passed}/${report.summary?.checks}`),check('metrics','Metrics are machine-readable',report.metrics&&Object.keys(report.metrics).length>=5,`${Object.keys(report.metrics||{}).length} metrics`),check('artifact','Typed artifacts are present',(report.artifacts||[]).length>0&&report.artifacts.every(item=>/^axm\./.test(item.schema)),`${report.artifacts?.length||0} artifacts`),check('writes','No automatic write occurred',Array.isArray(report.writes)&&report.writes.length===0,'zero writes'),check('promotion','Candidate promotion remains off',report.promotion==='off','promotion off'),check('boundary','At least one honest limit is visible',(report.boundaries||[]).length>0,report.boundaries?.[0]||'missing')];const failed=checks.filter(item=>item.status==='fail').length;return{schema:'axm.p2-experience-verification/v1',id:report.id,status:failed?'blocked':'pass',summary:{passed:checks.length-failed,failed,checks:checks.length},checks}}
