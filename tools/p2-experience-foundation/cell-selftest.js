'use strict';
const assert=require('node:assert/strict'),crypto=require('node:crypto'),fs=require('node:fs'),path=require('node:path');
(async()=>{const cell=JSON.parse(fs.readFileSync(path.join(__dirname,'p2-experience-cell.json'),'utf8')),kernel=await import('./p2-kernel.mjs'),verifier=await import('./p2-verifier.mjs');let checks=0;const ok=(v,m)=>{assert.ok(v,m);checks++},core=structuredClone(cell),receipt=core.receiptSha256;delete core.receiptSha256;ok(crypto.createHash('sha256').update(JSON.stringify(core)).digest('hex').toUpperCase()===receipt,'cell receipt');ok(cell.moduleCount===12&&cell.modules.length===12,'all modules');ok(cell.moduleChecks===74&&cell.verificationChecks===96,'route totals');ok(cell.automaticWrites===0&&!cell.automaticApply&&cell.libraryPromotion==='off'&&cell.externalRuntimeDependencies===0,'boundaries');for(const stored of cell.modules){const live=await kernel.sealP2Scenario(kernel.runP2Scenario(stored.id));ok(live.receiptSha256===stored.receiptSha256,stored.id+' receipt');ok(verifier.verifyP2Scenario(live).status==='pass',stored.id+' verifies')}console.log('P2 experience cell self-test passed '+checks+' checks; '+receipt+'.')})().catch(e=>{console.error(e.stack||e);process.exitCode=1});

