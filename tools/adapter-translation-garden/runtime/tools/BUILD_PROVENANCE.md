# Build provenance script

`steward_build_provenance_script.py` records how this checkpoint was generated inside the ChatGPT working environment. It contains environment-specific `/mnt/data` paths and is **not** the beginner quick-start path.

Use `run_tests.py`, `QUICK_TEST_WINDOWS.bat`, and the independent module folders for normal local use and AXM intake.
