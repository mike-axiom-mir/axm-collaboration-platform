# Effective capability stewardship — Runs 66–75

The steward loop now ranks work by declared value, coverage, risk reduction, reuse, reversibility, complexity, compute cost, and coupling. Scores are planning heuristics, not performance benchmarks.

Composition remains a plan. Simulation supports only a small reviewed declarative language and never imports or runs modules. Containment remains advisory. Cross-module invariants block review when authority, source, loss, or proof boundaries fail.
