{
  "schema": "axm.tool-manifest/v1",
  "kind": "product",
  "id": "sandbox",
  "name": "AXM Sandbox",
  "version": "v0.2-session1",
  "status": "TEST",
  "entry": "index.html",
  "contract": "module.contract.json",
  "type": "local-module",
  "audience": "human",
  "integratedInto": "game-forge",
  "serviceRole": "legacy-game-project-and-sandbox-compatibility-view",
  "tags": ["sandbox", "engine", "projects", "loopback-host", "filesystem"],
  "notes": "Session 1 of the alpha slice: portable project folders with declared boundaries (W03.01, W03.06, W07.01, W07.02). Host pattern reused from Game Hub. No entities, no clock, no renderer yet — those are Sessions 2-3 and this module does not claim them.",
  "uses": ["local-filesystem", "loopback-http"],
  "permissions": [],
  "risk": "MEDIUM",
  "actions": ["list and validate Sandbox project folders", "create one explicitly named portable project folder", "inspect files inside the Sandbox projects boundary"],
  "accepts": ["explicit-local-project-name"],
  "produces": ["sandbox-project-folder-format-1", "sandbox-project-status-json"],
  "readiness": ["local-filesystem", "loopback-http"],
  "summary": "Create and inspect portable Session-1 Sandbox project folders through a loopback-only host with an explicit browser mutation boundary.",
  "long_goal": "Shapeable sandbox engine per AXM-SBX master route. Every session must leave declared seats (bodies, cores, migration) open rather than hardcoding today's tech into the spine."
}
