# Build receipt

Status: playable local alpha; production stewardship active.

Lane: `tools/game-hub/game-library/015-axm-mirrorshift`

## Outcome

- Four original AXM racers: Mike, Axiom/Mir, Codex, and Mirror.
- Lobby-only per-seat character draft on shared screen and phones; occupied picks atomically swap unique identities while stable seat ownership and equal performance remain unchanged.
- Four frozen non-mechanical expression profiles: character-specific signature callouts, reduced-motion-aware launch/boost flourishes, distinct four-note motifs, phone labels, and winner quotes.
- Four visually distinct same-footprint vehicles on one frozen equal-stat contract.
- Three-lap server-authoritative race, AI fill, slight 0/2.5/5/7.5% rank catch-up, 85% offensive item pool, three-segment Flux Guard, guard repair, full wipeout after guard loss, results, and replay.
- Shared-screen keyboard/touch UI and dedicated responsive phone controller route.
- Resumable per-client input sessions with one-second heartbeats, tied action acknowledgements, visible round-trip timing, server-replacement recovery, and same-seat screen/phone coexistence.
- Four-profile deterministic adaptive procedural score for the three circuits and Mirror Core, with state-driven layers and a shared mute contract.
- Original generated roster key art copied into the package; runtime itself needs no internet.

## Focused verification

- `npm.cmd test` from this directory: PASS. Thirty focused core/package checks, 24 default-track races, 36 held-out cross-track races, 32 Mirror Core battles, a 30-virtual-minute reliability rotation, authoritative-process replacement recovery, and the live HTTP server/controller/session/receipt/heartbeat/state/input/art/observation/telemetry/track/assist/character/reset/traversal test passed.
- `node --check runtime/game-core.js`: PASS.
- `node --check runtime/server.js`: PASS.
- `node --check runtime/app.js`: PASS.
- Capability comparator: no declared capability is missing, but overall remains `UNKNOWN` by design. Identity, deterministic driving, content, accessibility, and local reconnect continuity are ready; audio listening/mix, physical LAN/hardware, real-time long-soak, human balance, and human approval gates remain open or degraded.
- Live Browser journey: PASS_WITH_BOUNDARIES. Prior roster/content/access/audio journeys remain sealed; this slice additionally observed controller reload, a visible reconnecting midpoint, automatic recovery after server replacement, same-seat screen/phone coexistence, tied acknowledgements, round-trip timing, and final default-lobby restoration.

## Production stewardship slice 07 — character draft and expression

- Separated stable seat/controller IDs from movable character IDs. Mike, Axiom/Mir, Codex, and Mirror now carry their name, title, signature, palette, and vehicle body between seats while score, assists, AI/human ownership, input sequence, spawn lane, and the frozen base-stat contract stay with the seat.
- Added a server-authoritative lobby-only `/api/character` route. An occupied choice atomically swaps identities rather than duplicating or rejecting the pick; exactly one of each AXM character remains in every match. Start locks the draft and reset/rematch preserves it.
- Promoted roster cards into accessible shared-screen buttons with pressed/locked state, current seat ownership, signature callouts, and equal-spec disclosure. Phone controllers gained previous/next driver controls and immediately adopt the chosen character color, signature, and vehicle label.
- Live Browser: PASS_WITH_BOUNDARIES. P1 selected occupied Codex and P3 received Mike; a P2 phone then selected occupied Codex and P1 received Axiom/Mir. Both surfaces updated, reload preserved the shared selection, race start disabled phone draft arrows, and reset restored lobby selection with the same assignments. The controller was visually clean and non-scrolling at the browser's 540x720 surface; a native post-change 390px hardware/browser viewport was unavailable and remains pending alongside physical touch/LAN QA.
- `npm.cmd test`: PASS with 29 focused checks plus the unchanged held-out race, track, battle, reliability, restart, and HTTP suites. The library package verifier reports no MIRRORSHIFT errors; the broader Game Hub gate currently fails only on foreign unfinished packages 016 and 017.

## Production stewardship slice 08 — signature character expression

- Added four frozen presentation-only contracts: Mike's RATCHET ROAR / piston chevrons / square-wave motif; Axiom/Mir's DUAL PHASE / counter-orbits / sine motif; Codex's ROUTE COMPILED / pixel stack / triangle motif; and Mirror's RETURN SIGNAL / prism echoes / sine motif. Every contract declares `changesPerformance: false` and the shared `BASE_STATS` path remains the only vehicle-performance source.
- Character identity now carries the expression id, label, and winner quote through draft swaps, seat observation, countdown, drift, go, finish, and results. The shared screen exposes character callouts on roster cards and countdown, short readable launch/boost labels over karts, and a winner quotation; phones expose the selected expression label beside the vehicle.
- Added a bounded motif scheduler under the existing SOUND control and DOM-tied `axm.character-expression-diagnostics/v1` receipts. Four-note scheduling, cue reason, active visual, visual-frame count, and the non-mechanical flag are observable without treating functional telemetry as listening-quality proof.
- Live Browser: PASS_WITH_BOUNDARIES. Selecting Mirror scheduled RETURN SIGNAL with four notes and `changesPerformance=false`; the countdown displayed COUNTER ANGLE // RETURN SIGNAL; repeated launch frames showed all four named flourish labels and tied `echo-prism` activity. A natural race ended at a legible CODEX WINS result with ROUTE COMPILED // “ROUTE COMPILED. KEEP UP.” and a tied 12-note victory-refrain receipt.
- The v0.9 preview sampled 24.22 ms average / 27.9 ms p95 across 240 recent frame deltas while expression activity logged 56 visual frames. This remains a development smoke sample above the 16.7 ms production target, not target-hardware proof.
- `npm.cmd test`: PASS with 30 focused checks, unchanged multi-winner race/track/battle outcomes, 30 virtual minutes, 1800 acknowledged actions, 59 resumes, 12 settled results, and 5.3 MB final heap delta in the final sealed run. Human listening, character attachment, physical phones, target LAN, and representative performance remain open gates.

## Production stewardship slice 01 — driving feel and combat readability

- Added a shared three-tier drift contract. Gas + brake + steer charges the drift; releasing brake grants a tiered boost. Hits cancel both the charge and boost.
- Added drift sparks, release bursts, boost trails, speed streaks, bounded hit shake, a segmented drift HUD, incoming Arc Bolt / nearby mine warnings, touch drift input, and a responsive synthesized engine layer.
- Added 18 focused core/package assertions and a 24-seed deterministic quality soak. The final run produced winners `{p1:9,p2:8,p3:2,p4:5}`, a 36.79-second average, and 68 total AI drift boosts.
- Added bounded client frame diagnostics for live local-browser smoke evidence. This does not replace representative-device profiling.
- Reframed capability comparison as a whole-product production gate. The gate is intentionally blocked on content, shield-battle mode, adaptive music, accessibility, physical LAN/device QA, and human approval.
- See `PRODUCTION_QUALITY_PLAN.md` and `evidence/production-quality-route.md` for the staged roadmap and claim-to-proof routing.

Live slice verification exposed and corrected two additional seams: an off-route nearest-point jump could counterfeit a lap, and lobby focus could leave the page scrolled far enough to hide combat warnings. Ordered lap checkpoints, a dedicated regression, start scroll reset, HUD stacking, and stronger warning contrast now pass. The live browser observed BLUE drift charge, SHIFT BOOST state, a visible MINE NEARBY warning, and a 390x844 controller with DRIFT READY and all six controls in bounds.

Client performance remains blocked: the in-app preview sampled 42.51 ms average and 55.6 ms p95 over 180 recent frames. That browser may impose its own cadence, but the reading cannot support a 60 fps claim; target-hardware profiling is required.

## Production stewardship slice 02 — Mirror Core battle

- Added a second complete server-authoritative mode selected only from the lobby. Circuit Crown remains unchanged; Mirror Core runs in a distinct magenta arena with twelve fast-respawn item pads and a 90% offensive item pool.
- Guard breaks score +1 CORE and clean unguarded wipes score +2. Twelve CORE wins immediately; otherwise the ranked score at 60 seconds wins. Equal base stats, rank catch-up, drift, repair, and all four character/vehicle identities remain shared.
- A clean wipe schedules a server-owned reboot, restores all three guard segments, applies 1800 ms spawn protection, and prevents item use, collision pressure, repeat score, and damage while offline/protected. Battle hits use a 980 ms anti-chain-stun cooldown.
- Battle AI hunts pads and opponents through the same semantic drive/item contract. Mode-aware observation, health, state, reset, result, controller HUD, and replay routes are covered.
- `npm.cmd test`: PASS with 22 focused checks, 24 race seeds, 32 Mirror Core seeds, and the HTTP mode/lock/reset contract. The battle soak produced winners `{p1:13,p2:6,p3:8,p4:5}`, 762 guard breaks, 142 wipes, and 125 completed re-entries at a 25.075-second average.
- Live Browser: PASS_WITH_BOUNDARIES. The lobby selection, rule copy, distinct arena, score/timer HUD, guard-break score event, high-intensity combat frame, complete battle result, mode-preserving rematch, and 390x844 phone HUD were observed. The high-cadence reboot midpoint and physical phone remain unobserved.
- Capability gap improved: `content.mode.shield-battle` is now available. At this slice boundary the overall production gate remained `BLOCKED` on two additional race circuits, adaptive music, remapping/assists, target hardware/LAN, performance, and human steward approval.

## Production stewardship slice 03 — three authored circuits

- Added Splitglass Gardens and Null Foundry beside Mirror Forge. Every circuit has independent generated geometry, material language, central and edge landmarks, item placement, two main-route hazards, and a labelled physical shortcut with its own timed risk hazard.
- Added lobby-only track selection and a server track catalog. Selection is locked after start and hidden in Mirror Core; health, observation, state, results, reset, and replay retain the selected race track without changing the frozen equal-stat vehicle contract.
- Shortcut traversal uses physical distance-to-corridor entry, ordered forward progress, explicit exit, and bounded recovery. Timed hazards slow/spin without consuming Flux Guard, preserving the combat/defence contract.
- `tests/track-soak.test.js` ran 12 held-out seeds per circuit. Final metrics: Mirror Forge winners `{p1:5,p4:3,p3:1,p2:3}`, 44.990-second average, 144 committed shortcut entries, 221 hazard hits, 95.8% finish rate; Splitglass winners `{p2:3,p1:5,p3:3,p4:1}`, 43.297-second average, 122 committed shortcuts, 192 hazard hits, 81.3% finish rate; Null Foundry winners `{p4:5,p2:2,p3:3,p1:2}`, 50.040-second average, 146 committed shortcuts, 218 hazard hits, 100% finish rate.
- Live Browser: PASS. The three circuits were individually selected and observed at desktop, the selector fit 390x844 and disappeared for battle, and a complete Splitglass race named the circuit in results and preserved it for rematch.
- Live observation rejected an initially valid Splitglass shortcut because its chord tracked too closely beside the main road. The final route visibly cuts the lower-right bend, carries a SHORTCUT label and timed prism-shear hazard, and was observed with several karts physically traversing it.
- `content.tracks.three` became available and the content requirement became ready. At this slice boundary the whole-product gate remained blocked on adaptive music, remapping/assists, representative hardware/LAN evidence, performance, and human steward approval.

## Production stewardship slice 04 — accessible controls

- Added three shared-screen keyboard layouts: WASD plus arrows, IJKL with U fire, and ESDF with A fire. The lobby selector, active binding copy, reload persistence, and default restoration were live-observed.
- Added player-specific LIGHT STEERING and AUTO GAS profiles. They are server-owned input transforms, configurable from shared screen or phone only in the lobby, included in seat observation, preserved across reset/rematch, and visibly named in the active HUD.
- The assist contract explicitly never changes the frozen vehicle stats. In the live tied receipt Mike reached speed 128.67 from raw throttle 0 with auto-accelerate active while base max speed remained 260. Focused tests verify the same boundary and configuration lock.
- Expanded the 390x844 phone controller with two labelled assist controls without losing steering, brake, gas, fire, start, status, or guard visibility. A live phone toggle changed AUTO GAS from ON to OFF while STEER HELP remained LIGHT.
- `npm.cmd test`: PASS with 27 focused checks, 24 default-track races, 36 cross-track races, 32 Mirror Core battles, and the HTTP assist/lock/reset contract. Race and battle results remain multi-winner under the unchanged equal-stat system.
- `accessibility.input-remap` and `accessibility.assists` are now available at their declared contracts, making the comparator accessibility requirement ready. Arbitrary per-key/gamepad remap, attack assists, color themes, and physical phone input remain outside the declared promotion evidence.
- The whole-product production gate remains blocked on adaptive music and unknown/degraded human, LAN, hardware, performance, and approval evidence; this slice does not promote the game out of alpha.

## Production stewardship slice 05 — adaptive score

- Added pure deterministic music-state selection and four distinct score profiles: Forge at 126 BPM, Gardens at 118 BPM, Foundry at 132 BPM, and Mirror Core at 138 BPM, each with its own root and scale language.
- The browser sequencer adds and removes pulse, bass, arpeggio, and spark layers across lobby, countdown charge, flow, chase, danger, overdrive, final lap, Core pressure/danger/showdown, victory, and debrief cues.
- The score uses the existing SOUND ON/OFF control and exposes bounded diagnostics through `window.__MIRRORSHIFT_AUDIO__` without changing authoritative simulation state.
- Focused tests verify profile identity and every cue family. Live receipts observed Forge charge at intensity 1 and 126 BPM, Forge chase at intensity 2 and 128 BPM, Foundry charge at 132 BPM, Mirror Core pressure at intensity 2 and 138 BPM, rising scheduled-note counts, and the scheduler reporting muted after SOUND OFF.
- Functional adaptation and mute behavior pass. Musical quality, engine/event balance, clipping, fatigue, and game-night speaker approval remain unknown until a human listening session; no listening-quality claim is inferred from scheduler telemetry.
- The capability comparator now has zero explicitly missing capabilities. The whole-product gate remains open because combat/balance/presentation/performance/reconnect capabilities are degraded or unknown and representative LAN, hardware, listening, and human steward approval are still absent.

## Production stewardship slice 06 — acknowledged reconnect reliability

- Replaced fragile page-local sequence counters with stable per-tab session identities and server-issued next client sequences. Page reload now resumes the existing session instead of silently sending stale actions.
- Every session action returns a tied request id, client sequence, server-global sequence, applied flag, server receipt time, processing time, and next sequence. Clients measure request-to-ack duration, report it to the authority, and show the latest round trip in the live link status.
- Added one-second heartbeats and explicit transport telemetry. Shared screen and phone sessions for the same player coexist independently rather than evicting one another.
- Added a 900 ms meaningful-input ownership lease. Neutral drive heartbeats from a second client are acknowledged but cannot cancel active throttle or steering from the current input owner; focused HTTP tests cover applied versus ignored receipts.
- `tests/reliability-soak.test.js` passed 30 virtual minutes across 12 race/battle rotations with 1800 acknowledged actions, 59 resumes, 12 settled results, zero rejected resumed actions, server tick p95 within the 5 ms local budget, and a bounded single-digit-megabyte heap delta.
- `tests/restart-recovery.test.js` proves the fresh authority rejects an unknown pre-handshake session, then accepts the same p1/Mike identity and the client's continuing sequence after resynchronization. Mid-match process persistence is intentionally not claimed.
- Live Browser sequence: the controller moved from LIVE · 3MS through a visible RECONNECTING… state and back to LIVE · 3MS after process replacement. A reload retained the same session id with one resume and no rejected actions. The final dual-client receipt reported two active p1 sessions, 125 accepted/applied actions, zero rejects, and 5.3 ms p95 local request-to-ack time.
- `state.reconnect.resume` is now available at its local declared contract. The wider game-night gate remains `UNKNOWN` until four physical phones, the target router, and a real-time hardware-duration run provide native evidence.

## Visual-test correction

The first live run exposed false AI lap wraps caused by a moving nearest-track search origin. The search now uses an immutable origin per query, a ten-second no-false-finish regression was added, all tests passed again, and the repeated live run remained actively racing at ten seconds before completing normally.

## Shared-workspace stewardship

No shared Game Hub router, registry, generated index, root package, or foreign game files were edited. The hub discovers this additive package through `game.manifest.json`. Concurrent work was observed in games 013, 014, and 016 and was preserved. Final snapshot showed no active changes inside the 015 lane.

## Broad library check

`node game-package-verifier-selftest.js`: PASS with 15 assertions. `node game-night-selftest.js` currently fails the moving-library hard seam gate on nine manifest/verification errors, all in concurrently active foreign game `016-hexbound-rooftops`; game 017 now passes the package scan. Classification: `FOREIGN_FAILURE`, not a MIRRORSHIFT regression. The focused MIRRORSHIFT verifier reports zero errors and only the declared physical-phone warning. No 014, 016, or 017 files were changed.

Final workspace snapshot: games 014 and 016 were actively changing; shared seams detected were MIRRORSHIFT's own just-written manifest/package plus foreign game 016's manifest. No foreign file overlapped the 015 lane, and the final complete MIRRORSHIFT suite used stable package inputs.

## Production stewardship slice 09 — equal-spec race signal conditions

- Added Clear Signal, Shardline Sprint, and Redline Gauntlet as frozen lobby-only race contracts. They run for 3, 2, and 4 laps and vary hazard cadence, item respawn, shortcut commitment/reward, off-road pressure, adaptive-score tempo, and visual atmosphere without changing vehicle statistics or the +7.5% catch-up ceiling.
- Server state, observation, health, reset, result, replay, launcher metadata, desktop HUD, lobby rule cards, controller status, track drawing, and music identity now carry the selected signal condition. Battle keeps its independent rules.
- Added focused invariant/lock/pressure checks plus `tests/variant-soak.test.js`: 72 races across all nine track/variant combinations. Every format settled within budget, all four seats won across the matrix, finish rate ranged from 86.5% to 93.8%, and Redline produced the expected highest hazard count.
- Live Browser: PASS_WITH_CORRECTIONS. The first 1280×720 lobby clipped the launch control; the shell was corrected to fit the full surface without page scroll. Shardline exposed an initial visual `4` and stale three-lap card copy; countdown display now clamps to `3` and the card derives 2/3/4 laps from the selected signal.
- A natural Null Foundry Redline journey then began at the corrected `3`, completed four laps, and produced CODEX WINS with `REDLINE GAUNTLET · 4 LAPS`, 100% equal base stats, 85% attacks, and three starting guards. The live phone displayed `LAP 1 / 4 · REDLINE` with all controls and guard state intact. Browser logs were empty.
- DOM-tied diagnostics reported 24369 Redline visual frames, `changesVehicleStats=false`, `changesCatchup=false`, and a 240-frame preview sample of 19.65 ms average / 27.8 ms p95. The sample improved materially but still fails the 16.7 ms production target; representative target-hardware profiling remains open.
- The user requested an active-memory checkpoint. `ACTIVE_RAM.md` now points to a 14-event sealed JSONL session segment with SHA-256 `c9d04ebd25c4598024eb3cca3df499848be81f9330ec570c11047bad1288372a`; no canonical or user-owned material was deleted.
- Final `npm.cmd test`: PASS with 31 focused checks, the 24-race default soak, 36 cross-track races, 72 track/variant races, 32 Mirror Core battles, 30 virtual minutes, 1800 acknowledged actions, 59 resumes, 12 settled results, restart recovery, and HTTP coverage. The final reliability heap delta was 6.4 MB.
- Focused package verification returned zero errors and only the declared physical-phone warning; the verifier's own 15 assertions passed. The broad Game Hub gate still reports nine hard failures, all inside concurrently owned `016-hexbound-rooftops`; MIRRORSHIFT, 014, and 017 pass. No foreign files were edited.

## Production stewardship slice 10 — presentation pipeline and honest frame diagnosis

- Added pure presentation-only pose interpolation between consecutive server packets. Ordinary positions follow a bounded linear path, heading uses the shortest wrap-safe arc, and corrections beyond 180 world pixels snap immediately. The client never mutates either authoritative packet and the interpolation layer cannot affect physics, collisions, ranking, input, catch-up, or vehicle statistics.
- Added `axm.render-performance-diagnostics/v1`, separating request-animation-frame cadence from Canvas execution, track/actor/effects/audio stages, HUD update cost, authority packet cadence, interpolation frames, pose snaps, and inferred scheduling wait. Workload samples reset on phase/mode/track transitions rather than mixing lobby, race, battle, and result data.
- Added `tests/presentation-pipeline-soak.test.js`: 45,504 held-out interpolated poses and 864 injected teleport snaps passed, with a 36-world-pixel maximum ordinary packet step and zero mutation of prior/current packets. The focused suite now has 32 checks.
- Live v0.11 Redline: the complete 1280×720 lobby and active Foundry pressure scene remained intact; a fresh active sample accrued 74 interpolated frames with zero snaps, measured 1.44 ms average / 3.3 ms p95 render work, 0.55 / 0.8 ms HUD work, and approximately 91.6 ms authority packets.
- Live v0.11 Mirror Core: the complete arena/HUD/combat composition remained intact; an isolated sample accrued 224 interpolated frames with zero snaps and measured 1.41 ms average / 2.8 ms p95 render work at approximately 91.2 ms authority packets.
- The preview's end-to-end frame p95 still ranged from 34.9 to 55.6 ms, with 28.5 to 43.7 ms outside measured render work and `schedulerLimited=true`. This closes only the bounded local render-work claim; perceived high-cadence smoothness, target-device 60 fps, thermal behavior, and 30-minute hardware evidence remain open.
- Final `npm.cmd test`: PASS with 32 focused checks, the presentation-pipeline soak, 24 default races, 36 cross-track races, 72 track/variant races, 32 Mirror Core battles, 30 virtual minutes, 1800 acknowledged actions, 59 resumes, 12 settled results, restart recovery, and HTTP coverage. Final observed reliability heap delta: 6.3 MB.
- Focused package verification returned zero errors and the one declared physical-phone warning; the package-verifier selftest passed all 15 assertions and all MIRRORSHIFT JSON parsed. The broad moving-library verifier could not produce a current verdict because concurrently created foreign lane `018-last-stop-nebula` had runtime files but no `game.manifest.json`; the required workspace snapshot reproduced that incomplete active package. Classification: `MOVING_WORKSPACE`, not a MIRRORSHIFT regression. No 014, 016, 017, or 018 files were edited.

## Production stewardship slice 11 — bounded target-session evidence hand

- Added opt-in `?perf=1` TARGET LAB without changing ordinary game-night launch. It records bounded 2-, 10-, or 30-minute sessions into `axm.target-session-performance/v1`, using fixed histograms, at most 64 workload segments, and at most 128 recent transition events rather than retaining raw frame logs.
- The receipt covers request-animation-frame pacing, Canvas render/HUD work, authority-packet cadence, server tick, client round-trip/server processing, interpolation/snaps, Long Tasks, supported JS heap/Battery signals, visibility/focus/offline interruptions, authority loss/recovery, target viewport, and explicit evidence boundaries. It retains no user agent, network address, microphone/camera data, raw screenshots, or external upload.
- The measurement layer issues read-only local telemetry GETs and records `authorityWrites=0`. It neither imports nor receives simulation state and cannot alter game physics, stats, ranking, collision, inputs, catch-up, or server authority.
- Added `tests/target-session-recorder.test.js`: a held-out synthetic 30-minute run summarized 108,000 frame samples in a compact receipt, exercised two workload segments, server/RTT/memory/battery/long-task/interruption paths, bounded the recent event list, preserved privacy fields, and proved complete-versus-partial behavior.
- Live Browser found and corrected two admission-language seams. A short requested smoke first used generic COMPLETE wording and left the visible selector on 30 minutes. The final schema exposes `durationClass` and `thirtyMinuteGateEligible`; the UI names query smokes explicitly and says `SMOKE COMPLETE // DOES NOT CLOSE 30-MINUTE GATE`.
- Final 1280×720 active-race smoke: 15,008.7 ms, four workload segments, 221 interpolation frames, zero snaps, 49 ms preview frame p95, 1.8 ms render p95, 0.05 ms server-tick p95, one Long Task, zero authority writes, false raw-frame/user-agent/network-address retention, and false 30-minute eligibility. The panel and closed full-race view were both visually intact; browser logs were empty.
- Final `npm.cmd test`: PASS with 33 focused checks, 45,504 interpolation samples plus 864 forced snaps, the 108,000-frame recorder test, 24 default races, 36 cross-track races, 72 track/variant races, 32 Mirror Core battles, 30 virtual reliability minutes, 1800 acknowledged actions, 59 resumes, 12 results, restart recovery, and HTTP delivery. Final reliability heap delta was 5.3 MB.
- Focused package admission reports zero errors and one expected physical-phone warning; package-verifier selftest passes 15 assertions; capability comparator selftest passes; all nine MIRRORSHIFT JSON files parse; comparator remains `UNKNOWN` with zero missing capabilities and explicitly lists the recorder as available while the 30-minute target soak remains unknown.
- The broad Game Hub verifier now completes and reports 12 foreign failures: nine in concurrently owned 016 and three missing required evidence paths in concurrently owned 017. Games 014, 015, and 018 pass. Classification: `FOREIGN_FAILURE`; no foreign files were edited.
- This slice builds the proof hand, not the proof substrate. A complete 30-minute run on representative hardware must still be joined with an external native profiler, thermal/compositor evidence, and high-cadence target-display observation before the 60 fps/hardware gate can change state.
- Durable session memory is sealed as 19 valid ordered JSONL events with SHA-256 `12540ad4d3b512048eaef32ece65a7b1aaf486b01d304eb11f5c12c2a7332dc8`; `ACTIVE_RAM.md` now points to that seal and preserves 60 durable events across four segments.
- Post-seal workspace snapshot was complete over 1,793 files: 66 active files across lanes 014–018, with four active shared seams belonging only to foreign 016/017/018. No active shared seam overlapped lane 015, whose final manifest/package hashes remained stable.

## Production stewardship slice 12 — four-phone LAN qualification hand

- Added the opt-in `?lanlab=1` FOUR-PHONE LAN LAB without changing ordinary game-night launch. It consumes read-only `/api/telemetry`, distinguishes controller/screen sessions, and emits bounded `axm.four-phone-lan-qualification/v1` receipts for P1–P4 connection overlap, stable identity, meaningful drive, fire attempts, heartbeats, RTT, reconnects, and same-seat input-owner protection.
- Extended transport telemetry with additive per-session controller kind, connected duration, drive/item counters, applied meaningful drive, heartbeat count, bounded RTT distribution, ignored actions, and resume count. No gameplay authority, stats, item weights, catch-up, guard rules, track rules, or race outcomes changed.
- The recorder has a 30-minute hard ceiling, at most 128 recent transitions, explicit never-auto-checked physical host attestations, `authorityWrites=0`, and false retention for session IDs, network addresses, user agent, and raw actions. Only a complete requested 30-minute run with all machine checks and explicit attestations can become eligible for external steward review; JavaScript can never change `physicalGateVerdict` from `requires-external-steward-review`.
- Added `tests/four-phone-lan-qualification.test.js`: 101 bounded synthetic snapshots over 30 minutes, one stable controller per seat, a P3 disconnect/resume, meaningful drive/item/heartbeat/RTT evidence, an ownership challenge whose rolling global evidence ages out while the durable per-session counter remains, duplicate-seat rejection, partial-run rejection, privacy, compactness, and the immutable external verdict all pass.
- Live local verification first exposed that ownership admission relied too heavily on the rolling global action window. The final recorder accepts the durable per-controller ignored-action delta; the held-out rollover regression and repeated live session pass. The live work also confirmed that background browser tabs are timer-throttled and therefore cannot stand in for four physical phones.
- Final foreground live receipt: 55.756 seconds of a requested two-minute smoke, P1–P4 machine-ready, 100% all-four overlap, one applied meaningful drive and one fire attempt per seat, RTT p95 `{p1:9.8,p2:10.3,p3:8.6,p4:9.6}` ms, nine resume handshakes, owner guard PASS, zero authority writes, false private/raw retention, and empty browser logs. All three physical attestations remained unchecked, eligibility remained false, status remained `PARTIAL RECEIPT · PHYSICAL GATE STILL OPEN`, and physical verdict remained external review.
- Final `npm.cmd test`: PASS with 34 focused checks, 45,504 interpolated poses plus 864 forced snaps, 108,000 target-recorder frame samples, 101 LAN snapshots, 24 default races, 36 cross-track races, 72 track/variant races, 32 Mirror Core battles, 30 virtual reliability minutes, 1,800 acknowledged actions, 59 resumes, 12 settled results, restart recovery, and expanded HTTP delivery. Final reliability heap delta was 5.3 MB.
- Focused package admission reports zero errors and one expected physical-phone warning; the package-verifier selftest passes 15 assertions; all 10 MIRRORSHIFT JSON files parse; the comparator remains `UNKNOWN` with zero missing capabilities, explicitly making the LAN recorder available while retaining `qa.physical-four-phone-lan` as unknown.
- The broad Game Hub verifier currently reports nine hard failures, all in concurrently owned `016-hexbound-rooftops`; games 014, 015, 017, and 018 pass. Classification: `FOREIGN_FAILURE`; no foreign file was edited. The final complete Game Hub snapshot scanned 1,875 files with 77 active files and seven active shared seams, all in foreign 017/018 paths; none overlapped lane 015.
- This slice builds and verifies the proof hand, not the physical proof substrate. The next native gate is an externally observed 30-minute run with four real phones on the intended router, explicit host attestations, visible seat confirmation, and review of the downloaded receipt.
- Durable session memory is sealed as 24 valid ordered JSONL events with SHA-256 `de2b133151a7c4e14add2a8023b4fd2eafef2cca9974ded44ead0e529a5e9925`; `ACTIVE_RAM.md` now points to that seal and preserves 84 durable events across five segments. No canonical, user-owned, or foreign-lane material was deleted.

## Production stewardship slice 13 — managed LAN route correction

- The external four-phone gate was probed before local work. ADB was unavailable, no portable/mobile device class was present, only the host Wi-Fi interface was discoverable, and port 8805 was initially down. Four independent phones and the intended router were therefore unavailable as executable evidence; tabs and LAN neighbors were not substituted.
- Source inspection found a concrete V13 run blocker: Game Hub constructs controller URLs with the host private-LAN address, but MIRRORSHIFT's managed child inherited its standalone `127.0.0.1` default. The recorder could be locally correct while every advertised off-host phone URL remained unreachable.
- Added a narrow `resolveHost` policy. `AXM_MANAGED_BY_GAME_HUB=1` now defaults to `0.0.0.0`, direct standalone launch still defaults to `127.0.0.1`, and explicit `HOST` always wins. `/health` exposes the active bind host/policy. No gameplay authority or gameplay contract changed.
- Added `transport.managed-lan-bind`, its capability contract, manifest/requirement routing, and `tests/managed-lan-bind.test.js`. The held-out test asserts all policy branches, opens a real all-interface socket, serves loopback health, and passes a same-machine private-interface probe when a private IPv4 interface is present.
- A fresh managed-style four-human runtime is live on port 8805 with `PLAYABLE ALPHA V14 · MANAGED LAN ROUTE READY`, bind host `0.0.0.0`, and policy `managed-lan-all-interfaces`. An independent Node HTTP probe reached it through the Wi-Fi interface. This proves the host bind route only; firewall policy, intended-router behavior, four physical phones, and 30-minute reliability remain unproven.
- Final `npm.cmd test`: PASS with 34 focused gameplay checks, 45,504 interpolated poses plus 864 forced snaps, the 108,000-frame target recorder, 101 LAN qualification snapshots, the new managed-bind test, 24 default races, 36 cross-track races, 72 track/variant races, 32 Mirror Core battles, 30 virtual reliability minutes, 1,800 acknowledged actions, 59 resumes, 12 settled results, restart recovery, and HTTP coverage. Final observed reliability heap delta was 4.3 MB.
- Focused package admission reports zero errors and one expected physical-phone warning; the package-verifier selftest passes 15 assertions; all 12 MIRRORSHIFT JSON files parse. The regenerated comparator remains `UNKNOWN` with zero missing capabilities, makes `transport.managed-lan-bind` available, and retains `qa.physical-four-phone-lan` as unknown.
- The broad Game Hub verifier reports nine failures, all in foreign `016-hexbound-rooftops`; games 014, 015, 017, and 018 pass. Classification: `FOREIGN_FAILURE`; no foreign package was edited.
- The original four-character, equal `260 / 178 / 2.45` vehicle contract, `+7.5%` catch-up ceiling, 85% attack-heavy race pool, and repairable three-segment Flux Guard remain frozen. Version promotion is limited to the managed LAN route; it is not a physical-phone, target-performance, human-quality, or AAA promotion.
- Durable V14 session memory is sealed as 17 valid ordered JSONL events with SHA-256 `d35bca137bc63d74cd1a64fd930f843ae357c17290cb7939478146ea04e0f42d`; `ACTIVE_RAM.md` preserves 101 durable events across six segments. The next native gate remains the externally observed four-real-phone 30-minute intended-router run with truthful attestations and steward receipt review.

## Production stewardship slice 14 — four-seat qualification start preflight

- The external four-phone gate was probed first. ADB remained unavailable and no present portable/mobile device class was exposed, so four real phones and the intended router were still unavailable as executable evidence. Local controller tabs and injected API sessions were used only to verify software behavior and were never counted as phones.
- Found a qualification-integrity defect: V13/V14 allowed the 30-minute clock to start before P1–P4 were connected, so an already-ineligible run could consume the full gate. V15 adds `axm.four-phone-lan-preflight/v1`; START remains disabled until healthy telemetry reports exactly one total and one connected controller identity on every seat.
- The start path rechecks telemetry immediately before recording, stores only sanitized per-seat counts, and records the passing preflight as a machine check. The first packet is baselined before the clock loop so pre-start drive/fire/heartbeat counters cannot satisfy post-start challenges.
- Missing, disconnected, duplicate, and unhealthy seats are visibly blocked. Closed controller identities remain blocked for a bounded 15-second authority-telemetry hold, then age out automatically so an accidental duplicate can recover without a server restart. A duplicate observed after recording begins remains durable receipt evidence.
- Held-out tests cover ready, missing P4, duplicate P1, stale disconnected P2, unhealthy telemetry, sanitized receipt output, missing-preflight rejection, and authority-side expiry under a controlled clock while preserving the prior 101-snapshot 30-minute qualification contract.
- At 1280×720, live local observation covered 0/4 blocked, 4/4 ready, start/stop partial receipt, duplicate P1 block, and automatic post-hold recovery. The partial receipt kept all physical attestations false, `eligibleForStewardReview=false`, `authorityWrites=0`, all private/raw retention flags false, and the physical verdict external. Browser warning/error logs were empty; no rolling visual buffer was available, so the visual evidence is repeated semantic snapshots and still frames rather than motion proof.
- Final `npm.cmd test`: PASS with 34 focused gameplay checks, 45,504 interpolated poses plus 864 forced snaps, the 108,000-frame target recorder, gated LAN qualification, managed bind, 24 default races, 36 cross-track races, 72 track/variant races, 32 Mirror Core battles, 30 virtual reliability minutes, restart recovery, and HTTP coverage. Final observed reliability heap delta was 4.7 MB.
- Focused package admission reports zero errors and one expected physical-phone warning; the package-verifier selftest passes 15 assertions; all 15 final MIRRORSHIFT JSON files parse. The regenerated comparator remains `UNKNOWN` with zero missing capabilities, promotes `qa.four-phone-lan.start-preflight` to available, and retains `qa.physical-four-phone-lan` as unknown.
- The broad Game Hub verifier still reports exactly nine failures, all in untouched foreign `016-hexbound-rooftops`; lane 015 has zero errors. No 014, 016, 017, or 018 file was edited.
- The supervised port-8805 process ended during the long regression and was restarted from current V15 source. Fresh health reports `PLAYABLE ALPHA V15 · FOUR-SEAT PREFLIGHT READY`, managed host `0.0.0.0`, and zero connected controller sessions; this pointer is useful but explicitly ephemeral.
- Mike, Axiom/Mir, Codex, and Mirror; equal `260 / 178 / 2.45` stats; the `+7.5%` catch-up ceiling; 85% attack-heavy race pool; repairable three-segment Flux Guard; and server authority remain frozen. This is a qualification-integrity promotion, not a physical-phone, target-performance, human-quality, content-depth, or AAA promotion.
- Durable V15 session memory is sealed as 25 valid ordered JSONL events with SHA-256 `853d07097ada195065d7c3e7672798f6a17e4d9942a246e85692b4ff8b1339fc`; `ACTIVE_RAM.md` preserves 126 durable events across seven segments. No canonical, user-owned, foreign-lane, or temporary capture file was deleted.

## Production stewardship slice 15 — explicit controller-seat confirmation

- The external gate was probed first. ADB was unavailable, zero Android devices and no present mobile-device substrate were exposed, so four real phones and the intended router remained unavailable. Local tabs were used only to verify software behavior and were never called phones.
- V16 adds lobby-only `axm.mirrorshift-seat-confirmation/v1`. Each controller session begins with `CONFIRM P1` through `CONFIRM P4`; only the matching controller-kind session can acknowledge, screen sessions are rejected, same-session reload restores acknowledgement, and a new session or authority process requires confirmation again.
- LAN preflight now requires exactly one total, one connected, and one confirmed controller session on every seat. The host distinguishes 4/4 live from 4/4 confirmed, names unconfirmed seats, and records only sanitized confirmation counts plus `controllerSeatConfirmationsReady`; no gameplay authority or frozen rule changed.
- Held-out coverage passes a connected-but-unconfirmed P3 negative, controller/screen authorization, unconfirmed-first handshake, same-session resume, additive telemetry, and confirmation-ready receipts while preserving the existing 101-snapshot 30-minute qualification, missing/duplicate/stale/unhealthy cases, privacy exclusions, and immutable external verdict.
- Live local verification passed the full confirmation journey at the default browser surface: four controller tabs produced 4/4 live with zero confirmations and disabled START; each unique real UI action changed `CONFIRM P#` to START; the host enabled only after four confirmations; P1 reload preserved confirmation. A 10.108-second partial receipt kept `controllerSeatConfirmationsReady=true`, `authorityWrites=0`, and `physicalGateVerdict=requires-external-steward-review`. Host and controller warning/error logs were empty.
- Responsive evidence remains honest. The in-app browser accepted a 390×844 override but both existing and newly opened tabs still reported 1280×720; resetting exposed a device-pixel-ratio screenshot crop despite in-bounds DOM geometry. Current V16 portrait confirmation pixels and physical-touch usability remain UNKNOWN rather than inferred from the default browser surface.
- Final `npm.cmd test`: PASS with 34 focused gameplay checks, 45,504 interpolated poses plus 864 forced snaps, the 108,000-frame target recorder, confirmation-gated LAN qualification, managed bind, 24 default races, 36 cross-track races, 72 track/variant races, 32 Mirror Core battles, 30 virtual reliability minutes, 1,800 acknowledged actions, 59 resumes, 12 settled results, restart recovery, and HTTP coverage. Final observed reliability heap delta was 6 MB.
- Focused package admission reports zero errors and one expected physical-phone warning; the verifier selftest passes 15 assertions; all 18 final lane JSON files parse. The comparator remains `UNKNOWN` with zero missing capabilities, makes `qa.four-phone-lan.controller-seat-confirmation` available, and retains `qa.physical-four-phone-lan` as unknown. The broad Game Hub verifier still reports exactly nine failures, all in untouched foreign `016-hexbound-rooftops`.
- Mike, Axiom/Mir, Codex, and Mirror; equal `260 / 178 / 2.45` stats; the `+7.5%` catch-up ceiling; 85% attack-heavy race pool; repairable three-segment Flux Guard; and server authority remain frozen. This is a run-integrity/seat-clarity promotion, not a physical-phone, target-performance, human-quality, content-depth, or AAA promotion.
- A complete final Game Hub workspace snapshot scanned 1,947 files with 29 active files and two active shared seams: this slice's lane-015 qualification contract and a separate foreign-018 manifest. Re-reading the lane-015 seam found no overlap; package SHA-256 remained `084efbd44f04b1fff89fac011bd36f0433a8b32c7c8604afd467413e02e8fc2b` and manifest SHA-256 remained `08d7c78258e6d39469a4b0600b0dc3302e4a29b8e8447563b4547cdecb3dab3e`.
- Durable V16 session memory is sealed as a 27-event source segment with SHA-256 `349eb34ffc65faa8b038ba9d1dd41aaf160f8d1d9eb6de972e97522605850040` plus a 3-event finalization segment with SHA-256 `8daff3863f29f7e4328a4d998e933fa2f04df53428513adebd90c14d93903180`; `ACTIVE_RAM.md` preserves 156 durable events across nine segments. No canonical, user-owned, foreign-lane, or temporary capture file was deleted.

## Production stewardship slice 16 — server-authoritative Signal Tour

- The external four-phone gate was probed first. ADB remained unavailable and no present portable/mobile substrate was exposed, so four independent phones and the intended router were unavailable. Local browser surfaces were used only for software and responsive-composition checks and were never counted as phones.
- V17 adds optional `axm.mirrorshift-signal-tour/v1`: Forge/Clear, Gardens/Sprint, then Foundry/Redline; 9/6/4/2 points per round; tie-break by total points, latest-round placement, then stable seat order. Clients cannot replace the itinerary.
- Core state v6 owns cumulative points, history, standings, champion identity, exactly-once awards, round-local reset, and explicit final-round-winner/champion separation. `/api/tour/advance` is the only non-final progression route and immediately starts the next authority countdown.
- Shared-screen and controller presentation now cover lobby selection, locked route/signal copy, round/point/lap HUD, cumulative debriefs, next round, champion, and new-tour restart. The original roster, equal `260 / 178 / 2.45` spec, `+7.5%` catch-up cap, 85% race attack pool, repairable three-segment Flux Guard, and server authority did not change.
- `tests/signal-tour.test.js` passes all three rounds, the fixed itinerary, 63 awarded points, latest-round tie-break, champion/final-winner split, duplicate-award prevention, round reset, profile retention, completed-tour rejection, and frozen mechanic assertions. Expanded HTTP coverage passes selection, locks, early-advance rejection, metadata, observation, and reset.
- Final `npm.cmd test` passes 34 focused checks, 45,504 interpolated poses plus 864 snaps, 108,000 target-recorder samples, 101 LAN snapshots, managed bind, 24 default races, 36 cross-track races, 72 track/variant races, 32 Mirror Core battles, the new tour lifecycle, 30 virtual reliability minutes, restart recovery, and HTTP coverage. Final observed reliability heap delta was 5.4 MB.
- One natural 1280×720 tour journey completed all three routes. Codex led the cumulative debriefs at 9 and 18 points, then the final screen showed CODEX CHAMPION with 27 points and NEW SIGNAL TOUR; restart returned to Round 1, Clear Signal, and zero points. Browser warning/error logs were empty.
- A 390×844 browser override rendered the P1 controller without clipping and exposed steer, brake, gas, fire, confirmation, tour/point/lap, drift, catch-up, and three guard segments. This closes only the responsive-browser composition check; physical phone/touch/router evidence remains open. Live inspection also found and corrected a broad variant selector that touched the HUD badge.
- Focused package admission has zero errors and one expected physical-phone warning; all 18 lane JSON files parse. Package SHA-256 is `30b1339b0306bd4b6b2dd2e55a610e7ca2f785341c56f7292ec535bb75ab241a`; manifest SHA-256 is `9b9a2215444cb9fa857947fe88b2138615b22a33e2b5cba2f5b8205b9b099457`. The comparator remains `UNKNOWN` overall with zero missing capabilities and admits only `content.mode.signal-tour`.
- The port-8825 verification authority was stopped and the browser tab finalized without retained raw captures. The stale V16 port-8805 process was replaced by current V17 source; fresh health reports `PLAYABLE ALPHA V17 · SIGNAL TOUR`, managed `0.0.0.0`, server authority, and the lobby. This runtime pointer remains ephemeral.
- Durable V17 session memory is sealed as 18 valid ordered JSONL events with SHA-256 `7faa1625dd21a84b50e722b7ba2178c3a6c8aef8acbc700a1d45d16f1019a15a`; `ACTIVE_RAM.md` preserves 174 durable events across ten segments. Four real phones/intended router, representative target-hardware 30 minutes, human listening/attachment, deeper animation/content, and explicit steward approval remain open; no AAA achievement is claimed.

## Production stewardship slice 17 — presentation-only kinetic identity

- The external gate was probed first. ADB was unavailable, zero present portable/mobile or mobile-like devices were exposed, and no qualifying network profile or representative target platform appeared. The physical LAN and hardware runs remained substrate/evidence gaps; browser surfaces were never counted as phones.
- V18 adds frozen `KINETIC_RIGS` and pure `axm.vehicle-animation-pose/v1`. Mike uses ratchet pistons, Axiom/Mir counter-orbit fins, Codex compile-stack fins, and Mirror phase-prism plates. All share bounded idle, drive, brake, drift, boost, impact, wheel, suspension, ride, roll, and signature-phase outputs with `changesPerformance=false`.
- Reduced motion scales ride, roll, load, and impact amplitudes to 12%, freezes wheel spin, and fixes identity phase at its midpoint. Pose calculation never mutates its racer input or server state. Canvas integration is presentational and adds no authority field, physics branch, network traffic, collision shape, or stat change.
- `tests/kinetic-rig.test.js` passes 108,000 finite/bounded poses across four rigs and responsive states, off-road vibration, reduced motion, identity uniqueness, no mutation, equal `260 / 178 / 2.45` stats, `+7.5%` catch-up, 85% race attacks, and three-segment Flux Guard.
- Final `npm.cmd test` passes 35 focused checks, 45,504 interpolated poses plus 864 forced snaps, 108,000 kinetic poses, 108,000 target-recorder samples, 101 LAN snapshots, managed bind, 24 default races, 36 cross-track races, 72 track/variant races, 32 Mirror Core battles, Signal Tour, 30 virtual reliability minutes, 1,800 actions, 59 resumes, 12 results, restart recovery, and HTTP coverage. Observed reliability heap delta was 4.2 MB.
- Live 1280×720 browser verification covered the complete lobby, countdown, active play, a later active frame, reset, and all four P1 character choices. P1 changed from `idle` to `drive`; more than 8,499 pose frames accrued; all four machines, authored track, effects, HUD, standings, item slot, three guard segments, interpolation, assist disclosure, and equal-stat footer remained visible.
- Live draft mapping reported `axiom-twin-rig`, `codex-stack-rig`, `mirror-prism-rig`, and `maker-piston-rig` with the correct identity motion labels. Active local work measured 1.48 ms average / 3 ms p95 total render, 0.4 ms actor average, and 1.1 ms HUD p95; warning/error logs were empty.
- Browser capabilities exposed no rolling buffer. The live receipt therefore proves repeated rendered states, semantic transitions, and local render work—not high-cadence smoothness, target-device frame pacing, thermal behavior, or representative hardware. Screenshots remained ephemeral tool output and no raw capture path was retained.
- Capability comparison remains `UNKNOWN` with zero missing identifiers, admitting only `visual.motion.kinetic-identity`. Package verification reports zero errors and the one expected physical-phone warning. Package SHA-256 is `c7f77c8ad2a7cbfced09d6985540476b67b7990651b1e863d0e2faa7518afee6`; manifest SHA-256 is `6aaf7cdd20e12c85fa6afffb368ccb5418f3de9d823eaee7d5b2687df229ebd7`.
- The V17 port-8805 listener was replaced with current V18 source. Fresh health reports `PLAYABLE ALPHA V18 · KINETIC IDENTITY`, managed `0.0.0.0`, server authority, lobby, and four racers. This pointer remains ephemeral.
- Durable V18 memory is sealed as 20 valid ordered JSONL events with SHA-256 `4e137c08f773ae8268bb37b8ef0ddc6de4569326b977cf9823146b1a3f5d0ae7`; `ACTIVE_RAM.md` preserves 194 durable events across eleven segments. Four real phones/intended router, representative target-hardware 30 minutes and cadence, human listening/attachment, broader animation/voice/content, and explicit steward approval remain open; no AAA achievement is claimed.

## Production stewardship slice 18 — bounded fastest-lap Mirror Echo

- The external gate was probed first. ADB remained unavailable, zero present portable/mobile or mobile-like devices were exposed, and no qualifying network profile or representative target platform appeared. Four real phones/intended router and target hardware were unavailable; browser views were never counted as phones.
- V19 adds pure `axm.mirror-echo/v1`. Up to nine browser-session records cover the three circuits by three signal conditions; each lap trace is capped at 720 samples, compacts deterministically rather than silently truncating, and replaces the course record only when strictly faster.
- Capture observes ordinary server packets but makes no authority write, mutates no packet, persists no identity/network data, uploads nothing, and freezes stored records/samples. Playback interpolates position/speed plus shortest-arc heading only on a matching race course and is excluded from Mirror Core.
- Shared-screen presentation adds ECHO EMPTY/ON/OFF, lobby/result/HUD disclosure, and a dashed translucent `ECHO // <CHARACTER>` hologram. Copy explicitly says `FASTEST LAP REPLAY // NON-COLLIDING // NOT A FIFTH RACER`; the echo cannot collide, score, collect items, block racers, or occupy a seat.
- `tests/mirror-echo.test.js` passes capture, immutable authority input/records, strict fastest replacement, course isolation, shortest-heading interpolation, bounded long-trace compaction, pause/resume, battle exclusion, and the unchanged equal `260 / 178 / 2.45`, `+7.5%`, 85% race attack, and three-segment guard contracts.
- One natural 1280×720 journey covered ECHO EMPTY, pause/resume, first-race seal, result disclosure, and rematch playback. Axiom/Mir sealed `0:10.356`; the rematch rendered the labelled hologram and matching HUD while Mike, Axiom/Mir, Codex, and Mirror all remained present. A 390×844 responsive browser view fit ECHO ON, four standings, attack slot, track, P1 HUD, three guard segments, and touch controls. Browser errors were empty.
- Active V19 local render work measured 1.65 ms average / 3.1 ms p95 total, 0.46 ms actor average, and 1 ms HUD p95 with the echo and complete authored composition present. Repeated stills and these metrics do not prove high-cadence motion, target-device frame pacing, or thermal behavior; no rolling-buffer capability or raw capture path existed.
- Final `npm.cmd test` passes 35 focused checks, 45,504 interpolated poses plus 864 snaps, 108,000 kinetic poses, Mirror Echo, 108,000 target-recorder samples, 101 LAN snapshots, managed bind, 24 default races, 36 cross-track races, 72 track/variant races, 32 Mirror Core battles, Signal Tour, 30 virtual reliability minutes, 1,800 actions, 59 resumes, 12 results, restart recovery, and HTTP coverage. Observed reliability heap delta was 6.4 MB.
- `content.replay.mirror-echo` is available. The independent capability comparator reproduces the checked-in report semantically; overall remains `UNKNOWN` with zero missing capabilities and all physical, hardware, audio-listening, human-balance/preference, and approval gates preserved.
- Focused package admission reports zero errors and one expected physical-phone warning; all 20 pre-seal lane JSON files parse. SHA-256: package `4cf79e56c1f8db87165f2069b419e23fdde12701749276adee4c91f604fbdc32`; manifest `b435ae4fee42830c374883186acbf95257e28a63703757dfd6b72fabdddc8b9f`; core `058ca51be4ccddbb8a05eaf670fc225b8f5811b226ea9a382b234617fac62fa3`; server `a4dba5b0072a2d9fc4ef8735fa4b460d53e9fb0ed3cd61c364d0512a52427431`; app `a28ccec01f3da1d20030da6e1202736b2742cf7da7fce7114426162d5f1630a5`; echo `0c8eee54dab76f9d28ac2e4570a6a4b2242d33544b0fcc11f7986e15ce92490a`.
- The current V19 managed runtime is healthy through `http://192.168.2.2:8805`: `PLAYABLE ALPHA V19 · MIRROR ECHO`, server authority, lobby, and four racers. A concurrently owned foreign 013 process holds the more-specific `127.0.0.1:8805` route; it was not stopped or edited, so loopback currently reaches that game. The private-interface pointer and the collision are ephemeral shared-runtime state.
- The final shared snapshot scanned 1,934 files with 66 active files and four active shared seams: this slice's package, manifest, and Mirror Echo contract plus foreign 016 manifest. All three lane-015 seams were re-read and matched the intended V19 state; no 014/016/017/018 file was edited.
- Durable V19 memory is sealed as 22 valid ordered JSONL events with SHA-256 `12a08552e4c85b11be7949ba35abe2852db83736ab34b9acffb214be0bbce5cd`; `ACTIVE_RAM.md` preserves 216 durable events across twelve segments. Browser stills remained ephemeral, the viewport was reset, the tab finalized, and the temporary generated comparator removed after semantic comparison.
- Mike, Axiom/Mir, Codex, and Mirror; equal `260 / 178 / 2.45` stats; the `+7.5%` catch-up ceiling; 85% attack-heavy race pool; repairable three-segment Flux Guard; and server authority remain frozen. Four real phones/intended router, representative target-hardware 30 minutes/cadence, human listening/attachment/replay preference, dedicated time-trial/persistent ghost breadth, broader animation/voice/content, and explicit steward approval remain open; no AAA achievement is claimed.

## Production stewardship slice 19 — server-authoritative Reflection Routes

- The external proof substrate was probed first. ADB was unavailable, zero present portable/mobile devices were exposed, and no qualifying router or representative target-hardware surface was available. Four real phones/intended router and target hardware therefore remained non-executable; browser views were never counted as phones.
- V20 adds lobby-only `axm.route-direction/v1` forward/reflection selection to Circuit Crown. Reflection preserves the finish reference, reverses remaining course traversal, remaps checkpoint/item/hazard indices, swaps and remaps shortcut direction, and derives a runtime course without mutating canonical forward tracks. Signal Tour is explicitly locked forward; Mirror Core has no route direction.
- `/api/route-direction` and `axm.mirrorshift-state/v7` keep the server authoritative. State, session, observe, result, health, shared HUD, and controller disclose the direction; unsupported, non-lobby, tour, and battle changes reject. Reset/rematch preserves the Circuit Crown choice.
- Mirror Echo advances to `axm.mirror-echo/v2` with up to 18 isolated track/variant/direction records. Forward and reflection cannot leak into one another; every existing non-colliding, non-scoring, no-authority-write, bounded-compaction contract remains.
- The deterministic reflection matrix completed 36 races across 3 tracks x 3 variants x 4 seeds: all four seats won, AI finish rate was 91%, with 420 shortcut commits and 831 hazard hits. Canonical forward track JSON remained unchanged. Frozen roster, equal `260 / 178 / 2.45` stats, `+7.5%` catch-up cap, 85% race attack pool, repairable three-segment Flux Guard, and server authority passed unchanged.
- The final regression passes 36 focused checks, 45,504 interpolation poses plus 864 snaps, 108,000 kinetic poses, Mirror Echo V2, 108,000 target-recorder samples, 101 LAN snapshots, managed bind/private-interface self-probe, 24 default races, 36 track races, 72 variant races, 36 reflection races, 32 battles, Signal Tour, 30 virtual reliability minutes, restart recovery, and HTTP. The latest rotation recorded 1800 actions, 59 resumes, 12 results, and 5.9 MB heap delta.
- Two live visual seams were found and corrected before admission: a 5 px inner lobby overflow at 1280x720 and an Echo-HUD/P4 overlap at 390x844. Repeated measurements then showed no overflow or overlap. One natural reflection journey covered unique selection, countdown, reversed active geometry, `MIRROR WINS`, all four rankings, Mirror's 0:10.259 echo seal, and direction-preserving rematch playback beside all four authoritative racers.
- Final 1280x720 local reflection render work measured 2.32 ms average / 3.4 ms p95 total, 0.4 ms track average, 0.53 ms actor average, and 0.74 ms average / 1.2 ms p95 UI work; 423 interpolation frames accrued with zero snaps. Diagnostics were scheduler-limited, and repeated stills cannot prove high-cadence or target-hardware frame pacing.
- Capability admission remains honestly `UNKNOWN` overall with zero missing declarations. `content.routes.reflection` and direction-isolated `content.replay.mirror-echo` are available only at their bounded local contracts. Physical phones/router, 30-minute target performance/cadence, human listening/attachment/replay and route preference, broader animation/voice/content, and steward approval remain open.
- The temporary port-8825 V20 verification runtime was intentionally stopped; the browser viewport was reset and its tab finalized without retained raw captures. No active runtime pointer is claimed because local listener state is ephemeral.
- Durable V20 memory is sealed as 26 valid ordered JSONL events with SHA-256 `0382d589459d53a24044587ffcfd59a653eb306f923bff8570d2d90a56fc76e0`. A separate append-only three-event finalization seal `cd08553c232935ee36ccd87449445aa8e5444a399b842fe94802c74d96150a73` preserves the discovery and exact-PID cleanup of the orphaned 8825 child plus the foreign-013 boundary on port 8805. `ACTIVE_RAM.md` preserves 245 durable events; the requested V13 four-phone seal `de2b133151a7c4e14add2a8023b4fd2eafef2cca9974ded44ead0e529a5e9925` remains linked.

## Production stewardship slice 20 - four-character Mirror Podium

- The external proof substrate was checked first. ADB was unavailable, device/network queries exposed no usable four-phone/intended-router or representative target-hardware surface, and access to some Windows hardware/profile inventory was denied. The external 30-minute gates were not executable; browser views were never counted as phones.
- V21 adds four frozen presentation moments: Mike's `ratchet-salute`, Axiom/Mir's `twin-orbit-lock`, Codex's `compile-complete`, and Mirror's `return-vector`. Pure `axm.result-stage-pose/v1` derives bounded entrance/lift/roll/scale/opacity/glow, settles statically under reduced motion, and changes neither performance nor authority.
- Circuit Crown, Signal Tour, and Mirror Core results now render exactly four stable server-ranked portrait cards with mode-native metrics, presentation-only disclosure, and `axm.result-stage-diagnostics/v1`. Polling retains an unchanged stage instead of rebuilding it every frame.
- `tests/result-stage.test.js` passes 112 bounded poses across four identities, four places, multiple times, and reduced motion, including finite bounds, distinct moments, static reduced poses, authority immutability, manifest/static wiring, and every frozen mechanic.
- The first complete-suite attempt rejected only a stale HTTP asset assertion expecting the V20 Mirror Echo query version. The assertion was corrected to V21 without weakening production behavior. The next full suite passed; after all documentation and capability admission, the final `npm.cmd test` passed again.
- Final regression passes 36 focused checks, 45,504 interpolation poses plus 864 snaps, 108,000 kinetic poses, 112 result poses, Reflection Routes, Mirror Echo V2, 108,000 target-recorder samples, 101 LAN snapshots, managed bind/private-interface self-probe, 24 default races, 36 track races, 72 variant races, 36 reflection races, 32 battles, Signal Tour, 30 virtual reliability minutes, restart recovery, and HTTP. The final rotation recorded 1,800 actions, 59 resumes, 12 results, and 5.9 MB heap delta; an earlier successful V21 run recorded 6.4 MB.
- One natural 1280x720 Circuit Crown run reached `MIRROR WINS` after 56.327 authority seconds. The 760x394.5 result card had equal client/scroll height, four non-overlapping portrait cards, distinct moments, truthful server order, and a visible `RUN IT BACK`; rematch returned to countdown.
- At an explicit 390x844 responsive browser viewport, the result card fit as a complete non-overlapping 2x2 tableau with equal client/scroll height and a visible 319.2x44 continuation action. This is browser composition evidence, not a physical-phone or touch claim.
- One natural Mirror Core run reached `CODEX WINS` at 12 CORE. All four distinct moments and truthful Core/break/wipe metrics remained visible without overlap or inner overflow; `RUN IT BACK` preserved battle and `RESET` restored its lobby. Browser warning/error logs were empty.
- After the responsive reload, a stale client lobby-phase guard ignored the first Mirror Core mode-card locator click while authority remained in race. The server-owned mode endpoint established battle, then the public start/rematch/reset journey passed. This slice admits the result-stage and continuation contracts, not a fresh end-to-end mode-card-selection receipt.
- Completed Mirror Core result work measured 2.55 ms average / 3.9 ms p95 render and 1.1 ms UI p95 locally. The same preview reported 76.4 ms frame p95, 100% long-frame rate, and `schedulerLimited=true`; this is explicit counterevidence against a preview-based 60 fps claim, not representative target-hardware evidence.
- `visual.motion.result-stage` is available at its bounded local contract. The regenerated comparator remains `UNKNOWN` overall with zero missing declarations, and every physical, target-performance, listening, human-attachment/preference, breadth, art-direction, and steward gate remains open.
- Focused package admission reports zero errors and one expected physical-phone warning; package-verifier selftest passes 15 assertions; comparator selftest passes; all 24 post-seal lane JSON files parse. SHA-256: package `93b7e4eabbe6a6c82e9624f9ae68cb3d080d6fdc572303fb99b9873bcdd6a305`; manifest `8816af233418733b77b84073684cf178ea2c21841ce1ea41adeb5c89256baaf0`; core `6b12851495270a7353f91c544b7f30816d350ad0f1ee6bec6c87b184bc85c246`; app `68aa8c24b98889d60927ab50d6089d0c8cb63564b817e91d1e3913bb63c98c74`; styles `6c8bec883b114126a2b5dbb4010a500af5ec49650bf687a28d368461deb6ee6f`; result test `d35f4ff8f353094447fd7b29cd7bf5156ec9da9e8bb924412e594dd41644df1c`.
- The browser viewport was reset and its tab finalized. The temporary V21 port-8826 child survived terminal interrupt; health confirmed exactly `015-axm-mirrorshift`, then exact PID 10360 was stopped and netstat found no listener. The latest port-8805 probe also found no listener; no foreign process was stopped.
- The complete game-library snapshot found active foreign work in 013/014/016/017/018, including shared manifest seams in foreign 016 and 018. Lane-015 package, manifest, capability, and evidence seams were re-read; no foreign file was edited.
- Durable V21 memory is sealed as 26 valid ordered JSONL events with SHA-256 `aba46028582ee838e630bc6f6592e7fbd3da052df21de4eeb4bdc68c064940ff`. Screenshots remained ephemeral, repeated polls were aggregated, and no raw capture or temporary comparator was retained.
- Mike, Axiom/Mir, Codex, and Mirror; equal `260 / 178 / 2.45`; the `+7.5%` catch-up ceiling; 85% attack-heavy race pool; repairable three-segment Flux Guard; and server authority remain frozen. Four real phones/intended router, representative target-hardware 30 minutes/cadence, human listening/attachment/replay/route preference, deeper animation/voice/content, independent art-direction judgment, and explicit steward approval remain open; no AAA achievement is claimed.

## Production stewardship slice 21 - four-character Start Grid

- The external proof substrate was checked first. ADB was unavailable, Windows hardware/profile inventory was denied, and no usable four-phone/intended-router or representative target-hardware surface was exposed. The external 30-minute gates were not executable; browser views were never counted as phones.
- Live V21 reproduction corrected the prior diagnosis. The visible Mirror Core button was enabled, but `elementsFromPoint` showed the transparent race HUD's item button above it. Hidden HUD visibility gating now removes that hit target. The client also always submits a visible mode request to `/api/mode` and reports acceptance/refusal, while the unchanged server `setMode` lock remains the sole authority outside the lobby.
- V22 adds four frozen presentation moments: Mike's `ratchet-ready`, Axiom/Mir's `signal-synced`, Codex's `compile-armed`, and Mirror's `vector-returned`. Pure `axm.start-grid-pose/v1` derives bounded entrance/lift/roll/scale/opacity/glow, settles statically under reduced motion, and changes neither performance nor authority.
- Circuit Crown, Signal Tour, and Mirror Core countdowns render exactly four authority-owned seat/character/vehicle cards with mode-aware heading copy, presentation-only disclosure, and `axm.start-grid-diagnostics/v1`. The countdown overlays the racing HUD, retains server timing, and launches through the existing authority action.
- `tests/start-grid.test.js` passes 112 bounded poses across four identities, four seats, multiple times, and reduced motion, including finite bounds, distinct moments, authority immutability, server-owned non-lobby mode rejection, hidden-HUD pointer gating, manifest/static wiring, and every frozen mechanic.
- The first post-implementation suite failed only because the unchanged Mirror Echo file's cache query was unnecessarily advanced to V22 while the existing selftest correctly expected V21. The query was restored; no behavior or contract was weakened. The complete suite then passed, and after final CSS/evidence changes it passed again.
- Final regression passes 36 focused checks, 45,504 interpolation poses plus 864 snaps, 108,000 kinetic poses, 112 result poses, 112 start-grid poses, Reflection Routes, Mirror Echo V2, 108,000 target-recorder samples, 101 LAN snapshots, managed bind/private-interface self-probe, 24 default races, 36 track races, 72 variant races, 36 reflection races, 32 battles, Signal Tour, 30 virtual reliability minutes, restart recovery, and HTTP. The final rotation recorded 1,800 actions, 59 resumes, 12 results, and 7.5 MB heap delta; this is accelerated local evidence, not a real-time hardware soak.
- Public 1280x720 controls selected Mirror Core after the hit-test repair, produced server mode `battle`, rendered four unobscured ready cards with `MIRROR CORE // FOUR SIGNALS ARMED`, and transitioned through countdown to server phase `racing`. At 390x844, Signal Tour rendered a complete 2x2 grid inside the countdown with document width equal to the viewport, and a configured Splitglass Gardens / reflection / Shardline Circuit Crown start showed `SPLITGLASS GARDENS // REFLECT // SPRINT`; all three modes had four cards and browser warning/error logs were empty.
- `visual.motion.start-grid` is available at its bounded local contract. The checked comparator remains `UNKNOWN` overall with zero missing declarations, and every physical, target-performance, listening, human-attachment/preference, breadth, art-direction, and steward gate remains open.
- Focused package admission passes 36 assertions; all 24 lane JSON files parse; the independent semantic capability check reports `UNKNOWN`, zero missing, and start-grid available. SHA-256: package `c86a7e7cc4cd4fa959e4f224616f178f88f1ff1015d073ffd814a3b25530563e`; manifest `d4dfdff95ff2a7bb63ba7bb5a5bf12c0d05b976a3dcad06f61bc2f28a6305ab0`; core `56a69860455b03062d45aac0b40b424360e401aca4c88b028c0f790d381c1334`; app `27c6fb73d75ac60b8732a38e02fd9e760cee07d2cc6e890827a7d3caed5a01b1`; styles `3d6047ca84e45cdb6b2b94a2c6a74a6256d07b12287d2801152fbe4a9c035706`; start-grid test `b38d059b25e607e2d9de6cb020e014bb03771f99456ba65ab979b23921d47298`.
- Browser stills remained ephemeral tool output; the 390x844 viewport override was reset and the tab finalized. The temporary port-8827 child survived terminal interrupt; health confirmed game ID `015-axm-mirrorshift`, then exact PID 25320 was stopped and netstat found no listener. The unidentified port-8805 404 service was never stopped or modified.
- Lane-015 package, manifest, capability, and evidence seams were re-read; no foreign 014/016/017/018 file was edited.
- Durable V22 memory is sealed as 26 valid ordered JSONL events with SHA-256 `f8b3193b82c4926cb85de5cccbd241af7df4cb0f62a0b927e3e53813a747a566`; `ACTIVE_RAM.md` preserves 297 durable events across the prior chain plus V22. Screenshots remained ephemeral and repeated polls were aggregated.
- Mike, Axiom/Mir, Codex, and Mirror; equal `260 / 178 / 2.45`; the `+7.5%` catch-up ceiling; 85% attack-heavy race pool; repairable three-segment Flux Guard; and server authority remain frozen. Four real phones/intended router, representative target-hardware 30 minutes/cadence, human listening/attachment/replay/route preference, deeper animation/voice/content, independent art-direction judgment, and explicit steward approval remain open; no AAA achievement is claimed.

## Production stewardship slice 22 - Living Circuits

- The external proof substrate was checked first. ADB remained unavailable, Windows device/network inventory was denied, port 8805 was not attributable to this package, and no usable four-phone/intended-router or representative target-hardware surface was exposed. The external 30-minute gates were not executable; browser views were never counted as phones.
- V23 adds four frozen presentation profiles: Mirror Forge `forge-countercycle`, Splitglass Gardens `garden-bloom-tide`, Null Foundry `foundry-heat-cycle`, and Mirror Core `core-vector-scan`. Pure `axm.environment-choreography-pose/v1` derives bounded phase, secondary phase, rotation, drift, glow, and density; every pose declares no performance or authority change.
- The canvas renders environment-native counter-orbit arcs, petal glints, lifted stack embers, and return-vector shards. Existing Forge/Gardens/Foundry landmarks now share the bounded pose phase, and Mirror Core retains its arena/rules. `axm.environment-choreography-diagnostics/v1` exposes the active identity on the game frame.
- `tests/environment-choreography.test.js` passes 72,000 bounded poses across the four environments, four distinct frozen identities, finite/amplitude bounds, invalid-id fallback, time-invariant reduced motion, authority immutability, manifest/static wiring, and the original equal `260/178/2.45`, +7.5% catch-up, 85% race attack, and three-segment Flux Guard contracts.
- The first complete V23 regression failed only because the V22 Start Grid test still pinned package/manifest version strings. Those two bookkeeping assertions and the changed core/app cache keys were advanced to V23 without weakening the 112-pose Start Grid contract. The next full suite passed.
- A live health probe found the runtime still advertised hardcoded `PLAYABLE ALPHA V21 · MIRROR PODIUM`. The runtime label and HTTP assertion were corrected to `PLAYABLE ALPHA V23 · LIVING CIRCUITS`; the next full run then exposed the same stale label in the managed-LAN assertion. That exact expectation was advanced to V23 without changing behavior or weakening coverage, and the final complete suite passed.
- Public desktop controls launched Gardens, Mirror Core, Foundry, and Forge. Each rendered its named visual identity with four racers, complete circuit/arena, HUD, items/hazards, and no horizontal overflow. Two Gardens frames 720 ms apart had different digests while the environment counter advanced 28 frames. Browser warning/error logs were empty.
- Local active render work measured Gardens `2.87 ms average / 4.7 ms p95`, settled Foundry `2.35 / 4`, Forge `1.45 / 2.7`, and Mirror Core `1.37 / 1.8`. These scheduler-limited in-app measurements support bounded render work only; they do not prove 60 fps, thermal safety, or representative hardware.
- The in-app browser accepted a requested 390x844 override, but existing and new tabs remained 1280x720 and the narrow media query stayed false. V23 responsive pixels therefore remain `UNKNOWN`; prior V22 responsive receipts were not recycled as V23 evidence.
- Final regression passes 36 focused checks, 45,504 interpolation poses plus 864 snaps, 108,000 kinetic poses, 112 result poses, 112 start-grid poses, 72,000 environment poses, Reflection Routes, Mirror Echo V2, target/LAN recorders, managed bind, 24 default races, 36 track races, 72 variant races, 36 reflection races, 32 battles, Signal Tour, 30 virtual reliability minutes, restart recovery, and HTTP. The final accelerated rotation recorded 1,800 actions, 59 resumes, 12 results, and 4.8 MB heap delta; an earlier successful V23 run recorded 6.4 MB. Neither is a real-time hardware soak.
- Capability comparison remains `UNKNOWN` overall with zero missing declarations. `visual.motion.environment-choreography` is available only at its bounded local contract; authored presentation remains degraded and every physical, target-performance, listening, human-attachment/preference, responsive-V23, art-direction, and steward gate remains open.
- Browser stills remained ephemeral tool output; the ineffective viewport override was reset and all verification tabs were finalized. Port 8828 health identified the owned V23 authority, exact PID 25492 was stopped, and final health/netstat probes found no listener. Port 8805 was untouched.
- Lane-015 package, manifest, capability, and evidence seams were re-read around mutations. Active foreign workspace edits remained present; no foreign 014/016/017/018 file was edited.
- Final integrity SHA-256: package `bdd01151f8b5f5fcc5ef046acea8e2fcc068269fcfc41b83d102c5072758e604`; manifest `38aecc130fc27ab7797ddf3daafe9dc532ef4ceeccea7209b2674dd8f262a83d`; core `32e534a91b1cadfa05d360902db5bf0e7a22c3fcb03b5375691cd55679499d2c`; app `2efd50ffd76a70d4b7b693904918d87edf62b9390bca773042523adc07f873cf`; server `89508bedd369bf94a312f8815143cc5f4f5b9788e1d3c4a10f599032a0b25010`; index `e1fa625ba94fe0a7e978b152396b64ba4a3f84b8154b5f72f1475298ab5e1493`; choreography test `132e8831432e9f882049d9ca68b1fd81609135c6f59069ce799f90f6323efe73`; managed-LAN test `780cf6a715528ed662cdafe5c03cc5309cbdc5e052e7949f781625dd0d39f1d5`.
- Durable V23 memory is sealed as 28 valid ordered JSONL events with SHA-256 `e441981cc0053d008bfa6c5bdc13d2018e06837c576cc3c36432aa8b7b400a5e`; the complete chain now preserves 325 durable events. The requested V13 seal `de2b133151a7c4e14add2a8023b4fd2eafef2cca9974ded44ead0e529a5e9925` remains linked. All 26 final lane JSON files parse.
- Mike, Axiom/Mir, Codex, and Mirror; equal `260 / 178 / 2.45`; the `+7.5%` catch-up ceiling; 85% attack-heavy race pool; repairable three-segment Flux Guard; and server authority remain frozen. Four real phones/intended router, representative target-hardware 30 minutes/cadence, human listening/attachment/replay/route preference, deeper animation/voice/content, independent art-direction judgment, and explicit steward approval remain open; no AAA achievement is claimed.
