# Evidence route

| Claim | Evidence |
|---|---|
| Four named AXM-native characters and distinct vehicle identities | `runtime/game-core.js`, lobby roster in `runtime/index.html` + `runtime/app.js`, generated roster key art |
| Unique per-seat lobby character draft | atomic identity-swap and lock assertions in `tests/mirrorshift-selftest.js`, `/api/character` + reset persistence in `tests/server-http.test.js`, shared-screen/phone cross-seat live observation, and manifest rules |
| Non-mechanical character expression | frozen `CHARACTER_EXPRESSIONS`, identity/event assertions, `drawCharacterFlourish`, `playCharacterMotif`, DOM-tied expression diagnostics, Mirror launch before/action/settled frames, and natural Codex result; perceived audio and character attachment remain human gates |
| Presentation-only kinetic identity rigs | frozen `KINETIC_RIGS`, pure `vehicleAnimationPose`, `tests/kinetic-rig.test.js` across 108,000 bounded poses, reduced-motion/no-mutation/frozen-mechanics assertions, `evidence/kinetic-identity-contract.md`, live P1 draft-to-rig mapping for all four identities, and a 1280x720 idle/countdown/drive/reset sequence with DOM-tied kinetic/render diagnostics; rolling motion, target hardware, and human attachment remain open |
| Presentation-only four-character start grid | frozen `START_GRID_MOMENTS`, pure `startGridPose`, `tests/start-grid.test.js` across 112 bounded poses, reduced-motion/authority-immutability/frozen-mechanics assertions, `evidence/start-grid-contract.md`, and public Mirror Core, Signal Tour, and configured reflection Circuit Crown selection/start journeys; 1280x720 and 390x844 grids expose exactly four distinct authority-owned seat assignments without horizontal overflow, while high-cadence target motion, physical phones, human attachment, and art direction remain open |
| Presentation-only Living Circuits choreography | frozen `ENVIRONMENT_CHOREOGRAPHIES`, pure `environmentChoreographyPose`, `tests/environment-choreography.test.js` across 72,000 bounded poses, time-invariant reduced motion, authority immutability, frozen-mechanics assertions, `evidence/environment-choreography-contract.md`, and live 1280x720 Forge/Gardens/Foundry/Core observations with named DOM diagnostics and zero horizontal overflow; local render work ranged from 1.37-2.87 ms average / 1.8-4.7 ms p95, while V23 responsive pixels, rolling/high-cadence motion, representative hardware, human response, and art direction remain open |
| Presentation-only four-character result stage | frozen `RESULT_STAGE_MOMENTS`, pure `resultStagePose`, `tests/result-stage.test.js` across 112 bounded poses, reduced-motion/authority-immutability/frozen-mechanics assertions, `evidence/mirror-podium-contract.md`, and natural race plus Mirror Core result/rematch journeys; the 1280x720 cards and button have no overlap or inner overflow, the race stage fits 390x844 as a 2x2 grid, and all four mode-native server rankings remain visible; high-cadence target motion and human attachment remain open |
| Presentation-only fastest-lap Mirror Echo | `runtime/mirror-echo.js`, `tests/mirror-echo.test.js`, `evidence/mirror-echo-contract.md`, immutable bounded session records, no-authority-write/frozen-mechanics assertions, and a natural 1280x720 empty-lobby → first-race seal → results → rematch hologram journey plus 390x844 responsive composition; high-cadence target motion and human replay preference remain open |
| Presentation-only authority-packet interpolation | pure `interpolatePresentationPose` wrap/snap contract, no-mutation focused assertion, `tests/presentation-pipeline-soak.test.js` with 45,504 interpolated poses and 864 teleport snaps, plus DOM-tied active Redline and Mirror Core receipts; high-cadence perceived smoothness remains a target-device visual gate |
| Direction-isolated Mirror Echo V2 | `runtime/mirror-echo.js`, `tests/mirror-echo.test.js`, and `evidence/mirror-echo-contract.md` cover immutable bounded `axm.mirror-echo/v2` records across 18 track/variant/direction identities; a natural reflection empty-to-seal-to-result-to-rematch journey and 390x844 responsive composition pass, while high-cadence target motion and human replay preference remain open |
| Identical car performance | frozen `BASE_STATS` object, self-test equality assertions, manifest rule |
| Equal three-tier drift and release boost | `METRICS` drift arrays, `releaseDrift`, deterministic self-tests, manifest rules |
| Slight catch-up | frozen `catchupByRank` values and focused self-test |
| 85% offensive item pool | `POWERUP_WEIGHTS`, manifest rule, focused self-test |
| 90% Mirror Core attack pool | `BATTLE_POWERUP_WEIGHTS`, manifest rule, focused self-test |
| Three-segment base shield and repair | `METRICS.shieldMax`, `applyHit`, `useItem`, focused self-test |
| Mirror Core guard-break/wipe scoring | deterministic scoring, cooldown, reboot, protection, timer, and result assertions in `tests/mirrorshift-selftest.js` |
| Mirror Core seed smoke balance | 32 held-out battles in `tests/battle-soak.test.js`; all four seats won, but human balance judgment remains open |
| Three authored Circuit Crown tracks | `TRACKS` catalog and geometry in `runtime/game-core.js`, lobby/server selection contracts, manifest inventory, and live desktop/phone observations |
| Three equal-spec race signal conditions | frozen `RACE_VARIANTS`, lobby-only `/api/variant`, dynamic race-lap/hazard/item/shortcut contracts, 31 focused checks, 72 held-out races in `tests/variant-soak.test.js`, live Shardline active play, natural four-lap Redline result, and active phone `LAP 1 / 4 · REDLINE`; every receipt denies stat/catch-up changes |
| Server-owned three-round Signal Tour | frozen `SIGNAL_TOUR` itinerary/points/tie-break contract, `advanceTour`, `/api/tour/advance`, manual route/variant rejection, `tests/signal-tour.test.js`, live HTTP checks, `evidence/signal-tour-contract.md`, and one natural three-round 1280x720 journey through cumulative debrief, champion, and new-tour restart; the tour preserves equal stats, +7.5% catch-up, 85% race attack draws, and three-segment repairable Flux Guard |
| Physical shortcut and timed-hazard behavior | ordered shortcut/hazard assertions in `tests/mirrorshift-selftest.js`, 36 held-out races in `tests/track-soak.test.js`, and the corrected Splitglass live visual sequence |
| Server-authoritative reflection routes | `deriveTrackForDirection`, lobby-only `/api/route-direction`, state/observe/result/health disclosure, `tests/reflection-route.test.js`, `evidence/reflection-route-contract.md`, a 36-race 3-track x 3-variant x 4-seed matrix, canonical-track immutability, indexed pad/hazard remapping, shortcut swap/remapping, and one complete 1280x720 select/countdown/race/result/rematch journey; Signal Tour remains forward-only and reflection is not counted as additional environments |
| Selected-track reset and replay persistence | `/api/track` and `/api/reset` HTTP assertions plus the complete Splitglass result/rematch journey |
| Server-authoritative simulation | `runtime/server.js`, health/state/action HTTP test |
| Game-night phone controller seam | responsive, mode-aware `runtime/controller.html`, launcher metadata HTTP test, prior 390x844 battle observation, v0.8 live character-selection observation, V16 live confirmation/start state, and a V17 390x844 browser composition showing tour/point/lap, steering, brake, gas, fire, drift, catch-up, and three guard segments without clipping; the override is responsive browser evidence only, so real-phone physical touch and intended-router checks remain pending |
| Explicit controller seat acknowledgement | lobby-only `POST /api/seat-confirm`, controller-kind/session authorization checks, unconfirmed-first handshake, same-session resume, additive transport telemetry, unconfirmed P3 qualification negative, and the live P1-P4 `CONFIRM` → `START` journey. This proves the software acknowledgement and gate only; it cannot identify the person or certify phone/router truth |
| Resumable acknowledged input | `/api/session`, `/api/heartbeat`, per-client/server sequence receipts, `/api/client-telemetry`, HTTP stale-sequence and dual-session assertions, complete controller reload/server-replacement Browser sequence, and `tests/restart-recovery.test.js` |
| Local rotation lifecycle | `tests/reliability-soak.test.js`: 30 virtual minutes, 12 race/battle rotations, 1800 acknowledged actions, 59 resumes, 12 settled results, zero rejected resumed actions, and bounded local heap delta; not evidence for target-hardware real-time safety |
| Keyboard layout remapping | visible WASD/arrows, IJKL, and ESDF selector in `runtime/index.html` + `runtime/app.js`; reload-persistence and active-copy live observation; static package assertions |
| Equal-stat driving assists | `ASSIST_CONTRACT`, `setAssists`, effective-input path, deterministic no-raw-throttle movement assertion, HTTP lock/reset receipt, live HUD/server-state pairing, and 390x844 phone toggle journey |
| Adaptive procedural score | pure `musicStateFor` state contract, four profile-specific scales/tempos, focused cue/layer assertions, live scheduler/profile/transition diagnostics, and live mute telemetry; listening and mix quality remain routed to human review |
| Seed-level race smoke balance | 24 held-out default-track races plus 36 held-out cross-track races in `tests/quality-soak.test.js` and `tests/track-soak.test.js`; this is not a human balance verdict |
| Production quality status | `PRODUCTION_QUALITY_PLAN.md`, `evidence/production-quality-route.md`, and capability comparator; blocked gates remain explicit |
| V18 kinetic-rig local render cost | active 1280x720 `axm.render-performance-diagnostics/v1`: 1.48 ms average / 3 ms p95 total render work, 0.4 ms average actor stage, and 1.1 ms HUD p95 while all four animated rigs, circuit, effects, HUD, and interpolation remained present; scheduler cadence and representative hardware remain separate |
| V19 Mirror Echo local render cost | active 1280x720 rematch `axm.render-performance-diagnostics/v1`: 1.65 ms average / 3.1 ms p95 total render work, 0.46 ms average actor stage, and 1 ms HUD p95 while the echo, all four authoritative racers, complete circuit, HUD, effects, kinetic rigs, and interpolation remained present; scheduler cadence and representative hardware remain separate |
| V20 reflection local render cost | active 1280x720 reflection rematch `axm.render-performance-diagnostics/v1`: 2.32 ms average / 3.4 ms p95 total render work, 0.4 ms track average, 0.53 ms actor average, and 0.74 ms average / 1.2 ms p95 UI work while Mirror Echo, four authoritative racers, full circuit, HUD, effects, rigs, and interpolation remained present; scheduler-limited cadence and representative hardware remain separate |
| V21 Mirror Podium local result cost | completed 1280x720 Mirror Core result `axm.render-performance-diagnostics/v1`: 2.55 ms average / 3.9 ms p95 total render work and 1.1 ms UI p95 with four portrait cards and the complete result composition present; the same preview reported 76.4 ms frame p95 and `schedulerLimited=true`, so this supports bounded render work while explicitly leaving target-device frame pacing open |
| V23 Living Circuits local render cost | active 1280x720 diagnostics measured Gardens at 2.87 ms average / 4.7 ms p95, Foundry at 2.35 / 4 ms after countdown, Forge at 1.45 / 2.7 ms, and Mirror Core at 1.37 / 1.8 ms; these are scheduler-limited local render-work receipts, not a target-device cadence or thermal claim |
| Local render-work and frame-pacing diagnosis | live `window.__MIRRORSHIFT_RENDER__` and `window.__MIRRORSHIFT_DIAGNOSTICS__`; v0.11 active Redline measured 1.44 ms average / 3.3 ms p95 render work and 0.55 / 0.8 ms HUD work, while active Mirror Core measured 1.41 / 2.8 ms render work. Preview frame p95 remained 34.9–55.6 ms and was classified scheduler-limited; representative hardware remains unverified |
| Visual quality and live interaction | browser screenshot + bounded keyboard interaction receipt in `evidence/visual-verification.json` |
| Target-device performance receipt hand | `runtime/performance-recorder.js`, `runtime/performance-ui.js`, `evidence/target-session-recorder-contract.md`, 108,000-frame held-out test, HTTP/static admission, and opt-in live smoke. A complete receipt proves only what that browser/runtime measured; external profiler, thermal/compositor, target-display motion, and human evidence remain separate |
| Four-phone LAN qualification receipt hand | `runtime/lan-qualification.js`, `runtime/lan-lab-ui.js`, additive per-session transport telemetry, privacy-bounded exactly-one-connected-and-confirmed-controller start preflight, bounded 15-second closed-identity recovery, `evidence/four-phone-lan-qualification-contract.md`, held-out ready/missing/duplicate/stale/unhealthy/unconfirmed and 101-snapshot four-seat tests, HTTP sender/receiver receipts, and the opt-in live four-confirmation/reload/partial-receipt journey. The hand may make a 30-minute receipt eligible for review but cannot prove physical phones/router, truthful human confirmation, or issue the steward verdict |
| Managed private-LAN reachability | `resolveHost` in `runtime/server.js`, `evidence/managed-lan-bind-contract.md`, `tests/managed-lan-bind.test.js`, `/health` bind policy, and a real same-machine private-interface health probe. This proves the managed child listens on the advertised interface; firewall, router isolation, Wi-Fi, and phones remain external |

No single test is treated as proof of all claims. Visual behavior is verified visually; deterministic rule behavior is verified at the game-core boundary; transport claims use tied sender/receiver acknowledgements; target hardware, Wi-Fi, and perceived quality remain routed to their native human/physical surfaces.
