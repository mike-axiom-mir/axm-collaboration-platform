# Build receipt - Neighborhood Debt Aftermath + hybrid low-poly 3D

Lane: `tools/game-hub/game-library/017-small-odds`

Status: **WORKING** for the bounded v0.11.0 local slice. Not CANON. The multi-game Steam/Game Hub objective remains active.

## What changed

- Save version 11 and package `0.11.0-neighborhood-debt-aftermath-3d-working` preserve all ten prior milestones.
- One unhandled replay-valid negative Starspite table receipt can open three authored Lopsided Lane responses: public solidarity rota, transparent Long Table/Kettle recovery work, or Hushglass breathing room.
- Eligibility replays the saved seed and validates table, stake, outcome, winning set, gross result, insurance arithmetic, and negative net. Wins, malformed receipts, cash, lots, and remembered luck fail closed.
- Start freezes source receipt, route, time/energy, due day, recovery cap/payment, and exact return effect. Return resolves once and leaves a route-specific home/district mark.
- Recovery is at most `min(saved loss, route cap)`. Starspite net remains final. Same-day storefront income is separately identified in the return receipt.
- v1-v10 migration adds neutral aftermath fields/counters without fabricating history. A small copy migration corrects singular “credit” grammar without changing causal facts.
- `runtime/world-three.js` adds original dependency-free WebGL 1 scenes for all six locations at 480x270, with depth testing, cube/pyramid low-poly forms, a block-built Pip, 16-step shader lighting, and three durable recovery markers.
- The existing Canvas renderer remains simulation/interaction authority, blends above WebGL, and returns to full opacity if WebGL initialization fails.

## Focused evidence

- Full package suite: **102 passed, 0 failed**.
- All package JSON: **39 parsed, 0 invalid**.
- Isolated Game Hub package verifier: **0 errors**.
- Debt tests cover authentic-source validation, visible gates, all three route effects, frozen/exact-once return, storefront separation, compact receipt state, RNG firewall, and neutral v10 migration.
- Visual static tests cover genuine WebGL, depth, 480x270 internal resolution, 16 steps, Canvas authority/fallback, six scene functions, three marker forms, local-only source, and runtime syntax.

## Desktop lived verification

- In-app browser at 1280x720 compiled the WebGL renderer and reported the declared resolution, palette steps, animated state, and Canvas authority.
- Room, kitchen, district, shore, market, and Starspite all routed correctly and produced six distinct screenshot digests.
- Two room frames 650 ms apart differed. This proves bounded change, not continuous frame pacing.
- A legitimate saved 1-credit Truth Coin loss exposed all three routes as OPEN. Long Table commitment moved Day 31 14:00→17:00 and energy 100→92, froze a Day 32 return, and held concurrent start.
- Sleep crossed Day 31→32. Credits 2,026→2,066 separated into 39 storefront + 1 recovery. Rating 11→12, Kettle 8→9, Long Table standing 5→6, and pressure 0→1. Starspite net stayed -3.
- Reload preserved the return, current -2 next source, rating 12, and the low-poly `repayment-stamp` marker.
- New route buttons measured 44px; the dialog was labeled; duplicate ids and unnamed visible buttons were zero; desktop document width equaled viewport; browser diagnostic log was empty.
- The final browser-local QA state is Lopsided Lane, Day 32 21:00, 2,066 credits, 100 energy, portal 3/3, one completed return, one remaining replay-valid -2 loss, and renderer marker `repayment-stamp`.

## Repository gates

All ten AGENTS.md commands exited 0:

1. `node verify.js` - 0 FAIL, 38 warnings.
2. `node hub/hub-selftest.js`
3. `node hub/route-selftest.js`
4. `node hub/graft-selftest.js`
5. `node hub/skin-selftest.js`
6. `node hub/verify-plus.js`
7. `node tests/html-script-syntax-test.js` - 55 PASS, 0 FAIL.
8. `node tests/tool-forge-package-test.js`
9. `node tools/agent-tool-forge/selftest.js` - 17 PASS, 0 FAIL.
10. `node tools/evidence-desk/selftest.js` - 36 PASS, 0 FAIL.

Warnings remain repository evidence/backlog limitations, not Small Odds failures. Required checks operated in a pre-existing dirty shared worktree. Five generated registry/index files were already modified before this pass and remain preserved: `tools-index.json`, `registry/modules.json`, `registry/capabilities.jsonl`, `registry/public-status.json`, and `registry/proofs.json`.

## Boundaries and unrun checks

- Fresh compact viewport, WebGL context-loss fallback, representative GPU breadth, physical controller/device, screen-reader, and independent long-session playtests were not run.
- No rolling capture hand existed; continuous frame pacing is UNKNOWN.
- Low-poly WebGL is a 3D baseline, not a production character rig/model/animation/LOD/cinematic pipeline.
- Steamworks, depot/build pipeline, Cloud, overlay, achievements, gamepad parity, store assets/copy, legal/privacy, install/update/uninstall, certification, and release authority remain unbuilt/unverified.
- The whole slot is still untracked in the current worktree. No commit, staging, merge, promotion, or canonization was performed.
