# Neighborhood Debt Aftermath + 3D evidence route

Status: **WORKING**. Deterministic and desktop live evidence pass within the boundaries below.

## Claim routes

| Claim | Source | Deterministic verifier | Live verifier |
|---|---|---|---|
| Only a replay-valid saved casino loss opens aftermath | `runtime/systems.js` `replayValidCasinoLoss` / `starspiteDebtProfile` | `tests/systems.test.mjs` receipt-validity test | Inspect Lane route console after a known loss |
| Three authored routes have visible exact gates | `runtime/game-data.js` `STARSPITE_DEBT_ROUTES` and `runtime/app.js` | systems gate test + package self-test | Open Lopsided Lane panel and inspect OPEN/LOCKED reasons |
| Commitments freeze and return resolves exactly once | `startStarspiteDebtAftermath` / `resolveStarspiteDebtReturns` | solidarity, Long Table, and Hushglass systems tests | Start, reload, cross due day, reload, inspect one return |
| Recovery is separate from storefront income | return `moneySeparation` receipt | Long Table same-day income test | Inspect return receipt after a storefront day crossing |
| Future RNG is unchanged | no RNG call in aftermath; receipt flags | same-seed firewall test | Compare published table receipt rules before/after |
| Six low-poly scenes use genuine WebGL at 480x270 / 16 steps | `runtime/world-three.js`, HTML, CSS | `tests/world-three.test.js` | Browser dataset + screenshots + scene travel |
| Three consequences leave distinct 3D memory | aftermath house marks + `debtMarker` | systems marker assertions + static 3D test | District scene after authored returns |
| WebGL loss preserves playable Canvas authority | constructor fallback | static 3D test | Browser context-loss simulation if available; otherwise unclaimed |

## Claim boundary

Source and deterministic checks can prove causal mechanics, syntax, declared rendering topology, and exact arithmetic. Only a live browser can prove current WebGL compilation, visible rendering, responsive containment, click behavior, reload continuity, and console condition. Physical devices, production GPU breadth, continuous frame pacing, Steam packaging, and player-duration balance require separate evidence.

## Desktop live receipt - 2026-08-16

- Surface: in-app browser, 1280x720, local `127.0.0.1:8817` route.
- Entry/reload: title → continued v10 life migrated to v11 → six travel destinations → reload → continued exact state.
- WebGL: live canvas reported `renderer=webgl-low-poly`, `resolution=480x270`, `paletteSteps=16`, `authority=visual-only-canvas2d-simulation-authority`; all six locations reported their correct route and produced distinct bounded screenshot digests.
- Motion: two room frames sampled 650 ms apart produced different SHA-256 hashes. This proves bounded visual change, not continuous frame pacing.
- Content: an existing replay-valid 1-credit Truth Coin loss exposed three OPEN routes. The Long Table route consumed 3 hours/8 energy, froze Day 32, and blocked a second active route.
- Return: sleep crossed Day 31→32. Wallet 2,026→2,066 consisted of 39 storefront credits plus 1 separately accounted recovery credit. Rating 11→12, Kettle 8→9, Long Table standing 5→6, and pressure 0→1. Starspite net remained -3.
- Persistence: reload preserved Day 32, rating 12, one return, the next distinct -2 loss source, and `debtMarkers=repayment-stamp` in the 3D renderer.
- Semantics/geometry: 44px route buttons; labeled dialog; zero duplicate ids; zero unnamed visible buttons; document width equaled the 1280 viewport; browser diagnostics returned an empty log.
- Polish iteration: live evidence found singular-credit grammar and an overlarge household snapshot. Source now pluralizes correctly, normalizes the one pre-fix QA history string during migration, and limits the receipt's household `before` state to warmth/trust/agreements. The 102-test rerun passed afterward.
- Temporary capture: screenshot buffers remained in memory; motion buffers were nulled after digesting and no temporary video/file path was created.
- Unobserved: fresh compact viewport, WebGL context loss/fallback, representative GPU matrix, physical controls/devices, rolling frame pacing, and Steam behavior.
