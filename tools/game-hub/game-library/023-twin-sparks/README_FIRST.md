# Twin Sparks: Reactor Run

Status: `TEST` · Mike-approved internal Workshop promotion · not `CANON`.

This package installs the exact byte-bound v0.4 Twin Sparks candidate under
Game Hub slot `023`. The 14 files in `candidate/` are copied without changes
from packet `sha256:6680703ed8ed2bbe652c60a07be9504981292972896f9466ea56a9b67538c246`,
generated by source commit `146155f6307a27f13c3073098616f6771cbba26c`.
The separate trusted local server exposes only `index.html`, `game.js`, and
`styles.css`; it cannot write, save, call providers, use outbound network, or
promote the game.

Run the package check from the Workshop root:

```powershell
node tools/game-hub/game-library/023-twin-sparks/tests/package-selftest.js
```

Then run the local `TEST` server:

```powershell
node tools/game-hub/game-library/023-twin-sparks/runtime/server.js
```

Open `http://127.0.0.1:8823/games/023/`.

The installed package is a review-branch result. The canonical checkout was not
changed, public release and direct public reuse remain held, and Mike remains
the final merge gate.
