{
  "capabilities": [
    { "id": "workspace.write.slot014", "status": "available", "constraints": ["No writes outside 014-bonk-and-bolt"] },
    { "id": "test.node.deterministic", "status": "available", "constraints": ["Systems, hero motion, co-op camera, package, HTTP and focused verifier"] },
    { "id": "browser.capture.repeated-screenshot", "status": "available", "constraints": ["1280x720 in-app browser; separated visible-state observations"] },
    { "id": "browser.runtime.dataset", "status": "available", "constraints": ["Direct parade mode, module count and position state; no screenshot timing inference"] },
    { "id": "browser.input.local", "status": "available", "constraints": ["Bounded continue, interact and map controls on localhost"] },
    { "id": "asset.character.skeletal-3d-production", "status": "unavailable", "constraints": ["Blender absent; no .blend, .fbx, .gltf, .glb, .dae or .obj production character/rig asset in the available workshop library"] },
    { "id": "visual.capture.ephemeral-rolling-buffer/v1", "status": "unavailable", "constraints": ["No sub-frame timing claims"] }
  ]
}
