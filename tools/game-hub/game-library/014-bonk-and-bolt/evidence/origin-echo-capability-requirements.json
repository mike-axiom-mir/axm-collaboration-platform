{
  "requirements": [
    { "id": "lane-edit", "capabilities": ["workspace.write.slot014"], "required": true },
    { "id": "deterministic-verification", "capabilities": ["test.node.deterministic"], "required": true },
    { "id": "live-visual-verification", "capabilities": ["browser.capture.repeated-screenshot"], "required": true },
    { "id": "subframe-timing", "capabilities": ["visual.capture.ephemeral-rolling-buffer/v1"], "required": false }
  ]
}
