{
  "capabilities": [
    { "id": "workspace.write.slot014", "status": "available", "constraints": ["No writes outside 014-bonk-and-bolt"] },
    { "id": "test.node.deterministic", "status": "available", "constraints": ["Systems, package, HTTP and focused verifier"] },
    { "id": "browser.capture.repeated-screenshot", "status": "available", "constraints": ["1280x720 in-app browser; separated frames"] },
    { "id": "visual.capture.ephemeral-rolling-buffer/v1", "status": "unavailable", "constraints": ["No sub-frame timing claims"] }
  ]
}
