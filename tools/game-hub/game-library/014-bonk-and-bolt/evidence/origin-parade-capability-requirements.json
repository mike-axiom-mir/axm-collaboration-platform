{
  "requirements": [
    { "id": "lane-edit", "capabilities": ["workspace.write.slot014"], "required": true },
    { "id": "deterministic-origin-parade-verification", "capabilities": ["test.node.deterministic"], "required": true },
    { "id": "live-day-night-growth-verification", "capabilities": ["browser.capture.repeated-screenshot", "browser.runtime.dataset", "browser.input.local"], "required": true },
    { "id": "production-rig-integration", "capabilities": ["asset.character.skeletal-3d-production"], "required": false },
    { "id": "subframe-timing", "capabilities": ["visual.capture.ephemeral-rolling-buffer/v1"], "required": false }
  ]
}
