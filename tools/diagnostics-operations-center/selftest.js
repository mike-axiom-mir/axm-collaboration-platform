'use strict';const assert=require('assert'),fs=require('fs'),path=require('path');const d=__dirname,m=JSON.parse(fs.readFileSync(path.join(d,'manifest.json'))),c=JSON.parse(fs.readFileSync(path.join(d,'module.contract.json'))),h=fs.readFileSync(path.join(d,'index.html'),'utf8'),a=fs.readFileSync(path.join(d,'app.js'),'utf8');assert.equal(m.id,'diagnostics-operations-center');assert(c.boundaries.refuses.includes('automatic-repair'));assert(c.boundaries.refuses.includes('secret-value-in-report'));assert(h.includes('Allowlisted log'));assert(a.includes('/api/diagnostics/logs')&&a.includes('explicit-export'));console.log('PASS Diagnostics Center · unified snapshot bounded logs issues evidence export no auto-repair');

