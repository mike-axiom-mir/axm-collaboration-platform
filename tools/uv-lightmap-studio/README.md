# UV & Lightmap Studio

P0 module #5. It preserves and measures source UV0, then authors a deterministic second UV set for lightmaps. The conservative v0.1 packer isolates every triangle in its own padded chart, guaranteeing zero UV1 overlap while reporting its low utilization rather than hiding it.

```powershell
node "C:\axm workshop\tools\uv-lightmap-studio\selftest.js"
```
