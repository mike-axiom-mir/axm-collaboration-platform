# UCP + Play Composer evidence — 2026-07-22

Scope: deterministic human controls → immutable Universal Components → typed
graph → ephemeral preview → explicit Asset Fabric incubation.

## Claims and evidence

- **Portable contract surface:** `service.contract.json` declares 93 local JSON
  Schemas, including component, graph, composition receipt and play-compose
  draft. `schema-contract-selftest.js` resolved every reference.
- **Deterministic composition:** the same human input produces the same draft;
  exact component versions/digests resolve; type mismatch, cycles, missing
  digests and tampered previews fail closed. Both UCP and Play Composer
  selftests passed.
- **Directed growth:** a live silhouette branch changed the shape from rounded
  square to capsule while preserving the palette. The emitted composition
  component recorded direction `silhouette`, energy `2`, branch `1` and the
  parent input digest.
- **Explicit persistence:** before **Keep in incubator**, the preview declared
  `ephemeral: true` and `persisted: false`. Clicking Keep added the declared
  need and a governed candidate with empty Mike/machine votes. The card reads
  `Human Play Composer · typed UCP graph`, `UCP DRAFT`, `novel unscored`; the
  promotion button remains disabled.
- **Truth boundary:** `READY_CONTRACT` proves graph resolution, type/canvas fit
  and receipt binding only. It does not claim final rendering, visual approval,
  accessibility, playability or manufacturing safety.
- **Live browser integration:** clean reload at `2026-07-22T05:48:19.032Z`
  produced no new browser warnings/errors, reported all 34 installed creation
  hands, showed `READY_CONTRACT`, and retained the typed UCP candidate.
- **Responsive surface:** checked at 390×844 and the default desktop viewport.
  Controls, graph and Keep action remained present; the mobile horizontal spill
  found during the first pass was repaired.

## Test receipt

- Asset Hands schemas: 93 PASS, all local references resolved.
- Universal Component Protocol: PASS.
- Play Composer: PASS.
- Asset Hands: PASS, 34 providers, 0 curated gaps.
- Asset Fabric: 32 PASS, 0 FAIL, v0.12.0.
- Upgrade integration: PASS.
- Workshop HTML/inline script syntax: 53 PASS, 0 FAIL.

## Modern 3D handoff boundary

The incoming Modern 3D Forge can consume the same component graph for meshes,
PBR materials, textures, animation, scene chunks and delivery policies. Its
Blender/OpenUSD authoring, GLB/KTX2 realization, browser renderer, benchmark and
validator receipts remain separate domain evidence. UCP makes those choices
composable; it does not substitute for those hands.
