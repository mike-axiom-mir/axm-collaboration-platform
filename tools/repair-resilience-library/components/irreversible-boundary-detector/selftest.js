"use strict";const assert=require("assert");const {detectIrreversibleBoundary}=require("./index");let r=detectIrreversibleBoundary({type:"payment-refund",tags:[]});assert.equal(r.boundaryDetected,true);assert.equal(r.requiredTier,"TIER_4_PROHIBITED_AUTOMATION");r=detectIrreversibleBoundary({type:"read-log",reversible:true});assert.equal(r.boundaryDetected,false);r=detectIrreversibleBoundary({type:"x",reversible:false});assert.equal(r.boundaryDetected,true);console.log("PASS irreversible-boundary-detector");
