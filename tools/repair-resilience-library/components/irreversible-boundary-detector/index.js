"use strict";const {sha256,iso}=require("../common");const RULES={legal:['legal','court','contract'],financial:['payment','bank','financial','purchase'],identity:['identity','passport','credential-root'],securityRoot:['security-root','trust-root','private-key'],destructive:['delete-permanent','format','wipe','destroy'],privacy:['personal-data','private-memory','secret-export']};
function detectIrreversibleBoundary(action={}){const text=[action.type,action.resource,...(action.tags||[])].filter(Boolean).join(' ').toLowerCase();const matches=[];for(const [kind,terms] of Object.entries(RULES))for(const term of terms)if(text.includes(term)){matches.push({kind,term});break;}if(action.reversible===false)matches.push({kind:'declaredIrreversible',term:'reversible=false'});const out={schema:"axm.irreversible-boundary-finding/v1",observedAt:iso(),boundaryDetected:matches.length>0,matches,requiredTier:matches.length?"TIER_4_PROHIBITED_AUTOMATION":"UNDETERMINED_BY_THIS_MODULE",automaticExecutionAuthorized:false};out.digest=sha256(out);return out;}module.exports={RULES,detectIrreversibleBoundary};
