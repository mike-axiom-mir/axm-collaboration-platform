"use strict";const assert=require("assert");const {buildGatePacket,recordDecision}=require("./index");const p=buildGatePacket({candidateId:"c1",preStateDigest:"a",proposedChangeDigest:"b"});assert.equal(p.decision,"PENDING");let d=recordDecision(p,{humanId:"Mike",phrase:"wrong"});assert.equal(d.confirmationValid,false);d=recordDecision(p,{humanId:"Mike",phrase:p.confirmationPhrase});assert.equal(d.decision,"APPROVED_BY_HUMAN");assert.equal(d.executionAuthorized,false);console.log("PASS human-merge-gate-adapter");
