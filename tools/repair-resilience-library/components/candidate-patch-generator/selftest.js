"use strict";const assert=require("assert");const {generate}=require("./index");const r=generate({input:{a:1,b:2},matches:[{id:"small",lineage:{r:1},verifiers:["t"],apply:x=>({...x,a:2})},{id:"bad",apply:()=>{throw new Error("no");}}]});assert.equal(r.status,"CANDIDATES");assert.equal(r.candidates[0].editSize,1);assert.equal(r.rejected.length,1);assert.equal(r.applicationAuthorized,false);console.log("PASS candidate-patch-generator");
