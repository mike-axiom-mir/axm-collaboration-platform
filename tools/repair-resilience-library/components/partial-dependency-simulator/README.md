# axm.repair.partial-dependency-simulator

Simulate a dependency that is slow, stale, intermittently wrong, or only partly available rather than simply offline.

Maturity: PROTOTYPED after authored local self-test only.

Overlap: ADAPTER_BETWEEN_EXISTING — Creates an in-memory dependency adapter with declared scenarios and no network access.
