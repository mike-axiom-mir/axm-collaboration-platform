"use strict";function createSimulator({schedule,values={fresh:null,stale:null,wrong:null},maxCalls=1000}){if(!Array.isArray(schedule)||!schedule.length||schedule.length>maxCalls)throw new RangeError("bounded schedule required");let call=0;return {schema:"axm.partial-dependency-simulator/v1",request(input){if(call>=maxCalls)return {status:"ABORTED",reason:"call-budget"};const mode=schedule[call%schedule.length],index=call++;if(mode==="error")return {status:"ERROR",index,error:"simulated-dependency-error",networkUsed:false};if(mode==="slow")return {status:"OK",index,value:values.fresh,simulatedLatencyMs:5000,networkUsed:false};if(mode==="stale")return {status:"OK_STALE",index,value:values.stale,stale:true,networkUsed:false};if(mode==="wrong")return {status:"OK_WRONG",index,value:values.wrong,knownWrong:true,networkUsed:false};if(mode==="partial")return {status:"OK_PARTIAL",index,value:input&&typeof input==="object"?Object.fromEntries(Object.entries(input).slice(0,1)):values.fresh,partial:true,networkUsed:false};return {status:"OK",index,value:values.fresh,networkUsed:false};},calls(){return call;},networkUsed:false};}module.exports={createSimulator};
