"use strict";const assert=require("assert");const {createSimulator}=require("./index");const s=createSimulator({schedule:["ok","stale","wrong","error"],values:{fresh:1,stale:0,wrong:99}});assert.equal(s.request().status,"OK");assert.equal(s.request().stale,true);assert.equal(s.request().knownWrong,true);assert.equal(s.request().status,"ERROR");assert.equal(s.networkUsed,false);console.log("PASS partial-dependency-simulator");
