"use strict";const assert=require("assert");const {CircuitBreaker}=require("./index");const b=new CircuitBreaker({failureThreshold:2,cooldownMs:10});assert.equal(b.allow(0),true);b.failure(0);b.failure(1);assert.equal(b.snapshot().state,"OPEN");assert.equal(b.allow(5),false);assert.equal(b.allow(20),true);assert.equal(b.allow(20),false);b.success();assert.equal(b.snapshot().state,"CLOSED");console.log("PASS circuit-breaker");
