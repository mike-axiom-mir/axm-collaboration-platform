"use strict";const assert=require("assert");const {classifyReversibility}=require("./index");assert.equal(classifyReversibility({exactPreState:true,deterministicInverse:true,inverseTested:true}).classification,"EXACTLY_REVERSIBLE");assert.equal(classifyReversibility({compensatingAction:true,compensationTested:true}).classification,"COMPENSABLE");assert.equal(classifyReversibility({snapshotAvailable:true,restoreTested:true}).classification,"APPROXIMATELY_RECOVERABLE");assert.equal(classifyReversibility({}).classification,"IRREVERSIBLE");console.log("PASS reversibility-classifier");
