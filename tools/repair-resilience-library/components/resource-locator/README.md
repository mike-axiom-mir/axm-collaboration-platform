# axm.repair.resource-locator

Find missing assets, binaries, models, fonts, schemas, or runtime files through hashes and provenance.

Maturity: PROTOTYPED after authored local self-test only.

Overlap: ADAPTER_BETWEEN_EXISTING — Connects hash/provenance lookup to existing resource and package surfaces without copying files.
