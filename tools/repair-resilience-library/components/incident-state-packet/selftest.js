"use strict";const assert=require("assert");const {createIncidentPacket,updateIncidentPacket}=require("./index");const a=createIncidentPacket({incidentId:"i",evidence:["e"]});const b=updateIncidentPacket(a,{action:{type:"observe"},uncertainty:"cause unknown"});assert.equal(b.revision,1);assert.equal(b.history[0].digest,a.digest);assert.equal(a.actions.length,0);assert.throws(()=>createIncidentPacket({}),/required/);console.log("PASS incident-state-packet");
