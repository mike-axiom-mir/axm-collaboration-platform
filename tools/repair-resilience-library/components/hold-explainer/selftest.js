"use strict";const assert=require("assert");const {explainHold}=require("./index");let r=explainHold({failedConditions:[{type:"verification",condition:"Verifier missing"},{type:"permission",condition:"Permission missing"}]});assert.equal(r.plainReason,"Permission missing");assert.equal(r.automaticActionTaken,false);r=explainHold({failedConditions:[]});assert.equal(r.status,"HOLD");console.log("PASS hold-explainer");
