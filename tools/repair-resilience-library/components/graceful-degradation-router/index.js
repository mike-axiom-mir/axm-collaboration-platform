"use strict";const {sha256,iso}=require("../common");function chooseRoute(input={}){const health=input.dependencyHealth||{},routes=[...(input.routes||[])].sort((a,b)=>(b.priority||0)-(a.priority||0));const eligible=routes.filter(r=>(r.requires||[]).every(d=>health[d]==="PASS")&&r.truthLabel);const selected=eligible[0]||null;const out={schema:"axm.degradation-route/v1",observedAt:iso(),selected:selected?selected.id:null,truthLabel:selected?selected.truthLabel:"UNAVAILABLE",missing:selected?[]:[...new Set(routes.flatMap(r=>(r.requires||[]).filter(d=>health[d]!=="PASS")))],degraded:selected?selected.degraded===true:false,hiddenFallback:false,executionAuthorized:false};out.digest=sha256(out);return out;}module.exports={chooseRoute};
