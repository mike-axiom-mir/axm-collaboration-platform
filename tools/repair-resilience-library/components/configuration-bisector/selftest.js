"use strict";const assert=require("assert");const {bisectFirstFailing}=require("./index");(async()=>{const s=[0,1,2,3,4].map(i=>({id:String(i),v:i}));let r=await bisectFirstFailing(s,x=>x.v>=3);assert.equal(r.firstFailingIndex,3);assert.equal(r.mutationAuthorized,false);r=await bisectFirstFailing(s,()=>false);assert.equal(r.firstFailingIndex,null);await assert.rejects(()=>bisectFirstFailing(s,null),/required/);console.log("PASS configuration-bisector");})().catch(e=>{console.error(e);process.exit(1)});
