"use strict";const {sha256,iso}=require("../common");
async function bisectFirstFailing(states=[],test){if(typeof test!=="function")throw new TypeError("test function required");const evidence=[];let lo=0,hi=states.length-1,answer=null;while(lo<=hi){const mid=Math.floor((lo+hi)/2);const failed=!!(await test(states[mid]));evidence.push({index:mid,id:states[mid]?.id??mid,failed});if(failed){answer=mid;hi=mid-1;}else lo=mid+1;}let monotonic=true;if(answer!=null){for(let i=0;i<answer;i++){const e=evidence.find(x=>x.index===i);if(e?.failed)monotonic=false;}for(let i=answer+1;i<states.length;i++){const e=evidence.find(x=>x.index===i);if(e&&!e.failed)monotonic=false;}}const out={schema:"axm.configuration-bisection/v1",observedAt:iso(),firstFailingIndex:answer,firstFailingState:answer==null?null:states[answer],tests:evidence,assumption:"failure is monotonic across ordered states",monotonicEvidence:monotonic,mutationAuthorized:false};out.digest=sha256(out);return out;}module.exports={bisectFirstFailing};
