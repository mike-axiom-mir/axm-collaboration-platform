"use strict";const assert=require("assert");const {DrainQuarantine}=require("./index");const d=new DrainQuarantine();assert.equal(d.start("a"),true);d.requestDrain("suspected");assert.equal(d.start("b"),false);assert.equal(d.snapshot().state,"DRAINING");d.finish("a");assert.equal(d.snapshot().state,"QUARANTINED");assert.equal(d.snapshot().networkMutationPerformed,false);console.log("PASS traffic-drain-quarantine");
