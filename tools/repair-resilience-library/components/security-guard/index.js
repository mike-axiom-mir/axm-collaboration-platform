"use strict";function set(x){return new Set(x||[]);}function diff(a,b){return [...b].filter(x=>!a.has(x));}function guardSecurity({before,after,approvedPermissionExpansions=[]}){const findings=[];const added=diff(set(before.permissions),set(after.permissions)),approved=set(approvedPermissionExpansions),unapproved=added.filter(x=>!approved.has(x));if(unapproved.length)findings.push({id:"permission-expansion",items:unapproved});const rootsBefore=set(before.trustRoots),rootsAfter=set(after.trustRoots);const removedRoots=[...rootsBefore].filter(x=>!rootsAfter.has(x));if(removedRoots.length)findings.push({id:"trust-root-removed",items:removedRoots});if(before.signatureChecks!==false&&after.signatureChecks===false)findings.push({id:"signature-check-disabled"});if((after.isolationLevel??0)<(before.isolationLevel??0))findings.push({id:"isolation-weakened"});if((after.exposedSecrets||[]).length)findings.push({id:"secret-exposure",count:after.exposedSecrets.length});return {schema:"axm.security-guard.report/v1",status:findings.length?"BLOCK":"PASS",findings,addedPermissions:added,approvedPermissionExpansions,promotionAuthorized:false,permissionGranted:false};}module.exports={guardSecurity};
