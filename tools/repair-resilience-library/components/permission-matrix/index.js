"use strict";const {sha256,iso}=require("../common");function match(p,v){if(p==="*")return true;if(p.endsWith("*"))return String(v).startsWith(p.slice(0,-1));return p===v;}
function resolvePermission(input={}){const matches=(input.rules||[]).filter(r=>match(r.actor||"",input.actor)&&match(r.action||"",input.action)&&match(r.resource||"",input.resource));const denies=matches.filter(r=>String(r.effect).toUpperCase()==="DENY"),allows=matches.filter(r=>String(r.effect).toUpperCase()==="ALLOW");const decision=denies.length?"DENY":allows.length?"ALLOW":"DENY";const out={schema:"axm.permission-resolution/v1",observedAt:iso(),actor:input.actor,action:input.action,resource:input.resource,decision,defaultDeny:!matches.length,matchedRuleIds:matches.map(r=>r.id),denyPrecedence:denies.length>0,grantCreated:false};out.digest=sha256(out);return out;}module.exports={resolvePermission};
