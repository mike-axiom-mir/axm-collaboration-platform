"use strict";const {sha256,iso}=require("../common");function previewRestore(snapshot={},current={},expectedCurrent={}){const paths=new Set([...Object.keys(snapshot),...Object.keys(current),...Object.keys(expectedCurrent)]),overwrite=[],preserve=[],conflict=[],remove=[];for(const p of [...paths].sort()){const s=snapshot[p],c=current[p],e=expectedCurrent[p];if(s===undefined&&c!==undefined){if(e===undefined||c===e)remove.push(p);else conflict.push({path:p,reason:"current-added-since-preview",current:c,expected:e});}else if(s!==undefined&&c===s)preserve.push(p);else if(e!==undefined&&c!==e)conflict.push({path:p,reason:"current-changed",current:c,expected:e,snapshot:s});else overwrite.push({path:p,from:c,to:s});}const out={schema:"axm.restore-preview/v1",createdAt:iso(),overwrite,preserve,remove,conflict,state:conflict.length?"HOLD_CONFLICT":"READY_FOR_GATE",restoreAuthorized:false};out.digest=sha256(out);return out;}module.exports={previewRestore};
