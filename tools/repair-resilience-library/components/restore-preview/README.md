# axm.repair.restore-preview

Show precisely what a restore would overwrite, preserve, conflict with, or leave unresolved.

Maturity: PROTOTYPED after authored local self-test only.

Overlap: EXTEND_EXISTING — Adds a pure map-level preview; Recovery Center remains the only active restore surface.
