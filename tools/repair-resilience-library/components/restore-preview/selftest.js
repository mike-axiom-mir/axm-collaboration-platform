"use strict";const assert=require("assert");const {previewRestore}=require("./index");let r=previewRestore({a:"old",b:"same"},{a:"new",b:"same"},{a:"new",b:"same"});assert.equal(r.state,"READY_FOR_GATE");assert.equal(r.overwrite.length,1);r=previewRestore({a:"old"},{a:"third"},{a:"new"});assert.equal(r.state,"HOLD_CONFLICT");assert.equal(r.restoreAuthorized,false);console.log("PASS restore-preview");
