# axm.repair.time-skew-injector

Test clock drift, timezone changes, timer jumps, expiry mistakes, and delayed heartbeats.

Maturity: PROTOTYPED after authored local self-test only.

Overlap: ADAPTER_BETWEEN_EXISTING — Provides a virtual clock only; it cannot modify the host clock or timezone.
