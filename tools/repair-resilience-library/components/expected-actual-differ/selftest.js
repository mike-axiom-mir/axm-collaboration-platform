"use strict";const assert=require("assert");const {threeWayDiff}=require("./index");const r=threeWayDiff({desired:{a:1,b:2},lastVerified:{a:1,b:1},actual:{a:1,b:3,c:4}});assert(r.driftPaths.includes("b"));assert(r.driftPaths.includes("c"));assert.equal(r.desiredVsLastGood.length,1);const z=threeWayDiff({desired:{a:1},lastVerified:{a:1},actual:{a:1}});assert.equal(z.driftPaths.length,0);console.log("PASS expected-actual-differ");
