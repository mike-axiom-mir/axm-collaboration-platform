"use strict";const {sha256,iso}=require("../common");function guardRestore(input={}){const failures=[];if(!input.snapshotDigest||input.snapshotDigest!==input.expectedSnapshotDigest)failures.push("snapshot-digest-mismatch");if(!input.currentDigest||input.currentDigest!==input.previewCurrentDigest)failures.push("current-state-changed");if(!Array.isArray(input.scope)||input.scope.length===0)failures.push("scope-missing");if(input.sourceVerified!==true)failures.push("source-unverified");if(Number.isFinite(input.maxAgeMs)&&Number.isFinite(input.snapshotAtMs)&&Date.now()-input.snapshotAtMs>input.maxAgeMs)failures.push("snapshot-stale");const out={schema:"axm.restore-lineage-decision/v1",createdAt:iso(),decision:failures.length?"HOLD":"ELIGIBLE_FOR_HUMAN_GATE",failures,restoreAuthorized:false};out.digest=sha256(out);return out;}module.exports={guardRestore};
