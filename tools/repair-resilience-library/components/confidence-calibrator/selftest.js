"use strict";const assert=require("assert");const {calibrateConfidence}=require("./index");const h=[{predicted:.8,outcome:true},{predicted:.82,outcome:false},{predicted:.85,outcome:true}];let r=calibrateConfidence(h,.83);assert(Math.abs(r.calibrated-2/3)<1e-9);assert.equal(r.uncertainty,"HIGH");r=calibrateConfidence([],2);assert.equal(r.raw,1);assert.equal(r.warning.includes("No historical"),true);console.log("PASS confidence-calibrator");
