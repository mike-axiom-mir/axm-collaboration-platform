# axm.repair.minimal-failing-fixture

Reduce a complex failure into the smallest inputs, state, and dependencies that still reproduce it.

Maturity: PROTOTYPED after authored self-test only.

Overlap: ADAPTER_BETWEEN_EXISTING — Performs bounded delta debugging on a copy and preserves the original fixture.
