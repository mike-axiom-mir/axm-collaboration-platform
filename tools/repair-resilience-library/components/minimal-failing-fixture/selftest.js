"use strict";const assert=require("assert");const {minimizeFixture}=require("./index");(async()=>{const original=["a","bad","c","d"];const r=await minimizeFixture(original,x=>x.includes("bad"));assert.deepEqual(r.minimal,["bad"]);assert.deepEqual(original,["a","bad","c","d"]);const n=await minimizeFixture([1,2],()=>false);assert.equal(n.status,"INPUT_NOT_FAILING");console.log("PASS minimal-failing-fixture");})().catch(e=>{console.error(e);process.exit(1)});
