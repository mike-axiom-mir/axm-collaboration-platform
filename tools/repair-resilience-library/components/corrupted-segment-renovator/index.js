"use strict";const {sha256,iso}=require("../common");function renovateSegments(input={}){const original=input.segments||{},next={...original},replaced=[],held=[];for(const [id,expected] of Object.entries(input.expectedHashes||{})){const actual=sha256(original[id]);if(actual===expected)continue;const cand=(input.candidates||{})[id];if(!cand||cand.verified!==true||sha256(cand.value)!==expected){held.push({id,reason:"no independently verified matching candidate",actual,expected});continue;}next[id]=cand.value;replaced.push({id,from:actual,to:expected});}const out={schema:"axm.segment-renovation.receipt/v1",createdAt:iso(),status:held.length?"PARTIAL_HOLD":replaced.length?"RENOVATED_COPY":"NO_CHANGE",replaced,held,result:next,originalTouched:false,applyAuthorized:false};out.digest=sha256(out);return out;}module.exports={renovateSegments};
