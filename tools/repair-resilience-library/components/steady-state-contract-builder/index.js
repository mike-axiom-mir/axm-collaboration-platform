"use strict";const {sha256,iso}=require("../common");function buildContract({name,metrics,abortConditions,recoveryProofs,scope}){const errors=[];if(!name)errors.push("name-required");if(!Array.isArray(metrics)||!metrics.length)errors.push("metrics-required");for(const [i,m] of (metrics||[]).entries()){if(!m.id)errors.push(`metric-${i}-id-required`);if(!Number.isFinite(m.baseline))errors.push(`metric-${m.id||i}-baseline-required`);if(!Number.isFinite(m.tolerance)&&!Number.isFinite(m.min)&&!Number.isFinite(m.max))errors.push(`metric-${m.id||i}-tolerance-required`);}if(!Array.isArray(abortConditions)||!abortConditions.length)errors.push("abort-conditions-required");if(!Array.isArray(recoveryProofs)||!recoveryProofs.length)errors.push("recovery-proofs-required");if(errors.length)return {status:"HOLD",errors,faultInjectionAuthorized:false};const core={schema:"axm.steady-state-contract/v1",name,scope:scope||"sandbox-only",metrics,abortConditions,recoveryProofs};return {...core,createdAt:iso(),contractDigest:sha256(core),status:"READY_FOR_SEPARATE_AUTHORIZATION",faultInjectionAuthorized:false};}module.exports={buildContract};
