"use strict";const assert=require("assert");const {runFuzz}=require("./index");const target=s=>{if(String(s).includes("BAD"))throw new Error("boom");return true;};const r=runFuzz({seeds:["GOOD"],mutate:(s,i)=>i===3?s+"BAD":s+i,target,maxCases:10});assert.equal(r.findings.length,1);assert(String(r.fixture.input).includes("BAD"));assert.equal(r.coverageClaim,"bounded-input-exploration-only");console.log("PASS fuzz-regression-harness");
