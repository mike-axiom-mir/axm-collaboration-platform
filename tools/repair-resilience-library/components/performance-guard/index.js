"use strict";function guard({baseline,candidate,budgets}){const regressions=[],checks=[];for(const [metric,policy] of Object.entries(budgets)){const b=baseline[metric],c=candidate[metric];if(!Number.isFinite(b)||!Number.isFinite(c)){checks.push({metric,status:"UNKNOWN"});continue;}const delta=c-b,percent=b===0?(c===0?0:Infinity):delta/Math.abs(b);const maxAbs=policy.maxAbsoluteIncrease??Infinity,maxPct=policy.maxPercentIncrease??Infinity,pass=delta<=maxAbs&&percent<=maxPct;const row={metric,baseline:b,candidate:c,delta,percent,pass};checks.push(row);if(!pass)regressions.push(row);}return {schema:"axm.performance-guard.report/v1",status:regressions.length?"REGRESSION":"PASS",checks,regressions,hardwareSpecific:true,promotionAuthorized:false};}module.exports={guard};
