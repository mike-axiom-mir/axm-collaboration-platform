"use strict";const assert=require("assert");const {applyCpuPulse,simulatePressure}=require("./index");const no=applyCpuPulse({explicit:false});assert.equal(no.status,"REFUSED");const r=applyCpuPulse({durationMs:3});assert.equal(r.hostWide,false);assert(r.elapsedMs<=100);const s=simulatePressure({resource:"memory",level:.8,durationMs:100});assert.equal(s.hostResourceChanged,false);console.log("PASS resource-exhaustion-injector");
