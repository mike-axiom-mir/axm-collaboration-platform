"use strict";const assert=require("assert");const {runVerification}=require("./index");const r=runVerification({candidate:{x:2},checks:[{id:"gone",class:"original-failure",independentFromRepair:true,run:c=>c.x===2},{id:"inv",class:"invariant",independentFromRepair:true,run:c=>c.x>0}]});assert.equal(r.status,"VERIFICATION_CHECKS_PASS");assert.equal(r.promotionAuthorized,false);const h=runVerification({candidate:{x:2},checks:[{class:"original-failure",run:()=>true}]});assert.equal(h.status,"HOLD");assert.equal(h.independenceGap,true);console.log("PASS verification-harness");
