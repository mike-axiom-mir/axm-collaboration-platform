"use strict";const {clamp,sha256,iso}=require("../common");
const W={impact:3,urgency:2,dataRisk:3,privacyRisk:3,scope:2,irreversibility:3};
function classifyFailure(input={}){let score=0,parts={};for(const [k,w] of Object.entries(W)){const v=clamp(Number(input[k]??0),0,4);parts[k]={value:v,weight:w,contribution:v*w};score+=v*w;}const max=Object.values(W).reduce((a,b)=>a+b,0)*4;const ratio=score/max;let severity=ratio>=.8?"P0":ratio>=.6?"P1":ratio>=.4?"P2":ratio>=.2?"P3":"P4";if(input.legalOrIdentity===true)severity="P0";const out={schema:"axm.failure-severity/v1",observedAt:input.observedAt||iso(),severity,score,maxScore:max,parts,affectedHumans:input.affectedHumans??null,affectedDevices:input.affectedDevices??null,uncertainty:Array.isArray(input.uncertainty)?input.uncertainty:[],authority:"CLASSIFICATION_ONLY"};out.digest=sha256(out);return out;}module.exports={classifyFailure};
