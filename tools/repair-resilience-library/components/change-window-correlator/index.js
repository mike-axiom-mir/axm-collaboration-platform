"use strict";const {sha256,iso}=require("../common");const TYPE_WEIGHT={code:1,configuration:.9,data:.8,dependency:.85,environment:.7,hardware:.7,unknown:.4};
function correlateChanges(input={}){const t=input.failureAtMs,windowMs=input.windowMs||3600000;const ranked=(input.changes||[]).map(c=>{const distance=Math.abs((c.atMs||0)-t);const proximity=Math.max(0,1-distance/windowMs);const typeWeight=TYPE_WEIGHT[c.type]??TYPE_WEIGHT.unknown;const counter=(c.counterEvidence||[]).length*.15;return {...c,distanceMs:distance,score:Math.max(0,proximity*typeWeight-counter)};}).filter(c=>c.distanceMs<=windowMs).sort((a,b)=>b.score-a.score);const out={schema:"axm.change-window-correlation/v1",observedAt:iso(),failureAtMs:t,windowMs,ranked,causalityClaimed:false,warning:"Temporal correlation is not causal proof."};out.digest=sha256(out);return out;}module.exports={correlateChanges};
