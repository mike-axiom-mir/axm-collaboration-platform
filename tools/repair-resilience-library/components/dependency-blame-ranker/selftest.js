"use strict";const assert=require("assert");const {rankDependencies}=require("./index");const r=rankDependencies([{id:"a",errorCorrelation:1,probeFailure:1},{id:"b",errorCorrelation:1,probeFailure:1,counterEvidenceStrength:1}]);assert.equal(r.topCandidate.id,"a");assert(r.ranked[0].score>r.ranked[1].score);assert.equal(r.causalityClaimed,false);console.log("PASS dependency-blame-ranker");
