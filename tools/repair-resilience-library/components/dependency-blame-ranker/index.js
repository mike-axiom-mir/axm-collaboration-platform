"use strict";const {sha256,iso,clamp}=require("../common");
function rankDependencies(deps=[]){const ranked=deps.map(d=>{const support=clamp(Number(d.errorCorrelation||0),0,1)*.35+clamp(Number(d.latencyCorrelation||0),0,1)*.2+clamp(Number(d.probeFailure||0),0,1)*.3+clamp(Number(d.changeProximity||0),0,1)*.15;const counter=clamp(Number(d.counterEvidenceStrength||0),0,1)*.5;return {...d,score:Math.max(0,support-counter),uncertainty:1-Math.min(1,support)};}).sort((a,b)=>b.score-a.score);const out={schema:"axm.dependency-blame-ranking/v1",observedAt:iso(),ranked,topCandidate:ranked[0]||null,causalityClaimed:false};out.digest=sha256(out);return out;}module.exports={rankDependencies};
