"use strict";const {sha256,iso}=require("../common");
function reconstructTrace(events=[]){const byId=new Map(events.map(e=>[e.eventId,e]));const ordered=[...events].sort((a,b)=>(a.atMs||0)-(b.atMs||0));const gaps=[];for(const e of ordered)if(e.parentEventId&&!byId.has(e.parentEventId))gaps.push({eventId:e.eventId,missingParent:e.parentEventId});const branches={};for(const e of ordered){const k=e.lineageId||"UNKNOWN";(branches[k]||(branches[k]=[])).push(e.eventId);}const out={schema:"axm.causal-trace-reconstruction/v1",observedAt:iso(),ordered,branches,gaps,complete:gaps.length===0,causalityClaimed:false};out.digest=sha256(out);return out;}module.exports={reconstructTrace};
