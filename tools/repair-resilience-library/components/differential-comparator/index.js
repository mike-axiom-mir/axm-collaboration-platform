"use strict";const {sha256}=require("../common");function compare(reference,candidate,{numericTolerance=0,ignorePaths=[]}={}){const diffs=[];const ignored=new Set(ignorePaths);function walk(a,b,p){if(ignored.has(p))return;if(typeof a==="number"&&typeof b==="number"&&Math.abs(a-b)<=numericTolerance)return;if(Object.is(a,b))return;const ao=a&&typeof a==="object",bo=b&&typeof b==="object";if(ao&&bo&&Array.isArray(a)===Array.isArray(b)){const keys=new Set([...Object.keys(a),...Object.keys(b)]);for(const k of keys)walk(a[k],b[k],p?`${p}.${k}`:String(k));return;}diffs.push({path:p||"$",reference:a,candidate:b});}walk(reference,candidate,"");const core={schema:"axm.differential-comparison.report/v1",referenceDigest:sha256(reference),candidateDigest:sha256(candidate),equal:diffs.length===0,differences:diffs,status:diffs.length?"DIVERGED":"MATCH"};return {...core,reportDigest:sha256(core),referenceAuthorityAssumed:false};}module.exports={compare};
