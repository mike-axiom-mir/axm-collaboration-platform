"use strict";const assert=require("assert");const {createLease,recordBeat,evaluateLease}=require("./index");let l=createLease({leaseId:"x",issuedAtMs:0,lastBeatAtMs:0,intervalMs:100,graceMs:20,clockSkewToleranceMs:5});assert.equal(evaluateLease(l,50).state,"ACTIVE");assert.equal(evaluateLease(l,110).state,"LATE");let e=evaluateLease(l,350);assert.equal(e.state,"EXPIRED");assert.equal(e.missedBeats,3);const rejected=recordBeat({...l,lastBeatAtMs:100},{atMs:90});assert.equal(rejected.lastRejectedBeat.reason,"CLOCK_REGRESSION");assert.equal(evaluateLease(l,-20).state,"CLOCK_SKEW");console.log("PASS heartbeat-lease-monitor");
