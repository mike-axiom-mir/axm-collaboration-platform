"use strict";const {percentile,sha256,iso}=require("../common");function deriveTimeoutPolicy(samples=[],options={}){const vals=samples.filter(Number.isFinite).filter(x=>x>=0),p99=percentile(vals,.99)??options.defaultMs??1000;const consequence=Math.max(.5,Math.min(4,options.consequenceMultiplier||1.5));const cap=options.maxMs||60000;const base=Math.max(options.minMs||10,Math.min(cap,p99*consequence));const out={schema:"axm.timeout-policy/v1",observedAt:iso(),sampleCount:vals.length,connectionMs:Math.round(base*.5),requestMs:Math.round(base),operationMs:Math.round(Math.min(cap,base*2)),progressMs:Math.round(Math.min(cap,base*1.5)),basis:{p99,consequence},fallbackUsed:vals.length===0,authority:"POLICY_PROPOSAL_ONLY"};out.digest=sha256(out);return out;}module.exports={deriveTimeoutPolicy};
