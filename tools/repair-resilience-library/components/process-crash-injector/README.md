# axm.repair.process-crash-injector

Safely terminate selected disposable processes or components to verify restart and state recovery.

Maturity: PROTOTYPED after authored local self-test only.

Overlap: ADAPTER_BETWEEN_EXISTING — Only creates and terminates its own disposable child; it cannot target active AXM processes.
