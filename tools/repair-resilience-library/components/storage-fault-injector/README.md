# axm.repair.storage-fault-injector

Simulate full disk, read-only media, slow I/O, missing files, corrupt blocks, and interrupted writes in a sandbox.

Maturity: PROTOTYPED after authored local self-test only.

Overlap: ADAPTER_BETWEEN_EXISTING — Operates on a copied in-memory store; no real filesystem fault is introduced.
