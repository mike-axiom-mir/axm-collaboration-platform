# axm.repair.network-fault-injector

Inject bounded latency, loss, disconnection, duplication, reordering, and partial reachability.

Maturity: PROTOTYPED after authored local self-test only.

Overlap: ADAPTER_BETWEEN_EXISTING — Transforms an in-memory event stream only; it never changes host or LAN networking.
