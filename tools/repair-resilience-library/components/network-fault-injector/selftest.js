"use strict";const assert=require("assert");const {injectNetwork}=require("./index");const e=[{id:1,target:"a"},{id:2,target:"b"},{id:3,target:"a"}];const r=injectNetwork(e,{reachableTargets:["a"],duplicateRate:1,reorderWindow:2},5);assert.equal(r.hostNetworkChanged,false);assert.equal(r.simulationOnly,true);assert(r.dropped.some(x=>x.reason==="unreachable"));assert(r.delivered.some(x=>x.duplicate));assert.deepEqual(e,[{id:1,target:"a"},{id:2,target:"b"},{id:3,target:"a"}]);console.log("PASS network-fault-injector");
