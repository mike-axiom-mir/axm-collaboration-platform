# axm.repair.state-reconciliation-loop

Move actual state toward declared desired state through small idempotent actions with verification after each step.

Maturity: PROTOTYPED after authored local self-test only.

Overlap: ADAPTER_BETWEEN_EXISTING — Runs only caller-supplied idempotent actions on a local copy with verification after each bounded step.
