"use strict";const assert=require("assert");const {clusterSymptoms}=require("./index");const r=clusterSymptoms([{component:"a",fingerprint:"x",atMs:0},{component:"a",fingerprint:"x",atMs:10},{component:"a",fingerprint:"y",atMs:11}],{windowMs:100});assert.equal(r.clusters.length,1);assert.equal(r.outliers.length,1);assert.equal(r.forcedSingleCause,false);const s=clusterSymptoms([{component:"a",fingerprint:"x",atMs:0},{component:"a",fingerprint:"x",atMs:1000}],{windowMs:10});assert.equal(s.clusters.length,0);console.log("PASS symptom-clusterer");
