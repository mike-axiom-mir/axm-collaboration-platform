# axm.repair.microrestart-controller

Restart the smallest faulty component while separating process recovery from persistent state recovery.

Maturity: PROTOTYPED after authored self-test only.

Overlap: ADAPTER_BETWEEN_EXISTING — Plans the smallest restart only when state and loop limits are known; it does not restart anything itself.
