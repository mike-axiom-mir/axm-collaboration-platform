"use strict";const assert=require("assert");const {runProperties}=require("./index");const a=runProperties({schema:{type:"integer",minimum:0,maximum:10},properties:[{id:"bounds",test:x=>x>=0&&x<=10}],cases:100,seed:7});assert.equal(a.pass,true);assert.equal(a.exhaustiveProof,false);const b=runProperties({schema:{type:"integer",minimum:0,maximum:10},properties:[{id:"too-small",test:x=>x<5}],cases:100,seed:7});assert.equal(b.pass,false);console.log("PASS property-test-generator");
