"use strict";const assert=require("assert");const {packageReproduction}=require("./index");let r=packageReproduction({fixtures:[{name:"x",content:"abc"}],deterministic:true,steps:["run"]});assert.equal(r.fixtures[0].size,3);assert.equal(r.deterministicClaim,true);r=packageReproduction({knownNondeterminism:["timing"],deterministic:true});assert.equal(r.deterministicClaim,false);console.log("PASS reproduction-packager");
