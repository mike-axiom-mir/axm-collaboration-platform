"use strict";function monitorFreshness({now=Date.now(),occurrences=[],pattern,currentEnvironment,maxPatternAgeMs=30*86400000,recurrenceWindowMs=7*86400000}){const times=occurrences.map(x=>new Date(x&&typeof x==="object"&&"at" in x?x.at:x).getTime()).filter(Number.isFinite).sort((a,b)=>a-b),recent=times.filter(t=>now-t<=recurrenceWindowMs),patternAt=pattern?.verifiedAt?new Date(pattern.verifiedAt).getTime():NaN,age=Number.isFinite(patternAt)?now-patternAt:null,environmentMatch=pattern?.environmentDigest&&currentEnvironment?.digest?pattern.environmentDigest===currentEnvironment.digest:null;const warnings=[];if(recent.length>=2)warnings.push("recurring-failure");if(age===null)warnings.push("pattern-verification-date-unknown");else if(age>maxPatternAgeMs)warnings.push("pattern-stale");if(environmentMatch===false)warnings.push("environment-mismatch");if(pattern?.superseded)warnings.push("pattern-superseded");return {schema:"axm.recurrence-freshness.report/v1",occurrenceCount:times.length,recentCount:recent.length,patternAgeMs:age,environmentMatch,warnings,status:warnings.length?"REVIEW_REQUIRED":"FRESH_NO_RECURRENCE_SIGNAL",automaticReuseAuthorized:false};}module.exports={monitorFreshness};
