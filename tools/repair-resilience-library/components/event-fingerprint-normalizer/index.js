"use strict";const {sha256,iso}=require("../common");
function normalizeText(text){return String(text??"").replace(/[A-F0-9]{8}-[A-F0-9-]{27,}/gi,"<UUID>").replace(/\b0x[a-f0-9]+\b/gi,"<HEX>").replace(/(?:[A-Za-z]:\\|\/)(?:[^\s:]+[\\/])+[^\s:]*/g,"<PATH>").replace(/\b\d{3,}\b/g,"<N>").replace(/\s+/g," ").trim().toLowerCase();}
function fingerprintEvent(event={}){const normalized={type:String(event.type||"unknown").toLowerCase(),component:String(event.component||"unknown").toLowerCase(),code:String(event.code||"unknown").toLowerCase(),message:normalizeText(event.message)};const out={schema:"axm.failure-fingerprint/v1",observedAt:event.observedAt||iso(),normalized,fingerprint:sha256(normalized).slice(0,32),redactions:["uuid","hex-address","path","large-number"],limitations:["Normalization can merge distinct events; original evidence must remain separately available."]};out.digest=sha256(out);return out;}module.exports={normalizeText,fingerprintEvent};
