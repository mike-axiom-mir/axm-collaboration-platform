"use strict";const {sha256}=require("../common");function sentence(x){return String(x||"").replace(/[\r\n]+/g," ").trim().slice(0,300);}function buildPacket({incidentId,whatHappened,impact,known=[],unknown=[],safeNextAction,evidence=[],technicalDetails={}}){if(!safeNextAction||!safeNextAction.action)return {status:"HOLD",reason:"one-safe-next-action-required",actionExecuted:false};const certainty=known.length?"What is confirmed is listed below. The cause is not assumed.":"The cause is not confirmed yet.";const human={title:"Something needs attention",summary:sentence(whatHappened)||"A system check found a problem.",impact:sentence(impact)||"The exact impact is still being checked.",certainty,confirmed:known.map(sentence),stillUnknown:unknown.map(sentence),nextAction:{action:sentence(safeNextAction.action),why:sentence(safeNextAction.why),risk:sentence(safeNextAction.risk||"Low when performed as described.")}};const expert={incidentId,evidence,technicalDetails,known,unknown};const core={schema:"axm.beginner-escalation.packet/v1",human,expert,actionExecuted:false,repairAuthorized:false};return {...core,status:"READY_FOR_HUMAN",packetDigest:sha256(core)};}module.exports={buildPacket};
