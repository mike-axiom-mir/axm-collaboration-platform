"use strict";const assert=require("assert");const {deduplicateIncidents}=require("./index");const r=deduplicateIncidents([{incidentId:"1",component:"a",startedAtMs:0,fingerprints:["x"]},{incidentId:"2",component:"a",startedAtMs:5,fingerprints:["x"]},{incidentId:"3",component:"b",startedAtMs:5,fingerprints:["x"]}],{windowMs:10});assert.equal(r.canonicalCount,2);assert.equal(r.aliases.length,1);assert.equal(r.evidenceDeleted,false);console.log("PASS incident-deduplicator");
