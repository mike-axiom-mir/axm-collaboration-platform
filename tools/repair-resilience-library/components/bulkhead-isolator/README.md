# axm.repair.bulkhead-isolator

Partition processes, queues, resource pools, or devices so one failure cannot consume the whole platform.

Maturity: PROTOTYPED after authored self-test only.

Overlap: ADAPTER_BETWEEN_EXISTING — Partitions bounded capacity by pool and refuses overflow instead of consuming the whole Workshop.
