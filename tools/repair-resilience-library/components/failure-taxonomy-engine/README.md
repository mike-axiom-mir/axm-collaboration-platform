# axm.repair.failure-taxonomy-engine

Normalize failures into reusable classes such as crash, corruption, timeout, overload, drift, contract mismatch, and partial completion.

Maturity: PROTOTYPED after authored local self-test only.

Overlap: EXTEND_EXISTING — Normalizes observations into reusable classes without claiming root cause.
