"use strict";const assert=require("assert");const {classifyEvent}=require("./index");assert.equal(classifyEvent({timeout:true}).primary,"TIMEOUT");const r=classifyEvent({exitCode:1,timeout:true});assert.equal(r.primary,"CRASH");assert(r.alternatives.includes("TIMEOUT"));assert.equal(r.rootCauseClaimed,false);assert.equal(classifyEvent({message:"odd"}).primary,"UNKNOWN");console.log("PASS failure-taxonomy-engine");
