"use strict";const assert=require("assert");const {proposeImportRepairs}=require("./index");const r=proposeImportRepairs({requested:"./lib/Foo.js",files:["lib/foo.js","src/Foo.js","lib/bar.js"]});assert.equal(r.status,"CANDIDATES");assert.equal(r.candidates[0].path,"lib/foo.js");assert(r.candidates[0].reason.includes("case-mismatch"));assert.equal(r.globalRewriteAuthorized,false);console.log("PASS import-path-repairer");
