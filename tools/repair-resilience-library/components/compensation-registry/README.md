# axm.repair.compensation-registry

Store domain-specific undo or counter-actions alongside every multi-step forward action.

Maturity: PROTOTYPED after authored local self-test only.

Overlap: ADAPTER_BETWEEN_EXISTING — Stores explicit compensators with applicability and builds reverse-order plans without executing them automatically.
