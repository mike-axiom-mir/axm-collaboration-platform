"use strict";const assert=require("assert");const {CompensationRegistry}=require("./index");const r=new CompensationRegistry().register("charge",{compensate:a=>({refund:a.amount})});let p=r.plan([{id:"1",type:"charge",amount:5}]);assert.equal(p.state,"READY_FOR_GATE");assert.deepEqual(p.steps[0].compensate({amount:5}),{refund:5});p=r.plan([{id:"x",type:"unknown"}]);assert.equal(p.state,"HOLD_MISSING_COMPENSATION");assert.equal(p.executionAuthorized,false);console.log("PASS compensation-registry");
