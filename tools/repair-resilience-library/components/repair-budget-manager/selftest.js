"use strict";const assert=require("assert");const {RepairBudget}=require("./index");const b=new RepairBudget({compute:10,attempts:1,mutations:0});assert.equal(b.spend({compute:5,attempts:1}).allowed,true);let r=b.spend({compute:1,attempts:1});assert.equal(r.status,"HOLD");r=b.spend({mutations:1});assert.equal(r.allowed,false);assert.equal(b.snapshot().used.compute,5);console.log("PASS repair-budget-manager");
