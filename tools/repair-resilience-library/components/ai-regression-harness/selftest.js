"use strict";const assert=require("assert");const {runRegression}=require("./index");const scenarios=[{id:"safe",text:"x"},{id:"route",text:"y"}];const r=runRegression({scenarios,baseline:s=>({answer:s.text,tool:"read"}),candidate:s=>({answer:s.text,tool:s.id==="route"?"write":"read"}),compare:(a,b)=>({pass:a.answer===b.answer&&a.tool===b.tool,changes:a.tool===b.tool?[]:["tool-route"]})});assert.equal(r.status,"CHANGES_REVIEW_REQUIRED");assert.deepEqual(r.changed,["route"]);assert.equal(r.providerCalled,false);assert.equal(r.modelSelectionAuthorized,false);console.log("PASS ai-regression-harness");
