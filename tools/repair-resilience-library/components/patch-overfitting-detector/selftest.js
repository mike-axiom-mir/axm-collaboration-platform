"use strict";const assert=require("assert");const {detectOverfitting}=require("./index");const bad=detectOverfitting({training:[1,1,1],holdout:[1,0,0],properties:[1,0],differential:[1,0]});assert.equal(bad.suspicious,true);assert(bad.reasons.includes("training-holdout-gap"));const good=detectOverfitting({training:[1,1],holdout:[1,1],properties:[1,1],differential:[1,1],mutation:[1,1]});assert.equal(good.suspicious,false);assert.equal(good.applicationAuthorized,false);console.log("PASS patch-overfitting-detector");
