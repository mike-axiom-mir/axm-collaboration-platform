"use strict";const {sha256,iso}=require("../common");
const ACTIONS=[{name:"OBSERVE",rank:0},{name:"DEGRADE_NONCRITICAL",rank:1},{name:"DRAIN_NEW_WORK",rank:2},{name:"QUARANTINE_COMPONENT",rank:3},{name:"FREEZE_MUTATIONS",rank:4},{name:"STOP_SYSTEM",rank:5}];
function recommendContainment(input={}){let min=0;const reasons=[];if(["P0","P1"].includes(input.severity)){min=3;reasons.push("high severity");}if((input.affectedData||[]).length||input.corruptionRisk){min=Math.max(min,4);reasons.push("data/corruption risk");}if(input.securityRootRisk){min=5;reasons.push("security-root risk");}const available=new Set(input.availableActions||ACTIONS.map(a=>a.name));const candidate=ACTIONS.find(a=>a.rank>=min&&available.has(a.name))||null;const out={schema:"axm.containment-recommendation/v1",observedAt:iso(),recommended:candidate?.name||"HOLD",reasons,alternatives:ACTIONS.filter(a=>available.has(a.name)&&a.rank>=min).map(a=>a.name),executionAuthorized:false,requiresHumanGate:(candidate?.rank||0)>=2,uncertainty:input.uncertainty||[]};out.digest=sha256(out);return out;}module.exports={recommendContainment};
