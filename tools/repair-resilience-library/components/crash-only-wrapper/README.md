# axm.repair.crash-only-wrapper

Give a component one tested stop path and one tested recovery path so partial shutdown states cannot accumulate.

Maturity: PROTOTYPED after authored self-test only.

Overlap: ADAPTER_BETWEEN_EXISTING — Enforces one declared stop path and one recovery path in a local lifecycle state machine.
