"use strict";const assert=require("assert");const {runDrill}=require("./index");const {createAbortController}=require("../chaos-abort-controller");const contract={schema:"axm.steady-state-contract/v1",contractDigest:"x",recoveryTimeTargetMs:100,maxLoss:0};const r=runDrill({authorized:true,sandbox:true,contract,fixture:{up:true,data:1},inject:s=>({...s,up:false}),recover:s=>({...s,up:true}),verify:s=>({pass:s.up===true,loss:0}),abortController:createAbortController({maxFaults:2,maxBlastRadius:1,maxDurationMs:1000})});assert.equal(r.status,"PASS");assert.equal(r.activeStateTouched,false);assert.equal(r.osTaskInstalled,false);const no=runDrill({authorized:false,sandbox:true});assert.equal(no.status,"REFUSED");console.log("PASS recovery-drill-runner");
