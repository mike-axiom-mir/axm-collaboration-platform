"use strict";const assert=require("assert");const {createRegistry,simulateMigration}=require("./index");const r=createRegistry().register({from:"v1",to:"v2",migrate:x=>({...x,schemaVersion:"v2",name:x.title}),rollback:x=>({...x,schemaVersion:"v1",title:x.name})}).register({from:"v2",to:"v3",migrate:x=>({...x,schemaVersion:"v3",active:true}),rollback:x=>{const y={...x,schemaVersion:"v2"};delete y.active;return y;}});const s=simulateMigration({registry:r,record:{schemaVersion:"v1",title:"A"},targetVersion:"v3"});assert.equal(s.status,"SIMULATED");assert.equal(s.result.active,true);assert.equal(s.applicationAuthorized,false);assert.deepEqual(s.lossReport.removedPaths,[]);const h=simulateMigration({registry:r,record:{schemaVersion:"v1"},targetVersion:"v9"});assert.equal(h.status,"HOLD");console.log("PASS schema-migration-repairer");
