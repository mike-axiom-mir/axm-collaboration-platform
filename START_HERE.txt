START HERE — PUBLIC-SAFE EXPERIMENTAL CHECKPOINT
================================================

1. Extract the complete ZIP into a normal fully local folder.

   Recommended examples:
     C:\AXM_WORKSHOP\
     D:\Projects\AXM_WORKSHOP\

   Avoid an online-only OneDrive Desktop folder or another cloud-placeholder
   location.

2. Start the Hub:

   Windows:       double-click START_HUB.bat
   macOS/Linux:   run ./start-hub.sh

   The starter opens the correct local Hub address automatically. You do not
   need to type, edit, or remember a port.

   START_AXM.bat / ./start.sh opens the launcher instead.
   START_AXM_FULL.bat / ./start-full.sh also starts the optional AI bridge.

3. Open AXM Agent Tool Forge from the Hub.

   The v0.2 success condition is bounded creation without silent installation:
     - edit the sample draft
     - validate it
     - build the blueprint
     - inspect manifest.json and the SHA-256 fingerprint
     - export the module ZIP
     - confirm Installed: NO and Promoted: NO

SIDEBAR LABELS
--------------
CLAIMED  discovered, not opened yet
TEST     opened and ready for checks
WORKING  checks passed
SAVED    checkpoint/reload persistence confirmed

The internal compatibility value behind TEST remains NEEDS VERIFY.

CURRENT HONEST TEST STATUS
--------------------------
- Windows START_HUB one-click startup: PASS, manually confirmed by Mike Tobi.
- Visible Hub result: 22 of 22 modules loaded; All Systems Operational.
- Server/API and deterministic cores: PASS in the build audit.
- Automated Playwright click/render: UNRUN.

THE FACTORY DOES
----------------
Draft · validate · render files · fingerprint · preview · save/resume locally ·
export a real ZIP or review JSON.

THE FACTORY DOES NOT
--------------------
Install · overwrite · execute · delete · promote · canonize.

Every generated manifest starts EXPERIMENTAL. Risk remains a separate field.
================================================
