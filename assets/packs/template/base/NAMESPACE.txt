Immutable namespace for template packs (shells, layouts, blocks).
Location never changes; contents version freely. EMPTY TODAY BY
DESIGN. Local fallback: /assets/local/template/ + index.json.
Registry kind: template.source. The template ENGINE (inheritance,
slot filling, warnings, cross-pattern matching) is a RESEARCH
MODULE for later — this is only the index-fetch + connectivity
infrastructure, per Mike Tobi's ask. See library-spec.txt.
