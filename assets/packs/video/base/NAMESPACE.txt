Immutable namespace for video packs. Location never changes; contents
version freely. EMPTY TODAY BY DESIGN — the local shelf
(/assets/local/video/) is the fallback. Served by the ONE resolver
(AXMAssets) via namespaced ids. See TEMPLATE_SEAMS.txt.
