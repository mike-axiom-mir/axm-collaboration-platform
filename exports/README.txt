The local server writes Action Reports, verifier evidence, route exports, and
tool exports here.

Public Git contains example/template files only. Real generated histories are
excluded because they may contain timestamps, actor names, paths, or project
context. Run `node verify.js` locally to create verify-report.txt.
