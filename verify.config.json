{
  "_note": "YOUR checks — add/delete freely here or in the Verifier module. The core floor (spine, manifests, DOM order, trust files) lives in verify.js and is NOT listed here; it can't be deleted. kinds: file-exists | file-contains | json-has-field | manifest-present",
  "userChecks": [
    { "id": "u1", "kind": "file-exists", "path": "prompts/local/axm-core.txt", "label": "core roots file is present" },
    { "id": "u2", "kind": "json-has-field", "path": "tools/hub-test-room/manifest.json", "field": "hubApiVersion", "label": "test-room declares a hub API version" }
  ],
  "acknowledgedWarnings": []
}
