AXM WORKSHOP — v0.2b ONE-CLICK START FIX
Generated: 2026-07-11

WHAT WAS FIXED

- START_HUB now starts the server first and opens the Hub only after the server is listening.
- START_HUB uses preferred local port 8790, preventing an older library server on 8788 from capturing /hub/.
- If the preferred port is occupied, the server automatically selects the next free local port.
- The server opens the exact live URL itself; no manual browser address or bypass is required.
- /hub, /hub/, and /hub/index.html all resolve to the real Hub.
- START_AXM uses the same reliable start path for the library.
- START_AXM_FULL keeps the bridge in a separate process and uses the same reliable Workshop start.
- Static path resolution is normalized for Windows and remains confined to the Workshop root.

STATUS

WORKING FIX · EXPERIMENTAL PACKAGE · NOT CANON

Manual browser confirmation on Mike Tobi's Windows machine remains the final platform check.
