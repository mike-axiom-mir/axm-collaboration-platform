# Mirror Schema-Selected Structural Feature Growth — Action Report

Date: 2026-07-19  
Status: TEST  
Claim ceiling: `TEST_SCHEMA_SELECTED_FINITE_MACHINE_FACTS_AND_PRIVATE_EXACT_CONTRAST_RETRIEVAL`

## Outcome

Mirror no longer needs a new JavaScript feature mapping for every safe finite
machine field used by private episodic applicability. A generic deterministic
cell now projects only values at bounded selector-declared own-property paths.
The current v1 selector observes whether normalized candidate actions omit a
recovery declaration.

This is not a learned language organ, semantic understanding, causal learning,
truth, confidence, permission, evidence admission, training admission, active
runtime authority, CANON, or world action.

## New machine organ

- Cell: `kernel/schema-structural-feature-projection-cell.js`
- Organ: `organs/reasoning-structural-feature-organ.js`
- Selector: `training/reasoning-structural-feature-selector.json`
- Contract: `contracts/reasoning-structural-feature-projection.schema.json`
- Inspection command: `npm run project:reasoning-structural-features`

The projector supports bounded `BOOLEAN`, `ENUM`, and `PRESENCE` declarations.
Paths preserve JSON field case, traverse own properties only, refuse prototype
segments, cap rules, depth, and array size, and bind selector content by digest.
Only finite declared values enter observations. Unselected prose, IDs, source
references, and object keys remain inert. Positive facts and negative
observations are separate; only positive `structural-fact:` values may enter
private exact binding.

## Historical receipt continuity

Existing v2–v5 reasoning-experience receipts were not rewritten. Their sealed
legacy training features remain intact. Reasoning Memory Context and Feature
Binding recompute the new structural projection from each preserved reasoning
session at read time and join positive facts only. Each retrieved summary and
binding inventory carries projection and selector digest lineage.

## End-to-end growth proof

The focused exam used an unseen strategy tag,
`require-declared-recovery-via-schema`:

1. Two independently evaluated tagged source groups omitted recovery.
2. Two independent contrast groups declared recovery.
3. The old hardcoded feature extractor supplied no tag-to-field mapping.
4. The generic selector produced exactly one eligible exclusive fact:
   `structural-fact:reasoning-feature-source-v1:principletrace.candidates.any.action.recovery:missing`.
5. The binder sealed that exact fact for the unseen strategy.
6. Guidance supplied `+12` only when the current machine state had that fact.
7. Recovery-present state produced `CONTEXT_FEATURE_MISMATCH`, zero adjustment,
   and no tag-only fallback.

This establishes extensible structural applicability under one narrow example.
It does not establish open-ended feature discovery, semantic field meaning,
general reasoning improvement, external independence, or safe runtime use.

## Failures caught and repaired during the gate

The development trace preserved three useful failures:

- The first smoke test exposed lowercased selector paths and incorrect terminal
  absence handling. Field case is now preserved and missing terminal fields are
  explicit `PRESENCE=missing` observations.
- The first full run exposed order-sensitive reconstruction after downstream
  organs canonically reordered JSON keys. Projection comparison now uses the
  canonical digest rather than serialization order.
- Contract-derived sessions exposed loss of non-rendered evidence kinds when a
  verifier reconstructed legacy features only from the human-facing problem
  view. Reconstruction now uses the preserved full Principle trace.

The first full integration run therefore remained a visible 332/339 failure.
After these repairs the complete repository gate passed 339/339.

## Current private evidence

- Structural selector digest:
  `27d4e3d8594e7ffe43e852ec8f7323cc5946871b8e3c9176ce006a7de88b91bd`
- Current feature-binding batch:
  `reasoning-memory-feature-bindings-7721bfb05fc691232c83f04e`
- Binding batch digest:
  `4d4df051e19dc5157d57edd4a1ac1eb4ebd000b7321be0e3b55ac272457c42e6`
- Current guidance exam:
  `reasoning-memory-guidance-exam-1dffe46a2424a5312b7ad468`
- Guidance result digest:
  `60dae2150e1e3cf42ac704c5b23b8f95a043ca97d26082557b92857711b072ad`
- Current context:
  `reasoning-memory-context-a9ab6b89a18ad6823bf8d074`
- Focused integration gate: 48/48
- Full repository gate: 339/339
- Doctor: 499 public files, 289 JavaScript, 206 JSON, 46/46 active
  organs statically test-reachable, structural integrity PASS
- Runtime PID remained 496; the active server was not restarted.

## Known limits

- Only one production selector rule exists.
- Selector additions remain steward-reviewed TEST declarations.
- No outside-authored exam has tested this feature vocabulary.
- A correlation that passes exact contrast still does not prove semantics or
  causality.
- Current memory guidance remains private and explicit; runtime requests expose
  no `reasoningMemory` field.
- Real-local blocking-unknown guidance still holds for evaluator diversity.
- Mirror still has no native learned language organ.

