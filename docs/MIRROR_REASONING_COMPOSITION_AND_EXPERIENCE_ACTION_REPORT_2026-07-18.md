# Mirror ordered reasoning and experience intake - action report

Date: 2026-07-18  
Identity: `axm.machine.mirror/seed-0`  
Report: `mirror-reasoning-composition-experience-20260718-seed0-v2`  
Status: **WORKING private challenger - PROPOSE_HUMAN_REVIEW - not CANON**

This report supersedes the current-state claims in
`MIRROR_REASONING_SKILL_GROWTH_ACTION_REPORT_2026-07-18.md` without deleting
that earlier single-strategy receipt. The machine view is
`exports/action-reports/MIRROR_REASONING_COMPOSITION_AND_EXPERIENCE_AUDIT_2026-07-18.json`.
Both views describe the same trace; this prose has no decision authority.

## Outcome

Mirror's private strategy learner now represents arbitrary ordered strategy
sequences as well as individual tags. Mixed problems can therefore teach an
order such as "ask the blocking unknown, then discriminate the remaining
conflict" without adding that sequence to a compiled strategy list.

A small hardcoded Reasoning Experience Organ now closes the fixture-list seam.
It can append a verified deterministic reasoning receipt under the standing
local Workshop policy. The next private reasoning cycle discovers receipt
files and incorporates them without a source-code or fixture-list edit.

The organ's write scope is deliberately narrow: ignored private receipt state,
append only. It refuses learned-model self-training, hidden reasoning fields,
unknown training rights, non-independent or non-digested evaluator lineage,
held-out material, overwrites, tools, world actions, active-model changes, and
promotion.

## Preserved cycle evidence

Latest cycle: `reasoning-skill-e87ee75fdb0453f346ec`.

- Verified training groups: **12** (8 single-strategy, 4 ordered composition).
- Frozen held-out groups: **12** (8 single-strategy, 4 ordered composition).
- Deterministic supplied-path baseline: **0/12**.
- Private ordered-strategy challenger: **12/12**.
- Adversarial transfer: **8/8**.
- Mixed-signal exact-order transfer: **4/4**.
- No-candidate deterministic baseline: **0/12**.
- Learned exact-order candidate origination and selection: **12/12**.
- Source, feature, authority, transfer, composition, origination, and
  self-training canaries: **12/12**.
- Independent Seam Cell open implementation seams: **0**.
- Active runtime model changed: **no**.
- Real private reasoning-experience receipts in this cycle: **0**.

The earlier composition-only cycle
`reasoning-skill-c8b667cf1cd08fb0a20e` and the earlier single-strategy cycle
`reasoning-skill-349ae37bac2166b02ced` remain preserved under ignored private
state. They were not overwritten.

## Automatic-growth proof

An integration test created one deterministic experience receipt with a named,
content-digested evaluator artifact in a temporary append-only private
directory. The next
cycle discovered it automatically, admitted 13 total verified sessions, kept
12/12 supplied-path and 12/12 originated held-out performance, and kept the
active runtime unchanged. Separate tests proved digest tampering, hidden
reasoning, an evaluator equal to the learner, an outside-policy provider, and a
learned-model session are refused.

This proves the intake and discovery mechanism on synthetic evidence. It does
not prove that Mirror has yet learned from ongoing real experience.

## Live runtime receipt

The pre-existing local process started before this organ existed and truthfully
reported no experience organ. After verifying its command line was exactly
`node runtime/server.js` in this workspace, it was restarted from PID `24364`
to PID `5212`. The tightened content-digest contract then received one final
bounded restart from PID `5212` to PID `24416`. Live `/health` then reported:

- organ `axm.mirror.organ/reasoning-experience-intake-v1`;
- status `WORKING_PRIVATE_APPEND_ONLY`;
- write permission `ignored-private-reasoning-receipt-append-only`;
- `learnedWeights=false`.

No learned strategy model was loaded during the restart.

## What now works

- Verified receipts can teach individual strategies and ordered combinations.
- Exact strategy order affects path comparison and learned candidate
  origination.
- Old artifacts lacking sequence records are refused rather than silently
  reused.
- A verified local experience receipt changes the next private training cycle
  without a code edit.
- Receipt files are digest-bound, filename-bound, append-only, and source-group
  attributed.
- Training and held-out source groups remain separated.
- Token-language and reasoning-strategy evidence remain separate tracks.

## What still does not work

- No real experience receipt has entered the preserved latest cycle.
- The 12+12 exam is small and synthetic; perfect performance is not broad
  reasoning evidence.
- The learner originates safe epistemic paths, not arbitrary domain solutions.
- Ordinary reasoning requests do not silently become training data; explicit
  verified intake is required.
- The private model is not loaded by the active runtime.
- The token-language cycle remains held on `training-corpus-small`.
- Mike has not approved promotion or accepted this result as CANON.

## Verification

- Core: **76/76** tests passed.
- Mirror Learning Forge: **99/99** tests passed.
- AXM Native Learning Shell: **4/4** tests passed.
- Forge build integrity: **PASS**, 148 files.
- Mirror Doctor: **PASS**.
- JSON parse sweep: **PASS**, 104 tracked-visible JSON files.
- Git whitespace check: **PASS** (existing LF-to-CRLF notices only).
- Latest private reasoning-skill cycle: `PROPOSE_HUMAN_REVIEW`, zero open
  independent seams.

## Decision

Keep the active learned-model pointer unchanged. Preserve the private ordered
challenger and the append-only experience organ as WORKING research. The next
meaningful test is source-diverse real receipt accumulation followed by a fresh
frozen evaluation; until then the overall stewardship goal remains active.
