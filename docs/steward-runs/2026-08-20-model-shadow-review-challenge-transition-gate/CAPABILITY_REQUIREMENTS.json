{
  "requirements": [
    {"id": "upstream-separated-chain-integrity", "required": true, "capabilities": ["model.shadow.review.challenge-separated-witness-exact-rebuild"]},
    {"id": "pairwise-exact-replay-classification", "required": true, "capabilities": ["model.shadow.review.challenge-pairwise-exact-replay-classification"]},
    {"id": "pairwise-forward-extension-classification", "required": true, "capabilities": ["model.shadow.review.challenge-pairwise-forward-extension-classification"]},
    {"id": "pairwise-anchor-identity-drift-detection", "required": true, "capabilities": ["model.shadow.review.challenge-pairwise-anchor-identity-drift-detection"]},
    {"id": "pairwise-anchor-epoch-rollback-detection", "required": true, "capabilities": ["model.shadow.review.challenge-pairwise-anchor-epoch-rollback-detection"]},
    {"id": "pairwise-same-epoch-anchor-equivocation-detection", "required": true, "capabilities": ["model.shadow.review.challenge-pairwise-same-epoch-anchor-equivocation-detection"]},
    {"id": "pairwise-checkpoint-equivocation-detection", "required": true, "capabilities": ["model.shadow.review.challenge-pairwise-checkpoint-equivocation-detection"]},
    {"id": "pairwise-entry-fork-detection", "required": true, "capabilities": ["model.shadow.review.challenge-pairwise-entry-fork-detection"]},
    {"id": "withheld-fork-counterexample", "required": true, "capabilities": ["model.shadow.review.challenge-withheld-fork-nonexclusion-proof"]},
    {"id": "pairwise-transition-receipt-integrity", "required": true, "capabilities": ["model.shadow.review.challenge-pairwise-transition-exact-rebuild"]},
    {"id": "fresh-process-transition-reload", "required": true, "capabilities": ["model.shadow.review.challenge-pairwise-transition-fresh-process"]},
    {"id": "transition-authority-boundary", "required": true, "capabilities": ["model.shadow.review.challenge-transition-authority-none"]},
    {"id": "authenticated-host-anchor-pin", "required": false, "capabilities": ["model.shadow.review.challenge-anchor-host-pin.actual"]},
    {"id": "globally-consistent-external-transition-log", "required": false, "capabilities": ["model.shadow.review.challenge-transition-log-global-consistency.actual", "model.shadow.review.challenge-transition-external-retention.actual"]},
    {"id": "protected-monotonic-state", "required": false, "capabilities": ["model.shadow.review.challenge-transition-monotonic-state.actual"]},
    {"id": "externally-trusted-time", "required": false, "capabilities": ["model.shadow.review.challenge-transition-time.external"]},
    {"id": "independent-real-world-controllers", "required": false, "capabilities": ["model.shadow.review.challenge-separation-controller-identity-independence.actual"]},
    {"id": "global-single-use", "required": false, "capabilities": ["model.shadow.review.challenge-global-single-use.actual"]},
    {"id": "actual-human-review", "required": false, "capabilities": ["model.shadow.challenger.human-review.actual"]},
    {"id": "executed-benefit-evidence", "required": false, "capabilities": ["model.shadow.challenger.shadow-execution.actual", "model.shadow.challenger.evaluation.actual", "model.shadow.challenger.human-benefit.held-out"]}
  ]
}
