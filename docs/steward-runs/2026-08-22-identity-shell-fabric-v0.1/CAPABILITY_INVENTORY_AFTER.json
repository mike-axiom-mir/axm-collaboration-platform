{
  "capabilities": [
    {"id": "json.canonical.strict", "status": "available", "constraints": ["Workshop deterministic JSON core; strict JSON values only"]},
    {"id": "digest.sha256", "status": "available", "constraints": ["Node built-in crypto"]},
    {"id": "verification.node.focused", "status": "available", "constraints": ["local Node selftests"]},
    {"id": "shell.blueprint.strict-validate", "status": "available", "constraints": ["v0.1 schemas and executable exact-key normalizers"]},
    {"id": "shell.compile.deterministic", "status": "available", "constraints": ["pure data compiler; no clock, environment, I/O, provider, or device access"]},
    {"id": "shell.identity-root.explicit", "status": "available", "constraints": ["chosen metadata, roots, authority, disclosure, ownership, export policy"]},
    {"id": "shell.manifest.portable", "status": "available", "constraints": ["canonical JSON; embedded inert descriptors; no hidden defaults"]},
    {"id": "shell.export.redacted", "status": "available", "constraints": ["static forbidden-field and secret/path/personal-data scans"]},
    {"id": "shell.export.import-canonical", "status": "available", "constraints": ["canonical UTF-8 JSON text only"]},
    {"id": "shell.continuity.candidate-memory", "status": "available", "constraints": ["candidate and acceptance events remain distinct"]},
    {"id": "shell.continuity.hash-chain", "status": "available", "constraints": ["caller-declared canonical UTC timestamps and exact previous digests"]},
    {"id": "shell.continuity.reconstruct", "status": "available", "constraints": ["accepted receipt state only; no raw memory payloads"]},
    {"id": "shell.continuity.rollback-verify", "status": "available", "constraints": ["target must exist in effective accepted history"]},
    {"id": "shell.adapter.inert-descriptor", "status": "available", "constraints": ["provider binding UNBOUND; proposal authority only for neural role"]},
    {"id": "shell.body.inert-descriptor", "status": "available", "constraints": ["observation/output interfaces only; actuation unavailable"]},
    {"id": "shell.resource.envelope-enforce", "status": "available", "constraints": ["aggregate requested and permitted ceilings; UNKNOWN emits HOLD"]},
    {"id": "shell.attachment.authority-nonexpansion", "status": "available", "constraints": ["permissions, network, resources, memory, identity, and actuation checked"]},
    {"id": "shell.lineage.parent-digest", "status": "available", "constraints": ["exact verified v0.1 parent manifest inputs"]},
    {"id": "shell.lineage.fork-new-id", "status": "available", "constraints": ["copied shell id reuse rejected"]},
    {"id": "shell.lineage.migration-disclosure", "status": "available", "constraints": ["adapter, body, and neural slot changes require explicit disclosures"]},
    {"id": "shell.lineage.reconstruction", "status": "available", "constraints": ["same shell id plus exact continuity evidence"]},
    {"id": "shell.lineage.retirement-proposal", "status": "available", "constraints": ["proposal only; never effective"]},
    {"id": "shell.succession.distinct", "status": "available", "constraints": ["distinct proposed shell id; not fork or continuation"]},
    {"id": "shell.succession.human-receipt-required", "status": "available", "constraints": ["exact digest-bound declarative human receipt; compiler does not authenticate authorship"]},
    {"id": "shell.succession.external-gate-only", "status": "available", "constraints": ["compiled lifecycle is SUCCESSION_PROPOSED; successionEffective false"]},
    {"id": "shell.verify.exact-rebuild", "status": "available", "constraints": ["rebuild from exact caller inputs"]},
    {"id": "shell.verify.independent-manifest", "status": "available", "constraints": ["separate module does not import compiler; static manifest evidence ceiling"]},
    {"id": "shell.visual-blueprint-editor.render-and-click", "status": "unavailable", "constraints": ["Phase 2 held; browser verification NOT_RUN"]},
    {"id": "shell.adapter-host.execute", "status": "unavailable", "constraints": ["explicitly outside inert compiler"]},
    {"id": "sandbox.process.os-isolated", "status": "unavailable", "constraints": ["no process runtime in v0.1"]},
    {"id": "shell.human-authorship.authenticate", "status": "unavailable", "constraints": ["external authenticated human gate required"]},
    {"id": "robot.body.actuate", "status": "unavailable", "constraints": ["v0.1 rejects all actuation"]},
    {"id": "robot.safety.live-verify", "status": "unavailable", "constraints": ["no robotic execution or safety claim"]}
  ]
}
