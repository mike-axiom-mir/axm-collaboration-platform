{
  "requirements": [
    {
      "id": "independent-nested-semantics",
      "capabilities": ["identity.shell.verify.nested-semantics"],
      "required": true
    },
    {
      "id": "fresh-process-verification",
      "capabilities": ["identity.shell.verify.fresh-process"],
      "required": true
    },
    {
      "id": "fail-closed-malformed-input",
      "capabilities": ["identity.shell.verify.malformed-fail-closed"],
      "required": true
    },
    {
      "id": "keel-lineage-memory-portability",
      "capabilities": ["identity.shell.keel.scenario.fork", "identity.shell.keel.scenario.memory", "identity.shell.keel.scenario.migration"],
      "required": true
    },
    {
      "id": "cross-process-determinism",
      "capabilities": ["identity.shell.determinism.cross-process"],
      "required": true
    },
    {
      "id": "live-host-binding",
      "capabilities": ["identity.shell.host.bind"],
      "required": false
    },
    {
      "id": "authenticated-human-acceptance",
      "capabilities": ["identity.shell.human.authenticate"],
      "required": false
    },
    {
      "id": "runtime-continuity-proof",
      "capabilities": ["identity.shell.runtime.continuity.observe"],
      "required": false
    }
  ]
}
