{
  "capabilities": [
    {"id": "selection.validate-candidate-packet", "status": "available", "constraints": ["exact v0.6 repair candidate contract"]},
    {"id": "selection.bind-consent-instance-subject", "status": "available", "constraints": ["sealed grounded-consent instance/evaluation", "inert code-selection scope only"]},
    {"id": "selection.hash-selected-bytes", "status": "available", "constraints": ["in-memory Buffer or Uint8Array", "8 MiB ceiling", "bytes not retained"]},
    {"id": "selection.bind-ordered-roots", "status": "available", "constraints": ["four ordered PASS references", "evidence content not independently verified"]},
    {"id": "selection.detect-expiry-and-declared-replay", "status": "available", "constraints": ["declared clock and ledger only", "maximum 24-hour declaration window"]},
    {"id": "selection.emit-inert-authentication-gap", "status": "available", "constraints": ["only AUTHENTICATION_REQUIRED can be hold-free", "authority NONE"]},
    {"id": "identity.authenticate-natural-person", "status": "unavailable", "constraints": ["no trusted identity service in this rung"]},
    {"id": "time.observe-trusted-clock", "status": "unavailable", "constraints": ["provided timestamp is not trusted-clock evidence"]},
    {"id": "revocation.check-live", "status": "unavailable", "constraints": ["no independent live revocation service"]},
    {"id": "replay.observe-independent-ledger", "status": "unavailable", "constraints": ["provided ledger is explicitly not independently trusted"]},
    {"id": "host.authorize-exact-action", "status": "unavailable", "constraints": ["selection does not grant host authority"]},
    {"id": "sandbox.execute-exact-candidate", "status": "unavailable", "constraints": ["repaired disposable executor remains unauthorized"]},
    {"id": "mike.integrate-on-clean-v0.6-base", "status": "degraded", "constraints": ["active recovery checkout is dirty", "current recovery HEAD does not contain v0.6"]}
  ]
}
