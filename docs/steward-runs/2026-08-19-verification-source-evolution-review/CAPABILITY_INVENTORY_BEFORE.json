{
  "capabilities": [
    { "id": "verification.continuity.tracked-source-current-compare", "status": "available", "constraints": ["The continuity leaf identifies exact byte drift but does not route it to later receipts."] },
    { "id": "file-add-remove-change-and-unchanged-evidence", "status": "available", "constraints": ["The Module Lineage Comparator compares two explicit bundles and refuses semantic or temporal inference."] },
    { "id": "silent-history-rewrite-refusal", "status": "available", "constraints": ["The Immutable History Guard refuses rewrite but does not externally anchor legacy verification receipts."] },
    { "id": "verification.evolution.legacy-pre-anchor-integrity.available", "status": "degraded", "constraints": ["Two legacy receipts have no declared self-digest, so integrity before a new anchor cannot be established."] }
  ]
}

