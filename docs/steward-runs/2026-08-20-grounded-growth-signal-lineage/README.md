# Grounded Growth signal-lineage successor

Status: `TEST`

This additive audit turns the retained multi-model research disposition into an
exact current lineage without reviving its rejected authority proposals.

The source disposition contains six accepted signals and six rejected or
deferred proposals. The current receipt proves:

- five signals bind exact current technical sources, with three also bound to
  admitted current portfolio claims;
- the human-comprehension signal remains
  `WAITING_VOLUNTARY_HUMAN_EVIDENCE` and no person is treated as having opted
  in or benefited;
- five proposals remain rejected or redundant with `action: NONE`;
- the proposed human-facing signal ledger remains deferred and unimplemented;
- portable inspection checks integrity and authority boundaries but holds
  source truth and currentness at `UNKNOWN`.

On eight bounded developer-visible adversarial cases, an ID-and-count-only
shortcut makes one expected decision; the exact lineage guard makes all eight.
This is engineering evidence for a deterministic AI-assisted workflow guard,
not model learning, changed weights, intelligence, provider performance, an
independent benchmark, or broad generalization.

The Grounded Growth portfolio forward-extends nine byte-identical earlier
outcomes to ten outcomes across six capability chains. The new candidate stays
`EXPERIMENTAL`, its cycle stays `AWAITING_STEWARD`, and its outcome stays
`CANDIDATE_ONLY`. The existing optional voluntary-human handoff remains valid
because this new capability is outside its protected simulation chain.

Run:

```powershell
node shared/grounded-growth-signal-lineage/selftest.js
node docs/steward-runs/2026-08-20-grounded-growth-signal-lineage/build-signal-lineage-growth.js --check-recorded
node docs/steward-runs/2026-08-20-grounded-growth-signal-lineage/selftest.js
```

No research package is executed. Nothing installs, grants permission, starts
participation, promotes, merges, changes the Foundation, or makes a `CANON`
decision.
