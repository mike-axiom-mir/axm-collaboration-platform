# Portable verification — Grounded Growth successor

Status: `TEST`

This additive audit links the previously sealed detached current-state verifier
into the longitudinal Grounded Growth portfolio under its own capability
identity: `growth.current-state.detached-integrity.verify`.

The candidate aggregate binds six implementation sources and four evidence
artifacts. Its Verified Capability Loop receipt is `AWAITING_STEWARD`; it is
verified material, not installed or governed availability. The Grounded Growth
outcome admits two bounded claims:

- a shared-system effect on eleven frozen adversarial cases;
- an AI-workflow benefit from making all eleven expected reject/hold/admit
  decisions, including seven contradictions missed by digest-only checking.

Human benefit remains `NOT_RUN`. The evaluation was used to develop and test
the deterministic guard, so the result is not claimed as an independently
blinded benchmark, model learning, changed weights, provider performance,
intelligence, or generalization.

The successor portfolio contains nine outcomes across five capability chains.
All eight earlier outcome receipts remain byte-identical. Because the new
capability is separate from the protected `simulation.run-envelope.verify`
chain, the existing voluntary research-human handoff remains preserved as one
optional review candidate; no participation starts automatically.

Run:

```powershell
node docs/steward-runs/2026-08-19-portable-verification-grounded-growth/build-portable-verification-growth.js --check-recorded
node docs/steward-runs/2026-08-19-portable-verification-grounded-growth/selftest.js
```

Nothing here installs, grants permission, promotes, merges, changes the
Foundation, or makes a `CANON` decision.
