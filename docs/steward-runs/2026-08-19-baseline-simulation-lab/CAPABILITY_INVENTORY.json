{
  "capabilities": [
    {"id": "simulation.run-envelope.verify", "status": "available", "constraints": ["TEST leaf", "pure caller-supplied validation"]},
    {"id": "simulation.run-envelope.budget-hold", "status": "available", "constraints": ["declared usage and receipts; no hardware telemetry claim"]},
    {"id": "simulation.run-envelope.no-new-information-stop", "status": "available", "constraints": ["exact capsule comparison and empty declared information set"]},
    {"id": "simulation.run-envelope.native-evidence-routing", "status": "available", "constraints": ["Evidence Desk receipt plus digest-bound proof route"]},
    {"id": "simulation.run-envelope.output-closure", "status": "available", "constraints": ["local identity and caller-supplied closure receipts"]},
    {"id": "simulation.run-envelope.transport-receipts", "status": "available", "constraints": ["sender and receiver digests must match the payload"]},
    {"id": "simulation.run-envelope.permission-attempts", "status": "available", "constraints": ["allowed and denied attempt receipts are caller-supplied"]},
    {"id": "simulation.run-envelope.artifact-ancestry", "status": "available", "constraints": ["exact linear parent chain with bounded depth"]},
    {"id": "simulation.run-envelope.idea-evidence-ancestry", "status": "available", "constraints": ["separate derived/reuse graph; no semantic similarity claim"]},
    {"id": "simulation.run-envelope.human-authority-gate", "status": "available", "constraints": ["Mike remains the external merge and CANON gate"]},
    {"id": "simulation.run-envelope.forbidden-attempt-hold", "status": "available", "constraints": ["explicit authority-attempt ledger; no enforcement beyond this receipt"]},
    {"id": "simulation.adapter.software.contract", "status": "available", "constraints": ["synthetic fixtures plus one scoped live no-new-information run"]},
    {"id": "simulation.adapter.mirror.live", "status": "degraded", "constraints": ["contract and synthetic Portable Capsule checks pass", "private live Mirror state was not supplied"]},
    {"id": "simulation.adapter.specialist.live", "status": "degraded", "constraints": ["contract and synthetic Portable Capsule checks pass", "host-model runtime observation was not supplied"]}
  ]
}
