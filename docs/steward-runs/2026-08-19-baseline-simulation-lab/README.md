# Baseline Simulation Lab run-envelope steward run

Date: 2026-08-19

Status: `TEST`

Technical steward disposition: `TEST_READY_WITH_LIVE_ADAPTER_LIMITS`

Human acceptance: `NOT_RUN`

## Outcome

The 5YFF research did not justify a recursive model system, majority vote,
central simulation database, global score, auto-builder, or new Evidence Plane.
It did expose one grounded missing seam: a deterministic run envelope joining
the existing Portable Baseline Capsule, Evidence Desk, and Verified Capability
Loop.

That seam now exists at `shared/baseline-simulation-lab`. It validates exact
baseline identity, declared new information, seat provenance, adapter schemas,
budgets, output closure, separate ancestry, native proof routes, transport,
permissions, handoffs, human comprehension, forbidden authority attempts, and
one capability-cycle receipt. It invokes no model and executes no supplied
source.

## Live stopping result

The stored portable-baseline leaf was compared with the same exact verified
capsule. No new evidence, counterexample, requirement, failure, or staged
signal was declared. The generation-stage budget was zero. The live run
therefore returned `NO_NEW_INFORMATION`, emitted one `REUSE_EXISTING` cycle,
and performed no recursive generation.

`build-live-run.js` reconstructs that receipt from current stored evidence;
`selftest.js` verifies the capsule, comparison, Evidence Desk seal, prior cycle,
output closures, proof file digests, zero usage, and authority ceiling.

## Evidence status

- Focused run-envelope suite: `36 PASS`, `0 FAIL`.
- Live scoped no-new-information audit: `18 PASS`, `0 FAIL`.
- Capability comparator: required capabilities `READY`; no missing capability
  or proposed new hand. Optional live Mirror and specialist effectiveness remain
  `DEGRADED` because the relevant private/runtime observations were not supplied.
- All ten AGENTS.md checks exited `0`.
- Workshop verification remains `VERIFIED_WITH_LIMITS`.
- Browser render/click: `NOT_APPLICABLE`; this leaf has no browser surface and
  makes no visual UI claim.

## Limits preserved

- The live baseline is the previously declared Portable Baseline Capsule leaf,
  not the entire heavily dirty shared Workshop.
- Mirror and specialist-mask behavior is contract-tested synthetically. No
  private Mirror data or host-model runtime behavior was inferred.
- Output closure proves digest identity against caller-supplied receipts; it is
  not a network storage or distributed availability guarantee.
- Budget checks validate declared usage receipts; no hardware telemetry claim
  is made.
- The existing modified `tests/tool-forge-package-test.js` still routes an
  EXPERIMENTAL proof ZIP through Node's Windows temporary directory on C. This
  run did not overwrite that foreign worktree change and does not call it fixed.

The module is technical steward material only. It is neither installed nor
promoted. Mike Tobi remains the human direction, merge, and CANON gate.
