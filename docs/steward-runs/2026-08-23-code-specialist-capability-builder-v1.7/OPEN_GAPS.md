# Open Capability and Authority Gaps

- The other 13 code-specialist profiles still lack exact source-reviewed
  implementation recipes. HTML/markup is a logical next hand, but it needs its
  own closed input/output contract, escaping rules, accessibility evidence,
  adversarial fixtures, and a browser test before any visual claim.
- Consent identity is reference-bound but not authenticated. Nonce replay is
  disclosed as unverified because no authenticated decision ledger exists yet.
- Generated and candidate source remains research-only and on direct-reuse and
  public-copy hold pending Mike's rights decision.
- A repaired disposable executor remains not authorized. Runtime behavior and
  the emitted selftest remain unproven.
- Memory and duration ceilings are declared but not independently enforced by
  this in-memory builder.
- The portable builder-id schemas are now synchronized manually with the five
  active builders. A future admission should either update those schemas in the
  same transaction or replace the enum with a digest-bound registry reference.

