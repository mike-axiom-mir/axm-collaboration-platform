# Evidence route — closed object-contract adapter specialist v2.17

Status: `TEST`

This rung preserves the historical inactive v1 proposal, builds a hardened v2
counterpart, and routes only that exact recipe through an explicit JavaScript
application-logic adapter profile. It does not claim domain semantic
equivalence, arbitrary data conversion, nested schemas, general JavaScript,
workspace mutation, or general code execution.

| Claim | Native proof surface | Required verdict |
| --- | --- | --- |
| The prior Fabric could inspect but not build the adapter, and same-language profile uniqueness prevented an exact second JavaScript lane. | Before-inventory comparator plus direct registry, catalog, and profile-registry inspection. | `BLOCKED` before this rung. |
| Identical reviewed requests rebuild byte-identical detached adapter candidates. | Capability Fabric, Code Specialist, and independent package rebuild assertions. | `PASS` only when candidate bytes and bound digests agree. |
| The active v2 recipe does not rewrite or silently activate the historical v1 proposal. | Registry and foundry/admission continuity tests plus direct changed-path inspection. | `PASS`; v1 remains an inactive historical review candidate. |
| Runtime input is a closed own enumerable primitive data-property record; accessors, symbols, custom prototypes, unsafe fields, undeclared fields, malformed primitives, excess properties, and excess bytes fail closed. | Emitted selftest executed only by the explicit trusted package harness from a disjoint temporary copy plus independent adversarial fixtures. | `PASS` only for the declared flat primitive schemas. |
| Every source field is explicitly mapped or dropped, every target field is mapped, defaults satisfy the target shape, widening-only structural compatibility is enforced, and loss remains visible. | Builder validation fixtures, emitted behavior assertions, mapping digest, and returned dropped-field declaration. | `PASS` for structural compatibility; semantic equivalence remains `UNKNOWN`. |
| A second JavaScript profile cannot create ambiguous selection because mode, profile digest, artifact, recipe, and consent subject are exact. | Build-profile registry and Code Specialist ambiguity/drift tests. | `PASS` only when exact selection succeeds and substitutions hold. |
| Input, output, property, schema, candidate-file, candidate-byte, attempt, process, duration declaration, and cost ceilings remain bound. | Builder boundary fixtures plus Fabric resource observations and contract inspection. | `PASS` for enforced byte/property/file/attempt/process/cost boundaries; independent memory/duration enforcement remains `UNKNOWN`. |
| The Fabric did not execute, install, integrate, publish, promote, grant permissions, use network, or change `CANON`. | Candidate receipts, authority objects, static imports scan, and before/after repository observation. | Must remain false. |
| The adapter works with arbitrary host objects, hostile proxies, nested schemas, or establishes domain meaning. | Evidence not gathered and capability explicitly excludes these surfaces. | `UNKNOWN` or out of scope; never inferred from structural tests. |
| Direct reuse/public copying rights are resolved. | Mike decision plus rights evidence. | `HOLD`; research-only/direct-reuse hold remains. |

The trusted package harness may execute emitted fixture selftests only in its
own temporary execution copy, disjoint from detached candidate storage. That
does not authorize the general repaired executor or grant lifecycle authority.
