# Model Shadow review challenge transition declared disclosure evidence

Status: `TEST`

This bounded evidence package covers v1.0: a caller-declared roster binds two to
sixty-four member labels to exact v0.9 presentation references, every submitted
presentation exact-rebuilds, and every pair of matching commitments is
reconciled through v0.9.

Observed scope:

- exact roster normalization, unique member labels, unique commitments, and digest;
- typed missing declared commitments and valid-but-mismatched submissions;
- exact all-pairs reconciliation for matching commitments;
- compatible, incomplete-coverage, and contradiction decisions;
- a 64-member maximum producing exactly 2,016 pair references;
- 128 KiB roster and 32 MiB assessment canonical-input bounds;
- fresh-process rebuild and verification;
- bounded receipts with no transient packages, raw keys, signatures, model
  output, private context, or machine paths; and
- an independently valid unlisted presentation absent from a complete
  declared-set receipt.

The capability comparator moves the bounded route from `BLOCKED` to
`DEGRADED`, not complete. Declared coverage is relative to caller data. It does
not authenticate the roster or labels, compel disclosure, enumerate unlisted
roots, prove independent controllers, create a globally consistent log, retain
state externally, resist rollback, establish trusted time, prove human review,
invoke a provider, evaluate outcomes, adopt a branch, demonstrate benefit or
learning, promote, merge, or establish `CANON`.

`CHECK_RESULTS.json` records 28 passing commands: 18 focused lineage checks and
the 10 required `AGENTS.md` checks, with 1,174 focused assertions. Passing
stdout is not retained. `SOURCE_SNAPSHOT.json` binds 68 normalized source
inputs.
