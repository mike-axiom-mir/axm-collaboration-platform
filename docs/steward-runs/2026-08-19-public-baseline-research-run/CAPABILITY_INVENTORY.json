{
  "capabilities": [
    {"id": "research.git-object.observe", "status": "available", "constraints": ["local Git object database", "does not prove remote freshness"]},
    {"id": "simulation.baseline.capsule.verify", "status": "available", "constraints": ["TEST portable capsule", "historical public baseline only"]},
    {"id": "research.input.hash-verify", "status": "available", "constraints": ["seven externally quarantined D-drive inputs"]},
    {"id": "research.input.no-source-execution", "status": "available", "constraints": ["read-only byte hashing and inert document inspection"]},
    {"id": "research.seat.user-reported-provenance", "status": "available", "constraints": ["provider families only", "exact model IDs undisclosed"]},
    {"id": "research.seat.proof-authority-none", "status": "available", "constraints": ["all six run seats explicitly carry proofAuthority NONE"]},
    {"id": "research.signal.stage-preserve", "status": "available", "constraints": ["OBSERVED, INFERENCE, and UNKNOWN remain distinct"]},
    {"id": "research.proposal.refusal-preserve", "status": "available", "constraints": ["six rejected or deferred proposals retained with reasons"]},
    {"id": "research.consensus.not-proof", "status": "available", "constraints": ["no ranking, majority decision, or agreement-as-proof"]},
    {"id": "simulation.run-envelope.verify", "status": "available", "constraints": ["TEST pure validator", "no model invocation or source execution"]},
    {"id": "simulation.run-envelope.output-closure", "status": "available", "constraints": ["caller-supplied identity receipts plus focused external hash checks"]},
    {"id": "simulation.run-envelope.capability-cycle", "status": "available", "constraints": ["one REUSE_EXISTING receipt", "improvement not established"]},
    {"id": "research.model-artifact-attribution.exact", "status": "unknown", "constraints": ["no provider runtime receipts map individual artifacts to model seats"]},
    {"id": "research.cross-model-independence.prove", "status": "unknown", "constraints": ["prior-output exposure is unknown"]},
    {"id": "research.human-comprehension.live-evaluate", "status": "degraded", "constraints": ["protocol and voluntary bridge exist", "no opt-in live comparison occurred"]}
  ]
}
