# Code Capability Fabric independent assurance v1.6 steward run

Status: `TEST`

This append-only receipt covers implementation commit
`ba5af784b5322284d0f6da8a5f272648745053f9` on branch
`codex/code-capability-fabric-independent-assurance-v1.6`, based on the prior
readiness receipt tip `89bc241caa8fa03fb9fd41fb9adceeb2b1eaf5b6`.

The four AXM roots are the ordered technical acceptance gate:

1. truth;
2. agency / non-domination;
3. continuity; and
4. wisdom over speed.

Mike remains the human Git merge and policy-decision authority after that
technical gate. A human action cannot make a root-violating claim technically
sound, and a passing test cannot promote this module beyond `TEST`.

The additive layer verifies a v1 readiness assessment, a digest-bound verifier
trust policy, exact Ed25519 public-key records, and signed attestations for each
assurance record. The host observer cannot count as an assurance verifier, even
through a renamed copy of its key bytes, and one public key cannot masquerade as
multiple quorum members.

A complete quorum stops at `HUMAN_POLICY_ACCEPTANCE_REQUIRED` with authority
`NONE`. Separate keys do not prove separate people, organizations, competence,
or measurement quality. The v2 request did not bind the trust-policy digest, so
the exact policy still needs explicit human acceptance before any future
executor decision.

No supplied runtime, provider, executor, private workspace, network,
installation, promotion, merge, or `CANON` path ran. Generated-package reuse
rights and a future repaired disposable-sandbox executor remain deferred.

Evidence is split by purpose across audit, capability, evidence-routing,
verification, integration, ordered session, seal, summary, and curation files.
No raw command log, private key, or absolute machine path is retained.
