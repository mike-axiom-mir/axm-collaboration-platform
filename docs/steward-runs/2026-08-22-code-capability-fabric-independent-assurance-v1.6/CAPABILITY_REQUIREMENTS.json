{
  "schema": "capability-requirements/v1",
  "mission": "Add a provider-neutral, deterministic, non-dominating assurance quorum over existing Code Capability Fabric readiness records without adding an executor or replacing Foundation or Atlas responsibilities.",
  "rootPrecedence": [
    "truth",
    "agency-non-domination",
    "continuity",
    "wisdom-over-speed"
  ],
  "requirements": [
    {
      "id": "independent-assurance-review",
      "required": true,
      "acceptance": [
        "exact digest-bound policy",
        "exact Ed25519 key bytes",
        "observer exclusion by reference and key material",
        "unique public-key material per quorum member",
        "record/readiness/policy replay resistance",
        "strict deterministic review output",
        "authority NONE"
      ]
    },
    {
      "id": "historical-continuity",
      "required": true,
      "acceptance": [
        "v1 planner selftest passes",
        "v2 planner selftest passes",
        "readiness selftest passes",
        "three earlier steward receipts pass",
        "earlier implementation files remain byte-identical to the base"
      ]
    },
    {
      "id": "authorized-provider-execution",
      "required": false,
      "acceptance": [
        "not in scope",
        "must remain blocked without a separately authorized repaired disposable-sandbox executor"
      ]
    },
    {
      "id": "generated-source-direct-reuse",
      "required": false,
      "acceptance": [
        "hold by default until Mike decides reuse rights"
      ]
    }
  ]
}
