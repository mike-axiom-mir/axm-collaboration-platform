# Code Capability Fabric grounded consent scope v1.8 steward run

Status: `TEST`

This append-only receipt covers implementation commit
`e43deced6ebff26a36febd77aff9cc591846e33a` on branch
`codex/code-capability-fabric-grounded-consent-v1.8`, based on verified v1.7 tip
`cdf17eb7b01fc8216e23569bbcc2b5e127664334`.

The run adds one provider-neutral consent grammar: a standing settings policy,
an exact bounded attempt proposal, and a deterministic subset evaluation. The
proposal binds subject, domain profile, pre-decision evidence, byte-length input
references, actions, permissions, network domains, data classes, source uses,
lifecycle effects, resources, outcome evidence, re-consent triggers, stop
conditions, and a time window.

The four AXM roots remain the ordered technical gate. For the code case, the
instance must reference the earlier policy-bound four-root review before Mike's
later decision. A reference does not verify its content, so the terminal next
gate explicitly requires pre-decision evidence verification before an exact
authenticated human decision.

A valid scope fit stops at `AUTHENTICATED_HUMAN_DECISION_REQUIRED` with
authority `NONE`. It does not prove policy authorship, a natural person,
informed understanding, input bytes, trusted time, live non-revocation, domain
safety, host authorization, execution, output, persistent learning, publishing,
installation, hardware actuation, promotion, or `CANON`.

Seven additive `TEST` files were committed. No existing Fabric file changed.
The original intake receipt initially caught an optional root README edit; that
edit was removed, and all five historical receipt selftests passed. This
preserves continuity instead of rewriting old evidence to accept drift.

No supplied runtime, provider, executor, real private workspace, decision key,
network, installation, publishing, promotion, merge, hardware path, or `CANON`
path ran. Generated-source reuse rights and any repaired disposable-sandbox
executor remain deferred to Mike.
