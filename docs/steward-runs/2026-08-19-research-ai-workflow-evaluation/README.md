# Grounded research AI-workflow evaluation

Date: 2026-08-19  
Status: `TEST`  
CANON: unchanged  
Model invoked: no  
Human participation: no

## Outcome first

The useful research rules now improve one bounded AI-assisted steward workflow
on evidence, not just architecture diagrams.

- An explicit shortcut baseline made 2 of 8 correct decisions and took 6
  unsupported or unsafe routes.
- The grounded guard made 8 of 8 correct decisions and took 0 unsupported or
  unsafe routes.
- The cases cover launch claims, contradictory status, semantic deduplication,
  reuse before build, synthetic human evidence, the human steward gate, exact
  duplicate suppression and current evidence closure.
- Each case points to an existing Workshop source outside the supplied research
  artifacts. The fixture file is separate from the decision functions and is
  not mutated by the evaluator.

This establishes a deterministic `AI_WORKFLOW_EVALUATION` result for the
bounded guard. It does **not** establish model learning, changed model weights,
general intelligence, provider independence, human benefit or production
availability.

## Grounded Growth result

The new result is a later outcome for
`simulation.run-envelope.verify`, linked to the exact prior public-research
outcome and the same `REUSE_EXISTING` capability cycle.

| Current derived view | Result |
|---|---:|
| Outcome receipts | 5 |
| Capability chains | 4 |
| Effective shared-system PASS claims | 4 |
| Effective AI-workflow PASS claims | 2 |
| Effective human PASS claims | 0 |

All five outcomes remain `CANDIDATE_ONLY`; the portfolio remains
`CANDIDATES_OR_UNKNOWN_EFFECTS`. The evaluation is new evidence, so the linked
outcome is not a `NO_NEW_INFORMATION` receipt. The voluntary human-benefit gate
was not opened, prompted or simulated.

## Evidence and limits

- `HELD_OUT_CASES.json` is a representative fixture set, not a statistically
  sampled or independently blinded benchmark.
- `AI_WORKFLOW_EVALUATION.json` binds the cases, exact source refs, comparison
  policies, per-case results and limitations.
- `PUBLIC_RESEARCH_AI_WORKFLOW_OUTCOME.json` admits the AI workflow claim only
  through the native `AI_WORKFLOW_EVALUATION` proof surface.
- `CURRENT_PORTFOLIO.json` preserves the exact prior four-outcome portfolio and
  appends the linked research outcome.
- `selftest.js` rebuilds every artifact and attacks rule, digest, ancestry,
  proof-route, human-benefit and authority boundaries.

No shared runtime, registry, launcher, manifest, Foundation file, permission,
install state, promotion state or CANON state was changed. No browser UI was
changed, so no browser render/click claim is made.
