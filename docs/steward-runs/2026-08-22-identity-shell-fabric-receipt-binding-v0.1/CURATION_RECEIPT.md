# Curation receipt

This run retains the smallest useful evidence set: normalized capability inputs and comparison, exact check outcomes, routed claims and evidence ceilings, a compact JSONL event segment, a deterministic seal, file hashes, and a human-readable summary.

Raw terminal output, duplicate retries, process telemetry, full repository listings, machine-local paths, temporary package paths, and unrelated Workshop verification detail were not retained. The initial failures were preserved as typed counterevidence rather than erased. The 41 repository-wide verifier warnings and all unrun surfaces remain visible.

No API keys, authorization headers, session or task identifiers, private chats, raw prompts, hidden reasoning, personal account data, or unrelated personal information are present. The Keel material is identified only as the already-disclosed inert Workshop role proposal; this receipt does not manufacture or authenticate an identity.

Retention decision: keep this append-only receipt directory with the reviewed branch. Do not treat its existence as installation, promotion, merge approval, human acceptance, or `CANON`.
