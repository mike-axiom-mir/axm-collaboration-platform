# Code Capability Fabric Python Source v2.1 — steward receipt

Status: `TEST`

Implementation source commit: `ff3269778298796776bbb0e90eb45eaf65e2d297`

Base commit: `ee0c63743f4ecca1e49f14d4d8f24b429672e564` from
`codex/code-capability-fabric-code-family-extension-base-v2.0` (PR #50).

## Bounded change

- Added one provider-neutral, deterministic Python record-transform HAND recipe.
- Added a Python application-logic build profile and specialist-builder route.
- Generalized HAND artifact layout so a recipe can byte-bind its language-native
  entry and selftest paths while retaining the existing JavaScript defaults.
- Tightened portable candidate-path validation for Windows aliases, reserved
  names, alternate data streams, case collisions, traversal, and separators.
- Rebuilt the deterministic City, schema, and twin views.

The generated `capability.py` and `selftest.py` are inert candidate data. They
were not executed, installed, integrated, published, promoted, or called CANON.
No supplied experimental Python runtime was executed or reused.

## Truth boundary

Static structure, lineage, schema closure, deterministic generation, authority,
permission, privacy, resource declarations, and path handling were tested.
Python runtime correctness remains `UNKNOWN`. Browser testing was not run because
this steward rung changes no visual surface.

Direct reuse and public copying remain on hold. A future disposable executor
remains blocked pending Mike's separate explicit authorization.

Mike remains the final merge gate. This receipt grants no merge, installation,
promotion, publication, or CANON authority.

