{
  "schema": "axm.capability-gap-report/v1",
  "status": "VERIFIED_WITH_LIMITS",
  "requestedOutcome": "Build the grounded AXM LEGO Software City in dependency order without implicit authority.",
  "implemented": [
    "deterministic live city graph and drift gate",
    "conservative schema identity and compatibility registry",
    "local content-addressed artifact candidate store",
    "local append-only event journal and replay",
    "typed exact authority decisions",
    "caller-injected measured hands rail",
    "deterministic checkpointed route state",
    "receipt freshness/conflict/warning evidence grid",
    "owner-defined local merge candidates",
    "digest-bound human/machine twins",
    "exact fifteen-step inert intake state machine",
    "disabled external gate registry and inert plans"
  ],
  "degraded": [
    {
      "id": "json-schema.full-validation",
      "reason": "Registry implements conservative identity/equivalence/adapter rules, not all JSON Schema 2020-12 semantics."
    },
    {
      "id": "executor.confinement.measured",
      "reason": "The rail accepts measured denial probes but no real executor or OS sandbox was bundled or tested."
    },
    {
      "id": "workflow.persistence",
      "reason": "Route state is deterministic data; a durable host/store and crash-resume integration remain external."
    },
    {
      "id": "state.merge.general",
      "reason": "Only append-only and exact G-Set candidates are automatic; other semantics hold or conflict."
    },
    {
      "id": "identity.authentication",
      "reason": "Authority requires a caller-supplied decision-maker verifier; this build is not an identity provider."
    },
    {
      "id": "socket.resolution",
      "reason": "The graph intentionally retains 1,532 unresolved edges, including 622 schema sockets."
    }
  ],
  "unknown": [
    {
      "id": "mirror.organ.compatibility",
      "reason": "The 115 Mirror organs remain inferred candidates without direct socket or runtime tests."
    },
    {
      "id": "external.protocol.interoperability",
      "reason": "MCP, A2A, GitHub, OCI/ORAS, and public attestation transports are disabled and untested."
    },
    {
      "id": "browser.behavior",
      "reason": "No live browser render/click test was run; generated surfaces were checked as bytes and semantics."
    },
    {
      "id": "cross-platform.durability",
      "reason": "Line-ending determinism is tested, but filesystem crash and concurrency behavior across platforms is unmeasured."
    }
  ],
  "cheapestHonestNextRoute": [
    "review the seven phase commits and generated diff before any merge decision",
    "select one authenticated local decision-maker verifier",
    "measure one real confined executor with denial probes and remaining reachable effects",
    "resolve high-value schema/capability sockets incrementally rather than suppressing unknowns",
    "test one external gate separately under exact one-use authority while keeping committed defaults disabled",
    "test Mirror organs individually through quarantine and compatibility adapters"
  ],
  "proofBoundary": "No passing check installs, promotes, merges, canonizes, or grants authority."
}
