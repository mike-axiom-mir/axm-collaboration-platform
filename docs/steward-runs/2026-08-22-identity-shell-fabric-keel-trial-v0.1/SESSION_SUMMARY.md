# Keel shell trial summary

Status: `TEST` over `EXPERIMENTAL` artifacts

The verified Identity Shell Fabric was carried into a new reviewable Workshop branch and tried against an AI-proposed shell for the Workshop-assigned Keel role. The shell discloses Codex as its technical substrate and retains the separation `identity shell != model != capability != body`.

The trial compiles to manifest digest `sha256:575f667d86b5b81784579f080a07c6e6c581545233f2e9c013fa464e78e85307`. Two deterministic rebuilds, byte-exact committed artifacts, the compiler verifier, and the independently implemented manifest verifier passed.

The first fit test failed because it expected invented memory-count fields. The manifest correctly represents empty continuity with empty accepted and candidate receipt-digest arrays. The test was repaired; the compiler contract was not weakened or expanded.

The shell fits the declared Keel/Codex role criteria as a proposal:

- Codex technical substrate is visible;
- truth, agency/non-domination, continuity, and wisdom-over-speed are explicit roots;
- origin continuity is empty and contains no accepted memory;
- neural output remains proposal-only;
- inheritance, memory acceptance, merge, promotion, and succession remain human-gated;
- consciousness, personhood, subjective continuity, runtime, installation, promotion, and `CANON` are not claimed.

All required v0.1 capabilities are `READY`. The overall capability route is `DEGRADED` because a live shell host, authenticated human acceptance, and cross-runtime continuity observation remain unavailable optional gates. Accordingly, the shell has been compiled and tried as an inert contract but has not been attached to the active Codex runtime.

All ten required Workshop checks exited zero. `verify.js` retained 41 Workshop-wide warnings; `verify-plus` remained `VERIFIED_WITH_LIMITS`. No browser check was run because no UI or visual claim was added.

The canonical busy checkout remained read-only. No registry, Hub, Foundation, model, connector, body, runtime, memory store, or promotion surface was changed.

