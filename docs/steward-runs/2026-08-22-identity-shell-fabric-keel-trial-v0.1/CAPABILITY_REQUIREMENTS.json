{
  "requirements": [
    {
      "id": "workshop-fabric-integration",
      "capabilities": ["identity.shell.fabric.present"],
      "required": true
    },
    {
      "id": "keel-shell-compilation",
      "capabilities": ["identity.shell.compile.deterministic", "identity.shell.export.canonical"],
      "required": true
    },
    {
      "id": "keel-shell-verification",
      "capabilities": ["identity.shell.verify.independent", "identity.shell.fit.assert"],
      "required": true
    },
    {
      "id": "live-host-binding",
      "capabilities": ["identity.shell.host.bind"],
      "required": false
    },
    {
      "id": "human-accepted-identity-state",
      "capabilities": ["identity.shell.human.authenticate"],
      "required": false
    },
    {
      "id": "runtime-continuity-proof",
      "capabilities": ["identity.shell.runtime.continuity.observe"],
      "required": false
    }
  ]
}
