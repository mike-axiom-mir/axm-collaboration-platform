# Steward receipt — strict SVG status badge specialist v2.15

Status: `TEST`

This append-only receipt seals one bounded growth rung. It does not install,
integrate, promote, merge, or canonize the result.

## Lineage and review

- Branch: `codex/code-capability-fabric-svg-status-badge-v2.15`
- Technical source commit:
  `52de17f30cbf429c926af48b4604dd2a0102e52e`
- Exact parent:
  `211fa5e0b1e8f3d54b5e772e8c9c049162fc3cab`
- Stacked target: `codex/code-capability-fabric-bounded-css-stylesheet-v2.14`
- Draft review: <https://github.com/mike-axiom-mir/axm-collaboration-platform/pull/65>
- Target review: <https://github.com/mike-axiom-mir/axm-collaboration-platform/pull/64>

Immediately before publication, the remote v2.14 branch and PR64 head both
matched the exact parent, and PR64 was `CLEAN`. The canonical Workshop checkout
was separately observed clean at
`7dd800d09731607b2936b580ab972d8033801736` and remained untouched.
Publication used a separate clean, attached review worktree.

## What changed

The Fabric can now deterministically plan and emit one detached JavaScript
renderer candidate for a fixed text-and-rectangle SVG status badge. The
existing SVG recipe was hardened rather than duplicated. It now accepts only a
closed optional `label`/`value` record, refuses accessors, custom serialization
hooks, XML-invalid control/surrogate text, undeclared fields, and over-budget
input or output. It XML-escapes accepted text and emits only fixed `svg`,
`title`, `rect`, and `text` structure.

One exact SVG + markup-structure Code Specialist profile now binds the SVG
language catalog, specialist Organ, recipe, builder, consent, candidate bytes,
and evidence route. The HTML and SVG lanes can share the markup specialist only
through distinct exact profiles; neither can borrow the other's recipe.

The active ceilings are 128 input bytes and 1024 output bytes. The rung also
updates the v2.2 request/candidate contract identity, adversarial tests,
documentation, and official Workshop City/schema/twin views. The capability-gap
comparator moved this exact rung from `BLOCKED` to `READY`; it did not declare a
general SVG capability.

Exact lineage:

- Builder: `sha256:c6a33c681bf6f267dab1e269dfb65ab19708747d59ddf7573468ebb4d65436d8`
- Recipe: `sha256:4ac6de7c2f7bc8be396612aa0db9301659a0a299cf5902a3af31dc27d4f286ee`
- SVG specialist profile: `sha256:e40aad951f90f81b1dfe3667ddbb991a8c124165fe0a03d1cb6841f100ad2226`
- Build-profile catalog: `sha256:fcacefcc17d265e1af4b7a64f06356d00840f29fed3d222be54ad8273b5d39d0`
- Exact specialist candidate: `sha256:25f1152849791c0d3ba8df8c9fb61690f3a4050c77177ad56ce74ff15bbb79a7`

## Authority and resource boundary

The emitted candidate remains `EXPERIMENTAL`, detached, uninstalled, and
unexecuted by the Code Specialist Fabric. It has no provider, network,
filesystem, environment, clock, randomness, installation, integration,
publication, promotion, merge, or `CANON` authority. Raw SVG, paths, style,
script, events, URLs, external resources, animation, and arbitrary visual scenes
remain outside this hand.

The trusted package harness executed emitted fixture selftests only in disjoint
temporary copies. This is recorded separately from Fabric/Nursery behavior and
does not authorize a general executor.

## Four-root evaluation

- Truth: `PASS` for exact deterministic bytes, lineage, strict record
  validation, XML-valid escaped output, static fixture parity, and the tested
  refusal cases. Browser appearance, accessibility behavior, and visual quality
  remain `UNKNOWN`.
- Agency / non-domination: `PASS`; generation grants no lifecycle authority and
  cannot approve, execute, install, integrate, promote, merge, or canonize itself.
- Continuity: `PASS`; the exact v2.14 parent, language/Organ/profile/recipe/
  builder bytes, official projections, candidate lineage, and deterministic
  rebuilds are bound and checked.
- Wisdom over speed: `PASS`; the rung hardens and wires one existing closed SVG
  hand instead of creating a competing visual system or arbitrary SVG executor.

These results make the packet reviewable. They do not replace Mike's final
hold, rejection, or merge decision.

## Verification and warnings

All ten required `AGENTS.md` checks passed. Focused results include 287 builder
assertions, 69 profile assertions, 144 shared Fabric checks, 15 tool checks, 27
admission checks, 114 package checks, and 51/51 recursive Fabric entry points.
`verify.js` ended at `0 FAIL · 25 warn · spine b618c5762240070c`. The 25 inherited
warnings are not hidden or reclassified. Exact results, limitations, and unrun
surfaces are in `TEST_REPORT.md`.

## Holds and decisions still owned by Mike

- Direct reuse or public copying of research-derived/generated source remains
  `HOLD` until rights are evidenced.
- A repaired disposable general executor remains unauthorized.
- Browser proof is required before any appearance, accessibility, or visual
  quality claim.
- General SVG, paths, scenes, animation, Visual Mold composition, installation,
  integration, product publication, promotion, merge, and `CANON` remain
  separate future capabilities or decisions.

The exact no-surprise review and possible later fast-forward route is recorded
in `INTEGRATION_HANDOFF.md`. Target drift must be checked again at decision time.
