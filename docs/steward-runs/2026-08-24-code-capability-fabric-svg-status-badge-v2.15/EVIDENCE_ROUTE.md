# Evidence route — strict SVG status badge specialist v2.15

Status: `TEST`

This rung reuses and hardens the existing native SVG status-badge hand, then
routes it through one exact Code Specialist profile. It does not claim arbitrary
SVG, editable vector paths, animation, Visual Mold composition, or general
visual-scene generation.

| Claim | Native proof surface | Required verdict |
| --- | --- | --- |
| The prior Fabric lacked a strict runtime badge record, explicit input/output byte bounds, and an exact SVG specialist profile. | Normalized before-inventory comparator report plus direct source/catalog inspection. | `BLOCKED` before this rung. |
| Identical exact requests rebuild byte-identical detached SVG badge candidates. | Code Specialist builder selftest, Capability Fabric selftest, and exact package digest comparison. | `PASS` only when all agree. |
| SVG language, markup-structure Organ, profile, recipe, and builder bytes are exact. | Build-profile registry selftest plus candidate lineage verification. | `PASS` only for exact digests. |
| Runtime values are a closed plain JSON record, reject undeclared or hostile fields, and stay within declared byte ceilings. | Emitted selftest executed only by the trusted package harness from a disjoint temporary copy; adversarial source checks. | `PASS` only for the bounded renderer contract. |
| Generated SVG is text-only, XML-escaped, and contains no script, event handler, external resource, URL, style, animation, or dynamic code surface. | Static generated-source assertions plus emitted selftest. | `PASS` for the asserted closed output grammar, not for browser rendering. |
| The Fabric did not execute, install, integrate, publish, promote, grant permissions, use network, or change `CANON`. | Candidate receipts, resource observation, authority objects, and module contracts. | Must remain false. |
| The badge is visually correct, readable, accessible in browsers, or aesthetically approved. | Actual browser render and human visual review. | `UNKNOWN`; no browser claim without that separate evidence. |
| Direct reuse/public copying rights are resolved. | Mike decision plus rights evidence. | `HOLD`; research-only/direct-reuse hold remains. |

The trusted package harness may execute the emitted fixture selftest only in its
own temporary execution copy, disjoint from the detached Nursery source. That
does not authorize a general executor or turn visual claims into `PASS`.
