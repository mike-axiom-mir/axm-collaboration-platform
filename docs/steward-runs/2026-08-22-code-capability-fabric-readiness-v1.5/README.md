# Code Capability Fabric readiness v1.5 steward run

Status: `TEST`

This append-only receipt covers the additive cryptographic readiness gate in
implementation commit `3579073aaa5854fba7bbf1e985b5a23d0f98ac1a` on branch
`codex/code-capability-fabric-readiness-v1.5`.

The bounded outcome is technically useful but deliberately smaller than an
executor. A deterministically rebuilt v2 route plan can now be joined to an
exact Ed25519 observer key, a valid signature over the exact host observation,
and complete digest-bound assurance records. When all checks pass, the result
is `AUTHORIZATION_REQUIRED`, never execution authority.

No supplied experimental runtime, provider, executor, workspace, private
source, network, installation, promotion, merge, or `CANON` path was run. The
generated-package direct-reuse question remains on hold for Mike. A future
repaired disposable-sandbox executor also remains unauthorized.

Evidence is split by purpose:

- `AUDIT_FINDINGS.json` records the closed and open seams;
- `CAPABILITY_*` records contain the capability scout and comparator result;
- `EVIDENCE_ROUTE.json` maps atomic claims to proof and counterevidence;
- `VERIFICATION_RECEIPT.json` records tests, warnings, and unrun surfaces;
- `INTEGRATION_HANDOFF.json` records source/base facts and the safe review route;
- `SESSION_EVENTS.jsonl`, `SESSION_SEAL.json`, and `SESSION_SUMMARY.md` preserve
  ordered durable events without raw command logs; and
- `CURATION_RECEIPT.json` records retention choices.

Mike remains the merge and authority gate.
