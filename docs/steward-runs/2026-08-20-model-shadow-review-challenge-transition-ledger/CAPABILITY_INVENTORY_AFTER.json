{
  "capabilities": [
    {"id": "model.shadow.review.challenge-pairwise-transition-exact-rebuild", "status": "available", "constraints": ["Every attempted local append exact-rebuilds its v0.7 transition package before inspecting or writing the local ledger."]},
    {"id": "model.shadow.review.challenge-transition-forward-only-local-append", "status": "available", "constraints": ["Only CANDIDATE_EXTENDS_PRESENTED_CHAIN with forwardTransitionAdmissible true and reviewRequired false can append."]},
    {"id": "model.shadow.review.challenge-transition-manifest-log-genesis-binding", "status": "available", "constraints": ["An immutable exclusive-created, file-synced manifest binds one caller log id and exact genesis separated-witness reference to the namespace."]},
    {"id": "model.shadow.review.challenge-transition-exact-local-head-match", "status": "available", "constraints": ["The presented previous separated-witness reference must exactly equal the head derived from the preserved local chain."]},
    {"id": "model.shadow.review.challenge-transition-contiguous-local-sequence", "status": "available", "constraints": ["Entries reload only as a contiguous sequence of exact 12-digit filenames beginning at one."]},
    {"id": "model.shadow.review.challenge-transition-local-digest-chain", "status": "available", "constraints": ["Every entry binds the immutable manifest, preceding entry reference, preceding local head, candidate head, fixed truth boundary, and its own digest."]},
    {"id": "model.shadow.review.challenge-transition-local-concurrent-fork-exclusion", "status": "available", "constraints": ["Two fresh processes contending to extend one preserved local head yield one exclusive-create winner and one typed loser."]},
    {"id": "model.shadow.review.challenge-transition-local-corruption-fail-closed", "status": "available", "constraints": ["Truncated entries, sequence gaps, missing manifest with existing state, and truth-boundary tampering fail closed and remain for steward inspection."]},
    {"id": "model.shadow.review.challenge-transition-fresh-process-reload", "status": "available", "constraints": ["A fresh Node process validates the manifest, contiguous entries, local digest chain, and exact derived head."]},
    {"id": "model.shadow.review.challenge-transition-caller-package-persisted-exact-rebuild", "status": "available", "constraints": ["When the caller re-presents the signature-bearing v0.7 package, a fresh process exact-rebuilds it and compares the exact persisted entry."]},
    {"id": "model.shadow.review.challenge-transition-independent-root-fork-counterexample", "status": "available", "constraints": ["Two caller-owned roots initialized at the same genesis demonstrably accept different pairwise-valid candidate heads."]},
    {"id": "model.shadow.review.challenge-transition-deletion-reopen-counterexample", "status": "available", "constraints": ["Deleting one bounded caller-owned namespace demonstrably reinitializes the genesis and accepts a different branch."]},
    {"id": "model.shadow.review.challenge-transition-bounded-persisted-data", "status": "available", "constraints": ["Entries persist the bounded transition receipt and digest references, not exact-rebuild inputs, raw public keys, raw signatures, private context, raw model output, or machine paths."]},
    {"id": "model.shadow.review.challenge-transition-ledger-authority-none", "status": "available", "constraints": ["Receipts and snapshots grant no authenticated host, identity, human, provider, execution, adoption, installation, promotion, merge, Foundation, or CANON authority."]},
    {"id": "model.shadow.review.challenge-transition-host-authorization.actual", "status": "unknown", "constraints": ["The explicit confirmation phrase and library call authenticate no host or trusted entrypoint."]},
    {"id": "model.shadow.review.challenge-transition-log-global-consistency.actual", "status": "unknown", "constraints": ["The independent-root counterexample proves local serialization is not consensus, total order, or global fork exclusion."]},
    {"id": "model.shadow.review.challenge-transition-external-retention.actual", "status": "unknown", "constraints": ["Fresh-process reload uses the same caller-owned root; no independent receiver or retention authority is proven."]},
    {"id": "model.shadow.review.challenge-transition-monotonic-state.actual", "status": "unknown", "constraints": ["The deletion counterexample proves the caller-owned local head is not protected monotonic state."]},
    {"id": "model.shadow.review.challenge-transition-time.external", "status": "unknown", "constraints": ["Ordering checks use caller timestamps; no external trusted-time receipt was supplied."]},
    {"id": "model.shadow.review.challenge-separation-controller-identity-independence.actual", "status": "unknown", "constraints": ["Synthetic keys and declared digests do not authenticate independent real-world controllers."]},
    {"id": "model.shadow.review.challenge-global-single-use.actual", "status": "unknown", "constraints": ["Separate roots and deleted state can accept alternate branches from the same genesis."]},
    {"id": "model.shadow.challenger.human-review.actual", "status": "unknown", "constraints": ["Only synthetic fixtures were used; no real person reviewed a current transition."]},
    {"id": "model.shadow.challenger.shadow-execution.actual", "status": "unknown", "constraints": ["No model provider or experiment was invoked."]},
    {"id": "model.shadow.challenger.evaluation.actual", "status": "unknown", "constraints": ["No current baseline/challenger evaluation was executed or supplied."]},
    {"id": "model.shadow.challenger.human-benefit.held-out", "status": "unknown", "constraints": ["No voluntary held-out human outcome was observed."]}
  ]
}
