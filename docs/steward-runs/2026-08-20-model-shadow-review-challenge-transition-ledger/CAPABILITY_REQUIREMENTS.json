{
  "requirements": [
    {"id": "upstream-pairwise-transition-integrity", "required": true, "capabilities": ["model.shadow.review.challenge-pairwise-transition-exact-rebuild"]},
    {"id": "forward-only-local-append", "required": true, "capabilities": ["model.shadow.review.challenge-transition-forward-only-local-append"]},
    {"id": "manifest-log-and-genesis-binding", "required": true, "capabilities": ["model.shadow.review.challenge-transition-manifest-log-genesis-binding"]},
    {"id": "exact-current-local-head-match", "required": true, "capabilities": ["model.shadow.review.challenge-transition-exact-local-head-match"]},
    {"id": "contiguous-local-sequence", "required": true, "capabilities": ["model.shadow.review.challenge-transition-contiguous-local-sequence"]},
    {"id": "local-entry-digest-chain", "required": true, "capabilities": ["model.shadow.review.challenge-transition-local-digest-chain"]},
    {"id": "local-concurrent-fork-exclusion", "required": true, "capabilities": ["model.shadow.review.challenge-transition-local-concurrent-fork-exclusion"]},
    {"id": "local-corruption-fail-closed", "required": true, "capabilities": ["model.shadow.review.challenge-transition-local-corruption-fail-closed"]},
    {"id": "fresh-process-local-reload", "required": true, "capabilities": ["model.shadow.review.challenge-transition-fresh-process-reload"]},
    {"id": "caller-package-persisted-exact-rebuild", "required": true, "capabilities": ["model.shadow.review.challenge-transition-caller-package-persisted-exact-rebuild"]},
    {"id": "independent-root-fork-counterexample", "required": true, "capabilities": ["model.shadow.review.challenge-transition-independent-root-fork-counterexample"]},
    {"id": "deletion-reopen-counterexample", "required": true, "capabilities": ["model.shadow.review.challenge-transition-deletion-reopen-counterexample"]},
    {"id": "bounded-persisted-transition-data", "required": true, "capabilities": ["model.shadow.review.challenge-transition-bounded-persisted-data"]},
    {"id": "transition-ledger-authority-boundary", "required": true, "capabilities": ["model.shadow.review.challenge-transition-ledger-authority-none"]},
    {"id": "authenticated-host-entrypoint", "required": false, "capabilities": ["model.shadow.review.challenge-transition-host-authorization.actual"]},
    {"id": "globally-consistent-transition-log", "required": false, "capabilities": ["model.shadow.review.challenge-transition-log-global-consistency.actual"]},
    {"id": "independent-external-retention", "required": false, "capabilities": ["model.shadow.review.challenge-transition-external-retention.actual"]},
    {"id": "protected-monotonic-state", "required": false, "capabilities": ["model.shadow.review.challenge-transition-monotonic-state.actual"]},
    {"id": "externally-trusted-time", "required": false, "capabilities": ["model.shadow.review.challenge-transition-time.external"]},
    {"id": "independent-real-world-controllers", "required": false, "capabilities": ["model.shadow.review.challenge-separation-controller-identity-independence.actual"]},
    {"id": "global-single-use", "required": false, "capabilities": ["model.shadow.review.challenge-global-single-use.actual"]},
    {"id": "actual-human-review", "required": false, "capabilities": ["model.shadow.challenger.human-review.actual"]},
    {"id": "provider-execution-and-evaluation", "required": false, "capabilities": ["model.shadow.challenger.shadow-execution.actual", "model.shadow.challenger.evaluation.actual"]},
    {"id": "held-out-human-benefit", "required": false, "capabilities": ["model.shadow.challenger.human-benefit.held-out"]}
  ]
}

