{
  "requirements": [
    {"id": "upstream-v0.8-entry-package-rebuild", "required": true, "capabilities": ["model.shadow.review.challenge-transition-caller-package-persisted-exact-rebuild"]},
    {"id": "complete-presentation-exact-rebuild", "required": true, "capabilities": ["model.shadow.review.challenge-transition-presentation-exact-rebuild"]},
    {"id": "presentation-data-minimization", "required": true, "capabilities": ["model.shadow.review.challenge-transition-presentation-data-minimization"]},
    {"id": "history-exact-replay-classification", "required": true, "capabilities": ["model.shadow.review.challenge-transition-history-exact-replay-classification"]},
    {"id": "history-prefix-classification", "required": true, "capabilities": ["model.shadow.review.challenge-transition-history-prefix-classification"]},
    {"id": "history-sibling-fork-detection", "required": true, "capabilities": ["model.shadow.review.challenge-transition-history-sibling-fork-detection"]},
    {"id": "history-entry-id-equivocation-detection", "required": true, "capabilities": ["model.shadow.review.challenge-transition-history-entry-id-equivocation-detection"]},
    {"id": "history-alternate-record-detection", "required": true, "capabilities": ["model.shadow.review.challenge-transition-history-alternate-record-detection"]},
    {"id": "history-domain-drift-detection", "required": true, "capabilities": ["model.shadow.review.challenge-transition-history-domain-drift-detection"]},
    {"id": "withheld-root-counterexample", "required": true, "capabilities": ["model.shadow.review.challenge-transition-history-withheld-root-nonexclusion-proof"]},
    {"id": "presentation-resource-bounds", "required": true, "capabilities": ["model.shadow.review.challenge-transition-presentation-resource-bounds"]},
    {"id": "reconciliation-receipt-exact-rebuild", "required": true, "capabilities": ["model.shadow.review.challenge-transition-reconciliation-exact-rebuild"]},
    {"id": "fresh-process-reconciliation", "required": true, "capabilities": ["model.shadow.review.challenge-transition-reconciliation-fresh-process"]},
    {"id": "reconciliation-authority-boundary", "required": true, "capabilities": ["model.shadow.review.challenge-transition-reconciliation-authority-none"]},
    {"id": "authenticated-host-entrypoint", "required": false, "capabilities": ["model.shadow.review.challenge-transition-host-authorization.actual"]},
    {"id": "globally-consistent-transition-log", "required": false, "capabilities": ["model.shadow.review.challenge-transition-log-global-consistency.actual"]},
    {"id": "compelled-multi-party-disclosure", "required": false, "capabilities": ["model.shadow.review.challenge-transition-all-roots-disclosure.actual"]},
    {"id": "independent-external-retention", "required": false, "capabilities": ["model.shadow.review.challenge-transition-external-retention.actual"]},
    {"id": "protected-monotonic-state", "required": false, "capabilities": ["model.shadow.review.challenge-transition-monotonic-state.actual"]},
    {"id": "externally-trusted-time", "required": false, "capabilities": ["model.shadow.review.challenge-transition-time.external"]},
    {"id": "independent-real-world-controllers", "required": false, "capabilities": ["model.shadow.review.challenge-separation-controller-identity-independence.actual"]},
    {"id": "actual-human-review", "required": false, "capabilities": ["model.shadow.challenger.human-review.actual"]},
    {"id": "provider-execution-and-evaluation", "required": false, "capabilities": ["model.shadow.challenger.shadow-execution.actual", "model.shadow.challenger.evaluation.actual"]},
    {"id": "held-out-benefit-and-learning", "required": false, "capabilities": ["model.shadow.challenger.human-benefit.held-out", "model.shadow.challenger.learning.held-out"]}
  ]
}

