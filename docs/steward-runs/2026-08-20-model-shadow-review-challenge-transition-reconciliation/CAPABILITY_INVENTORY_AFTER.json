{
  "capabilities": [
    {"id": "model.shadow.review.challenge-transition-caller-package-persisted-exact-rebuild", "status": "available", "constraints": ["v0.9 reuses v0.8 pure builders to exact-rebuild every presented entry and its complete upstream transition package in memory."]},
    {"id": "model.shadow.review.challenge-transition-presentation-exact-rebuild", "status": "available", "constraints": ["A presentation exact-rebuilds its manifest, every ordered entry, each upstream signature-bearing package, previous-entry references, and derived head."]},
    {"id": "model.shadow.review.challenge-transition-presentation-data-minimization", "status": "available", "constraints": ["Presentation receipts retain bounded references only; transient caller packages, raw public keys, raw signatures, private context, model output, and machine paths are not embedded."]},
    {"id": "model.shadow.review.challenge-transition-history-exact-replay-classification", "status": "available", "constraints": ["Two complete presented histories with identical entry references and counts are classified as exact replay without excluding withheld roots."]},
    {"id": "model.shadow.review.challenge-transition-history-prefix-classification", "status": "available", "constraints": ["Either complete presented history can be classified as the exact prefix of the other without granting adoption authority or total-order status."]},
    {"id": "model.shadow.review.challenge-transition-history-sibling-fork-detection", "status": "available", "constraints": ["Different candidate heads after the exact common prefix produce a typed sibling-fork contradiction at the exact presented sequence."]},
    {"id": "model.shadow.review.challenge-transition-history-entry-id-equivocation-detection", "status": "available", "constraints": ["The same local entry id at one presented sequence with different entry digest and candidate head produces typed equivocation."]},
    {"id": "model.shadow.review.challenge-transition-history-alternate-record-detection", "status": "available", "constraints": ["Different exact local entry records leading to the same candidate head produce a distinct typed hold."]},
    {"id": "model.shadow.review.challenge-transition-history-domain-drift-detection", "status": "available", "constraints": ["Different caller log ids or different exact genesis references produce typed domain holds without authenticating either domain."]},
    {"id": "model.shadow.review.challenge-transition-history-withheld-root-nonexclusion-proof", "status": "available", "constraints": ["A third independently valid history exact-rebuilds but is withheld; its head is absent from the pairwise fork receipt and unpresented histories remain explicitly unexcluded."]},
    {"id": "model.shadow.review.challenge-transition-presentation-resource-bounds", "status": "available", "constraints": ["Each presentation is limited to 4096 entries and 16 MiB of canonical UTF-8 input before entry rebuild."]},
    {"id": "model.shadow.review.challenge-transition-reconciliation-exact-rebuild", "status": "available", "constraints": ["Reconciliation content and digest rebuild deterministically from both complete presentation inputs and receipts plus caller reconciliation id and time."]},
    {"id": "model.shadow.review.challenge-transition-reconciliation-fresh-process", "status": "available", "constraints": ["A fresh Node process rebuilds both complete serialized presentations and the exact reconciliation receipt."]},
    {"id": "model.shadow.review.challenge-transition-reconciliation-authority-none", "status": "available", "constraints": ["Receipts grant no authenticated host, log, identity, human, provider, execution, adoption, installation, promotion, merge, Foundation, or CANON authority."]},
    {"id": "model.shadow.review.challenge-transition-host-authorization.actual", "status": "unknown", "constraints": ["The read-only library call authenticates no host or trusted entrypoint."]},
    {"id": "model.shadow.review.challenge-transition-log-global-consistency.actual", "status": "unknown", "constraints": ["Pairwise replay, prefix, and fork classifications do not establish consensus, total ordering, or absence of other histories."]},
    {"id": "model.shadow.review.challenge-transition-all-roots-disclosure.actual", "status": "unknown", "constraints": ["The withheld-third-history counterexample proves the leaf cannot compel disclosure or enumerate all roots."]},
    {"id": "model.shadow.review.challenge-transition-external-retention.actual", "status": "unknown", "constraints": ["Fresh-process reload uses a caller-retained package; no independent receiver or retention authority is proven."]},
    {"id": "model.shadow.review.challenge-transition-monotonic-state.actual", "status": "unknown", "constraints": ["Read-only comparison creates no protected monotonic state and prevents no deletion or rollback."]},
    {"id": "model.shadow.review.challenge-transition-time.external", "status": "unknown", "constraints": ["Ordering checks use caller timestamps; no external trusted-time receipt was supplied."]},
    {"id": "model.shadow.review.challenge-separation-controller-identity-independence.actual", "status": "unknown", "constraints": ["Synthetic keys and declared digests do not authenticate independent real-world controllers."]},
    {"id": "model.shadow.challenger.human-review.actual", "status": "unknown", "constraints": ["Only synthetic fixtures were used; no real person reviewed a reconciliation."]},
    {"id": "model.shadow.challenger.shadow-execution.actual", "status": "unknown", "constraints": ["No model provider or experiment was invoked."]},
    {"id": "model.shadow.challenger.evaluation.actual", "status": "unknown", "constraints": ["No current baseline/challenger evaluation was executed or supplied."]},
    {"id": "model.shadow.challenger.human-benefit.held-out", "status": "unknown", "constraints": ["No voluntary held-out human outcome was observed."]},
    {"id": "model.shadow.challenger.learning.held-out", "status": "unknown", "constraints": ["No held-out learning evaluation was run."]}
  ]
}
