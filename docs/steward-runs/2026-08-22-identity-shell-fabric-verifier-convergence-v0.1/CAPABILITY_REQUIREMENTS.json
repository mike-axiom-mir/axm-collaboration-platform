{
  "requirements": [
    {"id": "primary-manifest-semantic-gate", "capabilities": ["identity.shell.manifest.verify.primary"], "required": true},
    {"id": "independent-manifest-semantic-gate", "capabilities": ["identity.shell.manifest.verify.independent"], "required": true},
    {"id": "schema-contract-topology-check", "capabilities": ["identity.shell.schema.topology.verify"], "required": true},
    {"id": "resigned-adversarial-corpus", "capabilities": ["identity.shell.adversarial.resign.reject"], "required": true},
    {"id": "portable-boundary-replay", "capabilities": ["identity.shell.export-import.verify"], "required": true},
    {"id": "workshop-regression-checkpoint", "capabilities": ["workshop.regression.verify"], "required": true},
    {"id": "future-live-host-binding", "capabilities": ["identity.shell.host.bind"], "required": false},
    {"id": "future-human-authentication", "capabilities": ["identity.shell.human.authenticate"], "required": false},
    {"id": "future-runtime-continuity", "capabilities": ["identity.shell.runtime.continuity.observe"], "required": false}
  ]
}
