{
  "capabilities": [
    {"id": "identity.shell.manifest.verify.primary", "status": "available", "constraints": ["static inert manifest verification only"]},
    {"id": "identity.shell.manifest.verify.independent", "status": "available", "constraints": ["does not import the compiler", "static inert manifest verification only"]},
    {"id": "identity.shell.schema.topology.verify", "status": "available", "constraints": ["JSON parsing, local reference topology, and declared hardening clauses; not a general-purpose JSON Schema engine"]},
    {"id": "identity.shell.adversarial.resign.reject", "status": "available", "constraints": ["deterministic held-out corpus of 26 JSON-safe re-signed mutations"]},
    {"id": "identity.shell.export-import.verify", "status": "available", "constraints": ["canonical inert manifest bytes only"]},
    {"id": "workshop.regression.verify", "status": "available", "constraints": ["ten repository-required Node checks", "browser UI excluded"]},
    {"id": "identity.shell.host.bind", "status": "unavailable", "constraints": ["held future surface"]},
    {"id": "identity.shell.human.authenticate", "status": "unavailable", "constraints": ["external authenticated human gate required"]},
    {"id": "identity.shell.runtime.continuity.observe", "status": "unavailable", "constraints": ["no runtime host or persistence substrate in v0.1"]}
  ]
}
