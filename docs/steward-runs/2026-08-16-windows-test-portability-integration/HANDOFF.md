# Windows test portability integration handoff

Status: **TEST**  
Purpose: integration evidence only  
Base: remote `main` at `7147b97f4852401b118a50422d4d58cdb92c9923`  
Branch: `codex/windows-test-portability-integration-v0.1`

This branch combines two independently reviewable portability repairs. It is not CANON, has not been pushed, and changed no live Workshop or GitHub state.

## Integrated commits

1. `fe4dd8ca54f89765215448545360cd1405211116` — Command Center source-inspection newline normalization.
2. `4aae8b5c` — integration cherry-pick of observatory digest commit `064eaf426804c9cbdbe0f7fd950f3385c4d069cd`.

The source branches remain separate:

- `codex/command-center-newline-portability-v0.1`
- `codex/observatory-digest-cross-platform-v0.1`

## Integrated evidence

- PASS: Observatory Heartbeat deck — 230 adapter checks and all 18 reviewed modules executed
- PASS: Workshop Command Center selftest
- PASS: both repairs when reached through aggregate `npm test`
- PASS: Command Center discovery seam — 10 checks
- PASS: integration rerun of all ten repository-required commands
- PASS: HTML script syntax — 55 pages, 0 failures

The verifier remained `VERIFIED_WITH_LIMITS` with the existing warning backlog. Passing the mandatory checks does not erase that limitation.

## Aggregate result

`npm test` advanced beyond both repaired Windows boundaries and then stopped in:

```text
shared/services/intake-2026-07-14-seam-review.js
AXM intake seam/stance review: 23/25 PASS
Stance integrity: PARTIAL
```

The two failing checks were:

1. Accidental Symmetry museum record provenance.
2. LUX-5 vendored Three.js license provenance.

Both failures are exactly explained by Git's Windows checkout newline transform:

| Artifact | Raw Windows SHA-256 | Git/LF and expected SHA-256 |
|---|---|---|
| `museum/records/2026-07-14-accidental-symmetry.txt` | `e640afc2a0cbdff455d5a36ff76c6288158cd7dc8b2a8f04aa7c5b8b7caaafe4` | `8c04b7bd9511af9e1a937d3c9a4229c8d142aa1f0abe7efa22800a54f48884b3` |
| `tools/game-hub/game-library/007-casino/lux-5-prototype/client/vendor/LICENSE` | `a70d6dc2d2a6464ee9ee503b1881d5aa884abdec63b4c7064f0c5ad827a7d2b4` | `852e0e8699169bf9f6fdc6bda3e682d078dcbc738b5d33e74df594721bff271d` |

Git reports both files as `i/lf w/crlf`. For each file, the repository blob digest and the CRLF-to-LF canonical digest equal the stored provenance digest exactly. No substantive artifact drift was observed.

The current live checkout happens to retain LF working-tree bytes for both artifacts, so its raw hashes already match the stored provenance values. However, `git check-attr` reports no explicit `text` or `eol` policy for either path, while the fresh isolated checkout produced CRLF. The raw-byte result therefore depends on checkout state and configuration; the live LF copies are evidence of the intended bytes, not evidence that remote main enforces them portably.

## Next contract decision

The next repair must preserve the meaning of provenance rather than merely make the test green. Review and choose one explicit text-artifact contract:

- keep raw-byte identity and enforce LF checkout for the exact provenance artifacts through narrow `.gitattributes` rules; or
- define and label a canonical Git-text digest contract in the stance review, preserving lone-CR and all non-newline bytes.

Do not silently call a canonical-text digest an exact raw working-tree byte digest.

## Cleanup and boundaries

Tracked registry, index, and animation outputs generated by `npm test` were restored to their exact pre-test state. The integration branch was clean before this receipt was added.

Not run:

- GitHub Actions on this unpushed integration branch
- Linux or macOS runtime execution
- browser render/click testing; neither repair changes browser runtime source

Mike remains the review and merge gate. This receipt records evidence and the next unresolved contract; it grants no promotion or canon authority.
