{
  "capabilities": [
    {"id": "game.content.exact-byte-source", "status": "available", "constraints": ["The v0.2 content and v0.2.1 package manifest are bound to canonical UTF-8/LF bytes and verified in a fresh Windows checkout."]},
    {"id": "game.trailer.plan.deterministic", "status": "available", "constraints": ["Two identical requests produce the same request and plan digests; 13 adversarial planner cases pass."]},
    {"id": "game.trailer.claims.evidence-bound", "status": "available", "constraints": ["All eight claims cite the exact content or manifest reference and marketing inflation fails closed."]},
    {"id": "game.trailer.root-order.preserved", "status": "available", "constraints": ["Truth, Agency/non-domination, Continuity, and Wisdom-over-speed are exact and ordered; HOLD, FAIL, reorder, or missing evidence is rejected."]},
    {"id": "game.trailer.rgba-frame-renderer", "status": "available", "constraints": ["A bounded native adapter emits exactly 48 unique 640x360 RGBA frames and 360 sparse references."]},
    {"id": "video.native-vips-runtime.observed", "status": "available", "constraints": ["The optional resolver now falls back to the pinned Workshop wasm-vips Node runtime; direct encoding and decode report libvips 8.18.3."]},
    {"id": "video.native-mp4-motion-jpeg", "status": "available", "constraints": ["A real 10,835,439-byte Motion-JPEG MP4 was built locally and its ISO BMFF structure passed the native inspector."]},
    {"id": "video.native-webm-vp8", "status": "available", "constraints": ["A real 4,605,018-byte VP8 all-keyframe WebM was built locally and its EBML structure passed the native inspector."]},
    {"id": "video.sparse-sequence.bounded", "status": "available", "constraints": ["The render uses 48 unique frames, 360 samples, 44,236,800 raw unique-frame bytes, 30 seconds, zero children, and zero network."]},
    {"id": "game.trailer.controls.explained", "status": "available", "constraints": ["The visible tutorial and captions state WASD/arrows and E/Space exactly as the game manifest does."]},
    {"id": "game.trailer.proven-counts.advertised", "status": "available", "constraints": ["Five zones, six quests, ten discoveries, and four ordered roots are derived from the exact content release."]},
    {"id": "game.trailer.caption-companion", "status": "available", "constraints": ["Six English WebVTT cues cover the exact 30-second timeline and the browser reports the captions track as showing."]},
    {"id": "game.trailer.limitations.visible", "status": "available", "constraints": ["The player, captions disclosure, receipt, and trailer frames keep TEST, silent, local-only, and public-release HOLD visible."]},
    {"id": "video.container.structure.verify", "status": "available", "constraints": ["Native MP4 sample-table and WebM EBML inspectors passed on the committed bytes."]},
    {"id": "video.first-middle-last.decode", "status": "available", "constraints": ["Both formats decoded first, middle, and last samples through the pinned runtime; three PNG proofs passed structural inspection."]},
    {"id": "video.browser.playback.observe", "status": "available", "constraints": ["The real local WebM loaded at readyState 4 with no error and visibly advanced from 2.69 to 8.03 seconds; a separate narrow 390x844 render had no horizontal overflow."]},
    {"id": "video.duration-and-frame-count.verify", "status": "available", "constraints": ["Codec receipts and live browser both report exactly 30 seconds; container inspectors report 360 frames at 12 fps."]},
    {"id": "video.output.test-only", "status": "available", "constraints": ["The manifest, frames, player, and receipt label the output TEST."]},
    {"id": "video.publish.separate-human-gate", "status": "available", "constraints": ["The planner, renderer, server, and player expose no publish action and publishAuthorized remains false."]},
    {"id": "video.public-rights.hold", "status": "available", "constraints": ["Public distribution and direct reuse remain HOLD pending Mike's decision."]},
    {"id": "video.canon.self-change.denied", "status": "available", "constraints": ["Planner and render receipts carry authority NONE and canonChanged false."]}
  ]
}
