# Current Grounded Growth voluntary-choice frontier

Status: `TEST`

The exact six-chain Grounded Growth current state still carried one older
optional research-handoff candidate. The later human-route coverage proves five
current optional routes and one held route. This lane reconciles those facts
without choosing for a person.

## Current posture

- Five routes are technically available only after an explicit human request.
- One signal-lineage route remains visible and commandless pending its exact
  external event.
- There is no default, priority, ranking, recommendation, preselection, prompt,
  session, or review action.
- Waiting or opting out is allowed without penalty.
- Shared-system and AI-workflow evidence each have six current passes; human
  evidence has zero passes and six `NOT_RUN` claims. Grounded growth for AI and
  humans together is therefore not established.

This is a machine-readable support frontier, not a UI. It creates no new
Grounded Growth outcome, because doing so would create another benefit claim
and route-for-route chain.

## Rebuild and verify

```powershell
node docs/steward-runs/2026-08-20-grounded-growth-voluntary-choice-frontier/build-current-voluntary-choice-frontier.js
node docs/steward-runs/2026-08-20-grounded-growth-voluntary-choice-frontier/selftest.js
node shared/grounded-growth-voluntary-choice-frontier/selftest.js
```

The current frontier is `CURRENT_VOLUNTARY_CHOICE_FRONTIER.json`. Its command
strings remain inert data. No optional human session command was run by this
lane.

## Limits

Native rebuild verifies current source relationships. Detached inspection
checks receipt integrity and neutral-choice boundaries while leaving source
truth/currentness `UNKNOWN`. No human selection, identity authentication,
participation, usefulness, or benefit is claimed. Nothing is installed,
promoted, merged, written into the Foundation, or made `CANON`.
