# Steward Run Receipt — Consent-Bound Recipe Planning v1.2

Status: `TEST`

Date: 2026-08-23  
Base: `bedd499795f7fe7591fad109e52aef50479f82fb`  
Technical commit: `9e48ce29bfe9079852835ec7f86d140f6f3f0aaf`  
Source branch: `codex/code-capability-fabric-consent-bound-recipe-plan-v1.2`

## Outcome

The Fabric can now bind one exact sealed blueprint, installed Code Recipe
Foundry discovery evidence, an exact requester-selected `REFERENCE_ONLY`
packet, explicit one-to-one recipe mappings, grounded consent scope, resource
ceilings, and ordered four-root evidence into one deterministic inert
independent-native-reimplementation plan.

The first evidence fixture is a small game-rule pure function. It maps exact
Foundry `map` and `filter` metadata to two compatible declared blueprint steps.
The planner emits operation outlines only. Implementation is `NOT_STARTED`, the
candidate is `null`, every acceptance check is `NOT_RUN` with `UNKNOWN`
evidence, and the next gate is exact authenticated human decision followed by a
new detached-candidate request.

## Authority and source boundary

- Authority remains `NONE` and lifecycle status remains `TEST`.
- Recipe snippets are neither emitted, copied, applied, executed, nor
  reuse-authorized.
- The adapter loads no provider, uses no network, spawns no process, reads no
  target workspace, writes nothing, and inherits no environment or host clock.
- It cannot generate or materialize a candidate, run tests, install, integrate,
  promote, publish, learn, alter Foundation, or change `CANON`.
- Deterministic consent validation is not represented as authentication,
  natural-person identity, or understanding.
- Mike Tobi remains the integration and merge gate.

## Changed paths

- `shared/code-capability-fabric/code-recipe-application-planner-v1.js`
- `shared/code-capability-fabric/code-recipe-application-request.schema.json`
- `shared/code-capability-fabric/code-recipe-application-plan.schema.json`
- `shared/code-capability-fabric/module-code-recipe-application-planner-v1.contract.json`
- `shared/code-capability-fabric/selftest-code-recipe-application-planner-v1.js`
- `shared/code-capability-fabric/README-code-recipe-application-planner-v1.md`
- `shared/code-capability-fabric/README.md`
- this append-only steward-run directory

No Code Recipe Foundry catalog, Code Mirror, RepairBuddy, Foundation, game,
candidate materializer, or `CANON` source path changed.

## Verification

- Focused v1.2 application planner: 87 checks passed.
- Complete Fabric continuity: all 25 selftest scripts passed.
- Code Recipe Foundry: 61 checks passed.
- Code Recipe Foundry discovery seam: passed.
- All ten commands required by `AGENTS.md`: passed.
- `verify.js`: exit 0 with 22 warning lines.
- JSON parsing, JavaScript syntax checks, and `git diff --check`: passed.
- Browser render/click test: not run and N/A; no visual surface changed.
- Experimental or supplied runtimes: not executed.

The 22 `verify.js` warnings remain visible as the clean integration-target
baseline. They include physical-phone QA reminders, promotion claims requiring
reverification, and a stale tool index. This bounded run did not rewrite those
unrelated Workshop surfaces.

## Capability decision and open seams

The required plan-only route is `READY`. The later detached game-rule
candidate route remains `OPTIONAL_GAP` because authenticated human-decision and
arbitrary native-candidate materialization contracts are not available for this
general route. The existing narrower candidate materializer was not bypassed.

The next technical review should specify those two hands independently. It must
preserve exact candidate bytes, consent tier, nonce/revocation/replay evidence,
source rights, disposable output boundaries, and the separation between
candidate creation and installation.

## Evidence retention

Six durable events are retained in `events.jsonl`. The structural seal is
`events.seal.json` with SHA-256
`ca00231bbc9d8955a4bbc02d62f4eab4d90f72e18cd0b1549969ef307358f0c7`.
Raw command logs, recipe source, private content, stdout/stderr payloads, and
machine paths were not committed as durable evidence.

Passing these gates does not make the adapter `CANON` and does not authorize
integration into a busy canonical checkout.
