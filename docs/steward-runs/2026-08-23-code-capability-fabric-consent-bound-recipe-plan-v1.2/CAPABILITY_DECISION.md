# Capability Decision — Consent-Bound Recipe Planning v1.2

Status: `TEST`

The required v1.2 route is `READY`: the Fabric can verify exact installed
recipe discovery, exact `REFERENCE_ONLY` selection, an exact sealed blueprint,
deterministic consent scope, and requester-declared one-to-one mappings before
emitting an inert native reimplementation plan.

The requested later route to a detached game-rule candidate remains an
`OPTIONAL_GAP`. Two capabilities are intentionally absent:

- an authenticated human-decision record; and
- an arbitrary native-candidate materializer with a reviewed bounded contract.

The planner does not silently substitute machine inference for the requester
mapping, claim that deterministic consent evaluation authenticates Mike, or
bypass the existing narrow reviewed materializer. Its next gate is therefore
`AUTHENTICATE_EXACT_HUMAN_DECISION_THEN_REQUEST_ONE_DETACHED_CANDIDATE`.

This is a truthful degraded result, not a failed v1.2 milestone: planning is
available, while candidate creation remains visibly unavailable until a later
reviewed hand supplies both missing contracts.
