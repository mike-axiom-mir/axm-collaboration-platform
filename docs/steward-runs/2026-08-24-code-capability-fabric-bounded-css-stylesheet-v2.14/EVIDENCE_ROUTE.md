# Evidence route — bounded CSS token stylesheet v2.14

Status: `TEST`

This rung closes one exact making gap. It does not claim general CSS generation,
browser correctness, visual quality, installation readiness, or `CANON` status.

| Claim | Native proof surface | Current verdict |
| --- | --- | --- |
| The prior Fabric lacked one bounded CSS source renderer and exact CSS specialist build profile. | Normalized before-inventory comparator report. | `BLOCKED` before this rung. |
| Identical exact requests rebuild byte-identical detached candidates. | Specialist capability builder selftest and Capability Fabric shared selftest. | `PASS` when checks pass. |
| CSS language, style-presentation Organ, profile, recipe, and builder bytes are exact. | Build-profile registry selftest plus candidate lineage verification. | `PASS` when checks pass. |
| Generated text is syntactically valid and structurally limited to typed `:root` custom properties. | Static generated-source checks; trusted package harness syntax/selftest in a disjoint temporary copy. | Separate static and trusted-host verdicts recorded in the receipt. |
| Fabric did not execute, install, integrate, publish, promote, grant permissions, use network, or change `CANON`. | Candidate receipts, resource observation, authority objects, and module contracts. | Must remain false. |
| CSS cascade, browser compatibility, accessibility effect, motion timing, and visual quality work. | Live browser render and bounded interaction journey. | `UNKNOWN`; browser test not run in this rung. |
| Direct reuse/public copying rights are resolved. | Mike decision plus rights evidence. | `HOLD`; research-only/direct-reuse hold remains. |

The trusted package harness may execute the emitted selftest only in its own
temporary execution copy, disjoint from the detached Nursery source. That result
does not turn live browser or visual claims into `PASS` and grants no lifecycle
authority.
