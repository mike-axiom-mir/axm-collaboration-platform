# Steward receipt — bounded CSS token stylesheet v2.14

Status: `TEST`

This append-only receipt seals one bounded growth rung. It does not install,
integrate, promote, merge, or canonize the result.

## Lineage and review

- Branch: `codex/code-capability-fabric-bounded-css-stylesheet-v2.14`
- Technical source commit:
  `420bdc3dbcd159d6445b8cfea7c963ce729c5e38`
- Exact parent:
  `b9955f285ad7500a8693267bff64463b8e3248e7`
- Stacked target: `codex/code-capability-fabric-evidence-bound-lessons-v2.13`
- Draft review: <https://github.com/mike-axiom-mir/axm-collaboration-platform/pull/64>
- Target review: <https://github.com/mike-axiom-mir/axm-collaboration-platform/pull/63>

Before publication, both the remote v2.13 branch and PR 63 head were re-read and
matched the exact parent. The canonical Workshop checkout was clean and remained
untouched. Publication used a separate clean, attached review worktree.

## What changed

The Fabric can now deterministically plan and emit one detached JavaScript
renderer candidate for a fixed CSS `:root` design-token stylesheet. The closed
input vocabulary contains only `COLOR_HEX`, `LENGTH_PX`, `INTEGER`, `PERCENT`,
and `TIME_MS` tokens. Token names and values are validated, token order is
stable, and input/output byte ceilings are explicit.

The rung also adds one exact CSS style-presentation specialist profile, binds the
CSS language Organ and specialist bytes into candidate lineage, synchronizes the
request/candidate schema versions with builder version 2.1.0, extends the module
contract, regenerates official Workshop indexes, and preserves before/after
capability-gap evidence.

The source-reviewed builder digest is
`sha256:1a9fa4c3a0ce626fa1adfe709dd3bbea7aa91e867c20794e9213a6e5da82d163`.
The recipe digest is
`sha256:b0663c70aed5a9c59ef35edf52ae5165b9f09e927ec837506fdcb00114eb4623`.
The exact specialist digest is
`sha256:18cdb0ce1f613185e5071d7ebfdfab8f6a7ca02740f0aac85dca1417810ccab6`.

## Authority and resource boundary

The emitted candidate remains `EXPERIMENTAL`, detached, uninstalled, and
unexecuted by the Fabric. It has no provider, network, filesystem, environment,
clock, randomness, installation, integration, publication, promotion, merge, or
`CANON` authority. Arbitrary selectors, properties, declarations, at-rules,
imports, URLs, comments, escapes, and raw CSS input are outside this hand.

The trusted package harness executed emitted fixture selftests only in disjoint
temporary copies. This is recorded separately from the Fabric/Nursery behavior
and does not authorize a general executor.

## Four-root evaluation

- Truth: `PASS` for the bounded deterministic/static claims proved by the listed
  tests. Live cascade, compatibility, accessibility, motion, and visual quality
  remain `UNKNOWN`.
- Agency / non-domination: `PASS`; generation grants no lifecycle authority and
  cannot approve, install, promote, merge, or canonize itself.
- Continuity: `PASS`; the exact v2.13 parent, recipe/profile/builder bytes, City
  projections, candidate lineage, and deterministic rebuilds are bound and
  checked.
- Wisdom over speed: `PASS`; the rung adds one closed, typed CSS surface rather
  than an arbitrary stylesheet generator or unauthorized executor.

These root results make the packet reviewable. They do not replace Mike's final
hold, rejection, or merge decision.

## Verification and warnings

All ten required `AGENTS.md` checks passed. Focused results include 257 builder
assertions, 62 profile assertions, 131 shared Fabric checks, 15 tool checks, 27
admission checks, 114 package checks, and 51/51 recursive Fabric entry points.
`verify.js` ended at `0 FAIL · 25 warn · spine b618c5762240070c`. The 25 visible
warnings are inherited and are not hidden or reclassified. Exact results,
limits, and unrun surfaces are in `TEST_REPORT.md`.

## Holds and decisions still owned by Mike

- Direct reuse or public copying of research-derived/generated source remains
  `HOLD` until rights are evidenced.
- A repaired disposable general executor remains unauthorized.
- Browser and visual proof must be added before any live CSS behavior claim.
- Installation, integration, publication, promotion, merge, and `CANON` remain
  separate decisions.

The exact no-surprise review and possible later fast-forward route is recorded
in `INTEGRATION_HANDOFF.md`. Target drift must be checked again at decision time.
