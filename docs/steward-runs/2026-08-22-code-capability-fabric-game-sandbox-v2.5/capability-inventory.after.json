{
  "capabilities": [
    {"id": "game.brief.typed", "status": "available", "constraints": ["v1 is one exact 16 by 10, single-player, session-only native recipe."]},
    {"id": "game.candidate.generate.deterministic", "status": "available", "constraints": ["Native recipe only; AI off; identical sealed input yields byte-identical packet."]},
    {"id": "game.project.game-forge-compatible", "status": "available", "constraints": ["axm.game-forge-project/v1 with 160 supported terrain cells and typed root checkpoint system."]},
    {"id": "sandbox.source-output-evidence.disjoint", "status": "available", "constraints": ["Four ordinary direct-child roots; junction, symlink, traversal, alias, and overlap checks fail closed."]},
    {"id": "sandbox.candidate.build.allowlisted", "status": "available", "constraints": ["Exact deterministic static-game packet only; not a general executor."]},
    {"id": "sandbox.network.default-deny", "status": "available", "constraints": ["Static scanning plus candidate CSP connect-src none; trusted host binds 127.0.0.1 only."]},
    {"id": "sandbox.host-authority.not-inherited", "status": "available", "constraints": ["Candidate receives zero processes, no environment, no credentials, no filesystem API, and no command seat."]},
    {"id": "sandbox.resource.bytes-enforced", "status": "available", "constraints": ["Input, file, file-count, total-output, iteration, process, and cost ceilings are enforced."]},
    {"id": "sandbox.candidate.repair.immutable-iterations", "status": "available", "constraints": ["Allowlisted file replacement only; exact base and old-byte digests; prior iterations remain unchanged."]},
    {"id": "sandbox.lesson.candidate.privacy-safe", "status": "available", "constraints": ["Typed inactive lesson candidate only; no source, prompt, streams, private content, or machine paths."]},
    {"id": "game.runtime.browser", "status": "available", "constraints": ["Trusted fixed-file loopback preview inside sandbox=allow-scripts iframe; exact first candidate only."]},
    {"id": "visual.capture.live", "status": "available", "constraints": ["In-app browser; five selected frames observed and hashed, raw images not durably retained."]},
    {"id": "game.play.journey", "status": "available", "constraints": ["One exact player journey reached four ordered roots and visible victory at move 20."]},
    {"id": "game.accessibility.keyboard", "status": "available", "constraints": ["Arrow keyboard and visible-button routes tested; semantic labels, live status, visible focus, and 600 by 800 layout observed."]},
    {"id": "game.install.review-packet", "status": "available", "constraints": ["Byte-bound packet, installation-gap record, static evidence, TEST banner, and review preview; not an install adapter."]},
    {"id": "game.install.human-gate", "status": "available", "constraints": ["Install, integration, publication, promotion, and CANON are false; Mike remains the decision gate."]},
    {"id": "game.persistence.restart", "status": "degraded", "constraints": ["Reload reset was proven; durable save/recovery is intentionally unsupported in this first candidate."]}
  ]
}
