{
  "capabilities": [
    {"id": "game.brief.typed", "status": "degraded", "constraints": ["Game Forge has a typed project document, but no exact deterministic generation brief."]},
    {"id": "game.candidate.generate.deterministic", "status": "unavailable"},
    {"id": "game.project.game-forge-compatible", "status": "available", "constraints": ["axm.game-forge-project/v1 with a 16 by 10 semantic world."]},
    {"id": "sandbox.source-output-evidence.disjoint", "status": "unavailable"},
    {"id": "sandbox.candidate.build.allowlisted", "status": "unavailable"},
    {"id": "sandbox.network.default-deny", "status": "degraded", "constraints": ["Existing Sandbox host is loopback-only but does not execute candidate previews."]},
    {"id": "sandbox.host-authority.not-inherited", "status": "unavailable"},
    {"id": "sandbox.resource.bytes-enforced", "status": "degraded", "constraints": ["Existing request bytes are bounded; candidate build bytes are not yet modeled."]},
    {"id": "sandbox.candidate.repair.immutable-iterations", "status": "unavailable"},
    {"id": "sandbox.lesson.candidate.privacy-safe", "status": "unavailable"},
    {"id": "game.runtime.browser", "status": "degraded", "constraints": ["Workshop has game runtimes, but no exact generated-game sandbox route."]},
    {"id": "visual.capture.live", "status": "available", "constraints": ["In-app browser only."]},
    {"id": "game.play.journey", "status": "available", "constraints": ["Bounded browser input and visible outcome only."]},
    {"id": "game.accessibility.keyboard", "status": "degraded", "constraints": ["Verifier exists; generated candidate behavior is unknown."]},
    {"id": "game.install.review-packet", "status": "degraded", "constraints": ["Review Inbox exists; no generated-game packet is bound to it."]},
    {"id": "game.install.human-gate", "status": "available", "constraints": ["Mike remains the final installation and merge gate."]},
    {"id": "game.persistence.restart", "status": "degraded", "constraints": ["Save/replay service exists; first generated game is intentionally session-only."]}
  ]
}

