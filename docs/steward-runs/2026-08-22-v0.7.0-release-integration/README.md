# v0.7.0 experimental integration evidence

This folder records the compact public evidence for the large v0.7.0
integration checkpoint. It preserves runnable products, tests, receipts, and
known holds without retaining 126 raw session-event traces.

The full required Workshop command set and the comprehensive npm suite passed.
Live browser verification covered desktop Simple and Advanced modes, the
Workshop Observatory, and a 390 x 844 mobile viewport. The Hub begins at the top
of the viewport with no horizontal overflow; this is separate evidence from
script compilation and HTTP smoke checks.

The candidate remains `EXPERIMENTAL`. Forty-three known RepairBuddy warnings,
the Sensorium adapter hold, and physical-phone checks remain visible. Mike Tobi
retains merge and CANON authority.
