{
  "capabilities": [
    {"id": "candidate.nursery.exact-bundle-parity", "status": "available", "constraints": ["structural inspection only", "does not prove runtime usefulness"]},
    {"id": "candidate.authority.detached", "status": "available", "constraints": ["all authority flags false"]},
    {"id": "organ.input.affected-surfaces-sensitive", "status": "unavailable", "constraints": ["affectedSurfaces is guarded but not read by the selected graph"]},
    {"id": "fabric.gate.required-input-usage", "status": "unavailable", "constraints": ["current capability-coverage gate checks port presence, not graph use"]},
    {"id": "verification.route.token-registry", "status": "unavailable", "constraints": ["route strings have no exact versioned registry in the candidate"]},
    {"id": "verification.route.native-surface-binding", "status": "unavailable", "constraints": ["route strings are not bound to Verification Spine claim IDs or Evidence Desk proof surfaces"]},
    {"id": "organ.host.read-only-plan-action", "status": "unavailable", "constraints": ["candidate remains detached and exposes no trusted host adapter"]},
    {"id": "evidence-desk.route-prefill-adapter", "status": "unavailable", "constraints": ["Evidence Desk accepts typed evidence fields, not this candidate output"]},
    {"id": "organ.held-out.surface-differentiation", "status": "unavailable", "constraints": ["held-out fixtures do not compare different surface sets"]},
    {"id": "organ.metamorphic.surface-sensitivity", "status": "unavailable", "constraints": ["trusted-runtime probe produced identical outputs for three distinct surface sets"]}
  ]
}
