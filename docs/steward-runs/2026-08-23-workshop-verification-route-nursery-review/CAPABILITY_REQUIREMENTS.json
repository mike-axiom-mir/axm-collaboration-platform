{
  "requirements": [
    {"id": "detached-structure", "capabilities": ["candidate.nursery.exact-bundle-parity", "candidate.authority.detached"], "required": true},
    {"id": "declared-input-semantics", "capabilities": ["organ.input.affected-surfaces-sensitive", "fabric.gate.required-input-usage"], "required": true},
    {"id": "verification-route-resolution", "capabilities": ["verification.route.token-registry", "verification.route.native-surface-binding"], "required": true},
    {"id": "bounded-host-integration", "capabilities": ["organ.host.read-only-plan-action", "evidence-desk.route-prefill-adapter"], "required": true},
    {"id": "repair-proof", "capabilities": ["organ.held-out.surface-differentiation", "organ.metamorphic.surface-sensitivity"], "required": true}
  ]
}
