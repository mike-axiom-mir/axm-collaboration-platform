# Test report

Status: `TEST`

## Focused v1.5

Command:

```powershell
node shared/code-capability-fabric/selftest-code-specialization-router-v1.js
```

Result: `PASS` — 143 checks.

Coverage includes catalog/schema/contract closure, 68 language and format
profiles across 17 families, 14 specialist organs, deterministic repeat, exact final-byte
measurement, HTML/CSS/JavaScript/JSON/test routing, exact existing-mask
lineage, Hand Specification Foundry compatibility, empty knowledge lanes,
ambiguous extensions, declaration conflicts, unknown languages/frameworks,
stale/future observations, root HOLD, permissions/network, physical actuation,
digest drift, Windows/UNC/ADS/reserved/case/Unicode aliases, dependency cycles,
resource ceilings, and truth/learning tampering.

## Fabric continuity

Command: every `shared/code-capability-fabric/selftest*.js`, sorted by filename.

Result: `PASS` — 27/27 files, including v1.4's 77-check native game-rule
candidate generator.

## Required `AGENTS.md` checks

All exited `0`:

1. `node verify.js`
2. `node hub/hub-selftest.js`
3. `node hub/route-selftest.js`
4. `node hub/graft-selftest.js`
5. `node hub/skin-selftest.js`
6. `node hub/verify-plus.js`
7. `node tests/html-script-syntax-test.js`
8. `node tests/tool-forge-package-test.js`
9. `node tools/agent-tool-forge/selftest.js`
10. `node tools/evidence-desk/selftest.js`

`verify.js`: `0 FAIL · 22 warn · spine b618c5762240070c`.

The 22 warnings are preserved as the current repository baseline. They were
not repaired or suppressed by this rung.

## Unrun / not applicable

- Browser render/click: `NOT_APPLICABLE`; no visual surface changed.
- Generated candidate execution: `NOT_RUN`; this rung generates no candidate.
- Supplied experimental runtime execution: `NOT_RUN` and still unauthorized.
- Repaired disposable executor: `NOT_BUILT_OR_AUTHORIZED` by this rung.
- Installation, integration, publication, promotion, learning admission,
  physical actuation, and `CANON`: `NOT_RUN`.
