# Open Decisions and Capability Gaps

- Direct reuse rights for generated or uploaded source remain on research-only
  hold until Mike decides otherwise. This rung includes and copies no candidate
  source bytes.
- A repaired disposable executor remains not authorized. This rung is a pure
  intent adapter and provides no execution path.
- The canonical `software-workshop@1.1.0` field pack expresses bounded
  verification-route composition, not implementation semantics for HTML, CSS,
  JavaScript, schemas, tests, shaders, audio, or every future code family.
  Building those requires a separately reviewed implementation-organ field-pack
  contract, fixtures, countertests, resource model, rights state, and evidence.
- Specialist knowledge lanes remain `REFERENCE_ONLY_EMPTY`. Knowledge must not
  be loaded merely because a code family was classified; each future knowledge
  packet needs exact provenance, rights, evidence, expiry, and human scope.
- The canonical target drifted and is busy. Integration must wait for a clean,
  rechecked target commit and Mike's explicit merge decision.

