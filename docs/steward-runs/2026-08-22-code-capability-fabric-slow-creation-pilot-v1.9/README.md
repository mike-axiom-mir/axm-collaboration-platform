# Code Capability Fabric slow-creation pilot v1.9 steward run

Status: `TEST`

This append-only receipt covers implementation commit
`09c6bce30e50d9a960848a3c953c35cedcac6742` on branch
`codex/code-capability-fabric-slow-creation-pilot-v1.9`, based on the verified
v1.8 receipt tip `8ad50f7573d3e58c16f77706c4474cd77745d8bd`.

The run adds a deliberately small creation rung to the provider-neutral Code
Capability Fabric. One exact intent, one grounded-consent scope evaluation, one
external interactive pilot declaration, and one host-selected new root produce
one fixed inert candidate module shell. Candidate bytes are written exclusively,
read back, and structurally inspected by the existing Detached Candidate
Nursery. A path-free receipt binds the declaration, profile, intent, consent
evaluation, exact six-file set, byte lengths, file digests, Nursery digest,
resource observations, authority, and truth ceiling.

The four AXM roots remain the ordered technical gate. Mike remains the later
human review and Git merge gate. The interactive declaration is not presented
as authenticated identity or durable consent: predecision evidence content,
trusted time, live revocation, and replay prevention remain unproved.

The post-hardening trial emitted six files totaling 7,372 bytes. Repeat Nursery
inspection was deterministic and returned `READY_FOR_LATER_INTAKE` with zero
structural errors. That status means candidate data is structurally ready for
later review; it does not prove runtime behavior or quality.

Eight additive `TEST` implementation files were committed. No prior Fabric,
Foundation, Atlas, Machine Host, or Nursery file changed. The pilot did not read
source artifacts, use the network, spawn a child process, execute generated
code, install, register, stage, promote, merge, change `CANON`, activate a
machine-wide default, or admit persistent learning.

Two inert external trial roots were retained. Trial `001` was the development
observation before declaration-lineage and replay truth were hardened. Trial
`002` is the final verified observation represented by
`CREATION_TRIAL_RECEIPT.json`. Neither candidate was opened or executed.

Supplied v0.1/v0.2 experimental runtimes remain unexecuted. Generated-source
public reuse rights and any repaired disposable-sandbox executor remain
deferred to Mike.
