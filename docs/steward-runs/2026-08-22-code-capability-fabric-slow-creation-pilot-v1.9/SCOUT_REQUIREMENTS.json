{
  "schema": "axm.capability-scout-requirements/v1",
  "mission": "Emit and inspect one deterministic inert candidate under explicit bounded authority without execution, installation, promotion, learning, or CANON.",
  "requirements": [
    {"id": "creation.inert-candidate.emit", "required": true, "proof": "exclusive writes into one new direct-child root"},
    {"id": "creation.candidate.readback.verify", "required": true, "proof": "byte-for-byte readback against deterministic expected bytes"},
    {"id": "creation.nursery.inspect", "required": true, "proof": "existing Detached Candidate Nursery returns a deterministic structural record"},
    {"id": "creation.receipt.strict-normalize", "required": true, "proof": "strict path-free receipt normalizer binds exact lineage and truth"},
    {"id": "consent.scope.bind", "required": true, "proof": "verified grounded-consent evaluation binds exact intent and emitter profile"},
    {"id": "authority.creation.ceiling", "required": true, "proof": "external disposable write authority is separated from Fabric-granted authority"},
    {"id": "creation.failure.cleanup", "required": true, "proof": "faulted attempts remove only their validated newly owned root"},
    {"id": "creation.resource.bytes.enforce", "required": true, "proof": "actual candidate bytes are bounded by fixed and consented ceilings"}
  ]
}
