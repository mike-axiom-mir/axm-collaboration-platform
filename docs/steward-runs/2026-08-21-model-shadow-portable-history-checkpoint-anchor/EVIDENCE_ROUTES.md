# Evidence routes

Status: `TEST`

| Claim | Primary evidence | Verifier | Counterevidence / boundary |
|---|---|---|---|
| A caller policy can admit a bounded threshold of detached Ed25519 witness signatures over one exact v2.1 checkpoint | `buildWitnessDetailed`, `normalizeWitnessPolicy`, and `verifyWitnessAttestation` in the v2.2 runtime | Focused tests rebuild the witness, reject wrong signatures, expired policy, insufficient threshold, duplicate fingerprints/principals, and private-key PEM | Policy authority, signer identity, checkpoint origin, trusted time, and external retention remain unproved |
| A second caller policy and expected digest bind the checkpoint, witness, witness policy, anchor policy, and witness-set commitments | `anchorCommitment`, `verifyAnchorAuthorization`, and `buildAnchoredCheckpoint` | Focused tests exact-rebuild the receipt and reject wrong expected digest, wrong authorization, witness substitution, expired policy, and insufficient threshold | Both policies and the expected anchor are caller presented and unauthenticated |
| The verified witness and anchor signature seats have no shared SPKI fingerprint or declared-principal digest | `intersection` refusals and `separationEvidence` in `buildAnchoredCheckpoint` | Focused tests use the same Ed25519 keys across layers and separately reuse declared-principal digests; both are refused | Distinct keys/digests do not prove distinct controllers or exclude collusion; the base passing fixture intentionally generates both layers in one process |
| A later audit composes the unchanged v2.1 relative-history classifier only after exact anchored-checkpoint rebuild | `buildAnchoredAudit` calls `History.auditCheckpoint` after exact rebuild | Focused tests prove exact match, typed absence/hold, and fresh-process exact rebuild | This remains relative to the presented anchored checkpoint; it proves no pin, retention, atomicity, later currentness, or rollback prevention |
| Public receipts are data-minimized and runtime authority is read-only | Receipt constructors, `module.contract.json`, and runtime source | Focused tests scan receipts for PEM, signature, private-key marker, and ledger path; source scan finds no write, network, key-generation, or private-key API | Caller inputs still contain public policies, signatures, and ledger paths where required; the module does not retain them |
| One controller can satisfy both layers with distinct keys/digests | Base focused fixture creates all four keypairs and all declared digests in one process | Truth surface asserts `realWorldControllerIndependenceProven: false` and `sameControllerWithDistinctKeysAndDigestsStillPossible: true` | Observable non-overlap is not controller independence or human participation |
| Joint replacement remains possible | Focused test creates a distinct self-validating checkpoint, new policies, new keys/digests, new signatures, and a new expected anchor; the package passes | Distinct receipt digest and `originalCheckpointContinuityProven: false` are asserted | No authenticated original pin, host authority, protected monotonic store, or independent retention exists |
| Broader grounded-growth authority remains open | `CAPABILITY_GAP_AFTER.json`, contract refusals, receipt truth maps | Evidence selftest requires 18 routes to remain `OPTIONAL_UNKNOWN` | No provider execution, evaluation, learning, human benefit, permission, adoption, install, promotion, merge, or `CANON` evidence |

Browser render/click evidence is not applicable: v2.2 is a pure nonvisual Node.js adapter with no browser route or visual surface.
