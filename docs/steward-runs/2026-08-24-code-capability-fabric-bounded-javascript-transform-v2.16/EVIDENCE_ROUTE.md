# Evidence route — bounded JavaScript record transform specialist v2.16

Status: `TEST`

This rung reuses and hardens the existing native `pure-json-transform` hand,
then routes it through one exact JavaScript application-logic specialist
profile. It does not claim arbitrary JavaScript, browser behavior, UI creation,
package management, workspace mutation, or general code execution.

| Claim | Native proof surface | Required verdict |
| --- | --- | --- |
| The prior Fabric lacked a strict string-record contract, accessor refusal, explicit input/output byte bounds, and an exact JavaScript specialist profile. | Normalized before-inventory comparator report plus direct source/catalog inspection. | `BLOCKED` before this rung. |
| Identical exact requests rebuild byte-identical detached JavaScript candidates. | Code Specialist builder selftest, Capability Fabric selftest, and exact package digest comparison. | `PASS` only when all agree. |
| JavaScript language, application-logic Organ, profile, recipe, and builder bytes are exact. | Build-profile registry selftest plus candidate lineage verification. | `PASS` only for exact digests. |
| Runtime input is a closed own-data-property string record; accessors, symbols, custom prototypes, unsafe fields, non-string values, excessive fields, and excessive bytes fail closed. | Emitted selftest executed only by the trusted package harness from a disjoint temporary copy; adversarial source checks. | `PASS` only for this bounded transform contract. |
| Output is one declared string field with deterministic fallback and byte enforcement; no input reference is copied. | Emitted selftest plus exact output assertions. | `PASS` only for the asserted transform behavior. |
| The Fabric did not execute, install, integrate, publish, promote, grant permissions, use network, or change `CANON`. | Candidate receipts, resource observation, authority objects, and module contracts. | Must remain false. |
| The candidate works in arbitrary browsers, applications, packages, or hostile proxy environments. | Domain runtime and integration evidence not gathered by this rung. | `UNKNOWN`; the narrow emitted fixture selftest is not general runtime proof. |
| Direct reuse/public copying rights are resolved. | Mike decision plus rights evidence. | `HOLD`; research-only/direct-reuse hold remains. |

The trusted package harness may execute the emitted fixture selftest only in its
own temporary execution copy, disjoint from the detached Nursery source. That
does not authorize the general repaired executor or grant lifecycle authority.
