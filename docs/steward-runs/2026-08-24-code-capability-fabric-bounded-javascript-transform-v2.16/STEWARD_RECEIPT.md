# Steward receipt — bounded JavaScript string-record transform v2.16

Status: `TEST`

This append-only receipt seals one bounded growth rung. It does not install,
integrate, publish as a product, promote, merge, or canonize the result.

## Lineage and review

- Branch: `codex/code-capability-fabric-bounded-javascript-transform-v2.16`
- Technical source commit:
  `6287aadea5a2ab2a3f9af6d19adcfc96dd6544ad`
- Exact parent:
  `934be41cb21af5cdb561366e04050e727b691fb1`
- Stacked target: `codex/code-capability-fabric-svg-status-badge-v2.15`
- Draft review: <https://github.com/mike-axiom-mir/axm-collaboration-platform/pull/66>
- Target review: <https://github.com/mike-axiom-mir/axm-collaboration-platform/pull/65>

At technical publication, the remote v2.15 branch and PR65 head both matched
the exact parent. PR65 was open, draft, and mergeable. The canonical Workshop
checkout was separately observed clean at
`7dd800d09731607b2936b580ab972d8033801736` and remained untouched.
Publication used a separate clean guarded worktree.

## What changed

The Fabric can now deterministically plan and emit one detached JavaScript
application-logic candidate for an exact bounded string-record transform. It
hardens the existing `pure-json-transform` recipe rather than creating a
parallel factory. The recipe is now `pure-json-transform@1.1.0`; the stable
builder identity remains `pure-json-transform-v1`.

The hand accepts only closed own enumerable data-property records containing
at most eight safe keys and string values of at most 128 characters. It refuses
arrays, null, custom prototypes, symbols, accessors without invoking getters,
non-enumerable or unsafe fields, non-string values, excess keys, and input or
output byte-budget violations. It emits a fresh one-field record using a
deterministic fallback, freezes configuration, and does not copy arbitrary
references. Its UTF-8 measurement has no emitted `Buffer` dependency.

Active ceilings are 512 input bytes and 256 output bytes. The exact profile
`code-family.bounded-javascript-record-transform` binds JavaScript, the
application-logic Organ, the `code-module` family, the recipe, the builder,
zero authority, consent, candidate bytes, and a deterministic/static evidence
route. The capability-gap comparator moved this exact rung from `BLOCKED` with
three missing capabilities to `READY` with none missing. It did not declare a
general JavaScript capability.

Exact lineage:

- Builder: `sha256:3cd18478277083397fde86db89c07138da98b756edd47d44b075283ab4ab18c0`
- Recipe: `sha256:9b9e5463bc126b21549ac6245ed88c063d43bc0e4d94a380fb092b40b035d24d`
- JavaScript specialist profile: `sha256:90130041a3e73d38b913c0d1c21caa59938de083c22c7ba6a45f99bd568f153c`
- Build-profile catalog: `sha256:baead0920e519e8118daffc241a53b284612ae412e0a2fc440d84bff2b19c4bd`
- Recipe catalog: `sha256:13a9348305afa80626e562476590c01b2d0d75600fdd8a6664da027501d6b671`
- Exact specialist candidate: `sha256:bbed3ab51fa1885a277cb4081c4f0097c1772edbbf18b95151a0c72a1cd2096f`

## Authority, privacy, and resource boundary

The emitted candidate remains `EXPERIMENTAL`, detached, uninstalled, and
unexecuted by the Code Specialist Fabric. It has no provider, network,
filesystem, environment, process, clock, randomness, dynamic-code,
installation, integration, publication, promotion, merge, or `CANON` authority.
It neither reads Workspace content nor changes Foundation or Atlas.

The trusted package harness executed emitted fixture selftests only in disjoint
temporary copies. This is recorded separately from Fabric/Nursery behavior and
does not authorize a general executor. Hostile proxies, arbitrary host objects,
general modules, browser use, and integration remain outside this proof.

## Four-root evaluation

- Truth: `PASS` for exact deterministic bytes, lineage, strict closed-record
  validation, resource ceilings, and the tested refusal cases. General runtime
  compatibility and hostile proxy behavior remain `UNKNOWN`.
- Agency / non-domination: `PASS`; generation grants no lifecycle authority and
  cannot approve, execute, install, integrate, publish, promote, merge, or
  canonize itself.
- Continuity: `PASS`; the exact v2.15 parent, language/Organ/profile/recipe/
  builder bytes, official projections, candidate lineage, and deterministic
  rebuilds are bound and checked.
- Wisdom over speed: `PASS`; the rung hardens and wires one existing closed
  transform hand instead of creating an arbitrary JavaScript executor.

These results make the packet reviewable. They do not replace Mike's final
hold, rejection, or merge decision.

## Verification and warnings

All ten required `AGENTS.md` checks passed. Focused results include 310 builder
assertions, 75 profile assertions, 152 shared Fabric checks, 15 tool checks, 27
admission checks, 114 package checks, 28 composition checks, and 51/51 recursive
Fabric entry points. `verify.js` ended at
`0 FAIL · 25 warn · spine b618c5762240070c`. The inherited warnings are not
hidden or reclassified. Exact results, limitations, and unrun surfaces are in
`TEST_REPORT.md`.

## Holds and decisions still owned by Mike

- Direct reuse or public copying of research-derived/generated source remains
  `HOLD` until rights are evidenced.
- A repaired disposable general executor remains unauthorized.
- General JavaScript, hostile proxy handling, arbitrary modules, provider use,
  installation, integration, product publication, promotion, merge, and
  `CANON` remain separate capabilities or decisions.

The exact no-surprise review and possible later fast-forward route is recorded
in `INTEGRATION_HANDOFF.md`. Target drift must be checked again at decision time.
