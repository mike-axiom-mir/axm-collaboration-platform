{
  "capabilities": [
    { "id": "cache.production.key.exact", "status": "available", "constraints": ["package, dependency package/path/digest/bytes, executor, verifier, and seed digest", "local SHA-256 canonical contract"] },
    { "id": "cache.production.cross-run.verified", "status": "available", "constraints": ["explicit external local cache root", "deterministic-v1 executors only", "EXPERIMENTAL"] },
    { "id": "cache.production.hit.current-verifier", "status": "available", "constraints": ["appointed verifier runs on every hit", "prior verifier testimony is not reused"] },
    { "id": "cache.production.tamper.fallback", "status": "available", "constraints": ["manifest and artifact byte integrity", "fresh execution fallback", "poisoned entry requires explicit invalidation"] },
    { "id": "cache.production.publish.atomic", "status": "available", "constraints": ["same-machine filesystem directory rename", "two separate Node publisher processes tested"] },
    { "id": "cache.production.invalidate.explicit", "status": "available", "constraints": ["one exact digest entry", "explicit CLI or API authority", "no automatic eviction"] },
    { "id": "cache.production.conflict.preserve", "status": "available", "constraints": ["same key and different manifest returns CONFLICT", "existing entry is not overwritten"] }
  ]
}
