{
  "schema":"axm.capability-requirements/v1",
  "objective":"Route one exact persisted held v2.8 observation toward the host-authorized Review Inbox without performing submission or inventing review authority.",
  "requirements":[
    {"id":"exact-v28-reload","capabilities":["model.shadow.retention-audit-review.v28-observation-exact-reload"],"required":true},
    {"id":"generic-review-candidate","capabilities":["review-inbox.exact-digest-candidate-compatible"],"required":true},
    {"id":"held-only","capabilities":["model.shadow.retention-audit-review.held-only"],"required":true},
    {"id":"minimized-artifact","capabilities":["model.shadow.retention-audit-review.minimized-review-artifact"],"required":true},
    {"id":"artifact-self-digest","capabilities":["model.shadow.retention-audit-review.artifact-self-digest"],"required":true},
    {"id":"candidate-binding","capabilities":["model.shadow.retention-audit-review.candidate-exact-artifact-binding"],"required":true},
    {"id":"host-route","capabilities":["model.shadow.retention-audit-review.explicit-host-route-declared"],"required":true},
    {"id":"data-only","capabilities":["model.shadow.retention-audit-review.data-only-no-submit"],"required":true},
    {"id":"receiver-match","capabilities":["model.shadow.retention-audit-review.initial-and-reloaded-review-item-match"],"required":true},
    {"id":"receiver-reload","capabilities":["model.shadow.retention-audit-review.fresh-process-receiver-reload"],"required":true},
    {"id":"pending-only","capabilities":["model.shadow.retention-audit-review.pending-zero-vote-zero-discussion"],"required":true},
    {"id":"time-ordering","capabilities":["model.shadow.retention-audit-review.caller-time-ordering"],"required":true},
    {"id":"upstream-nonmutation","capabilities":["model.shadow.retention-audit-review.no-durable-upstream-change"],"required":true},
    {"id":"resource-bounds","capabilities":["model.shadow.retention-audit-review.resource-bounds"],"required":true},
    {"id":"data-minimization","capabilities":["model.shadow.retention-audit-review.public-artifact-data-minimization"],"required":true},
    {"id":"exact-rebuild","capabilities":["model.shadow.retention-audit-review.request-and-handoff-exact-rebuild"],"required":true},
    {"id":"authority-none","capabilities":["model.shadow.retention-audit-review.authority-none"],"required":true},
    {"id":"closed-contract","capabilities":["model.shadow.retention-audit-review.closed-artifact-schemas"],"required":true},
    {"id":"host-authorization","capabilities":["model.shadow.retention-audit-review.host-mutation-authorization-proven"],"required":false},
    {"id":"actual-human-review","capabilities":["model.shadow.retention-audit-review.actual-human-review-proven"],"required":false},
    {"id":"authenticated-identity","capabilities":["model.shadow.retention-audit-review.authenticated-review-identity"],"required":false},
    {"id":"hold-resolution","capabilities":["model.shadow.retention-audit-review.review-decision-or-hold-resolution"],"required":false},
    {"id":"receiver-durability","capabilities":["model.shadow.retention-audit-review.receiver-file-fsync-or-external-durability"],"required":false},
    {"id":"provider-evaluation","capabilities":["model.shadow.retention-audit-review.provider-execution-or-evaluation"],"required":false},
    {"id":"benefit-learning","capabilities":["model.shadow.retention-audit-review.human-benefit-or-learning-proof"],"required":false},
    {"id":"promotion-authority","capabilities":["model.shadow.retention-audit-review.adoption-promotion-merge-canon-authority"],"required":false}
  ]
}
