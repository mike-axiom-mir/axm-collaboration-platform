# Code Specialist Markup Candidate v1.8 Steward Receipt

Status: `TEST`

Date: 2026-08-24

## Outcome

This bounded rung gives the exact `organ.code.markup-structure` specialist a
native construction route for one synthetic `game-index` artifact. It reuses
the existing Code Capability Fabric intent, consent, resource, lineage, and
candidate-package contracts and adds one closed Capability Fabric recipe and
builder: `static-accessible-html-page@0.1.0` /
`static-accessible-html-page-v1`.

After four technical root passes, exact intent rebuild, exact tier-1 consent,
catalog and recipe binding, path checks, and resource checks, the trusted
native builder emits one detached `EXPERIMENTAL` package in memory. The example
package binds 13 files and 34,073 bytes under digest
`sha256:8771defd2fdea3a8708393e73086126cd53b9e182a2885037552b041e22c133f`.
Its generated `capability.js` and `selftest.js` remain inert data. They were not
executed, rendered, materialized into the Workshop, installed, integrated,
published, promoted, or canonized.

## Lineage

- Review branch:
  `codex/code-capability-fabric-markup-specialist-candidate-v1.8`
- v1.7 parent: `ef7091406d963383be4de3f118812306ef697c2c`
- Assumed canonical target:
  `e20e6d4ad9efbbfe3ba0c6c4d720ef5cae1e0037`
- Technical commit: `8349261328b47121c8dd937c9b55daa23f4c6a6a`
- Exact technical integration payload from the assumed target: 427 paths with
  path-set digest
  `sha256:21484edf9705b1fbb03d7f93863c739a78ee88a0ff8f1abe5f8d42c17fd2e5da`.

## What changed

- Added a deterministic static semantic-page HAND recipe and native builder.
- The runtime input is closed to `heading`, `intro`, and bounded
  `{heading, body}` sections; all text is HTML escaped.
- The emitted page has a doctype, declared language, charset, viewport, skip
  link, one page heading, main landmark, and section headings.
- The emitted candidate has no scripts, inline event handlers, stylesheets,
  external resources, provider calls, filesystem access, network, child
  processes, dynamic evaluation, permissions, or authority.
- Extended the v1.7 specialist builder to two exact, non-interchangeable modes:
  the prior data-schema validator and this markup-structure route.
- Converted native builder refusal into a typed `CANDIDATE_HOLD` instead of an
  uncaught error.
- Added adversarial coverage for cross-mode impersonation, recipe and catalog
  drift, consent/resource expansion, invalid language and section inputs,
  active content, deterministic bytes, markup escaping, runtime/visual truth
  ceilings, path safety, and zero authority.
- Synchronized portable schemas, the admission continuity check, tools index,
  and generated City graph/schema/twin views.

## Evidence

- v1.8 markup/data specialist suite: 193 PASS.
- All 29 Code Capability Fabric selftest files: PASS.
- v1.6 intent adapter: 133 PASS.
- v1.5 specialization router: 143 PASS.
- Shared Capability Fabric: 92 PASS; composition: 27 PASS.
- Shared and tool Deterministic Organ Fabric suites: PASS.
- Capability Recipe Foundry: 38 PASS.
- Capability Recipe Admission Gate: 23 PASS.
- Hand Specification Foundry: PASS.
- City graph, schema registry, twin, City-map, schema-registry, and LEGO
  blueprint checks: PASS.
- Static `vm.Script` parsing of emitted source and selftest: PASS; neither
  script was invoked.
- All ten required `AGENTS.md` commands: exit 0.
- Final branch-only verifier checkpoint: 0 FAIL, 25 warnings, spine
  `b618c5762240070c`.

The 25 warnings remain visible: 20 game QA warnings, one legacy `UNDECLARED`
manifest kind, and four promotion claims needing current selftest evidence.

An exploratory whole-tool selftest probe was not used as acceptance evidence.
It produced seven non-PASS tool records: `ai-habitat`,
`grounded-evolution-intelligence`, `human-capability-atlas`,
`human-interface-intelligence`, `universal-object-fabric`,
`visual-mirror-platform-clone`, and `visual-mold-foundry`. Three inspected
failures reproduced at the canonical target. `ai-habitat` had no v1.8 source
diff and exposed inherited build-info drift. The remaining three were not
triaged in this bounded lane. None of those local result records were committed
to `tools-index.json`.

## Evidence not claimed

Browser render/click testing was not run. Static HTML structure is proven, but
visual presentation, keyboard journey, screen-reader behavior, and practical
accessibility remain `UNKNOWN` until a separate rendered test.
`tests/capability-fabric-package-test.js` and
`tests/capability-composition-package-test.js` were intentionally not run
because they materialize packages and execute generated selftests. No candidate
runtime, supplied experimental runtime, provider, network, arbitrary command,
repaired sandbox executor, installation, integration, publication, promotion,
or CANON path was run.

## Four-root disposition

- Truth: source structure, deterministic bytes, runtime behavior, visual
  behavior, and human acceptance remain separate claims.
- Agency / non-domination: exact tier-1 consent creates only one detached
  candidate and grants no execution, write, permission, network, installation,
  integration, publication, promotion, or canon authority.
- Continuity: v1.7 and the selected canonical target remain explicit lineage;
  Fabric continuity and derived registries were rebuilt and checked.
- Wisdom over speed: only one bounded markup recipe was admitted; unsupported
  specialists stay typed gaps and the emitted candidate stays inert.

Technical result: `TEST`, ready for review. Mike remains the final merge gate.

