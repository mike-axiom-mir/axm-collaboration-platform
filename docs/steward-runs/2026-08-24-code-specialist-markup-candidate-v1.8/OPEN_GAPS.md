# Open Capability and Authority Gaps

- Twelve of the fourteen code-specialist profiles still lack exact,
  source-reviewed implementation recipes. The builder fails closed outside the
  data-schema and markup-structure modes.
- The markup candidate proves static semantic structure and escaping only.
  Visual quality, keyboard behavior, screen-reader behavior, responsive layout,
  browser compatibility, and content usefulness remain `UNKNOWN`.
- Consent identity is reference-bound but not authenticated. Nonce replay is
  disclosed as unverified because no authenticated decision ledger exists.
- Generated and candidate source remains research-only and on direct-reuse and
  public-copy hold pending Mike's rights decision.
- A repaired disposable executor remains not authorized. Runtime behavior and
  the emitted selftest remain unproven.
- Memory and duration ceilings are declared but are not independently enforced
  by this in-memory builder. File, byte, pass, attempt, process, network, and
  workspace-write constraints are enforced or directly observed.
- The portable builder-id schemas are synchronized manually with six active
  builders. A later admission should either update them in the same transaction
  or replace the enum with a digest-bound registry reference.
- The exploratory whole-tool run left seven non-PASS results outside this
  specialist lane; four received bounded lineage triage and three remain
  explicitly untriaged.

