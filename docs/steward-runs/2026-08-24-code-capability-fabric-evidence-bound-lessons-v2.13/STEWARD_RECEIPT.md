# Code Capability Fabric evidence-bound lessons v2.13 steward receipt

Status: `TEST`

This append-only receipt describes one review branch. It is not an execution,
lesson-admission, installation, canonical integration, publication, promotion,
merge, or `CANON` receipt.

## Outcome and lineage

- Review branch: `codex/code-capability-fabric-evidence-bound-lessons-v2.13`
- Draft review: GitHub PR 63
- Direct parent / PR 62 head:
  `386736aaae3993089dfaf970cf2360894959e3c0`
- Technical source commit:
  `8190df1e31914718a5f46ab69983a0dc72175035`
- PR 62 target branch: `codex/code-work-context-dock-v1`
- Expected PR 62 target commit:
  `370af1c9a506807610c18561a711650a6cbfb8d6`
- Technical status: `PASS_FOR_REVIEWABLE_TEST_BRANCH`
- Persistent lesson admission: `NOT_RUN`
- Canonical integration: `NOT_RUN`
- Mike decision: `NOT_DECIDED`

Remote target drift was checked before publication. The PR 62 head and its
target matched the commits above. The canonical checkout remained on its own
clean `codex/workshop-active-clean-20260823` branch at
`7dd800d09731607b2936b580ab972d8033801736` and was not modified.

## What changed

- Added exact closed contracts for lesson requests, proposed lessons,
  reference-only library snapshots, evaluation suites, assessments, inactive
  release candidates, release receipts, and the steward snapshot.
- Reused the existing fourteen code-specialization knowledge lanes and their
  four allowed lesson kinds.
- Preserved compact accepted recipes, countertests, tests, and privacy-safe
  failure rules without retaining raw source, raw attempts, prompts, private
  content, stdout/stderr, machine paths, or hidden reasoning.
- Bound all provenance and evidence references to ID, schema, SHA-256, and byte
  length; refused cross-role byte aliases.
- Kept fixed regression and independently authored held-out goals separate;
  duplicate goals/evidence and cross-suite held-out overlap fail closed.
- Added holds for unresolved rights, missing support, contradiction, unknowns,
  stale/future evidence, expiry, semantic duplicates, failures, candidate
  identity drift, and future-library observations.
- Added finite structural ceilings, exact emitted lesson/release byte
  measurement, one pass, zero network requests, and zero child processes.
- Added exact patch-version and derived-identifier bounds.
- Made rights uncertainty explicit: the authority record is byte-bound but its
  issuer is not independently verified by this pure steward.
- Added an immutable parent/rollback-bound release candidate that remains
  inactive, uninstalled, unintegrated, and unadmitted.
- Added 74 focused adversarial checks, documentation, and official regenerated
  City/schema/twin views.

## Technical paths

Authored Fabric paths:

- `shared/code-capability-fabric/capability-lesson-steward-v1.js`
- `shared/code-capability-fabric/selftest-capability-lesson-steward-v1.js`
- `shared/code-capability-fabric/module-capability-lesson-steward-v1.contract.json`
- `shared/code-capability-fabric/README-capability-lesson-steward-v1.md`
- `shared/code-capability-fabric/README.md`
- nine `capability-lesson*` / `capability-library*` schema files in the same
  Fabric directory

Official generated views:

- `docs/generated/LEGO_CITY_BEGINNER_MAP.md`
- `docs/generated/LEGO_CITY_MAP.md`
- ten `registry/generated/city-*` machine views

Evidence is contained in this steward-run directory and follows the technical
commit so it can name that source hash without self-reference.

## Four-root disposition

- Truth: declared capability, supplied observations, technical assessment,
  rights assurance, human admission, execution, and integration remain
  separate facts. Holds cannot be rehashed into passes.
- Agency / non-domination: the caller supplies the bounded lesson, evidence,
  and four-root records. No score chooses a winner, no lesson enters a library,
  and Mike remains final merge gate.
- Continuity: the rung reuses the specialization catalog, deterministic JSON,
  existing consent lineage, and official generated-view compilers. It does not
  replace Foundation, Atlas, Organ Fabric, Review Inbox, Evidence Desk, or the
  sandbox boundary.
- Wisdom over speed: one inspectable candidate rung was added. The trusted
  rights verifier, Tier-3 decision, admission host, and general executor remain
  explicit later gaps rather than simulated capability.

## Verification and warnings

- Focused lesson steward: `PASS` — 74 checks.
- Recursive Fabric continuity: `PASS` — 51/51 entry points.
- Capability Fabric package continuity: `PASS` — 102 checks.
- City gate and direct City selftest: `PASS` — 33 assertions each.
- All ten required `AGENTS.md` checks: `PASS`, exit 0.
- `verify.js`: `0 FAIL · 25 warn`.
- Browser render/click: `NOT_APPLICABLE`; no visual surface changed.
- Independent Draft 2020-12 validator: `NOT_RUN`; unavailable without adding a
  dependency, which this run did not do.

## Publication boundary and workspace continuity

The repository publication brake rejected an attempted push from the build
worktree and then rejected a detached publish checkout. Both refusals happened
before any remote ref changed. The technical commit was published only from a
clean, attached `codex/publish-*` worktree under the repository-approved
publish lane. Draft PR 63 was then created; nothing was merged.

A failed initial checkout from this run remains recoverably quarantined outside
the repository. It contains no selected work and was not silently deleted.

## Deferred Mike decisions

- Whether the generated/supplied source rights state can ever move beyond the
  default `RESEARCH_ONLY_HOLD`.
- Whether to build the independent rights verifier and authenticated Tier-3
  library-admission hand.
- Whether a repaired general executor may later run an exact candidate in a
  disposable sandbox.
- Whether to accept PR 62, then review/integrate PR 63.

Passing this receipt does not mean CANON. The four roots gate first; Mike makes
the final merge decision.
