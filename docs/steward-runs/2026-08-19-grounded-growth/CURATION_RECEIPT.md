# Grounded growth evidence curation receipt

Status: open persistent stewardship increment; no claim that the broader goal is
finished.

## Retained

- `DURABLE_EVENT`: before and after capability-gap reports;
- `DURABLE_EVENT`: exact AI-workflow evaluation receipt;
- `DURABLE_EVENT`: exact grounded-growth outcome receipt;
- `DERIVED_VIEW`: current longitudinal portfolio;
- `DERIVED_VIEW`: this audit summary and evidence-route map;
- `PRIVATE_OR_USER_SOURCE`: no user source was copied into this lane.

## Noise avoided

- no raw model conversations, hidden reasoning, provider logs or screenshot
  collections were retained;
- no repeating Heartbeat or healthy-poll telemetry was generated;
- evaluation receipts contain case IDs and aggregate decisions, not temporary
  filesystem paths or source contents;
- the portfolio embeds exact bounded receipts and does not duplicate external
  simulation packages.

## Temporary material

The AI-workflow evaluation created one uniquely named child directory under the
explicit `AXM_TEST_TEMP` root. The script verified the resolved child boundary
and removed only that child after the receipt was built. No user source,
canonical state or external intake artifact was deleted.

## Seal and continuity

There is no standalone JSONL session segment for this increment, so no fake
session seal was created. Continuity is provided by exact receipt digests,
source hashes, outcome ancestry and the persistent task goal. The current
portfolio is rebuildable and remains a derived view, never memory authority.

## Open retention exception

The full Workshop worktree remains a large shared live state and the read-only
snapshot scan is truncated at its file limit. Only the narrow grounded-growth
lane is claimed here; foreign files and histories remain untouched.
