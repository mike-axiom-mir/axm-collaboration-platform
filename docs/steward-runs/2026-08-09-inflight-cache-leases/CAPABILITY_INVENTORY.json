{
  "schema": "axm.capability-inventory/v1",
  "capabilities": [
    { "id": "cache.production.lease.ledger", "status": "available", "constraints": ["one cache-local JSONL ledger per digest-bound run owner", "sealed digest chain with ACQUIRED, RENEWED, PROTECTED, and RELEASED events", "maximum 10000 events and 16 MiB per ledger"] },
    { "id": "cache.production.lease.owner-session-binding", "status": "available", "constraints": ["cache root, job root, run id, plan, and start time are stored only as digests", "newer same-run acquisition fences older session handles", "digest binding is continuity evidence, not principal authentication"] },
    { "id": "cache.production.lease.bounded-deadline", "status": "available", "constraints": ["duration covers cache-hit verification plus executor and verifier fallback for every remaining attempt, with margin", "maximum duration is seven days", "no daemon, heartbeat, renewal timer, or background scan"] },
    { "id": "cache.production.lease.crash-recovery", "status": "available", "constraints": ["abrupt exit leaves the last event intact", "active protection is time-derived and disappears after expiry without ledger rewrite", "same owner can append a recovery renewal"] },
    { "id": "cache.production.lease.runner-lifecycle", "status": "available", "constraints": ["cache-enabled runner acquires before package cache access", "exact key protected before load or publish", "release appended only after terminal receipt or graceful checkpoint persistence", "unexpected throw relies on bounded expiry"] },
    { "id": "cache.production.lease.discovery", "status": "available", "constraints": ["explicit read-only path-private sealed set", "maximum 20000 directory entries, 10000 ledgers, 100000 events, and 64 MiB aggregate", "invalid, linked, malformed, or over-limit evidence holds retention"] },
    { "id": "cache.production.retention.lease-binding", "status": "available", "constraints": ["inventory embeds exact lease set", "planning excludes active leased keys", "apply freshly rescans stable lease snapshot before mutation", "released and expired leases grant no protection"] },
    { "id": "cache.production.retention.key-coordination", "status": "available", "constraints": ["runner key protection and final retention check-delete use the same atomic cache-local key lock", "busy or changed coordination state deletes nothing for that key", "scope is the governed retention path, not direct explicit human invalidation"] },
    { "id": "cache.production.lease.checkpoint-handoff", "status": "available", "constraints": ["graceful checkpoint is sealed before lease release", "released lease grants no protection", "checkpoint-derived exact reference remains protected by retention"] }
  ]
}
