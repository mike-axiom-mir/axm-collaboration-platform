# Current Grounded Growth human-route coverage

Status: `TEST`

This lane binds the exact ten-outcome, six-chain Grounded Growth portfolio to
honest optional human-evidence dispositions. It creates no human result and
starts no participation.

## Result

- Five current claims have technically ready, named-local, voluntary routes.
- Three routes reuse the prior exact protocol and intervention link.
- Source closure reuses its exact surfaces and trials but refreshes the
  protocol and link because its current claim statement changed.
- Detached current-state trust has one new claim-native comparison route.
- Signal-lineage comprehension remains a typed zero-action hold because the
  required human-facing ledger proposal is still `DEFERRED_NO_ACTION`.
- Human evidence remains `NOT_RUN` for all six chains.

The coverage support receipt is not appended as another Grounded Growth
outcome. Doing so would create a new chain requiring another route and turn
route coverage into a self-referential expansion. The exact portfolio
reference in `CURRENT_HUMAN_ROUTE_COVERAGE.json` is the bounded binding.

## Rebuild and verify

```powershell
node docs/steward-runs/2026-08-20-grounded-growth-human-route-coverage/build-current-human-route-coverage.js
node docs/steward-runs/2026-08-20-grounded-growth-human-route-coverage/selftest.js
node shared/grounded-growth-human-route-coverage/selftest.js
```

The builder without `--write` is an exact recorded-source check. `--write` is
for deliberate local regeneration only.

## Optional human routes

No command below has been run by this lane. Each requires a local TTY and an
exact capability selector:

```powershell
node docs/steward-runs/2026-08-20-grounded-growth-human-route-coverage/run-current-human-route-interactive.js session <capability-id>
node docs/steward-runs/2026-08-20-grounded-growth-human-route-coverage/run-current-human-route-interactive.js handoff <capability-id> <external-session-receipt.json>
```

Session receipts must remain outside the Workshop. The driver refuses piped
input, repository-local session files, unsupported selectors, and the held
signal-lineage route before reading a session or prompting a person. It writes
nothing; any structured output goes only to standard output.

Read `CURRENT_HUMAN_ROUTE_CATALOG.json` for the five exact capability IDs and
their commands. The held sixth route intentionally has no command.

## Limits

Technical route coverage is not usefulness, human benefit, human identity, or
source authentication. Those remain `NOT_RUN` or `UNKNOWN`. This lane grants no
install, promotion, merge, `CANON`, Foundation-mutation, or model-training
authority.
