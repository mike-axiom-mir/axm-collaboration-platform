{
  "capabilities": [
    {"id": "module.inventory.complete", "status": "available", "constraints": ["214 registered modules in the bound platform catalog"]},
    {"id": "audit.ledger.persist", "status": "available", "constraints": ["append-only evidence-backed module results"]},
    {"id": "interface.inspect.source", "status": "available", "constraints": ["current shared Workshop files"]},
    {"id": "interface.test.deterministic", "status": "available", "constraints": ["module self-tests and focused assertions"]},
    {"id": "visual.capture.live", "status": "available", "constraints": ["in-app browser only for browser modules"]},
    {"id": "interaction.verify.browser", "status": "available", "constraints": ["bounded local browser interactions"]},
    {"id": "responsive.verify.browser", "status": "available", "constraints": ["explicit browser viewport override"]},
    {"id": "human.usability.independent-review", "status": "unavailable", "constraints": ["Mike or another appointed human must judge final usefulness and meaning"]}
  ]
}
