{
  "capabilities": [
    { "id": "representation.pixel-3d.profile-8-16/v1", "status": "unavailable", "constraints": ["8-bit and 16-bit currently resolve only to 2D pixel-raster profiles"] },
    { "id": "asset.pixel-3d.glb/v1", "status": "degraded", "constraints": ["static parametric GLB export exists, but it is not identity-bound, pixel-directed, hierarchical, or animated"] },
    { "id": "asset.pixel-3d.node-animation/v1", "status": "degraded", "constraints": ["the local runtime can play glTF node animation, but the choice-first asset system cannot author it"] },
    { "id": "asset.pixel-3d.identity-anchors/v1", "status": "unavailable", "constraints": ["no 3D delivery binds AXM asset identity, footprint, pivots, sockets, and semantic state into glTF extras"] },
    { "id": "hand.pixel-3d.representation/v1", "status": "unavailable", "constraints": ["no modular Asset Hand produces exact pixel-styled 3D representations"] },
    { "id": "visual.interact.webgl2-pixel-3d/v1", "status": "degraded", "constraints": ["a local WebGL2 GLB runtime exists, but no choice-first 3D pilot or profile switch is wired into the lab"] },
    { "id": "visual.capture.browser-screenshot/v1", "status": "available", "constraints": ["repeated screenshots are available; no rolling-buffer claim"] },
    { "id": "quality.taste.human-review/v1", "status": "unavailable", "constraints": ["Mike review remains required"] }
  ]
}
