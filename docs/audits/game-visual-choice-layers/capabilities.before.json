{
  "capabilities": [
    { "id": "game.visual.pack.contract/v1", "status": "unavailable", "constraints": ["per-asset representation profiles exist, but no game-wide visual pack contract binds a complete art direction to stable game truth"] },
    { "id": "game.visual.hardware.compatibility/v1", "status": "unavailable", "constraints": ["performance profiles and frame measurements exist, but no honest declared-hardware compatibility resolver exists"] },
    { "id": "game.visual.selection.player-authority/v1", "status": "unavailable", "constraints": ["no recommendation-versus-explicit-selection authority boundary"] },
    { "id": "game.visual.baseline-continuity/v1", "status": "unavailable", "constraints": ["no digest-bound bundled baseline policy or proof that minimum hardware retains a selectable representation"] },
    { "id": "game.visual.save-independence/v1", "status": "unavailable", "constraints": ["no game-level selection receipt proving save and simulation digests remain unchanged"] },
    { "id": "game.visual.pack.semantic-coverage/v1", "status": "unavailable", "constraints": ["individual assets can resolve 8-bit and 16-bit representations, but required whole-game visual slots are not coverage checked"] },
    { "id": "game.visual.selection.typed-failure/v1", "status": "unavailable", "constraints": ["per-asset MISSING_REPRESENTATION exists; game-wide missing, incompatible, and contract-mismatch outcomes do not"] },
    { "id": "visual.capture.browser-screenshot/v1", "status": "available", "constraints": ["repeated screenshots are available; no rolling-buffer claim"] },
    { "id": "visual.interact.browser/v1", "status": "available", "constraints": ["bounded in-app browser interaction is available"] },
    { "id": "quality.taste.human-review/v1", "status": "unavailable", "constraints": ["Mike review remains required and cannot be automated"] }
  ]
}
