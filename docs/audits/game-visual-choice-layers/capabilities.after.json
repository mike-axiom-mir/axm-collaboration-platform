{
  "capabilities": [
    { "id": "game.visual.pack.contract/v1", "status": "available", "constraints": ["versioned candidate contracts bind complete visual packs to stable game, simulation, gameplay-proxy, and save boundaries"] },
    { "id": "game.visual.hardware.compatibility/v1", "status": "available", "constraints": ["pure resolver compares declared capability profiles; automatic device detection is intentionally not claimed"] },
    { "id": "game.visual.selection.player-authority/v1", "status": "available", "constraints": ["recommendation returns READY_FOR_CHOICE; SELECTED requires an explicit pack id or accepted recommendation"] },
    { "id": "game.visual.baseline-continuity/v1", "status": "available", "constraints": ["digest-bound pixel-8 baseline is bundled, installed, semantically complete, and compatible with the minimum declared fixture"] },
    { "id": "game.visual.save-independence/v1", "status": "available", "constraints": ["deterministic receipts and live browser states retain an identical save digest across recommendation, selection, and refusal routes"] },
    { "id": "game.visual.pack.semantic-coverage/v1", "status": "available", "constraints": ["every pack must bind all nine pilot game visual slots before compatibility can pass"] },
    { "id": "game.visual.selection.typed-failure/v1", "status": "available", "constraints": ["missing, incompatible, and contract-mismatched packs return distinct results with fallback_used false"] },
    { "id": "visual.capture.browser-screenshot/v1", "status": "available", "constraints": ["two selected 1265x712 proof screenshots retained; no rolling-buffer claim"] },
    { "id": "visual.interact.browser/v1", "status": "available", "constraints": ["real select, button, zoom, simulation, and video-control interactions completed in the local browser"] },
    { "id": "quality.taste.human-review/v1", "status": "unavailable", "constraints": ["Mike review remains required for originality, readability, animation quality, and whether the direction is worth keeping"] }
  ]
}
