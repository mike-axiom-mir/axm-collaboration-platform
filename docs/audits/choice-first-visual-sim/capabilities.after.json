{
  "capabilities": [
    { "id": "representation.profile.resolve.exact/v1", "status": "available", "constraints": ["pure resolver; explicit axes override preset defaults; hard limits return typed errors"] },
    { "id": "representation.missing.typed/v1", "status": "available", "constraints": ["MISSING_REPRESENTATION only; fallback_used and nearest_substitute_used are always false"] },
    { "id": "simulation.causal.replay/v1", "status": "available", "constraints": ["event identifiers are exact-once; stable digest verifies replay"] },
    { "id": "simulation.idle-no-decay/v1", "status": "available", "constraints": ["elapsed-time events record time and produce zero condition or cleanliness delta"] },
    { "id": "simulation.object.upgrade-branches/v1", "status": "available", "constraints": ["resource-bound mutually exclusive slots; persistent object identity"] },
    { "id": "asset.raster.rgba8/v1", "status": "available", "constraints": ["local deterministic PNG codec"] },
    { "id": "asset.animation.apng/v1", "status": "available", "constraints": ["local deterministic APNG codec"] },
    { "id": "asset.pixel.profile-8-16/v1", "status": "available", "constraints": ["original AXM pilot art over RGBA8 sRGB; aesthetic approval pending"] },
    { "id": "video.sparse-rational-sequence/v1", "status": "available", "constraints": ["1..64 unique RGBA frames, 2..600 sample references, at most 30 seconds; legacy 120-frame encoder unchanged"] },
    { "id": "video.container.mp4-motion-jpeg/v1", "status": "available", "constraints": ["bounded local muxer; exact rational sample timing"] },
    { "id": "video.container.webm-vp8/v1", "status": "available", "constraints": ["bounded local muxer; millisecond container timecode"] },
    { "id": "visual.capture.browser-screenshot/v1", "status": "available", "constraints": ["repeated screenshots used; no rolling-buffer or realtime-observation claim"] },
    { "id": "visual.interact.browser/v1", "status": "available", "constraints": ["profile, missing-gap, sofa, causality, zoom and video controls exercised"] },
    { "id": "quality.taste.human-review/v1", "status": "unavailable", "constraints": ["Mike review remains required and cannot be automated"] }
  ]
}
