{
  "format": 1,
  "note": "THE TEMPLATE INDEX — templates are assets; this file is how they are found. Fetched directly or via AXMRegistry kind 'template.source' (actions: list, resolve). Entries list ONLY real files (no fake done); planned entries carry file:null + status. The 'inherits' and 'slots' fields are RESERVED for the future template engine (cross-pattern research module) — declared now so the index never needs a breaking change.",
  "layers": ["shell", "layout", "block"],
  "families": ["comic", "info", "faction", "social", "systemlog"],
  "templates": [
    {
      "id": "library-spec",
      "family": "meta",
      "layer": "shell",
      "name": "AXM Professional Template Library — design seed v0.1",
      "file": "library-spec.txt",
      "status": "design-seed",
      "inherits": null,
      "notes": "The cross-checked spec: stable shell / flexible slots / protected output. Every future template family derives from this document."
    },
    {
      "id": "comic-base-shell",
      "family": "comic",
      "layer": "shell",
      "name": "AXM Comic Base Shell",
      "file": null,
      "status": "planned — Axiom/Mir visual pass",
      "inherits": null
    }
  ]
}
