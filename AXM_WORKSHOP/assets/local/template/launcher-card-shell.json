{
  "format": "axm-template-shell",
  "v": 1,
  "id": "launcher-card-shell",
  "name": "Launcher Card Shell",
  "family": "launcher",
  "layer": "shell",
  "status": "design-seed",
  "output": {
    "size": "1200x675",
    "format": "png-or-html-export",
    "target": "Workshop launcher tool card"
  },
  "stableShell": [
    "card ratio locked",
    "title zone locked",
    "subtitle zone locked",
    "status badge zone locked",
    "launch button zone locked",
    "safe margins locked"
  ],
  "flexibleSlots": [
    { "id": "title", "type": "text", "required": true },
    { "id": "subtitle", "type": "text", "required": false },
    { "id": "cover", "type": "image", "required": false },
    { "id": "icon", "type": "image", "required": false },
    { "id": "accent", "type": "color", "required": false },
    { "id": "status", "type": "badge", "required": true },
    { "id": "tags", "type": "chips", "required": false }
  ],
  "protectedOutput": [
    "status badge stays readable",
    "title remains readable over cover art",
    "fallback cover works if no pack is installed",
    "no launcher dependency on Asset Vault at boot"
  ],
  "principle": "Outer stable. Inner transparent / flexible."
}
