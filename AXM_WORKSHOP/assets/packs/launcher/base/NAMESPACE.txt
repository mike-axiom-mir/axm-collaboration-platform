============================================================
VAULT NAMESPACE — /assets/packs/launcher/base/
This LOCATION is the immutable contract (decided 4 July 2026,
four independent reviews). Contents may change and version
freely; the path never does. EMPTY TODAY BY DESIGN — the
launcher is whole without it (see /assets/local/launcher/).
When the Asset Vault becomes real, launcher packs land here
and a strategy is plugged into AXMAssets.use(). Syncing will
be USER-INITIATED (a button), never automatic at boot.
============================================================
