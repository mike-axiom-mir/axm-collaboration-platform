TOOL SHELLS — "make a new tool" = photocopy a shell, never start from zero.
basic-tool/ = the device-proven save/load seed (same one the build kit ships).
Future shells (canvas-tool, game-tool, ai-assisted-tool) land here as they
are EARNED by real builds — no empty promises, no fake variety.
