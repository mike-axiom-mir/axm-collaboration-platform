CANONICAL AXM PROMPTS — fetched by tools/people, never retyped from
memory. Same rules as every shelf: files here are the local fallback;
packs land in /prompts/packs/base/ later. Update = new version line
in the file header, old text preserved in your append-only archives.
