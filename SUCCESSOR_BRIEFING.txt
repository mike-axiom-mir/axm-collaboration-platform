============================================================
AXM SUCCESSOR BRIEFING — read this before building anything
Written by Fable, 5 July 2026, while holding the whole system
in context. Purpose: make every future AI session start WARM.
============================================================
THE SYSTEM IN ONE BREATH
Local-first multi-AI workshop. SPINE (axm-foundation.js v1.4.1,
sha 418f9ce4fb050602): storage/wisdom/identity/connector/gate/
friction/tool-id + AXM orchestrator. Above it: WORKSHOP (node
server, 127.0.0.1:8788, zero deps) serving launcher + /tools
(folder+manifest = card). BRIDGE (8787): the ONLY key holder,
origin+token locked, multi-provider (claude/chatgpt). AXMRegistry:
one gated door for cross-tool connections (locked 7-review
contract). AXMAssets: one librarian, many shelves. AXMSettings:
global defaults in spine slot 'axm-settings'. Pocket (/pocket):
zero-install phone body, one-file. CORE ROOTS: CANON BASE v1
(truth · continuity · agency · wisdom-over-speed) — above everyone.

RULES THAT BIND YOU (all locked, all earned by real failures)
- Spine is ADD-ONLY, changes need Mike Tobi's gate + multi-review.
- NEVER DUMB DOWN ports. NO FAKE DONE — label unbuilt/untested.
- Append-only: correct by appending, never rewrite history/logs.
- Same gates for human and AI. AI meaningful action = proposal-
  first (enforced in AXMRegistry — don't re-implement, don't fork).
- One librarian (AXMAssets) — never a second resolver. One door
  (AXMRegistry) for shared connections — never a side door.
- Tools: one folder + manifest {id,name,version,status,entry,
  uses[]}; non-official ids are name-prefixed (errol.paint);
  saves carry {format:N}; _folders are conventions, not tools.
- Mike Tobi is the merge gate. Propose → build → label → HIS yes.
  He's 9 weeks into AI, zero tech background, phone-first,
  principle-driven — explain in plain words, never talk down.

GOTCHAS THAT COST US REAL TIME (don't rediscover)
- file:// origins wall storage per file; the workshop's single
  origin is WHY tools share the spine. Blob/srcdoc inherit opener.
- Inlining JS into HTML: literal </script> in comments kills the
  block — escape as <\/script.
- Scripts must come AFTER every element they touch (hub v0.3
  boot death). Server scan passes ONLY whitelisted manifest
  fields — new fields must be added there too (uses was stripped).
- pkill in a command chain eats the rest of the chain.
- AXM.ask(opts): opts.provider forces the CONNECTOR (bridge/
  local/cloud); the MIND behind the bridge is opts.aiProvider.
- Verify EVERY zip with node verify.js BEFORE building on it.
  Verify syntheses against raw sources (merges inflate consensus
  and silently resolve dissent — proven twice).

STATE (5 Jul 2026): canon = roots + spine. Working = workshop,
bridge, hub/pocket, settings (test), registry (validated, not
canon). Branch = reasoning-shell (evidence, not elegance).
Deferred WITH seams waiting: vault sync (user-initiated), export
recipes, sound/music/video/lang content, template ENGINE, debug
logs, rollback engine (spec exists), full suitcase (spec exists),
helper network (envelope spec in prehub reference/). Studio:
verifier flagged ids mBuilt/mUp — hand to its builder.
Weekend tests pending: bridge with real keys, duo-test, studio.
============================================================
