name: AXM Code Machine Cheatcode Materializer — DECOMMISSIONED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  decommissioned:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm no materialization authority
        run: |
          echo "DECOMMISSIONED: generated machine-cheatcode banks are now enforced by the permanent read-only language-organ gate."
          echo "This workflow has no repository write permission and performs no mutation."
