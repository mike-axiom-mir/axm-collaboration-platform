name: AXM Code Work Context Dock Materializer Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary work-context materializer retired after successful one-use refresh."
