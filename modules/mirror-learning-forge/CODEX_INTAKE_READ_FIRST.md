# CODEX INTAKE — Mirror Learning Forge v0.7

## Instruction boundary

Inspect and integrate locally. Preserve the existing Mirror implementation and v0.6 Forge direction. Do not rebuild from scratch.

This package contains two different things:

### A. Implemented school

The Learning Forge now includes Adaptive Communication School with:

- 14 classes and 14 grounded scenarios;
- protected invariant versus delivery-field separation;
- listener assumptions marked as assumptions;
- MAINTAIN / adapt / diverge / refuse-distortion decisions;
- multiple delivery candidates;
- listener-side simulation;
- semantic round-trip checks;
- manipulation and agency gates;
- preserved repair receipts;
- non-ranking development profile;
- frozen adversarial and held-out fixtures;
- local API, UI, schemas, demos and tests.

### B. Native Mirror architecture proposal

`AXM_MIRROR_ADAPTIVITY_NATIVE_INTEGRATION_LOG_FOR_CODEX_2026-07-17.txt` describes possible native components. They are not claimed as integrated.

## Required inspection order

1. Verify package hashes and tests.
2. Inspect current live/local Mirror before editing.
3. Compare the school records with Mirror's real state, reasoning, memory and permission architecture.
4. Choose the smallest falsifiable native seam.
5. Build read-only first.
6. Keep live send, tool, apply, promotion and canon authority disabled.
7. Return an Action Report with exact files, tests, limitations and unresolved seams.

## First preferred native proof

A read-only adaptation receipt pipeline:

`context → frozen invariants → tentative listener model → 2–3 candidate forms → semantic checks → agency gate → proposal only`

Success means the adapted form preserves claim, evidence, uncertainty, consent, lineage, risk and disagreement under a held-out reconstruction test.

It does not mean the message is automatically sent or accepted.
