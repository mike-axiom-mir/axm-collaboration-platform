# PR14 read-only source trace

Repository: `mike-axiom-mir/axm-collaboration-platform`  
Pull request: `#14 — Publish public-safe post-intake AXM workshop update`  
State at inspection: open draft, mergeable, not merged  
Base: `main` at `627b905a53628ec840abda24ce8410fa5edfe3de`  
Head: `agent/axm-public-safe-post-intake` at `d427a35f3dafa500d40ff66b86cb464563e4e42b`

No checkout, branch edit, comment, commit, push, merge, or GitHub mutation was performed.

## Contracts carried into this local module

### Mirror Core
- local loopback runtime and explicit start;
- proposal-first underlay between separately authoritative systems;
- source, target, and Mirror each keep distinct authority;
- permission and consent are independent and both default-deny;
- approval does not apply;
- application, verification, and rollback are separate, recorded operations;
- human and machine actors share record shapes without implicit power.

### Exploration / Discovery
- exploration defaults to opted out;
- one active bounded exploration per identity;
- evidence is required before a proposal enters discussion;
- wisdom remains a candidate and never auto-promotes;
- quality claim, AXM fit, risks, and next test remain visible;
- the cheapest disconfirming test is a first-class control.

### Growth
- growth metrics must not be inflated by copies, exports, logs, state, or caches;
- snapshots are explicit local actions and identical measurements deduplicate.

## Local adaptation

The Learning Forge uses the same geometry for learning:

`RAW → PERMITTED → REVIEWED EPISODE → CHALLENGER → HELD-OUT EVALUATION → SEAM VERDICT → HUMAN PROMOTION PROPOSAL`

It exports a Mirror Core-compatible DRAFT packet, but live Mirror Core application is intentionally absent.

## v0.3 technical-literacy extension

The structured JSON and coding tracks were built locally in this chat on top of the Forge v0.2 package. They preserve PR14's authority geometry:

- a valid record remains only a record until evidence and permission pass;
- a code artifact remains inert until a separate explicit execution service exists;
- track tasks, grades and curricula are proposals/evidence, not activation authority;
- per-track results enter the same Seam Cell and human-reviewed promotion path;
- no GitHub or Workshop change was made.

## v0.4 Creative Studio extension

For the selectable Creative Studio class path, PR14 was additionally inspected read-only at the same head:

- `tools/studio/engine.html`
- `tools/studio/studio-shell.js`

Observed and preserved:

- the 600 × 600 shared canvas;
- one master human-facing tool registry;
- the visible `axm.drawpacket/v1` machine handoff;
- the same `runDraw` and multi-layer engine for live AI and pasted packets;
- assigned human/AI layer ownership;
- 14-command machine-turn cap;
- erase, new-layer, move-layer and shift-layer repair operations;
- screenshot input in AI turns;
- visible refusal when Studio JSON cannot be parsed.

The v0.4 Forge mirrors these contracts for curriculum and static grading only. It does not connect to Studio or apply pixels.


## v0.5 Rooted Intelligence extension

The v0.5 Forge adds a local teaching track derived from AXM's four established root anchors. This is not a GitHub source claim and does not modify PR14. The track is implemented in the separate local Forge package with grounded scenarios, anti-parroting assessment, preserved dissent, and explicit no canon-edit authority. PR14 remains a read-only architectural source for proposal-first, consent, continuity, verification, and rollback boundaries.
