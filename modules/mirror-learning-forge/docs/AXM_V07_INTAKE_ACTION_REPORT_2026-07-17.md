# Mirror Learning Forge v0.7 intake action report

Date: 2026-07-17

## Evidence received

- Outer package SHA-256: `2c7e13b218ec0cc3037449cdba9a8740caba1f44249af73469a1947f2a5a2c85`
- The companion checksum matched the received outer archive.
- The nested Forge archive matched its own included checksum.
- Both archive path scans found no absolute paths, traversal entries, or other
  unsafe extraction names.
- The clean extracted package passed 94/94 supplied tests.
- The clean extracted package verified 136 recorded files and its local-only,
  proposal-first, default-deny contracts.

## Preserved before overlay

- Live opt-in/opt-out state, journal, models, episodes, and receipts
- Runtime-generated local bearer secret
- AXM module manifest and contract
- AXM intake decision and authentication regression test

A reversible local backup was written to:

`C:\AXM_MIRROR_LOCAL\state\intake-backups\mirror-learning-forge-v01-pre-v07-20260717-232656.zip`

Backup SHA-256:

`b33b6b06a530de497d0f1b8b97137b8f9759c49996d62b6ad62fbf88ac786e66`

## Adopted study and practice tracks

- Structured JSON literacy and semantic/evidence separation
- Static coding foundations without arbitrary execution
- Creative Studio classes with later screenshot receipts

## Adopted foundation-reasoning development

- Rooted-intelligence scenarios and preserved dissent
- Inspectable reasoning development without IQ or human-equivalence claims
- Adaptive communication with protected-meaning invariants
- Growth forecasting and per-track held-out evaluation

The upgrade also adopts expanded fixtures, curriculum, schemas, documentation,
UI, and tests.

## Repaired during intake

The supplied v0.7 loopback server omitted the bearer gate already added during
the AXM v0.1 intake. AXM restored runtime token creation, timing-safe bearer
comparison, token injection into the no-store local UI, authenticated state and
write APIs, and a no-referrer response policy. `/api/health` is intentionally
the only public API route and remains read-only.

## Held

- No automatic opt-in, training, promotion, or Workshop action
- No code execution, pixel authority, Studio tool authority, or external network
- No live Mirror Core application
- No canon, identity, permission, filesystem, runtime, or promotion authority
- Native adaptive-communication organs remain architecture candidates until a
  separate evidence-backed integration review

## State at intake

Mirror Learning Forge remained `OPTED_OUT`, revision `0`, authority revision
`0`, with no active model and no sessions, candidates, episodes, evaluations,
promotion packets, or applications. Package tests ran against temporary stores;
the live empty state was not used as training material.
