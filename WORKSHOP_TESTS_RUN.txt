============================================================
PUBLIC-SAFE CHECKPOINT NOTE — 2026-07-11
============================================================
This file contains historical test entries recorded at different build stages.
Some old lines correctly say "not tested" for the time they were written.
Current status is authoritative in PUBLIC_VERIFICATION_REPORT.md:
- Hub/browser manually passed by Mike Tobi through a fully local BAT route.
- OneDrive online-only Desktop path failed at the environment layer.
- Automated Playwright click/render remains UNRUN.
- Public-safe deterministic tests and API smoke checks passed.
============================================================

============================================================
WORKSHOP v0.1 — SANDBOX TESTS (server ran for real in sandbox)
============================================================
Server boots, binds 127.0.0.1:8788 (loopback-only by bind)   PASS
/api/health honest                                            PASS
/api/tools scans /tools, prehub card correct                  PASS
Launcher UI + tool pages served from one origin               PASS
/api/export writes a REAL file into /exports                  PASS
URL traversal (/../server.js) -> "path tricks refused"        PASS
Evil export filename (../../etc/x) -> sanitized INTO exports  PASS
Folder without manifest -> honest BROKEN card, not silence    PASS
404 -> styled page with a way back, never a dead end          PASS
Launcher UI: syntax + DOM-order check (the v0.3 lesson)       PASS
NOT TESTED (honest): START_AXM.bat on real Windows; the full
flow in a real browser (launch tool, save, shared storage
across tools) -> MIKE TOBI'S DEVICE TEST. Node.js required once.
============================================================

BRIDGE v0.2 — THE LOCK (ran live in sandbox, attacked on purpose)
Evil-origin POST (https://evil.com) -> 403 refused              PASS
No origin + no token (random script) -> 403 refused             PASS
Workshop-origin POST -> passes the lock (hit honest no-key
  error — sandbox has no key; the DOOR provably opened)         PASS
Correct token from bridge-token.txt -> passes the lock          PASS
Wrong token -> 403 refused                                      PASS
CORS: evil origins get NO allow headers (can't read replies)    PASS
All of it audited to bridge/bridge.log, append-only             PASS
Rate cap wired (30/min default)                                 built, not stress-tested
STILL YOURS: the real-AI call with your key = the weekend test.
Note: this v0.2 supersedes the v0.1 bridge in the prehub zip
(v0.1's own NOT DONE promised exactly this). Merging v0.2 back
into the prehub package is your canon call.

BRIDGE v0.3 — MULTI-PROVIDER (sandbox, attacked + routed)
/health lists both minds with per-key honesty                  PASS
Lock unchanged: evil origin still 403                          PASS
No keys -> honest; missing chatgpt key -> named; unknown
  provider -> lists what exists (asked for grok, got the list)  PASS
claude route fires end-to-end (fake key -> real Anthropic
  auth error came back through the lock)                       PASS
Default provider = first with a key                            PASS
chatgpt route: built to OpenAI's published shape, NOT live-
  tested (sandbox can't reach api.openai.com) -> WEEKEND TEST.
  Check AXM_OPENAI_MODEL name against your OpenAI dashboard.
DUO TEST tool added (tools/duo-test, status TEST): ask claude /
  chatgpt / both / relay, stability + error counters, every ask
  gate-logged. Library scan sees it (duo-test:TEST beside
  prehub:WORKING). UI syntax + DOM-order checked.
  Real two-mind conversation = Mike Tobi's laptop, both keys set.

v0.3 — VERIFIED v0.2a BASE + VAULT BOOTSTRAP (steps 1-3 of the
four-review decision brief; steps 4-5 deferred BY DECISION)
v0.2a patch verified before adopting: spine byte-identical, lock
  intact (evil origin still 403), old opts.provider shape AND new
  provider:'bridge'+aiProvider shape both route correctly          PASS
AssetResolver (launcher/axm-assets.js): all launcher art asked
  from ONE librarian; UI contains ZERO hardcoded asset paths
  (build check enforces it)                                        PASS
Future-strategy seam proven: plugged a fake Vault strategy via
  AXMAssets.use() -> logo re-routed to the namespace with zero
  caller changes; unknown assets still resolve (never empty)       PASS
Immutable namespace declared: /assets/packs/launcher/base/
  (empty TODAY BY DESIGN, NAMESPACE.txt states the contract)       done
Complete fallback skin ships: tokens.css, logo, favicon, default
  card cover + SKIN_README (Axiom/Mir reskin door: same-name
  swaps restyle everything, zero code edits). All four assets
  served 200 over the real server; resolver script in page         PASS
NOT built (honest, per the brief): Vault syncing (will be
  user-initiated when real), manifest/optional-flags system.
NOT tested: how the skin LOOKS on a real screen -> Mike Tobi's eyes.

v0.4 — TEMPLATE SEAMS (the charter build)
ONE LIBRARIAN, MANY SHELVES: rejected nine parallel resolvers;
  the existing AXMAssets serves shelf-qualified ids. Proven:
  launcher/sound/music/lang ids all resolve to their shelves     PASS
  (my first cut rooted every shelf under launcher/ — caught by
  my own verification, fixed, re-proven. Checks earn their keep.)
Shelves declared (empty BY DESIGN, NAMESPACE.txt each): sound,
  music, video, lang + packs namespaces for each                 done
PROMPTS shelf seeded with REAL canonical templates: driftcheck
  source-audit, bridge relay-test, AI handoff header (the
  NEVER-DUMB-DOWN + no-fake-done rules, now an asset not memory) done
Tool shells: /tools/_templates/basic-tool = the proven seed +
  manifest template w/ saveFormat rule. Library scan correctly
  skips _folders (second self-caught bug: _templates showed as a
  broken card -> underscore convention added to the scan)        PASS
TEMPLATE_SEAMS.txt charter: one-rule, gates NOT rebuilt (they
  ARE AXMGate — action-name convention only), one log shape
  (the gate envelope everywhere), settings slot reserved
  ('axm-settings'), save-format versioning rule, license-in-
  manifest rule, lang shelf for the Dutch club                   done
All new files served 200 over the real server                   PASS
DEFERRED BY DESIGN (not forgotten, charter says where): sound/
  music/video metadata manifests, export recipe engine, settings
  UI, prompt packs, any resolver-without-a-consumer.

v0.5 — TRUST CHARTER (seven commitments, adopted by Mike Tobi)
TRUST_CHARTER.txt: spine's add-only promise · suitcase shape +
  goodbye path (manual today, SAID SO) · name territory rule ·
  honest cartridges · bug suitcase · accessibility floor · the
  core as an asset                                              done
axm-core.txt on the prompts shelf — the four roots, fetchable
  by any mind. LABELED DRAFT inside the file: Mike Tobi's explicit
  yes (or his own rewording) makes it canon, not this build     done
"uses" declared on real tools (prehub: storage · duo-test:
  storage,ai,bridge) + template. Server API passes declarations
  through (self-caught: the scan was stripping the field)       PASS
Export Issue Report button BUILT: asks did/expected/happened,
  bundles status + tool list + last 15 gate decisions, writes a
  real file to /exports beside the Action Report                built; UI = device test
Charter + core file served 200 over the real server             PASS
Deferred honestly, in the charter itself: one-button suitcase
  (needs spine storage enumeration; manual path documented),
  gate-enforcement of uses declarations (a rule, later).

v0.7 — CONNECTION REGISTRY (Phase 0, per the locked 7-layer decision)
Base verified first: v0.6 spine byte-identical, studio manifest is a
  model honest cartridge, core roots now CANON BASE v1 (honored).
AXMRegistry built to the FIXED contract, BESIDE the spine (foundation
  untouched — ladder, no cliff). Locked TEST ACCEPTANCE, item by item:
  4 built-in local connectors register + list by kind              PASS
  canHandle that throws -> safely false                            PASS
  read resolves via local.assets, IDENTICAL to AXMAssets (one
    strategy system, two doors — Sharpened Decision 2 honored)     PASS
  write via the one door: envelope-wrapped, requestId, real save   PASS
  two sinks -> needsUserChoice with choices; explicit
    options.connectorId works — never a random meaningful write    PASS
  AI-initiated write -> proposal-first even with a single option   PASS
  gate log carries METADATA ONLY (payload text provably absent)    PASS
  AXM.ask() and AXMAssets.getAssetPath() unchanged                 PASS
First real consumers wired: launcher Action Report + Issue Report
  now write through asset.sink (human, approvedByUser). Registry +
  template index served 200; studio:TEST beside prehub + duo-test.
TEMPLATE INFRASTRUCTURE (Mike Tobi's ask): templates are assets now —
  /assets/local/template/ shelf, index.json (format 1; honest: real
  entries only, planned entries carry file:null and resolving them
  returns a no-fake-done error — proven), packs namespace declared,
  registry kind template.source (list + resolve) working. The
  library-spec doc ships as the first real template asset. Reserved
  in the index, NOT built: inherits/slots engine, warnings, cross-
  pattern matching — the research module, later, as decided.
Registry canon status: NOT CANON YET (per decision) — validated
  against first consumers here; Mike Tobi's device test + gate = canon.

v0.8 — GLOBAL SETTINGS TEST (ACCEPT FOR TEST ONLY · NOT CANON)
AXMSettings beside the spine, stored in reserved slot 'axm-settings'
  (declared in TEMPLATE_SEAMS back when it cost a paragraph).
Safe defaults ship exactly as Mike Tobi specified: provider none · bridge
  ask-every-time · AI propose-only (the ONLY option that exists — no
  silent AI write/export/share is enforced by the registry, not a
  checkbox) · export sink asks every time · logs minimal            PASS
set/get/persist/reset all gated; key logged, VALUE provably not;
  bad values + unknown keys refused                                 PASS
HONEST WIRED FLAGS: unwired settings say "(not wired yet)" in the
  UI and carry wired:false — stored for tools to adopt, never
  pretended (ai.defaultProvider/bridgeMode). Wired today: registry
  default sink, propose-only, accent, compact, theme, log level
  label (debug marked reserved/NOT BUILT in its own note)           PASS
Saved-default rule proven: multi-sink write ASKS before any save;
  after explicit save the registry uses it; reset restores asking   PASS
REGISTRY TRUTH PATCH: connector returning ok:false now surfaces
  ok:false + error at the top of the envelope (liar-sink test)      PASS
Settings tab in launcher: describe-driven UI, reset button, export
  settings .json (suitcase-ready format:1), export gate log via the
  registry door. NO clear-log button ON PURPOSE (append-only canon).
  Registered connectors listed live from AXMRegistry                built; UI = device test
Not broken: AXM.ask, AXMAssets, AXMRegistry acceptance behaviors.
Not built (honest): debug log level, theme packs (asset system
  later), per-tool adoption of ai.* defaults (next patches read
  them; the globals stay visible per Mike Tobi's rule).


v0.8a — TRUTH EVENT POLISH (Axiom/Mir follow-up)
Registry envelope truth patch from v0.8 verified: connector ok:false
  surfaces as top-level ok:false + error                               PASS
Runtime handled event now also emits ok:false when connector reports
  ok:false (previous v0.8 emitted ok:true there)                        PASS
No spine rewrite: AXM.ask, AXMAssets, AXMSettings unchanged             PASS
