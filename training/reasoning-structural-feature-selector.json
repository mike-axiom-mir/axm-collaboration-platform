{
  "schema": "axm.mirror.structural-feature-selector/v1",
  "selectorId": "axm.mirror.selector/reasoning-session-structural-growth-v1",
  "sourceSchema": "axm.mirror.reasoning-feature-source/v1",
  "rules": [
    {
      "path": ["principleTrace", "candidates", "*", "action", "recovery"],
      "mode": "PRESENCE",
      "positiveValues": ["missing"]
    }
  ],
  "boundary": "Version 1 selects only whether a normalized candidate lacks a recovery declaration. It reads no recovery prose, IDs, goals, labels, explanations, or source text. New selector versions may add finite typed machine paths without changing projector code, but remain proposals until independently tested."
}
